(* THIS FILE IS AUTOMATICALLY GENERATED, DO NOT EDIT BY HAND! *)

open Ctypes
open Torch_refcounted_bindings.Type_defs
open Torch_stubs
open Torch_wrapper_types
open Wrapper_utils
open C.Generated

let __and__ (self @ local) other =
  stubs___and__ (globalize_gc_tensor self) other |> with_tensor_gc

let __and__tensor_ (self @ local) (other @ local) =
  stubs___and__tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let __iand__ (self @ local) other =
  stubs___iand__ (globalize_gc_tensor self) other |> with_tensor_gc

let __iand__tensor_ (self @ local) (other @ local) =
  stubs___iand__tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let __ilshift__ (self @ local) other =
  stubs___ilshift__ (globalize_gc_tensor self) other |> with_tensor_gc

let __ilshift__tensor_ (self @ local) (other @ local) =
  stubs___ilshift__tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let __ior__ (self @ local) other =
  stubs___ior__ (globalize_gc_tensor self) other |> with_tensor_gc

let __ior__tensor_ (self @ local) (other @ local) =
  stubs___ior__tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let __irshift__ (self @ local) other =
  stubs___irshift__ (globalize_gc_tensor self) other |> with_tensor_gc

let __irshift__tensor_ (self @ local) (other @ local) =
  stubs___irshift__tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let __ixor__ (self @ local) other =
  stubs___ixor__ (globalize_gc_tensor self) other |> with_tensor_gc

let __ixor__tensor_ (self @ local) (other @ local) =
  stubs___ixor__tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let __lshift__ (self @ local) other =
  stubs___lshift__ (globalize_gc_tensor self) other |> with_tensor_gc

let __lshift__scalar_out_ ~(out @ local) (self @ local) other =
  stubs___lshift__scalar_out_ (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let __lshift__tensor_ (self @ local) (other @ local) =
  stubs___lshift__tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let __lshift__tensor_out_ ~(out @ local) (self @ local) (other @ local) =
  stubs___lshift__tensor_out_ (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let __or__ (self @ local) other =
  stubs___or__ (globalize_gc_tensor self) other |> with_tensor_gc

let __or__tensor_ (self @ local) (other @ local) =
  stubs___or__tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let __rshift__ (self @ local) other =
  stubs___rshift__ (globalize_gc_tensor self) other |> with_tensor_gc

let __rshift__scalar_out_ ~(out @ local) (self @ local) other =
  stubs___rshift__scalar_out_ (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let __rshift__tensor_ (self @ local) (other @ local) =
  stubs___rshift__tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let __rshift__tensor_out_ ~(out @ local) (self @ local) (other @ local) =
  stubs___rshift__tensor_out_ (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let __xor__ (self @ local) other =
  stubs___xor__ (globalize_gc_tensor self) other |> with_tensor_gc

let __xor__tensor_ (self @ local) (other @ local) =
  stubs___xor__tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let _adaptive_avg_pool2d (self @ local) ~output_size =
  stubs__adaptive_avg_pool2d (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) |> with_tensor_gc

let _adaptive_avg_pool2d_backward ~(grad_output @ local) (self @ local) =
  stubs__adaptive_avg_pool2d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) |> with_tensor_gc

let _adaptive_avg_pool2d_backward_out ~(out @ local) ~(grad_output @ local) (self @ local) =
  stubs__adaptive_avg_pool2d_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) |> with_tensor_gc

let _adaptive_avg_pool2d_out ~(out @ local) (self @ local) ~output_size =
  stubs__adaptive_avg_pool2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) |> with_tensor_gc

let _adaptive_avg_pool3d (self @ local) ~output_size =
  stubs__adaptive_avg_pool3d (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) |> with_tensor_gc

let _adaptive_avg_pool3d_backward ~(grad_output @ local) (self @ local) =
  stubs__adaptive_avg_pool3d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) |> with_tensor_gc

let _adaptive_avg_pool3d_backward_out ~(out @ local) ~(grad_output @ local) (self @ local) =
  stubs__adaptive_avg_pool3d_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) |> with_tensor_gc

let _adaptive_avg_pool3d_out ~(out @ local) (self @ local) ~output_size =
  stubs__adaptive_avg_pool3d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) |> with_tensor_gc

let _add_batch_dim (self @ local) ~batch_dim ~level =
  stubs__add_batch_dim (globalize_gc_tensor self) (Int64.of_int batch_dim) (Int64.of_int level) |> with_tensor_gc

let _add_relu ?alpha (self @ local) (other @ local) =
  stubs__add_relu (globalize_gc_tensor self) (globalize_gc_tensor other) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let _add_relu_ ?alpha (self @ local) (other @ local) =
  stubs__add_relu_ (globalize_gc_tensor self) (globalize_gc_tensor other) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let _add_relu_out ?alpha ~(out @ local) (self @ local) (other @ local) =
  stubs__add_relu_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let _add_relu_scalar ?alpha (self @ local) other =
  stubs__add_relu_scalar (globalize_gc_tensor self) other (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let _add_relu_scalar_ ?alpha (self @ local) other =
  stubs__add_relu_scalar_ (globalize_gc_tensor self) other (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let _add_relu_scalar_out ?alpha ~(out @ local) (self @ local) other =
  stubs__add_relu_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let _addmm_activation ?beta ?alpha (self @ local) ~(mat1 @ local) ~(mat2 @ local) ~use_gelu =
  stubs__addmm_activation (globalize_gc_tensor self) (globalize_gc_tensor mat1) (globalize_gc_tensor mat2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) (if use_gelu then 1 else 0) |> with_tensor_gc

let _addmm_activation_out ?beta ?alpha ~(out @ local) (self @ local) ~(mat1 @ local) ~(mat2 @ local) ~use_gelu =
  stubs__addmm_activation_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mat1) (globalize_gc_tensor mat2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) (if use_gelu then 1 else 0) |> with_tensor_gc

let _aminmax (self @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs__aminmax (CArray.start out__) (globalize_gc_tensor self);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _aminmax_dim (self @ local) ~dim ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs__aminmax_dim (CArray.start out__) (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _aminmax_dim_out ~(out0 @ local) ~(out1 @ local) (self @ local) ~dim ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs__aminmax_dim_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _aminmax_out ~(out0 @ local) ~(out1 @ local) (self @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs__aminmax_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor self);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _amp_update_scale (self @ local) ~(growth_tracker @ local) ~(found_inf @ local) ~scale_growth_factor ~scale_backoff_factor ~growth_interval =
  let out__ = CArray.make raw_tensor 2 in
  stubs__amp_update_scale (CArray.start out__) (globalize_gc_tensor self) (globalize_gc_tensor growth_tracker) (globalize_gc_tensor found_inf) scale_growth_factor scale_backoff_factor (Int64.of_int growth_interval);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _amp_update_scale_ (self @ local) ~(growth_tracker @ local) ~(found_inf @ local) ~scale_growth_factor ~scale_backoff_factor ~growth_interval =
  stubs__amp_update_scale_ (globalize_gc_tensor self) (globalize_gc_tensor growth_tracker) (globalize_gc_tensor found_inf) scale_growth_factor scale_backoff_factor (Int64.of_int growth_interval) |> with_tensor_gc

let _amp_update_scale_out ~(out @ local) (self @ local) ~(growth_tracker @ local) ~(found_inf @ local) ~scale_growth_factor ~scale_backoff_factor ~growth_interval =
  stubs__amp_update_scale_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor growth_tracker) (globalize_gc_tensor found_inf) scale_growth_factor scale_backoff_factor (Int64.of_int growth_interval) |> with_tensor_gc

let _assert_scalar self ~assert_msg =
  stubs__assert_scalar self assert_msg

let _assert_tensor_metadata ~(a @ local) ~size ~stride ~dtype ~device =
  stubs__assert_tensor_metadata (globalize_gc_tensor a) (match size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match size with | None -> -1 | Some v -> List.length v) (match stride with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match stride with | None -> -1 | Some v -> List.length v) (Kind.packed_to_int dtype)  (Device.option_to_int device) 

let _autocast_to_full_precision (self @ local) ~cuda_enabled ~cpu_enabled =
  stubs__autocast_to_full_precision (globalize_gc_tensor self) (if cuda_enabled then 1 else 0) (if cpu_enabled then 1 else 0) |> with_tensor_gc

let _autocast_to_reduced_precision (self @ local) ~cuda_enabled ~cpu_enabled ~cuda_dtype ~cpu_dtype =
  stubs__autocast_to_reduced_precision (globalize_gc_tensor self) (if cuda_enabled then 1 else 0) (if cpu_enabled then 1 else 0) (Kind.packed_to_int cuda_dtype) (Kind.packed_to_int cpu_dtype) |> with_tensor_gc

let _batch_norm_no_update (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~momentum ~eps =
  let out__ = CArray.make raw_tensor 4 in
  stubs__batch_norm_no_update (CArray.start out__) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) momentum eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let _batch_norm_no_update_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) ~(out3 @ local) (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~momentum ~eps =
  let out__ = CArray.make raw_tensor 4 in
  stubs__batch_norm_no_update_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor out3) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) momentum eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let _batch_norm_with_update (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~momentum ~eps =
  let out__ = CArray.make raw_tensor 4 in
  stubs__batch_norm_with_update (CArray.start out__) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor running_mean) (globalize_gc_tensor running_var) momentum eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let _batch_norm_with_update_functional (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~momentum ~eps =
  let out__ = CArray.make raw_tensor 6 in
  stubs__batch_norm_with_update_functional (CArray.start out__) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor running_mean) (globalize_gc_tensor running_var) momentum eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  let t5 = CArray.get out__ 5 |> with_tensor_gc in
  t0, t1, t2, t3, t4, t5

let _batch_norm_with_update_out ~(out @ local) ~(save_mean @ local) ~(save_invstd @ local) ~(reserve @ local) (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~momentum ~eps =
  let out__ = CArray.make raw_tensor 4 in
  stubs__batch_norm_with_update_out (CArray.start out__) (globalize_gc_tensor out) (globalize_gc_tensor save_mean) (globalize_gc_tensor save_invstd) (globalize_gc_tensor reserve) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor running_mean) (globalize_gc_tensor running_var) momentum eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let _cast_byte (self @ local) ~non_blocking =
  stubs__cast_byte (globalize_gc_tensor self) (if non_blocking then 1 else 0) |> with_tensor_gc

let _cast_char (self @ local) ~non_blocking =
  stubs__cast_char (globalize_gc_tensor self) (if non_blocking then 1 else 0) |> with_tensor_gc

let _cast_double (self @ local) ~non_blocking =
  stubs__cast_double (globalize_gc_tensor self) (if non_blocking then 1 else 0) |> with_tensor_gc

let _cast_float (self @ local) ~non_blocking =
  stubs__cast_float (globalize_gc_tensor self) (if non_blocking then 1 else 0) |> with_tensor_gc

let _cast_half (self @ local) ~non_blocking =
  stubs__cast_half (globalize_gc_tensor self) (if non_blocking then 1 else 0) |> with_tensor_gc

let _cast_int (self @ local) ~non_blocking =
  stubs__cast_int (globalize_gc_tensor self) (if non_blocking then 1 else 0) |> with_tensor_gc

let _cast_long (self @ local) ~non_blocking =
  stubs__cast_long (globalize_gc_tensor self) (if non_blocking then 1 else 0) |> with_tensor_gc

let _cast_short (self @ local) ~non_blocking =
  stubs__cast_short (globalize_gc_tensor self) (if non_blocking then 1 else 0) |> with_tensor_gc

let _cdist_backward ~(grad @ local) ~(x1 @ local) ~(x2 @ local) ~p ~(cdist @ local) =
  stubs__cdist_backward (globalize_gc_tensor grad) (globalize_gc_tensor x1) (globalize_gc_tensor x2) p (globalize_gc_tensor cdist) |> with_tensor_gc

let _cdist_backward_out ~(out @ local) ~(grad @ local) ~(x1 @ local) ~(x2 @ local) ~p ~(cdist @ local) =
  stubs__cdist_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad) (globalize_gc_tensor x1) (globalize_gc_tensor x2) p (globalize_gc_tensor cdist) |> with_tensor_gc

let _cholesky_solve_helper (self @ local) ~(a @ local) ~upper =
  stubs__cholesky_solve_helper (globalize_gc_tensor self) (globalize_gc_tensor a) (if upper then 1 else 0) |> with_tensor_gc

let _cholesky_solve_helper_out ~(out @ local) (self @ local) ~(a @ local) ~upper =
  stubs__cholesky_solve_helper_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor a) (if upper then 1 else 0) |> with_tensor_gc

let _chunk_cat (tensors @ local) ~dim ~num_chunks =
  stubs__chunk_cat (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) (Int64.of_int dim) (Int64.of_int num_chunks) |> with_tensor_gc

let _chunk_cat_out ~(out @ local) (tensors @ local) ~dim ~num_chunks =
  stubs__chunk_cat_out (globalize_gc_tensor out) (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) (Int64.of_int dim) (Int64.of_int num_chunks) |> with_tensor_gc

let _coalesce (self @ local) =
  stubs__coalesce (globalize_gc_tensor self) |> with_tensor_gc

let _coalesce_out ~(out @ local) (self @ local) =
  stubs__coalesce_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let _coalesced (self @ local) ~coalesced =
  stubs__coalesced (globalize_gc_tensor self) (if coalesced then 1 else 0) |> with_tensor_gc

let _coalesced_ (self @ local) ~coalesced =
  stubs__coalesced_ (globalize_gc_tensor self) (if coalesced then 1 else 0) |> with_tensor_gc

let _coalesced_out ~(out @ local) (self @ local) ~coalesced =
  stubs__coalesced_out (globalize_gc_tensor out) (globalize_gc_tensor self) (if coalesced then 1 else 0) |> with_tensor_gc

let _compute_linear_combination (input @ local) ~(coefficients @ local) =
  stubs__compute_linear_combination (globalize_gc_tensor input) (globalize_gc_tensor coefficients) |> with_tensor_gc

let _compute_linear_combination_out ~(out @ local) (input @ local) ~(coefficients @ local) =
  stubs__compute_linear_combination_out (globalize_gc_tensor out) (globalize_gc_tensor input) (globalize_gc_tensor coefficients) |> with_tensor_gc

let _conj (self @ local) =
  stubs__conj (globalize_gc_tensor self) |> with_tensor_gc

let _conj_copy (self @ local) =
  stubs__conj_copy (globalize_gc_tensor self) |> with_tensor_gc

let _conj_copy_out ~(out @ local) (self @ local) =
  stubs__conj_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let _conj_physical (self @ local) =
  stubs__conj_physical (globalize_gc_tensor self) |> with_tensor_gc

let _conj_physical_out ~(out @ local) (self @ local) =
  stubs__conj_physical_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let _conv_depthwise2d (self @ local) ~(weight @ local) ~kernel_size ~(bias @ local) ~stride ~padding ~dilation =
  stubs__conv_depthwise2d (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) |> with_tensor_gc

let _conv_depthwise2d_out ~(out @ local) (self @ local) ~(weight @ local) ~kernel_size ~(bias @ local) ~stride ~padding ~dilation =
  stubs__conv_depthwise2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) |> with_tensor_gc

let _convert_indices_from_coo_to_csr (self @ local) ~size ~out_int32 =
  stubs__convert_indices_from_coo_to_csr (globalize_gc_tensor self) (Int64.of_int size) (if out_int32 then 1 else 0) |> with_tensor_gc

let _convert_indices_from_coo_to_csr_out ~(out @ local) (self @ local) ~size ~out_int32 =
  stubs__convert_indices_from_coo_to_csr_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int size) (if out_int32 then 1 else 0) |> with_tensor_gc

let _convert_indices_from_csr_to_coo ~(crow_indices @ local) ~(col_indices @ local) ~out_int32 ~transpose =
  stubs__convert_indices_from_csr_to_coo (globalize_gc_tensor crow_indices) (globalize_gc_tensor col_indices) (if out_int32 then 1 else 0) (if transpose then 1 else 0) |> with_tensor_gc

let _convert_indices_from_csr_to_coo_out ~(out @ local) ~(crow_indices @ local) ~(col_indices @ local) ~out_int32 ~transpose =
  stubs__convert_indices_from_csr_to_coo_out (globalize_gc_tensor out) (globalize_gc_tensor crow_indices) (globalize_gc_tensor col_indices) (if out_int32 then 1 else 0) (if transpose then 1 else 0) |> with_tensor_gc

let _convert_weight_to_int4pack (self @ local) ~innerktiles =
  stubs__convert_weight_to_int4pack (globalize_gc_tensor self) (Int64.of_int innerktiles) |> with_tensor_gc

let _convert_weight_to_int4pack_for_cpu (self @ local) ~innerktiles =
  stubs__convert_weight_to_int4pack_for_cpu (globalize_gc_tensor self) (Int64.of_int innerktiles) |> with_tensor_gc

let _convolution (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~transposed ~output_padding ~groups ~benchmark ~deterministic ~cudnn_enabled ~allow_tf32 =
  stubs__convolution (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if transposed then 1 else 0) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (Int64.of_int groups) (if benchmark then 1 else 0) (if deterministic then 1 else 0) (if cudnn_enabled then 1 else 0) (if allow_tf32 then 1 else 0) |> with_tensor_gc

let _convolution_deprecated (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~transposed ~output_padding ~groups ~benchmark ~deterministic ~cudnn_enabled =
  stubs__convolution_deprecated (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if transposed then 1 else 0) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (Int64.of_int groups) (if benchmark then 1 else 0) (if deterministic then 1 else 0) (if cudnn_enabled then 1 else 0) |> with_tensor_gc

let _convolution_mode (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~groups =
  stubs__convolution_mode (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) padding (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let _convolution_out ~(out @ local) (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~transposed ~output_padding ~groups ~benchmark ~deterministic ~cudnn_enabled ~allow_tf32 =
  stubs__convolution_out (globalize_gc_tensor out) (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if transposed then 1 else 0) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (Int64.of_int groups) (if benchmark then 1 else 0) (if deterministic then 1 else 0) (if cudnn_enabled then 1 else 0) (if allow_tf32 then 1 else 0) |> with_tensor_gc

let _copy_from (self @ local) ~(dst @ local) ~non_blocking =
  stubs__copy_from (globalize_gc_tensor self) (globalize_gc_tensor dst) (if non_blocking then 1 else 0) |> with_tensor_gc

let _copy_from_and_resize (self @ local) ~(dst @ local) =
  stubs__copy_from_and_resize (globalize_gc_tensor self) (globalize_gc_tensor dst) |> with_tensor_gc

let _copy_from_and_resize_out ~(out @ local) (self @ local) ~(dst @ local) =
  stubs__copy_from_and_resize_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor dst) |> with_tensor_gc

let _copy_from_out ~(out @ local) (self @ local) ~(dst @ local) ~non_blocking =
  stubs__copy_from_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor dst) (if non_blocking then 1 else 0) |> with_tensor_gc

let _cslt_compress (input @ local) =
  stubs__cslt_compress (globalize_gc_tensor input) |> with_tensor_gc

let _cslt_sparse_mm ~(compressed_a @ local) ~(dense_b @ local) ~(bias @ local) ~(alpha @ local) ~out_dtype ~transpose_result ~alg_id ~split_k ~split_k_mode =
  stubs__cslt_sparse_mm (globalize_gc_tensor compressed_a) (globalize_gc_tensor dense_b) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor alpha) with | Some v -> v | None -> none_gc_tensor) (Kind.packed_to_int out_dtype) (if transpose_result then 1 else 0) (Int64.of_int alg_id) (Int64.of_int split_k) (Int64.of_int split_k_mode) |> with_tensor_gc

let _cslt_sparse_mm_search ~(compressed_a @ local) ~(dense_b @ local) ~(bias @ local) ~(alpha @ local) ~out_dtype ~transpose_result =
  stubs__cslt_sparse_mm_search (globalize_gc_tensor compressed_a) (globalize_gc_tensor dense_b) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor alpha) with | Some v -> v | None -> none_gc_tensor) (Kind.packed_to_int out_dtype) (if transpose_result then 1 else 0)

let _ctc_loss ~(log_probs @ local) ~(targets @ local) ~input_lengths ~target_lengths ~blank ~zero_infinity =
  let out__ = CArray.make raw_tensor 2 in
  stubs__ctc_loss (CArray.start out__) (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (List.map Int64.of_int input_lengths |> CArray.of_list int64_t |> CArray.start) (List.length input_lengths) (List.map Int64.of_int target_lengths |> CArray.of_list int64_t |> CArray.start) (List.length target_lengths) (Int64.of_int blank) (if zero_infinity then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _ctc_loss_backward ~(grad @ local) ~(log_probs @ local) ~(targets @ local) ~input_lengths ~target_lengths ~(neg_log_likelihood @ local) ~(log_alpha @ local) ~blank ~zero_infinity =
  stubs__ctc_loss_backward (globalize_gc_tensor grad) (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (List.map Int64.of_int input_lengths |> CArray.of_list int64_t |> CArray.start) (List.length input_lengths) (List.map Int64.of_int target_lengths |> CArray.of_list int64_t |> CArray.start) (List.length target_lengths) (globalize_gc_tensor neg_log_likelihood) (globalize_gc_tensor log_alpha) (Int64.of_int blank) (if zero_infinity then 1 else 0) |> with_tensor_gc

let _ctc_loss_backward_out ~(out @ local) ~(grad @ local) ~(log_probs @ local) ~(targets @ local) ~input_lengths ~target_lengths ~(neg_log_likelihood @ local) ~(log_alpha @ local) ~blank ~zero_infinity =
  stubs__ctc_loss_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad) (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (List.map Int64.of_int input_lengths |> CArray.of_list int64_t |> CArray.start) (List.length input_lengths) (List.map Int64.of_int target_lengths |> CArray.of_list int64_t |> CArray.start) (List.length target_lengths) (globalize_gc_tensor neg_log_likelihood) (globalize_gc_tensor log_alpha) (Int64.of_int blank) (if zero_infinity then 1 else 0) |> with_tensor_gc

let _ctc_loss_backward_tensor ~(grad @ local) ~(log_probs @ local) ~(targets @ local) ~(input_lengths @ local) ~(target_lengths @ local) ~(neg_log_likelihood @ local) ~(log_alpha @ local) ~blank ~zero_infinity =
  stubs__ctc_loss_backward_tensor (globalize_gc_tensor grad) (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (globalize_gc_tensor input_lengths) (globalize_gc_tensor target_lengths) (globalize_gc_tensor neg_log_likelihood) (globalize_gc_tensor log_alpha) (Int64.of_int blank) (if zero_infinity then 1 else 0) |> with_tensor_gc

let _ctc_loss_out ~(out0 @ local) ~(out1 @ local) ~(log_probs @ local) ~(targets @ local) ~input_lengths ~target_lengths ~blank ~zero_infinity =
  let out__ = CArray.make raw_tensor 2 in
  stubs__ctc_loss_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (List.map Int64.of_int input_lengths |> CArray.of_list int64_t |> CArray.start) (List.length input_lengths) (List.map Int64.of_int target_lengths |> CArray.of_list int64_t |> CArray.start) (List.length target_lengths) (Int64.of_int blank) (if zero_infinity then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _ctc_loss_tensor ~(log_probs @ local) ~(targets @ local) ~(input_lengths @ local) ~(target_lengths @ local) ~blank ~zero_infinity =
  let out__ = CArray.make raw_tensor 2 in
  stubs__ctc_loss_tensor (CArray.start out__) (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (globalize_gc_tensor input_lengths) (globalize_gc_tensor target_lengths) (Int64.of_int blank) (if zero_infinity then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _ctc_loss_tensor_out ~(out0 @ local) ~(out1 @ local) ~(log_probs @ local) ~(targets @ local) ~(input_lengths @ local) ~(target_lengths @ local) ~blank ~zero_infinity =
  let out__ = CArray.make raw_tensor 2 in
  stubs__ctc_loss_tensor_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (globalize_gc_tensor input_lengths) (globalize_gc_tensor target_lengths) (Int64.of_int blank) (if zero_infinity then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _cudnn_attention_backward ~(grad_out @ local) ~(query @ local) ~(key @ local) ~(value @ local) ~(out @ local) ~(logsumexp @ local) ~(philox_seed @ local) ~(philox_offset @ local) ~(attn_bias @ local) ~(cum_seq_q @ local) ~(cum_seq_k @ local) ~max_q ~max_k ~dropout_p ~is_causal ~scale =
  let out__ = CArray.make raw_tensor 3 in
  stubs__cudnn_attention_backward (CArray.start out__) (globalize_gc_tensor grad_out) (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) (globalize_gc_tensor out) (globalize_gc_tensor logsumexp) (globalize_gc_tensor philox_seed) (globalize_gc_tensor philox_offset) (globalize_gc_tensor attn_bias) (globalize_gc_tensor cum_seq_q) (globalize_gc_tensor cum_seq_k) (Int64.of_int max_q) (Int64.of_int max_k) dropout_p (if is_causal then 1 else 0)  (Option.value scale ~default:0.0) (match scale with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _cudnn_ctc_loss ~(log_probs @ local) ~(targets @ local) ~input_lengths ~target_lengths ~blank ~deterministic ~zero_infinity =
  let out__ = CArray.make raw_tensor 2 in
  stubs__cudnn_ctc_loss (CArray.start out__) (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (List.map Int64.of_int input_lengths |> CArray.of_list int64_t |> CArray.start) (List.length input_lengths) (List.map Int64.of_int target_lengths |> CArray.of_list int64_t |> CArray.start) (List.length target_lengths) (Int64.of_int blank) (if deterministic then 1 else 0) (if zero_infinity then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _cudnn_ctc_loss_out ~(out0 @ local) ~(out1 @ local) ~(log_probs @ local) ~(targets @ local) ~input_lengths ~target_lengths ~blank ~deterministic ~zero_infinity =
  let out__ = CArray.make raw_tensor 2 in
  stubs__cudnn_ctc_loss_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (List.map Int64.of_int input_lengths |> CArray.of_list int64_t |> CArray.start) (List.length input_lengths) (List.map Int64.of_int target_lengths |> CArray.of_list int64_t |> CArray.start) (List.length target_lengths) (Int64.of_int blank) (if deterministic then 1 else 0) (if zero_infinity then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _cudnn_ctc_loss_tensor ~(log_probs @ local) ~(targets @ local) ~(input_lengths @ local) ~(target_lengths @ local) ~blank ~deterministic ~zero_infinity =
  let out__ = CArray.make raw_tensor 2 in
  stubs__cudnn_ctc_loss_tensor (CArray.start out__) (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (globalize_gc_tensor input_lengths) (globalize_gc_tensor target_lengths) (Int64.of_int blank) (if deterministic then 1 else 0) (if zero_infinity then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _cudnn_init_dropout_state ~dropout ~train ~dropout_seed ~options =
  stubs__cudnn_init_dropout_state dropout (if train then 1 else 0) (Int64.of_int dropout_seed) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let _cudnn_init_dropout_state_out ~(out @ local) ~dropout ~train ~dropout_seed =
  stubs__cudnn_init_dropout_state_out (globalize_gc_tensor out) dropout (if train then 1 else 0) (Int64.of_int dropout_seed) |> with_tensor_gc

let _cudnn_rnn (input @ local) ~(weight @ local) ~weight_stride0 ~(weight_buf @ local) ~(hx @ local) ~(cx @ local) ~mode ~hidden_size ~proj_size ~num_layers ~batch_first ~dropout ~train ~bidirectional ~batch_sizes ~(dropout_state @ local) =
  let out__ = CArray.make raw_tensor 5 in
  stubs__cudnn_rnn (CArray.start out__) (globalize_gc_tensor input) (CArray.of_list gc_tensor (globalize_gc_tensor_list weight) |> CArray.start) (Base.List.length weight) (Int64.of_int weight_stride0) (match (Base.Option.globalize globalize_gc_tensor weight_buf) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor hx) (match (Base.Option.globalize globalize_gc_tensor cx) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int mode) (Int64.of_int hidden_size) (Int64.of_int proj_size) (Int64.of_int num_layers) (if batch_first then 1 else 0) dropout (if train then 1 else 0) (if bidirectional then 1 else 0) (List.map Int64.of_int batch_sizes |> CArray.of_list int64_t |> CArray.start) (List.length batch_sizes) (match (Base.Option.globalize globalize_gc_tensor dropout_state) with | Some v -> v | None -> none_gc_tensor);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  t0, t1, t2, t3, t4

let _cudnn_rnn_flatten_weight ~(weight_arr @ local) ~weight_stride0 ~input_size ~mode ~hidden_size ~proj_size ~num_layers ~batch_first ~bidirectional =
  stubs__cudnn_rnn_flatten_weight (CArray.of_list gc_tensor (globalize_gc_tensor_list weight_arr) |> CArray.start) (Base.List.length weight_arr) (Int64.of_int weight_stride0) (Int64.of_int input_size) (Int64.of_int mode) (Int64.of_int hidden_size) (Int64.of_int proj_size) (Int64.of_int num_layers) (if batch_first then 1 else 0) (if bidirectional then 1 else 0) |> with_tensor_gc

let _cudnn_rnn_flatten_weight_out ~(out @ local) ~(weight_arr @ local) ~weight_stride0 ~input_size ~mode ~hidden_size ~proj_size ~num_layers ~batch_first ~bidirectional =
  stubs__cudnn_rnn_flatten_weight_out (globalize_gc_tensor out) (CArray.of_list gc_tensor (globalize_gc_tensor_list weight_arr) |> CArray.start) (Base.List.length weight_arr) (Int64.of_int weight_stride0) (Int64.of_int input_size) (Int64.of_int mode) (Int64.of_int hidden_size) (Int64.of_int proj_size) (Int64.of_int num_layers) (if batch_first then 1 else 0) (if bidirectional then 1 else 0) |> with_tensor_gc

let _cudnn_rnn_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) ~(out3 @ local) ~(out4 @ local) (input @ local) ~(weight @ local) ~weight_stride0 ~(weight_buf @ local) ~(hx @ local) ~(cx @ local) ~mode ~hidden_size ~proj_size ~num_layers ~batch_first ~dropout ~train ~bidirectional ~batch_sizes ~(dropout_state @ local) =
  let out__ = CArray.make raw_tensor 5 in
  stubs__cudnn_rnn_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor out3) (globalize_gc_tensor out4) (globalize_gc_tensor input) (CArray.of_list gc_tensor (globalize_gc_tensor_list weight) |> CArray.start) (Base.List.length weight) (Int64.of_int weight_stride0) (match (Base.Option.globalize globalize_gc_tensor weight_buf) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor hx) (match (Base.Option.globalize globalize_gc_tensor cx) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int mode) (Int64.of_int hidden_size) (Int64.of_int proj_size) (Int64.of_int num_layers) (if batch_first then 1 else 0) dropout (if train then 1 else 0) (if bidirectional then 1 else 0) (List.map Int64.of_int batch_sizes |> CArray.of_list int64_t |> CArray.start) (List.length batch_sizes) (match (Base.Option.globalize globalize_gc_tensor dropout_state) with | Some v -> v | None -> none_gc_tensor);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  t0, t1, t2, t3, t4

let _debug_has_internal_overlap (self @ local) =
  stubs__debug_has_internal_overlap (globalize_gc_tensor self)

let _dim_arange ~(like @ local) ~dim =
  stubs__dim_arange (globalize_gc_tensor like) (Int64.of_int dim) |> with_tensor_gc

let _dimi (self @ local) =
  stubs__dimi (globalize_gc_tensor self)

let _dimv (self @ local) =
  stubs__dimv (globalize_gc_tensor self)

let _dirichlet_grad ~(x @ local) ~(alpha @ local) ~(total @ local) =
  stubs__dirichlet_grad (globalize_gc_tensor x) (globalize_gc_tensor alpha) (globalize_gc_tensor total) |> with_tensor_gc

let _dirichlet_grad_out ~(out @ local) ~(x @ local) ~(alpha @ local) ~(total @ local) =
  stubs__dirichlet_grad_out (globalize_gc_tensor out) (globalize_gc_tensor x) (globalize_gc_tensor alpha) (globalize_gc_tensor total) |> with_tensor_gc

let _dyn_quant_matmul_4bit ~(inp @ local) ~(packed_weights @ local) ~block_size ~in_features ~out_features =
  stubs__dyn_quant_matmul_4bit (globalize_gc_tensor inp) (globalize_gc_tensor packed_weights) (Int64.of_int block_size) (Int64.of_int in_features) (Int64.of_int out_features) |> with_tensor_gc

let _dyn_quant_pack_4bit_weight ~(weights @ local) ~(scales_zeros @ local) ~(bias @ local) ~block_size ~in_features ~out_features =
  stubs__dyn_quant_pack_4bit_weight (globalize_gc_tensor weights) (globalize_gc_tensor scales_zeros) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int block_size) (Int64.of_int in_features) (Int64.of_int out_features) |> with_tensor_gc

let _efficient_attention_backward ~(grad_out_ @ local) ~(query @ local) ~(key @ local) ~(value @ local) ~(bias @ local) ~(out @ local) ~(cu_seqlens_q @ local) ~(cu_seqlens_k @ local) ~max_seqlen_q ~max_seqlen_k ~(logsumexp @ local) ~dropout_p ~(philox_seed @ local) ~(philox_offset @ local) ~custom_mask_type ~bias_requires_grad ~scale ~num_splits_key ~window_size ~shared_storage_dqdkdv =
  let out__ = CArray.make raw_tensor 4 in
  stubs__efficient_attention_backward (CArray.start out__) (globalize_gc_tensor grad_out_) (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor out) (match (Base.Option.globalize globalize_gc_tensor cu_seqlens_q) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor cu_seqlens_k) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int max_seqlen_q) (Int64.of_int max_seqlen_k) (globalize_gc_tensor logsumexp) dropout_p (globalize_gc_tensor philox_seed) (globalize_gc_tensor philox_offset) (Int64.of_int custom_mask_type) (if bias_requires_grad then 1 else 0)  (Option.value scale ~default:0.0) (match scale with | Some _ -> 0 | None -> 1)   (match num_splits_key with | None -> Int64.zero | Some v -> Int64.of_int v) (match num_splits_key with | Some _ -> 0 | None -> 1)   (match window_size with | None -> Int64.zero | Some v -> Int64.of_int v) (match window_size with | Some _ -> 0 | None -> 1)  (if shared_storage_dqdkdv then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let _efficientzerotensor ~size ~options =
  stubs__efficientzerotensor (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let _efficientzerotensor_out ~(out @ local) ~size =
  stubs__efficientzerotensor_out (globalize_gc_tensor out) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let _embedding_bag ~(weight @ local) ~(indices @ local) ~(offsets @ local) ~scale_grad_by_freq ~mode ~sparse ~(per_sample_weights @ local) ~include_last_offset ~padding_idx =
  let out__ = CArray.make raw_tensor 4 in
  stubs__embedding_bag (CArray.start out__) (globalize_gc_tensor weight) (globalize_gc_tensor indices) (globalize_gc_tensor offsets) (if scale_grad_by_freq then 1 else 0) (Int64.of_int mode) (if sparse then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor per_sample_weights) with | Some v -> v | None -> none_gc_tensor) (if include_last_offset then 1 else 0) (Int64.of_int padding_idx);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let _embedding_bag_backward ~(grad @ local) ~(indices @ local) ~(offsets @ local) ~(offset2bag @ local) ~(bag_size @ local) ~(maximum_indices @ local) ~num_weights ~scale_grad_by_freq ~mode ~sparse ~(per_sample_weights @ local) ~padding_idx =
  stubs__embedding_bag_backward (globalize_gc_tensor grad) (globalize_gc_tensor indices) (globalize_gc_tensor offsets) (globalize_gc_tensor offset2bag) (globalize_gc_tensor bag_size) (globalize_gc_tensor maximum_indices) (Int64.of_int num_weights) (if scale_grad_by_freq then 1 else 0) (Int64.of_int mode) (if sparse then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor per_sample_weights) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int padding_idx) |> with_tensor_gc

let _embedding_bag_dense_backward ~(grad @ local) ~(indices @ local) ~(offset2bag @ local) ~(bag_size @ local) ~(maximum_indices @ local) ~num_weights ~scale_grad_by_freq ~mode ~(per_sample_weights @ local) ~padding_idx =
  stubs__embedding_bag_dense_backward (globalize_gc_tensor grad) (globalize_gc_tensor indices) (globalize_gc_tensor offset2bag) (globalize_gc_tensor bag_size) (globalize_gc_tensor maximum_indices) (Int64.of_int num_weights) (if scale_grad_by_freq then 1 else 0) (Int64.of_int mode) (match (Base.Option.globalize globalize_gc_tensor per_sample_weights) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int padding_idx) |> with_tensor_gc

let _embedding_bag_dense_backward_out ~(out @ local) ~(grad @ local) ~(indices @ local) ~(offset2bag @ local) ~(bag_size @ local) ~(maximum_indices @ local) ~num_weights ~scale_grad_by_freq ~mode ~(per_sample_weights @ local) ~padding_idx =
  stubs__embedding_bag_dense_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad) (globalize_gc_tensor indices) (globalize_gc_tensor offset2bag) (globalize_gc_tensor bag_size) (globalize_gc_tensor maximum_indices) (Int64.of_int num_weights) (if scale_grad_by_freq then 1 else 0) (Int64.of_int mode) (match (Base.Option.globalize globalize_gc_tensor per_sample_weights) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int padding_idx) |> with_tensor_gc

let _embedding_bag_forward_only ~(weight @ local) ~(indices @ local) ~(offsets @ local) ~scale_grad_by_freq ~mode ~sparse ~(per_sample_weights @ local) ~include_last_offset ~padding_idx =
  let out__ = CArray.make raw_tensor 4 in
  stubs__embedding_bag_forward_only (CArray.start out__) (globalize_gc_tensor weight) (globalize_gc_tensor indices) (globalize_gc_tensor offsets) (if scale_grad_by_freq then 1 else 0) (Int64.of_int mode) (if sparse then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor per_sample_weights) with | Some v -> v | None -> none_gc_tensor) (if include_last_offset then 1 else 0) (Int64.of_int padding_idx);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let _embedding_bag_forward_only_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) ~(out3 @ local) ~(weight @ local) ~(indices @ local) ~(offsets @ local) ~scale_grad_by_freq ~mode ~sparse ~(per_sample_weights @ local) ~include_last_offset ~padding_idx =
  let out__ = CArray.make raw_tensor 4 in
  stubs__embedding_bag_forward_only_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor out3) (globalize_gc_tensor weight) (globalize_gc_tensor indices) (globalize_gc_tensor offsets) (if scale_grad_by_freq then 1 else 0) (Int64.of_int mode) (if sparse then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor per_sample_weights) with | Some v -> v | None -> none_gc_tensor) (if include_last_offset then 1 else 0) (Int64.of_int padding_idx);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let _embedding_bag_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) ~(out3 @ local) ~(weight @ local) ~(indices @ local) ~(offsets @ local) ~scale_grad_by_freq ~mode ~sparse ~(per_sample_weights @ local) ~include_last_offset ~padding_idx =
  let out__ = CArray.make raw_tensor 4 in
  stubs__embedding_bag_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor out3) (globalize_gc_tensor weight) (globalize_gc_tensor indices) (globalize_gc_tensor offsets) (if scale_grad_by_freq then 1 else 0) (Int64.of_int mode) (if sparse then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor per_sample_weights) with | Some v -> v | None -> none_gc_tensor) (if include_last_offset then 1 else 0) (Int64.of_int padding_idx);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let _embedding_bag_per_sample_weights_backward ~(grad @ local) ~(weight @ local) ~(indices @ local) ~(offsets @ local) ~(offset2bag @ local) ~mode ~padding_idx =
  stubs__embedding_bag_per_sample_weights_backward (globalize_gc_tensor grad) (globalize_gc_tensor weight) (globalize_gc_tensor indices) (globalize_gc_tensor offsets) (globalize_gc_tensor offset2bag) (Int64.of_int mode) (Int64.of_int padding_idx) |> with_tensor_gc

let _embedding_bag_per_sample_weights_backward_out ~(out @ local) ~(grad @ local) ~(weight @ local) ~(indices @ local) ~(offsets @ local) ~(offset2bag @ local) ~mode ~padding_idx =
  stubs__embedding_bag_per_sample_weights_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad) (globalize_gc_tensor weight) (globalize_gc_tensor indices) (globalize_gc_tensor offsets) (globalize_gc_tensor offset2bag) (Int64.of_int mode) (Int64.of_int padding_idx) |> with_tensor_gc

let _embedding_bag_sparse_backward ~(grad @ local) ~(indices @ local) ~(offsets @ local) ~(offset2bag @ local) ~(bag_size @ local) ~num_weights ~scale_grad_by_freq ~mode ~(per_sample_weights @ local) ~padding_idx =
  stubs__embedding_bag_sparse_backward (globalize_gc_tensor grad) (globalize_gc_tensor indices) (globalize_gc_tensor offsets) (globalize_gc_tensor offset2bag) (globalize_gc_tensor bag_size) (Int64.of_int num_weights) (if scale_grad_by_freq then 1 else 0) (Int64.of_int mode) (match (Base.Option.globalize globalize_gc_tensor per_sample_weights) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int padding_idx) |> with_tensor_gc

let _empty_affine_quantized ~size ~options ~scale ~zero_point =
  stubs__empty_affine_quantized (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) scale (Int64.of_int zero_point) |> with_tensor_gc

let _empty_affine_quantized_out ~(out @ local) ~size ~scale ~zero_point =
  stubs__empty_affine_quantized_out (globalize_gc_tensor out) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) scale (Int64.of_int zero_point) |> with_tensor_gc

let _empty_per_channel_affine_quantized ~size ~(scales @ local) ~(zero_points @ local) ~axis ~options =
  stubs__empty_per_channel_affine_quantized (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (globalize_gc_tensor scales) (globalize_gc_tensor zero_points) (Int64.of_int axis) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let _empty_per_channel_affine_quantized_out ~(out @ local) ~size ~(scales @ local) ~(zero_points @ local) ~axis =
  stubs__empty_per_channel_affine_quantized_out (globalize_gc_tensor out) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (globalize_gc_tensor scales) (globalize_gc_tensor zero_points) (Int64.of_int axis) |> with_tensor_gc

let _euclidean_dist ~(x1 @ local) ~(x2 @ local) =
  stubs__euclidean_dist (globalize_gc_tensor x1) (globalize_gc_tensor x2) |> with_tensor_gc

let _euclidean_dist_out ~(out @ local) ~(x1 @ local) ~(x2 @ local) =
  stubs__euclidean_dist_out (globalize_gc_tensor out) (globalize_gc_tensor x1) (globalize_gc_tensor x2) |> with_tensor_gc

let _fake_quantize_learnable_per_channel_affine (self @ local) ~(scale @ local) ~(zero_point @ local) ~axis ~quant_min ~quant_max ~grad_factor =
  stubs__fake_quantize_learnable_per_channel_affine (globalize_gc_tensor self) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) (Int64.of_int axis) (Int64.of_int quant_min) (Int64.of_int quant_max) grad_factor |> with_tensor_gc

let _fake_quantize_learnable_per_channel_affine_backward ~(grad @ local) (self @ local) ~(scale @ local) ~(zero_point @ local) ~axis ~quant_min ~quant_max ~grad_factor =
  let out__ = CArray.make raw_tensor 3 in
  stubs__fake_quantize_learnable_per_channel_affine_backward (CArray.start out__) (globalize_gc_tensor grad) (globalize_gc_tensor self) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) (Int64.of_int axis) (Int64.of_int quant_min) (Int64.of_int quant_max) grad_factor;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _fake_quantize_learnable_per_channel_affine_out ~(out @ local) (self @ local) ~(scale @ local) ~(zero_point @ local) ~axis ~quant_min ~quant_max ~grad_factor =
  stubs__fake_quantize_learnable_per_channel_affine_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) (Int64.of_int axis) (Int64.of_int quant_min) (Int64.of_int quant_max) grad_factor |> with_tensor_gc

let _fake_quantize_learnable_per_tensor_affine (self @ local) ~(scale @ local) ~(zero_point @ local) ~quant_min ~quant_max ~grad_factor =
  stubs__fake_quantize_learnable_per_tensor_affine (globalize_gc_tensor self) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) (Int64.of_int quant_min) (Int64.of_int quant_max) grad_factor |> with_tensor_gc

let _fake_quantize_learnable_per_tensor_affine_backward ~(grad @ local) (self @ local) ~(scale @ local) ~(zero_point @ local) ~quant_min ~quant_max ~grad_factor =
  let out__ = CArray.make raw_tensor 3 in
  stubs__fake_quantize_learnable_per_tensor_affine_backward (CArray.start out__) (globalize_gc_tensor grad) (globalize_gc_tensor self) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) (Int64.of_int quant_min) (Int64.of_int quant_max) grad_factor;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _fake_quantize_learnable_per_tensor_affine_out ~(out @ local) (self @ local) ~(scale @ local) ~(zero_point @ local) ~quant_min ~quant_max ~grad_factor =
  stubs__fake_quantize_learnable_per_tensor_affine_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) (Int64.of_int quant_min) (Int64.of_int quant_max) grad_factor |> with_tensor_gc

let _fake_quantize_per_tensor_affine_cachemask_tensor_qparams (self @ local) ~(scale @ local) ~(zero_point @ local) ~(fake_quant_enabled @ local) ~quant_min ~quant_max =
  let out__ = CArray.make raw_tensor 2 in
  stubs__fake_quantize_per_tensor_affine_cachemask_tensor_qparams (CArray.start out__) (globalize_gc_tensor self) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) (globalize_gc_tensor fake_quant_enabled) (Int64.of_int quant_min) (Int64.of_int quant_max);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _fake_quantize_per_tensor_affine_cachemask_tensor_qparams_out ~(out0 @ local) ~(out1 @ local) (self @ local) ~(scale @ local) ~(zero_point @ local) ~(fake_quant_enabled @ local) ~quant_min ~quant_max =
  let out__ = CArray.make raw_tensor 2 in
  stubs__fake_quantize_per_tensor_affine_cachemask_tensor_qparams_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor self) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) (globalize_gc_tensor fake_quant_enabled) (Int64.of_int quant_min) (Int64.of_int quant_max);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _fft_c2c (self @ local) ~dim ~normalization ~forward =
  stubs__fft_c2c (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (Int64.of_int normalization) (if forward then 1 else 0) |> with_tensor_gc

let _fft_c2c_out ~(out @ local) (self @ local) ~dim ~normalization ~forward =
  stubs__fft_c2c_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (Int64.of_int normalization) (if forward then 1 else 0) |> with_tensor_gc

let _fft_c2r (self @ local) ~dim ~normalization ~last_dim_size =
  stubs__fft_c2r (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (Int64.of_int normalization) (Int64.of_int last_dim_size) |> with_tensor_gc

let _fft_c2r_out ~(out @ local) (self @ local) ~dim ~normalization ~last_dim_size =
  stubs__fft_c2r_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (Int64.of_int normalization) (Int64.of_int last_dim_size) |> with_tensor_gc

let _fft_r2c (self @ local) ~dim ~normalization ~onesided =
  stubs__fft_r2c (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (Int64.of_int normalization) (if onesided then 1 else 0) |> with_tensor_gc

let _fft_r2c_out ~(out @ local) (self @ local) ~dim ~normalization ~onesided =
  stubs__fft_r2c_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (Int64.of_int normalization) (if onesided then 1 else 0) |> with_tensor_gc

let _fill_mem_eff_dropout_mask_ (self @ local) ~dropout_p ~seed ~offset =
  stubs__fill_mem_eff_dropout_mask_ (globalize_gc_tensor self) dropout_p (Int64.of_int seed) (Int64.of_int offset) |> with_tensor_gc

let _flash_attention_backward ~(grad_out @ local) ~(query @ local) ~(key @ local) ~(value @ local) ~(out @ local) ~(logsumexp @ local) ~(cum_seq_q @ local) ~(cum_seq_k @ local) ~max_q ~max_k ~dropout_p ~is_causal ~(rng_state @ local) ~(unused @ local) ~scale ~window_size_left ~window_size_right =
  let out__ = CArray.make raw_tensor 3 in
  stubs__flash_attention_backward (CArray.start out__) (globalize_gc_tensor grad_out) (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) (globalize_gc_tensor out) (globalize_gc_tensor logsumexp) (globalize_gc_tensor cum_seq_q) (globalize_gc_tensor cum_seq_k) (Int64.of_int max_q) (Int64.of_int max_k) dropout_p (if is_causal then 1 else 0) (globalize_gc_tensor rng_state) (globalize_gc_tensor unused)  (Option.value scale ~default:0.0) (match scale with | Some _ -> 0 | None -> 1)   (match window_size_left with | None -> Int64.zero | Some v -> Int64.of_int v) (match window_size_left with | Some _ -> 0 | None -> 1)   (match window_size_right with | None -> Int64.zero | Some v -> Int64.of_int v) (match window_size_right with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _flash_attention_forward_no_dropout_inplace ~(out @ local) ~(query @ local) ~(key @ local) ~(value @ local) ~(cum_seq_q @ local) ~(cum_seq_k @ local) ~max_q ~max_k ~dropout_p ~is_causal ~return_debug_mask ~scale ~window_size_left ~window_size_right ~(seqused_k @ local) ~(alibi_slopes @ local) ~(block_table @ local) ~num_splits =
  stubs__flash_attention_forward_no_dropout_inplace (globalize_gc_tensor out) (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) (match (Base.Option.globalize globalize_gc_tensor cum_seq_q) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor cum_seq_k) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int max_q) (Int64.of_int max_k) dropout_p (if is_causal then 1 else 0) (if return_debug_mask then 1 else 0)  (Option.value scale ~default:0.0) (match scale with | Some _ -> 0 | None -> 1)   (match window_size_left with | None -> Int64.zero | Some v -> Int64.of_int v) (match window_size_left with | Some _ -> 0 | None -> 1)   (match window_size_right with | None -> Int64.zero | Some v -> Int64.of_int v) (match window_size_right with | Some _ -> 0 | None -> 1)  (match (Base.Option.globalize globalize_gc_tensor seqused_k) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor alibi_slopes) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor block_table) with | Some v -> v | None -> none_gc_tensor)  (match num_splits with | None -> Int64.zero | Some v -> Int64.of_int v) (match num_splits with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _foobar (self @ local) ~arg1 ~arg2 ~arg3 =
  stubs__foobar (globalize_gc_tensor self) (if arg1 then 1 else 0) (if arg2 then 1 else 0) (if arg3 then 1 else 0) |> with_tensor_gc

let _foobar_out ~(out @ local) (self @ local) ~arg1 ~arg2 ~arg3 =
  stubs__foobar_out (globalize_gc_tensor out) (globalize_gc_tensor self) (if arg1 then 1 else 0) (if arg2 then 1 else 0) (if arg3 then 1 else 0) |> with_tensor_gc

let _functional_assert_async (self @ local) ~assert_msg ~(dep_token @ local) =
  stubs__functional_assert_async (globalize_gc_tensor self) assert_msg (globalize_gc_tensor dep_token) |> with_tensor_gc

let _functional_assert_scalar self ~assert_msg ~(dep_token @ local) =
  stubs__functional_assert_scalar self assert_msg (globalize_gc_tensor dep_token) |> with_tensor_gc

let _functional_sym_constrain_range ~size ~min ~max ~(dep_token @ local) =
  stubs__functional_sym_constrain_range size  (match min with | None -> Int64.zero | Some v -> Int64.of_int v) (match min with | Some _ -> 0 | None -> 1)   (match max with | None -> Int64.zero | Some v -> Int64.of_int v) (match max with | Some _ -> 0 | None -> 1)  (globalize_gc_tensor dep_token) |> with_tensor_gc

let _functional_sym_constrain_range_for_size ~size ~min ~max ~(dep_token @ local) =
  stubs__functional_sym_constrain_range_for_size size  (match min with | None -> Int64.zero | Some v -> Int64.of_int v) (match min with | Some _ -> 0 | None -> 1)   (match max with | None -> Int64.zero | Some v -> Int64.of_int v) (match max with | Some _ -> 0 | None -> 1)  (globalize_gc_tensor dep_token) |> with_tensor_gc

let _fused_adagrad ~(out @ local) (self @ local) ~(grads @ local) ~(state_sums @ local) ~(state_steps @ local) ~lr ~lr_decay ~weight_decay ~eps ~maximize ~(grad_scale @ local) ~(found_inf @ local) =
  stubs__fused_adagrad (CArray.of_list gc_tensor (globalize_gc_tensor_list out) |> CArray.start) (Base.List.length out) (CArray.of_list gc_tensor (globalize_gc_tensor_list self) |> CArray.start) (Base.List.length self) (CArray.of_list gc_tensor (globalize_gc_tensor_list grads) |> CArray.start) (Base.List.length grads) (CArray.of_list gc_tensor (globalize_gc_tensor_list state_sums) |> CArray.start) (Base.List.length state_sums) (CArray.of_list gc_tensor (globalize_gc_tensor_list state_steps) |> CArray.start) (Base.List.length state_steps) lr lr_decay weight_decay eps (if maximize then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor grad_scale) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor found_inf) with | Some v -> v | None -> none_gc_tensor)

let _fused_adagrad_ (self @ local) ~(grads @ local) ~(state_sums @ local) ~(state_steps @ local) ~lr ~lr_decay ~weight_decay ~eps ~maximize ~(grad_scale @ local) ~(found_inf @ local) =
  stubs__fused_adagrad_ (CArray.of_list gc_tensor (globalize_gc_tensor_list self) |> CArray.start) (Base.List.length self) (CArray.of_list gc_tensor (globalize_gc_tensor_list grads) |> CArray.start) (Base.List.length grads) (CArray.of_list gc_tensor (globalize_gc_tensor_list state_sums) |> CArray.start) (Base.List.length state_sums) (CArray.of_list gc_tensor (globalize_gc_tensor_list state_steps) |> CArray.start) (Base.List.length state_steps) lr lr_decay weight_decay eps (if maximize then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor grad_scale) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor found_inf) with | Some v -> v | None -> none_gc_tensor)

let _fused_adagrad_tensor_lr_ (self @ local) ~(grads @ local) ~(state_sums @ local) ~(state_steps @ local) ~(lr @ local) ~lr_decay ~weight_decay ~eps ~maximize ~(grad_scale @ local) ~(found_inf @ local) =
  stubs__fused_adagrad_tensor_lr_ (CArray.of_list gc_tensor (globalize_gc_tensor_list self) |> CArray.start) (Base.List.length self) (CArray.of_list gc_tensor (globalize_gc_tensor_list grads) |> CArray.start) (Base.List.length grads) (CArray.of_list gc_tensor (globalize_gc_tensor_list state_sums) |> CArray.start) (Base.List.length state_sums) (CArray.of_list gc_tensor (globalize_gc_tensor_list state_steps) |> CArray.start) (Base.List.length state_steps) (globalize_gc_tensor lr) lr_decay weight_decay eps (if maximize then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor grad_scale) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor found_inf) with | Some v -> v | None -> none_gc_tensor)

let _fused_adagrad_tensor_lr_out ~(out @ local) (self @ local) ~(grads @ local) ~(state_sums @ local) ~(state_steps @ local) ~(lr @ local) ~lr_decay ~weight_decay ~eps ~maximize ~(grad_scale @ local) ~(found_inf @ local) =
  stubs__fused_adagrad_tensor_lr_out (CArray.of_list gc_tensor (globalize_gc_tensor_list out) |> CArray.start) (Base.List.length out) (CArray.of_list gc_tensor (globalize_gc_tensor_list self) |> CArray.start) (Base.List.length self) (CArray.of_list gc_tensor (globalize_gc_tensor_list grads) |> CArray.start) (Base.List.length grads) (CArray.of_list gc_tensor (globalize_gc_tensor_list state_sums) |> CArray.start) (Base.List.length state_sums) (CArray.of_list gc_tensor (globalize_gc_tensor_list state_steps) |> CArray.start) (Base.List.length state_steps) (globalize_gc_tensor lr) lr_decay weight_decay eps (if maximize then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor grad_scale) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor found_inf) with | Some v -> v | None -> none_gc_tensor)

let _fused_adam ~(out @ local) (self @ local) ~(grads @ local) ~(exp_avgs @ local) ~(exp_avg_sqs @ local) ~(max_exp_avg_sqs @ local) ~(state_steps @ local) ~lr ~beta1 ~beta2 ~weight_decay ~eps ~amsgrad ~maximize ~(grad_scale @ local) ~(found_inf @ local) =
  stubs__fused_adam (CArray.of_list gc_tensor (globalize_gc_tensor_list out) |> CArray.start) (Base.List.length out) (CArray.of_list gc_tensor (globalize_gc_tensor_list self) |> CArray.start) (Base.List.length self) (CArray.of_list gc_tensor (globalize_gc_tensor_list grads) |> CArray.start) (Base.List.length grads) (CArray.of_list gc_tensor (globalize_gc_tensor_list exp_avgs) |> CArray.start) (Base.List.length exp_avgs) (CArray.of_list gc_tensor (globalize_gc_tensor_list exp_avg_sqs) |> CArray.start) (Base.List.length exp_avg_sqs) (CArray.of_list gc_tensor (globalize_gc_tensor_list max_exp_avg_sqs) |> CArray.start) (Base.List.length max_exp_avg_sqs) (CArray.of_list gc_tensor (globalize_gc_tensor_list state_steps) |> CArray.start) (Base.List.length state_steps) lr beta1 beta2 weight_decay eps (if amsgrad then 1 else 0) (if maximize then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor grad_scale) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor found_inf) with | Some v -> v | None -> none_gc_tensor)

let _fused_adam_ (self @ local) ~(grads @ local) ~(exp_avgs @ local) ~(exp_avg_sqs @ local) ~(max_exp_avg_sqs @ local) ~(state_steps @ local) ~lr ~beta1 ~beta2 ~weight_decay ~eps ~amsgrad ~maximize ~(grad_scale @ local) ~(found_inf @ local) =
  stubs__fused_adam_ (CArray.of_list gc_tensor (globalize_gc_tensor_list self) |> CArray.start) (Base.List.length self) (CArray.of_list gc_tensor (globalize_gc_tensor_list grads) |> CArray.start) (Base.List.length grads) (CArray.of_list gc_tensor (globalize_gc_tensor_list exp_avgs) |> CArray.start) (Base.List.length exp_avgs) (CArray.of_list gc_tensor (globalize_gc_tensor_list exp_avg_sqs) |> CArray.start) (Base.List.length exp_avg_sqs) (CArray.of_list gc_tensor (globalize_gc_tensor_list max_exp_avg_sqs) |> CArray.start) (Base.List.length max_exp_avg_sqs) (CArray.of_list gc_tensor (globalize_gc_tensor_list state_steps) |> CArray.start) (Base.List.length state_steps) lr beta1 beta2 weight_decay eps (if amsgrad then 1 else 0) (if maximize then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor grad_scale) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor found_inf) with | Some v -> v | None -> none_gc_tensor)

let _fused_adam_tensor_lr_ (self @ local) ~(grads @ local) ~(exp_avgs @ local) ~(exp_avg_sqs @ local) ~(max_exp_avg_sqs @ local) ~(state_steps @ local) ~(lr @ local) ~beta1 ~beta2 ~weight_decay ~eps ~amsgrad ~maximize ~(grad_scale @ local) ~(found_inf @ local) =
  stubs__fused_adam_tensor_lr_ (CArray.of_list gc_tensor (globalize_gc_tensor_list self) |> CArray.start) (Base.List.length self) (CArray.of_list gc_tensor (globalize_gc_tensor_list grads) |> CArray.start) (Base.List.length grads) (CArray.of_list gc_tensor (globalize_gc_tensor_list exp_avgs) |> CArray.start) (Base.List.length exp_avgs) (CArray.of_list gc_tensor (globalize_gc_tensor_list exp_avg_sqs) |> CArray.start) (Base.List.length exp_avg_sqs) (CArray.of_list gc_tensor (globalize_gc_tensor_list max_exp_avg_sqs) |> CArray.start) (Base.List.length max_exp_avg_sqs) (CArray.of_list gc_tensor (globalize_gc_tensor_list state_steps) |> CArray.start) (Base.List.length state_steps) (globalize_gc_tensor lr) beta1 beta2 weight_decay eps (if amsgrad then 1 else 0) (if maximize then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor grad_scale) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor found_inf) with | Some v -> v | None -> none_gc_tensor)

let _fused_adam_tensor_lr_out ~(out @ local) (self @ local) ~(grads @ local) ~(exp_avgs @ local) ~(exp_avg_sqs @ local) ~(max_exp_avg_sqs @ local) ~(state_steps @ local) ~(lr @ local) ~beta1 ~beta2 ~weight_decay ~eps ~amsgrad ~maximize ~(grad_scale @ local) ~(found_inf @ local) =
  stubs__fused_adam_tensor_lr_out (CArray.of_list gc_tensor (globalize_gc_tensor_list out) |> CArray.start) (Base.List.length out) (CArray.of_list gc_tensor (globalize_gc_tensor_list self) |> CArray.start) (Base.List.length self) (CArray.of_list gc_tensor (globalize_gc_tensor_list grads) |> CArray.start) (Base.List.length grads) (CArray.of_list gc_tensor (globalize_gc_tensor_list exp_avgs) |> CArray.start) (Base.List.length exp_avgs) (CArray.of_list gc_tensor (globalize_gc_tensor_list exp_avg_sqs) |> CArray.start) (Base.List.length exp_avg_sqs) (CArray.of_list gc_tensor (globalize_gc_tensor_list max_exp_avg_sqs) |> CArray.start) (Base.List.length max_exp_avg_sqs) (CArray.of_list gc_tensor (globalize_gc_tensor_list state_steps) |> CArray.start) (Base.List.length state_steps) (globalize_gc_tensor lr) beta1 beta2 weight_decay eps (if amsgrad then 1 else 0) (if maximize then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor grad_scale) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor found_inf) with | Some v -> v | None -> none_gc_tensor)

let _fused_adamw ~(out @ local) (self @ local) ~(grads @ local) ~(exp_avgs @ local) ~(exp_avg_sqs @ local) ~(max_exp_avg_sqs @ local) ~(state_steps @ local) ~lr ~beta1 ~beta2 ~weight_decay ~eps ~amsgrad ~maximize ~(grad_scale @ local) ~(found_inf @ local) =
  stubs__fused_adamw (CArray.of_list gc_tensor (globalize_gc_tensor_list out) |> CArray.start) (Base.List.length out) (CArray.of_list gc_tensor (globalize_gc_tensor_list self) |> CArray.start) (Base.List.length self) (CArray.of_list gc_tensor (globalize_gc_tensor_list grads) |> CArray.start) (Base.List.length grads) (CArray.of_list gc_tensor (globalize_gc_tensor_list exp_avgs) |> CArray.start) (Base.List.length exp_avgs) (CArray.of_list gc_tensor (globalize_gc_tensor_list exp_avg_sqs) |> CArray.start) (Base.List.length exp_avg_sqs) (CArray.of_list gc_tensor (globalize_gc_tensor_list max_exp_avg_sqs) |> CArray.start) (Base.List.length max_exp_avg_sqs) (CArray.of_list gc_tensor (globalize_gc_tensor_list state_steps) |> CArray.start) (Base.List.length state_steps) lr beta1 beta2 weight_decay eps (if amsgrad then 1 else 0) (if maximize then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor grad_scale) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor found_inf) with | Some v -> v | None -> none_gc_tensor)

let _fused_adamw_ (self @ local) ~(grads @ local) ~(exp_avgs @ local) ~(exp_avg_sqs @ local) ~(max_exp_avg_sqs @ local) ~(state_steps @ local) ~lr ~beta1 ~beta2 ~weight_decay ~eps ~amsgrad ~maximize ~(grad_scale @ local) ~(found_inf @ local) =
  stubs__fused_adamw_ (CArray.of_list gc_tensor (globalize_gc_tensor_list self) |> CArray.start) (Base.List.length self) (CArray.of_list gc_tensor (globalize_gc_tensor_list grads) |> CArray.start) (Base.List.length grads) (CArray.of_list gc_tensor (globalize_gc_tensor_list exp_avgs) |> CArray.start) (Base.List.length exp_avgs) (CArray.of_list gc_tensor (globalize_gc_tensor_list exp_avg_sqs) |> CArray.start) (Base.List.length exp_avg_sqs) (CArray.of_list gc_tensor (globalize_gc_tensor_list max_exp_avg_sqs) |> CArray.start) (Base.List.length max_exp_avg_sqs) (CArray.of_list gc_tensor (globalize_gc_tensor_list state_steps) |> CArray.start) (Base.List.length state_steps) lr beta1 beta2 weight_decay eps (if amsgrad then 1 else 0) (if maximize then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor grad_scale) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor found_inf) with | Some v -> v | None -> none_gc_tensor)

let _fused_adamw_tensor_lr_ (self @ local) ~(grads @ local) ~(exp_avgs @ local) ~(exp_avg_sqs @ local) ~(max_exp_avg_sqs @ local) ~(state_steps @ local) ~(lr @ local) ~beta1 ~beta2 ~weight_decay ~eps ~amsgrad ~maximize ~(grad_scale @ local) ~(found_inf @ local) =
  stubs__fused_adamw_tensor_lr_ (CArray.of_list gc_tensor (globalize_gc_tensor_list self) |> CArray.start) (Base.List.length self) (CArray.of_list gc_tensor (globalize_gc_tensor_list grads) |> CArray.start) (Base.List.length grads) (CArray.of_list gc_tensor (globalize_gc_tensor_list exp_avgs) |> CArray.start) (Base.List.length exp_avgs) (CArray.of_list gc_tensor (globalize_gc_tensor_list exp_avg_sqs) |> CArray.start) (Base.List.length exp_avg_sqs) (CArray.of_list gc_tensor (globalize_gc_tensor_list max_exp_avg_sqs) |> CArray.start) (Base.List.length max_exp_avg_sqs) (CArray.of_list gc_tensor (globalize_gc_tensor_list state_steps) |> CArray.start) (Base.List.length state_steps) (globalize_gc_tensor lr) beta1 beta2 weight_decay eps (if amsgrad then 1 else 0) (if maximize then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor grad_scale) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor found_inf) with | Some v -> v | None -> none_gc_tensor)

let _fused_adamw_tensor_lr_out ~(out @ local) (self @ local) ~(grads @ local) ~(exp_avgs @ local) ~(exp_avg_sqs @ local) ~(max_exp_avg_sqs @ local) ~(state_steps @ local) ~(lr @ local) ~beta1 ~beta2 ~weight_decay ~eps ~amsgrad ~maximize ~(grad_scale @ local) ~(found_inf @ local) =
  stubs__fused_adamw_tensor_lr_out (CArray.of_list gc_tensor (globalize_gc_tensor_list out) |> CArray.start) (Base.List.length out) (CArray.of_list gc_tensor (globalize_gc_tensor_list self) |> CArray.start) (Base.List.length self) (CArray.of_list gc_tensor (globalize_gc_tensor_list grads) |> CArray.start) (Base.List.length grads) (CArray.of_list gc_tensor (globalize_gc_tensor_list exp_avgs) |> CArray.start) (Base.List.length exp_avgs) (CArray.of_list gc_tensor (globalize_gc_tensor_list exp_avg_sqs) |> CArray.start) (Base.List.length exp_avg_sqs) (CArray.of_list gc_tensor (globalize_gc_tensor_list max_exp_avg_sqs) |> CArray.start) (Base.List.length max_exp_avg_sqs) (CArray.of_list gc_tensor (globalize_gc_tensor_list state_steps) |> CArray.start) (Base.List.length state_steps) (globalize_gc_tensor lr) beta1 beta2 weight_decay eps (if amsgrad then 1 else 0) (if maximize then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor grad_scale) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor found_inf) with | Some v -> v | None -> none_gc_tensor)

let _fused_dropout (self @ local) ~p =
  let out__ = CArray.make raw_tensor 2 in
  stubs__fused_dropout (CArray.start out__) (globalize_gc_tensor self) p;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _fused_dropout_out ~(out0 @ local) ~(out1 @ local) (self @ local) ~p =
  let out__ = CArray.make raw_tensor 2 in
  stubs__fused_dropout_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor self) p;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _fused_moving_avg_obs_fq_helper (self @ local) ~(observer_on @ local) ~(fake_quant_on @ local) ~(running_min @ local) ~(running_max @ local) ~(scale @ local) ~(zero_point @ local) ~averaging_const ~quant_min ~quant_max ~ch_axis ~per_row_fake_quant ~symmetric_quant =
  let out__ = CArray.make raw_tensor 2 in
  stubs__fused_moving_avg_obs_fq_helper (CArray.start out__) (globalize_gc_tensor self) (globalize_gc_tensor observer_on) (globalize_gc_tensor fake_quant_on) (globalize_gc_tensor running_min) (globalize_gc_tensor running_max) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) averaging_const (Int64.of_int quant_min) (Int64.of_int quant_max) (Int64.of_int ch_axis) (if per_row_fake_quant then 1 else 0) (if symmetric_quant then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _fused_moving_avg_obs_fq_helper_functional (self @ local) ~(observer_on @ local) ~(fake_quant_on @ local) ~(running_min @ local) ~(running_max @ local) ~(scale @ local) ~(zero_point @ local) ~averaging_const ~quant_min ~quant_max ~ch_axis ~per_row_fake_quant ~symmetric_quant =
  let out__ = CArray.make raw_tensor 6 in
  stubs__fused_moving_avg_obs_fq_helper_functional (CArray.start out__) (globalize_gc_tensor self) (globalize_gc_tensor observer_on) (globalize_gc_tensor fake_quant_on) (globalize_gc_tensor running_min) (globalize_gc_tensor running_max) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) averaging_const (Int64.of_int quant_min) (Int64.of_int quant_max) (Int64.of_int ch_axis) (if per_row_fake_quant then 1 else 0) (if symmetric_quant then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  let t5 = CArray.get out__ 5 |> with_tensor_gc in
  t0, t1, t2, t3, t4, t5

let _fused_moving_avg_obs_fq_helper_out ~(out0 @ local) ~(out1 @ local) (self @ local) ~(observer_on @ local) ~(fake_quant_on @ local) ~(running_min @ local) ~(running_max @ local) ~(scale @ local) ~(zero_point @ local) ~averaging_const ~quant_min ~quant_max ~ch_axis ~per_row_fake_quant ~symmetric_quant =
  let out__ = CArray.make raw_tensor 2 in
  stubs__fused_moving_avg_obs_fq_helper_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor self) (globalize_gc_tensor observer_on) (globalize_gc_tensor fake_quant_on) (globalize_gc_tensor running_min) (globalize_gc_tensor running_max) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) averaging_const (Int64.of_int quant_min) (Int64.of_int quant_max) (Int64.of_int ch_axis) (if per_row_fake_quant then 1 else 0) (if symmetric_quant then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _fused_rms_norm (input @ local) ~normalized_shape ~(weight @ local) ~eps =
  let out__ = CArray.make raw_tensor 2 in
  stubs__fused_rms_norm (CArray.start out__) (globalize_gc_tensor input) (List.map Int64.of_int normalized_shape |> CArray.of_list int64_t |> CArray.start) (List.length normalized_shape) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor)  (Option.value eps ~default:0.0) (match eps with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _fused_sdp_choice ~(query @ local) ~(key @ local) ~(value @ local) ~(attn_mask @ local) ~dropout_p ~is_causal ~scale ~enable_gqa =
  stubs__fused_sdp_choice (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) (match (Base.Option.globalize globalize_gc_tensor attn_mask) with | Some v -> v | None -> none_gc_tensor) dropout_p (if is_causal then 1 else 0)  (Option.value scale ~default:0.0) (match scale with | Some _ -> 0 | None -> 1)  (if enable_gqa then 1 else 0)

let _fused_sgd ~(out @ local) (self @ local) ~(grads @ local) ~(momentum_buffer_list @ local) ~weight_decay ~momentum ~lr ~dampening ~nesterov ~maximize ~is_first_step ~(grad_scale @ local) ~(found_inf @ local) =
  stubs__fused_sgd (CArray.of_list gc_tensor (globalize_gc_tensor_list out) |> CArray.start) (Base.List.length out) (CArray.of_list gc_tensor (globalize_gc_tensor_list self) |> CArray.start) (Base.List.length self) (CArray.of_list gc_tensor (globalize_gc_tensor_list grads) |> CArray.start) (Base.List.length grads) (CArray.of_list gc_tensor (globalize_gc_tensor_list momentum_buffer_list) |> CArray.start) (Base.List.length momentum_buffer_list) weight_decay momentum lr dampening (if nesterov then 1 else 0) (if maximize then 1 else 0) (if is_first_step then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor grad_scale) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor found_inf) with | Some v -> v | None -> none_gc_tensor)

let _fused_sgd_ (self @ local) ~(grads @ local) ~(momentum_buffer_list @ local) ~weight_decay ~momentum ~lr ~dampening ~nesterov ~maximize ~is_first_step ~(grad_scale @ local) ~(found_inf @ local) =
  stubs__fused_sgd_ (CArray.of_list gc_tensor (globalize_gc_tensor_list self) |> CArray.start) (Base.List.length self) (CArray.of_list gc_tensor (globalize_gc_tensor_list grads) |> CArray.start) (Base.List.length grads) (CArray.of_list gc_tensor (globalize_gc_tensor_list momentum_buffer_list) |> CArray.start) (Base.List.length momentum_buffer_list) weight_decay momentum lr dampening (if nesterov then 1 else 0) (if maximize then 1 else 0) (if is_first_step then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor grad_scale) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor found_inf) with | Some v -> v | None -> none_gc_tensor)

let _fused_sgd_tensor_lr_ (self @ local) ~(grads @ local) ~(momentum_buffer_list @ local) ~weight_decay ~momentum ~(lr @ local) ~dampening ~nesterov ~maximize ~is_first_step ~(grad_scale @ local) ~(found_inf @ local) =
  stubs__fused_sgd_tensor_lr_ (CArray.of_list gc_tensor (globalize_gc_tensor_list self) |> CArray.start) (Base.List.length self) (CArray.of_list gc_tensor (globalize_gc_tensor_list grads) |> CArray.start) (Base.List.length grads) (CArray.of_list gc_tensor (globalize_gc_tensor_list momentum_buffer_list) |> CArray.start) (Base.List.length momentum_buffer_list) weight_decay momentum (globalize_gc_tensor lr) dampening (if nesterov then 1 else 0) (if maximize then 1 else 0) (if is_first_step then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor grad_scale) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor found_inf) with | Some v -> v | None -> none_gc_tensor)

let _fused_sgd_tensor_lr_out ~(out @ local) (self @ local) ~(grads @ local) ~(momentum_buffer_list @ local) ~weight_decay ~momentum ~(lr @ local) ~dampening ~nesterov ~maximize ~is_first_step ~(grad_scale @ local) ~(found_inf @ local) =
  stubs__fused_sgd_tensor_lr_out (CArray.of_list gc_tensor (globalize_gc_tensor_list out) |> CArray.start) (Base.List.length out) (CArray.of_list gc_tensor (globalize_gc_tensor_list self) |> CArray.start) (Base.List.length self) (CArray.of_list gc_tensor (globalize_gc_tensor_list grads) |> CArray.start) (Base.List.length grads) (CArray.of_list gc_tensor (globalize_gc_tensor_list momentum_buffer_list) |> CArray.start) (Base.List.length momentum_buffer_list) weight_decay momentum (globalize_gc_tensor lr) dampening (if nesterov then 1 else 0) (if maximize then 1 else 0) (if is_first_step then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor grad_scale) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor found_inf) with | Some v -> v | None -> none_gc_tensor)

let _fw_primal (self @ local) ~level =
  stubs__fw_primal (globalize_gc_tensor self) (Int64.of_int level) |> with_tensor_gc

let _fw_primal_copy (self @ local) ~level =
  stubs__fw_primal_copy (globalize_gc_tensor self) (Int64.of_int level) |> with_tensor_gc

let _fw_primal_copy_out ~(out @ local) (self @ local) ~level =
  stubs__fw_primal_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int level) |> with_tensor_gc

let _gather_sparse_backward (self @ local) ~dim ~(index @ local) ~(grad @ local) =
  stubs__gather_sparse_backward (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor grad) |> with_tensor_gc

let _grid_sampler_2d_cpu_fallback (input @ local) ~(grid @ local) ~interpolation_mode ~padding_mode ~align_corners =
  stubs__grid_sampler_2d_cpu_fallback (globalize_gc_tensor input) (globalize_gc_tensor grid) (Int64.of_int interpolation_mode) (Int64.of_int padding_mode) (if align_corners then 1 else 0) |> with_tensor_gc

let _grid_sampler_2d_cpu_fallback_backward ~(grad_output @ local) (input @ local) ~(grid @ local) ~interpolation_mode ~padding_mode ~align_corners =
  let out__ = CArray.make raw_tensor 2 in
  stubs__grid_sampler_2d_cpu_fallback_backward (CArray.start out__) (globalize_gc_tensor grad_output) (globalize_gc_tensor input) (globalize_gc_tensor grid) (Int64.of_int interpolation_mode) (Int64.of_int padding_mode) (if align_corners then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _grid_sampler_2d_cpu_fallback_out ~(out @ local) (input @ local) ~(grid @ local) ~interpolation_mode ~padding_mode ~align_corners =
  stubs__grid_sampler_2d_cpu_fallback_out (globalize_gc_tensor out) (globalize_gc_tensor input) (globalize_gc_tensor grid) (Int64.of_int interpolation_mode) (Int64.of_int padding_mode) (if align_corners then 1 else 0) |> with_tensor_gc

let _grouped_mm (self @ local) ~(mat2 @ local) ~(offs @ local) ~(bias @ local) ~out_dtype =
  stubs__grouped_mm (globalize_gc_tensor self) (globalize_gc_tensor mat2) (match (Base.Option.globalize globalize_gc_tensor offs) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (Kind.packed_to_int out_dtype) |> with_tensor_gc

let _has_compatible_shallow_copy_type (self @ local) ~(from @ local) =
  stubs__has_compatible_shallow_copy_type (globalize_gc_tensor self) (globalize_gc_tensor from)

let _has_same_storage_numel (self @ local) (other @ local) =
  stubs__has_same_storage_numel (globalize_gc_tensor self) (globalize_gc_tensor other)

let _histogramdd_bin_edges (self @ local) ~bins ~range ~(weight @ local) ~density =
  stubs__histogramdd_bin_edges (globalize_gc_tensor self) (List.map Int64.of_int bins |> CArray.of_list int64_t |> CArray.start) (List.length bins) (range |> CArray.of_list double |> CArray.start) (List.length range) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (if density then 1 else 0) |> to_tensor_list

let _histogramdd_bin_edges_out ~(out @ local) (self @ local) ~bins ~range ~(weight @ local) ~density =
  stubs__histogramdd_bin_edges_out (CArray.of_list gc_tensor (globalize_gc_tensor_list out) |> CArray.start) (Base.List.length out) (globalize_gc_tensor self) (List.map Int64.of_int bins |> CArray.of_list int64_t |> CArray.start) (List.length bins) (range |> CArray.of_list double |> CArray.start) (List.length range) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (if density then 1 else 0)

let _histogramdd_from_bin_cts (self @ local) ~bins ~range ~(weight @ local) ~density =
  stubs__histogramdd_from_bin_cts (globalize_gc_tensor self) (List.map Int64.of_int bins |> CArray.of_list int64_t |> CArray.start) (List.length bins) (range |> CArray.of_list double |> CArray.start) (List.length range) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (if density then 1 else 0) |> with_tensor_gc

let _histogramdd_from_bin_cts_out ~(out @ local) (self @ local) ~bins ~range ~(weight @ local) ~density =
  stubs__histogramdd_from_bin_cts_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int bins |> CArray.of_list int64_t |> CArray.start) (List.length bins) (range |> CArray.of_list double |> CArray.start) (List.length range) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (if density then 1 else 0) |> with_tensor_gc

let _histogramdd_from_bin_tensors (self @ local) ~(bins @ local) ~(weight @ local) ~density =
  stubs__histogramdd_from_bin_tensors (globalize_gc_tensor self) (CArray.of_list gc_tensor (globalize_gc_tensor_list bins) |> CArray.start) (Base.List.length bins) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (if density then 1 else 0) |> with_tensor_gc

let _histogramdd_from_bin_tensors_out ~(out @ local) (self @ local) ~(bins @ local) ~(weight @ local) ~density =
  stubs__histogramdd_from_bin_tensors_out (globalize_gc_tensor out) (globalize_gc_tensor self) (CArray.of_list gc_tensor (globalize_gc_tensor_list bins) |> CArray.start) (Base.List.length bins) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (if density then 1 else 0) |> with_tensor_gc

let _indices (self @ local) =
  stubs__indices (globalize_gc_tensor self) |> with_tensor_gc

let _indices_copy (self @ local) =
  stubs__indices_copy (globalize_gc_tensor self) |> with_tensor_gc

let _indices_copy_out ~(out @ local) (self @ local) =
  stubs__indices_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let _int_mm (self @ local) ~(mat2 @ local) =
  stubs__int_mm (globalize_gc_tensor self) (globalize_gc_tensor mat2) |> with_tensor_gc

let _int_mm_out ~(out @ local) (self @ local) ~(mat2 @ local) =
  stubs__int_mm_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mat2) |> with_tensor_gc

let _is_all_true (self @ local) =
  stubs__is_all_true (globalize_gc_tensor self) |> with_tensor_gc

let _is_any_true (self @ local) =
  stubs__is_any_true (globalize_gc_tensor self) |> with_tensor_gc

let _is_zerotensor (self @ local) =
  stubs__is_zerotensor (globalize_gc_tensor self)

let _lazy_clone (self @ local) =
  stubs__lazy_clone (globalize_gc_tensor self) |> with_tensor_gc

let _linalg_check_errors ~(info @ local) ~api_name ~is_matrix =
  stubs__linalg_check_errors (globalize_gc_tensor info) api_name (if is_matrix then 1 else 0)

let _linalg_det ~(a @ local) =
  let out__ = CArray.make raw_tensor 3 in
  stubs__linalg_det (CArray.start out__) (globalize_gc_tensor a);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _linalg_det_result (result @ local) ~(lu @ local) ~(pivots @ local) ~(a @ local) =
  let out__ = CArray.make raw_tensor 3 in
  stubs__linalg_det_result (CArray.start out__) (globalize_gc_tensor result) (globalize_gc_tensor lu) (globalize_gc_tensor pivots) (globalize_gc_tensor a);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _linalg_eigh ~(a @ local) ~uplo ~compute_v =
  let out__ = CArray.make raw_tensor 2 in
  stubs__linalg_eigh (CArray.start out__) (globalize_gc_tensor a) uplo (if compute_v then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _linalg_eigh_eigenvalues ~(eigenvalues @ local) ~(eigenvectors @ local) ~(a @ local) ~uplo ~compute_v =
  let out__ = CArray.make raw_tensor 2 in
  stubs__linalg_eigh_eigenvalues (CArray.start out__) (globalize_gc_tensor eigenvalues) (globalize_gc_tensor eigenvectors) (globalize_gc_tensor a) uplo (if compute_v then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _linalg_eigvals (self @ local) =
  stubs__linalg_eigvals (globalize_gc_tensor self) |> with_tensor_gc

let _linalg_slogdet ~(a @ local) =
  let out__ = CArray.make raw_tensor 4 in
  stubs__linalg_slogdet (CArray.start out__) (globalize_gc_tensor a);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let _linalg_slogdet_sign ~(sign @ local) ~(logabsdet @ local) ~(lu @ local) ~(pivots @ local) ~(a @ local) =
  let out__ = CArray.make raw_tensor 4 in
  stubs__linalg_slogdet_sign (CArray.start out__) (globalize_gc_tensor sign) (globalize_gc_tensor logabsdet) (globalize_gc_tensor lu) (globalize_gc_tensor pivots) (globalize_gc_tensor a);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let _linalg_solve_ex ~(a @ local) ~(b @ local) ~left ~check_errors =
  let out__ = CArray.make raw_tensor 4 in
  stubs__linalg_solve_ex (CArray.start out__) (globalize_gc_tensor a) (globalize_gc_tensor b) (if left then 1 else 0) (if check_errors then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let _linalg_solve_ex_result (result @ local) ~(lu @ local) ~(pivots @ local) ~(info @ local) ~(a @ local) ~(b @ local) ~left ~check_errors =
  let out__ = CArray.make raw_tensor 4 in
  stubs__linalg_solve_ex_result (CArray.start out__) (globalize_gc_tensor result) (globalize_gc_tensor lu) (globalize_gc_tensor pivots) (globalize_gc_tensor info) (globalize_gc_tensor a) (globalize_gc_tensor b) (if left then 1 else 0) (if check_errors then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let _linalg_svd ~(a @ local) ~full_matrices ~compute_uv ~driver =
  let out__ = CArray.make raw_tensor 3 in
  stubs__linalg_svd (CArray.start out__) (globalize_gc_tensor a) (if full_matrices then 1 else 0) (if compute_uv then 1 else 0)  (Option.value driver ~default:"") (match driver with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _linalg_svd_u ~(u @ local) ~(s @ local) ~(vh @ local) ~(a @ local) ~full_matrices ~compute_uv ~driver =
  let out__ = CArray.make raw_tensor 3 in
  stubs__linalg_svd_u (CArray.start out__) (globalize_gc_tensor u) (globalize_gc_tensor s) (globalize_gc_tensor vh) (globalize_gc_tensor a) (if full_matrices then 1 else 0) (if compute_uv then 1 else 0)  (Option.value driver ~default:"") (match driver with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _log_softmax (self @ local) ~dim ~half_to_float =
  stubs__log_softmax (globalize_gc_tensor self) (Int64.of_int dim) (if half_to_float then 1 else 0) |> with_tensor_gc

let _log_softmax_backward_data ~(grad_output @ local) ~(output @ local) ~dim ~input_dtype =
  stubs__log_softmax_backward_data (globalize_gc_tensor grad_output) (globalize_gc_tensor output) (Int64.of_int dim) (Kind.packed_to_int input_dtype) |> with_tensor_gc

let _log_softmax_backward_data_out ~(out @ local) ~(grad_output @ local) ~(output @ local) ~dim ~input_dtype =
  stubs__log_softmax_backward_data_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (globalize_gc_tensor output) (Int64.of_int dim) (Kind.packed_to_int input_dtype) |> with_tensor_gc

let _log_softmax_out ~(out @ local) (self @ local) ~dim ~half_to_float =
  stubs__log_softmax_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (if half_to_float then 1 else 0) |> with_tensor_gc

let _logcumsumexp (self @ local) ~dim =
  stubs__logcumsumexp (globalize_gc_tensor self) (Int64.of_int dim) |> with_tensor_gc

let _logcumsumexp_out ~(out @ local) (self @ local) ~dim =
  stubs__logcumsumexp_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) |> with_tensor_gc

let _lstm_mps (input @ local) ~(hx @ local) ~(params @ local) ~has_biases ~num_layers ~dropout ~train ~bidirectional ~batch_first =
  let out__ = CArray.make raw_tensor 6 in
  stubs__lstm_mps (CArray.start out__) (globalize_gc_tensor input) (CArray.of_list gc_tensor (globalize_gc_tensor_list hx) |> CArray.start) (Base.List.length hx) (CArray.of_list gc_tensor (globalize_gc_tensor_list params) |> CArray.start) (Base.List.length params) (if has_biases then 1 else 0) (Int64.of_int num_layers) dropout (if train then 1 else 0) (if bidirectional then 1 else 0) (if batch_first then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  let t5 = CArray.get out__ 5 |> with_tensor_gc in
  t0, t1, t2, t3, t4, t5

let _lstm_mps_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) ~(out3 @ local) ~(out4 @ local) ~(out5 @ local) (input @ local) ~(hx @ local) ~(params @ local) ~has_biases ~num_layers ~dropout ~train ~bidirectional ~batch_first =
  let out__ = CArray.make raw_tensor 6 in
  stubs__lstm_mps_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor out3) (globalize_gc_tensor out4) (globalize_gc_tensor out5) (globalize_gc_tensor input) (CArray.of_list gc_tensor (globalize_gc_tensor_list hx) |> CArray.start) (Base.List.length hx) (CArray.of_list gc_tensor (globalize_gc_tensor_list params) |> CArray.start) (Base.List.length params) (if has_biases then 1 else 0) (Int64.of_int num_layers) dropout (if train then 1 else 0) (if bidirectional then 1 else 0) (if batch_first then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  let t5 = CArray.get out__ 5 |> with_tensor_gc in
  t0, t1, t2, t3, t4, t5

let _lu_with_info (self @ local) ~pivot ~check_errors =
  let out__ = CArray.make raw_tensor 3 in
  stubs__lu_with_info (CArray.start out__) (globalize_gc_tensor self) (if pivot then 1 else 0) (if check_errors then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _make_dep_token ~options =
  stubs__make_dep_token (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let _make_dual ~(primal @ local) ~(tangent @ local) ~level =
  stubs__make_dual (globalize_gc_tensor primal) (globalize_gc_tensor tangent) (Int64.of_int level) |> with_tensor_gc

let _make_dual_copy ~(primal @ local) ~(tangent @ local) ~level =
  stubs__make_dual_copy (globalize_gc_tensor primal) (globalize_gc_tensor tangent) (Int64.of_int level) |> with_tensor_gc

let _make_dual_copy_out ~(out @ local) ~(primal @ local) ~(tangent @ local) ~level =
  stubs__make_dual_copy_out (globalize_gc_tensor out) (globalize_gc_tensor primal) (globalize_gc_tensor tangent) (Int64.of_int level) |> with_tensor_gc

let _make_per_channel_quantized_tensor (self @ local) ~(scale @ local) ~(zero_point @ local) ~axis =
  stubs__make_per_channel_quantized_tensor (globalize_gc_tensor self) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) (Int64.of_int axis) |> with_tensor_gc

let _make_per_channel_quantized_tensor_out ~(out @ local) (self @ local) ~(scale @ local) ~(zero_point @ local) ~axis =
  stubs__make_per_channel_quantized_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) (Int64.of_int axis) |> with_tensor_gc

let _make_per_tensor_quantized_tensor (self @ local) ~scale ~zero_point =
  stubs__make_per_tensor_quantized_tensor (globalize_gc_tensor self) scale (Int64.of_int zero_point) |> with_tensor_gc

let _make_per_tensor_quantized_tensor_out ~(out @ local) (self @ local) ~scale ~zero_point =
  stubs__make_per_tensor_quantized_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) scale (Int64.of_int zero_point) |> with_tensor_gc

let _masked_scale (self @ local) ~(mask @ local) ~scale =
  stubs__masked_scale (globalize_gc_tensor self) (globalize_gc_tensor mask) scale |> with_tensor_gc

let _masked_scale_out ~(out @ local) (self @ local) ~(mask @ local) ~scale =
  stubs__masked_scale_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mask) scale |> with_tensor_gc

let _masked_softmax (self @ local) ~(mask @ local) ~dim ~mask_type =
  stubs__masked_softmax (globalize_gc_tensor self) (globalize_gc_tensor mask)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)   (match mask_type with | None -> Int64.zero | Some v -> Int64.of_int v) (match mask_type with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _masked_softmax_backward ~(grad_output @ local) ~(output @ local) ~(mask @ local) ~dim =
  stubs__masked_softmax_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor output) (globalize_gc_tensor mask)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _masked_softmax_backward_out ~(out @ local) ~(grad_output @ local) ~(output @ local) ~(mask @ local) ~dim =
  stubs__masked_softmax_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (globalize_gc_tensor output) (globalize_gc_tensor mask)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _masked_softmax_out ~(out @ local) (self @ local) ~(mask @ local) ~dim ~mask_type =
  stubs__masked_softmax_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mask)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)   (match mask_type with | None -> Int64.zero | Some v -> Int64.of_int v) (match mask_type with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _mixed_dtypes_linear (input @ local) ~(weight @ local) ~(scale @ local) ~(bias @ local) ~activation =
  stubs__mixed_dtypes_linear (globalize_gc_tensor input) (globalize_gc_tensor weight) (globalize_gc_tensor scale) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor)  (Option.value activation ~default:"") (match activation with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _mkldnn_reshape (self @ local) ~shape =
  stubs__mkldnn_reshape (globalize_gc_tensor self) (List.map Int64.of_int shape |> CArray.of_list int64_t |> CArray.start) (List.length shape) |> with_tensor_gc

let _mkldnn_reshape_out ~(out @ local) (self @ local) ~shape =
  stubs__mkldnn_reshape_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int shape |> CArray.of_list int64_t |> CArray.start) (List.length shape) |> with_tensor_gc

let _mkldnn_transpose (self @ local) ~dim0 ~dim1 =
  stubs__mkldnn_transpose (globalize_gc_tensor self) (Int64.of_int dim0) (Int64.of_int dim1) |> with_tensor_gc

let _mkldnn_transpose_ (self @ local) ~dim0 ~dim1 =
  stubs__mkldnn_transpose_ (globalize_gc_tensor self) (Int64.of_int dim0) (Int64.of_int dim1) |> with_tensor_gc

let _mkldnn_transpose_out ~(out @ local) (self @ local) ~dim0 ~dim1 =
  stubs__mkldnn_transpose_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim0) (Int64.of_int dim1) |> with_tensor_gc

let _mps_convolution (self @ local) ~(weight @ local) ~(bias @ local) ~padding ~stride ~dilation ~groups =
  stubs__mps_convolution (globalize_gc_tensor self) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let _mps_convolution_out ~(out @ local) (self @ local) ~(weight @ local) ~(bias @ local) ~padding ~stride ~dilation ~groups =
  stubs__mps_convolution_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let _mps_convolution_transpose (self @ local) ~(weight @ local) ~padding ~output_padding ~stride ~dilation ~groups =
  stubs__mps_convolution_transpose (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let _mps_convolution_transpose_out ~(out @ local) (self @ local) ~(weight @ local) ~padding ~output_padding ~stride ~dilation ~groups =
  stubs__mps_convolution_transpose_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let _native_batch_norm_legit (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~training ~momentum ~eps =
  let out__ = CArray.make raw_tensor 3 in
  stubs__native_batch_norm_legit (CArray.start out__) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor running_mean) (globalize_gc_tensor running_var) (if training then 1 else 0) momentum eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _native_batch_norm_legit_functional (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~training ~momentum ~eps =
  let out__ = CArray.make raw_tensor 5 in
  stubs__native_batch_norm_legit_functional (CArray.start out__) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor running_mean) (globalize_gc_tensor running_var) (if training then 1 else 0) momentum eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  t0, t1, t2, t3, t4

let _native_batch_norm_legit_no_stats (input @ local) ~(weight @ local) ~(bias @ local) ~training ~momentum ~eps =
  let out__ = CArray.make raw_tensor 3 in
  stubs__native_batch_norm_legit_no_stats (CArray.start out__) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (if training then 1 else 0) momentum eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _native_batch_norm_legit_no_stats_out ~(out @ local) ~(save_mean @ local) ~(save_invstd @ local) (input @ local) ~(weight @ local) ~(bias @ local) ~training ~momentum ~eps =
  let out__ = CArray.make raw_tensor 3 in
  stubs__native_batch_norm_legit_no_stats_out (CArray.start out__) (globalize_gc_tensor out) (globalize_gc_tensor save_mean) (globalize_gc_tensor save_invstd) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (if training then 1 else 0) momentum eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _native_batch_norm_legit_no_training (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~momentum ~eps =
  let out__ = CArray.make raw_tensor 3 in
  stubs__native_batch_norm_legit_no_training (CArray.start out__) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor running_mean) (globalize_gc_tensor running_var) momentum eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _native_batch_norm_legit_no_training_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~momentum ~eps =
  let out__ = CArray.make raw_tensor 3 in
  stubs__native_batch_norm_legit_no_training_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor running_mean) (globalize_gc_tensor running_var) momentum eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _native_batch_norm_legit_out ~(out @ local) ~(save_mean @ local) ~(save_invstd @ local) (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~training ~momentum ~eps =
  let out__ = CArray.make raw_tensor 3 in
  stubs__native_batch_norm_legit_out (CArray.start out__) (globalize_gc_tensor out) (globalize_gc_tensor save_mean) (globalize_gc_tensor save_invstd) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor running_mean) (globalize_gc_tensor running_var) (if training then 1 else 0) momentum eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _native_multi_head_attention ~(query @ local) ~(key @ local) ~(value @ local) ~embed_dim ~num_head ~(qkv_weight @ local) ~(qkv_bias @ local) ~(proj_weight @ local) ~(proj_bias @ local) ~(mask @ local) ~need_weights ~average_attn_weights ~mask_type =
  let out__ = CArray.make raw_tensor 2 in
  stubs__native_multi_head_attention (CArray.start out__) (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) (Int64.of_int embed_dim) (Int64.of_int num_head) (globalize_gc_tensor qkv_weight) (globalize_gc_tensor qkv_bias) (globalize_gc_tensor proj_weight) (globalize_gc_tensor proj_bias) (match (Base.Option.globalize globalize_gc_tensor mask) with | Some v -> v | None -> none_gc_tensor) (if need_weights then 1 else 0) (if average_attn_weights then 1 else 0)  (match mask_type with | None -> Int64.zero | Some v -> Int64.of_int v) (match mask_type with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _native_multi_head_attention_out ~(out0 @ local) ~(out1 @ local) ~(query @ local) ~(key @ local) ~(value @ local) ~embed_dim ~num_head ~(qkv_weight @ local) ~(qkv_bias @ local) ~(proj_weight @ local) ~(proj_bias @ local) ~(mask @ local) ~need_weights ~average_attn_weights ~mask_type =
  let out__ = CArray.make raw_tensor 2 in
  stubs__native_multi_head_attention_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) (Int64.of_int embed_dim) (Int64.of_int num_head) (globalize_gc_tensor qkv_weight) (globalize_gc_tensor qkv_bias) (globalize_gc_tensor proj_weight) (globalize_gc_tensor proj_bias) (match (Base.Option.globalize globalize_gc_tensor mask) with | Some v -> v | None -> none_gc_tensor) (if need_weights then 1 else 0) (if average_attn_weights then 1 else 0)  (match mask_type with | None -> Int64.zero | Some v -> Int64.of_int v) (match mask_type with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _neg_view (self @ local) =
  stubs__neg_view (globalize_gc_tensor self) |> with_tensor_gc

let _neg_view_copy (self @ local) =
  stubs__neg_view_copy (globalize_gc_tensor self) |> with_tensor_gc

let _neg_view_copy_out ~(out @ local) (self @ local) =
  stubs__neg_view_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let _nested_compute_contiguous_strides_offsets ~(nested_size @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs__nested_compute_contiguous_strides_offsets (CArray.start out__) (globalize_gc_tensor nested_size);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _nested_from_padded ~(padded @ local) ~(cpu_nested_shape_example @ local) ~fuse_transform_0213 =
  stubs__nested_from_padded (globalize_gc_tensor padded) (globalize_gc_tensor cpu_nested_shape_example) (if fuse_transform_0213 then 1 else 0) |> with_tensor_gc

let _nested_from_padded_and_nested_example ~(padded @ local) ~(nt_example @ local) =
  stubs__nested_from_padded_and_nested_example (globalize_gc_tensor padded) (globalize_gc_tensor nt_example) |> with_tensor_gc

let _nested_from_padded_and_nested_example_out ~(out @ local) ~(padded @ local) ~(nt_example @ local) =
  stubs__nested_from_padded_and_nested_example_out (globalize_gc_tensor out) (globalize_gc_tensor padded) (globalize_gc_tensor nt_example) |> with_tensor_gc

let _nested_from_padded_out ~(out @ local) ~(padded @ local) ~(cpu_nested_shape_example @ local) ~fuse_transform_0213 =
  stubs__nested_from_padded_out (globalize_gc_tensor out) (globalize_gc_tensor padded) (globalize_gc_tensor cpu_nested_shape_example) (if fuse_transform_0213 then 1 else 0) |> with_tensor_gc

let _nested_from_padded_tensor ~(padded @ local) ~(offsets @ local) ~(dummy @ local) ~ragged_idx ~(min_seqlen @ local) ~(max_seqlen @ local) ~sum_s =
  stubs__nested_from_padded_tensor (globalize_gc_tensor padded) (globalize_gc_tensor offsets) (globalize_gc_tensor dummy) (Int64.of_int ragged_idx) (match (Base.Option.globalize globalize_gc_tensor min_seqlen) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor max_seqlen) with | Some v -> v | None -> none_gc_tensor)  (match sum_s with | None -> Int64.zero | Some v -> Int64.of_int v) (match sum_s with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _nested_get_jagged_dummy ~(any @ local) =
  stubs__nested_get_jagged_dummy (globalize_gc_tensor any) |> with_tensor_gc

let _nested_get_lengths (self @ local) =
  stubs__nested_get_lengths (globalize_gc_tensor self) |> with_tensor_gc

let _nested_get_max_seqlen (self @ local) =
  stubs__nested_get_max_seqlen (globalize_gc_tensor self) |> with_tensor_gc

let _nested_get_min_seqlen (self @ local) =
  stubs__nested_get_min_seqlen (globalize_gc_tensor self) |> with_tensor_gc

let _nested_get_offsets (self @ local) =
  stubs__nested_get_offsets (globalize_gc_tensor self) |> with_tensor_gc

let _nested_get_ragged_idx (self @ local) =
  stubs__nested_get_ragged_idx (globalize_gc_tensor self)

let _nested_get_values (self @ local) =
  stubs__nested_get_values (globalize_gc_tensor self) |> with_tensor_gc

let _nested_get_values_copy (self @ local) =
  stubs__nested_get_values_copy (globalize_gc_tensor self) |> with_tensor_gc

let _nested_get_values_copy_out ~(out @ local) (self @ local) =
  stubs__nested_get_values_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let _nested_select_backward ~(grad_output @ local) (self @ local) ~dim ~index =
  stubs__nested_select_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (Int64.of_int dim) (Int64.of_int index) |> with_tensor_gc

let _nested_sum_backward ~(grad @ local) (self @ local) ~dim ~keepdim =
  stubs__nested_sum_backward (globalize_gc_tensor grad) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if keepdim then 1 else 0) |> with_tensor_gc

let _nested_view_from_buffer (self @ local) ~(nested_size @ local) ~(nested_strides @ local) ~(offsets @ local) =
  stubs__nested_view_from_buffer (globalize_gc_tensor self) (globalize_gc_tensor nested_size) (globalize_gc_tensor nested_strides) (globalize_gc_tensor offsets) |> with_tensor_gc

let _nested_view_from_buffer_copy (self @ local) ~(nested_size @ local) ~(nested_strides @ local) ~(offsets @ local) =
  stubs__nested_view_from_buffer_copy (globalize_gc_tensor self) (globalize_gc_tensor nested_size) (globalize_gc_tensor nested_strides) (globalize_gc_tensor offsets) |> with_tensor_gc

let _nested_view_from_buffer_copy_out ~(out @ local) (self @ local) ~(nested_size @ local) ~(nested_strides @ local) ~(offsets @ local) =
  stubs__nested_view_from_buffer_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor nested_size) (globalize_gc_tensor nested_strides) (globalize_gc_tensor offsets) |> with_tensor_gc

let _nested_view_from_jagged (self @ local) ~(offsets @ local) ~(dummy @ local) ~(lengths @ local) ~ragged_idx ~(min_seqlen @ local) ~(max_seqlen @ local) =
  stubs__nested_view_from_jagged (globalize_gc_tensor self) (globalize_gc_tensor offsets) (globalize_gc_tensor dummy) (match (Base.Option.globalize globalize_gc_tensor lengths) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int ragged_idx) (match (Base.Option.globalize globalize_gc_tensor min_seqlen) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor max_seqlen) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let _nested_view_from_jagged_copy (self @ local) ~(offsets @ local) ~(dummy @ local) ~(lengths @ local) ~ragged_idx ~(min_seqlen @ local) ~(max_seqlen @ local) =
  stubs__nested_view_from_jagged_copy (globalize_gc_tensor self) (globalize_gc_tensor offsets) (globalize_gc_tensor dummy) (match (Base.Option.globalize globalize_gc_tensor lengths) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int ragged_idx) (match (Base.Option.globalize globalize_gc_tensor min_seqlen) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor max_seqlen) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let _nested_view_from_jagged_copy_out ~(out @ local) (self @ local) ~(offsets @ local) ~(dummy @ local) ~(lengths @ local) ~ragged_idx ~(min_seqlen @ local) ~(max_seqlen @ local) =
  stubs__nested_view_from_jagged_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor offsets) (globalize_gc_tensor dummy) (match (Base.Option.globalize globalize_gc_tensor lengths) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int ragged_idx) (match (Base.Option.globalize globalize_gc_tensor min_seqlen) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor max_seqlen) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let _new_zeros_with_same_feature_meta (self @ local) (other @ local) ~self_num_batch_dims =
  stubs__new_zeros_with_same_feature_meta (globalize_gc_tensor self) (globalize_gc_tensor other) (Int64.of_int self_num_batch_dims) |> with_tensor_gc

let _new_zeros_with_same_feature_meta_out ~(out @ local) (self @ local) (other @ local) ~self_num_batch_dims =
  stubs__new_zeros_with_same_feature_meta_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) (Int64.of_int self_num_batch_dims) |> with_tensor_gc

let _nnpack_available  =
  stubs__nnpack_available 

let _nnpack_spatial_convolution (input @ local) ~(weight @ local) ~(bias @ local) ~padding ~stride =
  stubs__nnpack_spatial_convolution (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) |> with_tensor_gc

let _nnpack_spatial_convolution_out ~(out @ local) (input @ local) ~(weight @ local) ~(bias @ local) ~padding ~stride =
  stubs__nnpack_spatial_convolution_out (globalize_gc_tensor out) (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) |> with_tensor_gc

let _nnz (self @ local) =
  stubs__nnz (globalize_gc_tensor self)

let _pack_padded_sequence (input @ local) ~(lengths @ local) ~batch_first =
  let out__ = CArray.make raw_tensor 2 in
  stubs__pack_padded_sequence (CArray.start out__) (globalize_gc_tensor input) (globalize_gc_tensor lengths) (if batch_first then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _pack_padded_sequence_backward ~(grad @ local) ~input_size ~(batch_sizes @ local) ~batch_first =
  stubs__pack_padded_sequence_backward (globalize_gc_tensor grad) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (globalize_gc_tensor batch_sizes) (if batch_first then 1 else 0) |> with_tensor_gc

let _pack_padded_sequence_out ~(out0 @ local) ~(out1 @ local) (input @ local) ~(lengths @ local) ~batch_first =
  let out__ = CArray.make raw_tensor 2 in
  stubs__pack_padded_sequence_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor input) (globalize_gc_tensor lengths) (if batch_first then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _pad_circular (self @ local) ~pad =
  stubs__pad_circular (globalize_gc_tensor self) (List.map Int64.of_int pad |> CArray.of_list int64_t |> CArray.start) (List.length pad) |> with_tensor_gc

let _pad_enum (self @ local) ~pad ~mode ~value =
  stubs__pad_enum (globalize_gc_tensor self) (List.map Int64.of_int pad |> CArray.of_list int64_t |> CArray.start) (List.length pad) (Int64.of_int mode)  (Option.value value ~default:0.0) (match value with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _pad_packed_sequence ~(data @ local) ~(batch_sizes @ local) ~batch_first ~padding_value ~total_length =
  let out__ = CArray.make raw_tensor 2 in
  stubs__pad_packed_sequence (CArray.start out__) (globalize_gc_tensor data) (globalize_gc_tensor batch_sizes) (if batch_first then 1 else 0) padding_value (Int64.of_int total_length);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _pdist_backward ~(grad @ local) (self @ local) ~p ~(pdist @ local) =
  stubs__pdist_backward (globalize_gc_tensor grad) (globalize_gc_tensor self) p (globalize_gc_tensor pdist) |> with_tensor_gc

let _pdist_backward_out ~(out @ local) ~(grad @ local) (self @ local) ~p ~(pdist @ local) =
  stubs__pdist_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad) (globalize_gc_tensor self) p (globalize_gc_tensor pdist) |> with_tensor_gc

let _philox_key_fold_in ~(key @ local) ~data =
  stubs__philox_key_fold_in (globalize_gc_tensor key) (Int64.of_int data) |> with_tensor_gc

let _philox_key_split ~(key @ local) ~num_splits =
  stubs__philox_key_split (globalize_gc_tensor key) (Int64.of_int num_splits) |> with_tensor_gc

let _philox_normal (self @ local) ~(key @ local) ~mean ~std =
  stubs__philox_normal (globalize_gc_tensor self) (globalize_gc_tensor key) mean std |> with_tensor_gc

let _philox_normal_ (self @ local) ~(key @ local) ~mean ~std =
  stubs__philox_normal_ (globalize_gc_tensor self) (globalize_gc_tensor key) mean std |> with_tensor_gc

let _philox_normal_out ~(out @ local) (self @ local) ~(key @ local) ~mean ~std =
  stubs__philox_normal_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor key) mean std |> with_tensor_gc

let _philox_uniform (self @ local) ~(key @ local) ~low ~high =
  stubs__philox_uniform (globalize_gc_tensor self) (globalize_gc_tensor key) low high |> with_tensor_gc

let _philox_uniform_ (self @ local) ~(key @ local) ~low ~high =
  stubs__philox_uniform_ (globalize_gc_tensor self) (globalize_gc_tensor key) low high |> with_tensor_gc

let _philox_uniform_out ~(out @ local) (self @ local) ~(key @ local) ~low ~high =
  stubs__philox_uniform_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor key) low high |> with_tensor_gc

let _pin_memory (self @ local) ~device =
  stubs__pin_memory (globalize_gc_tensor self)  (Device.option_to_int device)  |> with_tensor_gc

let _pin_memory_out ~(out @ local) (self @ local) ~device =
  stubs__pin_memory_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (Device.option_to_int device)  |> with_tensor_gc

let _prelu_kernel (self @ local) ~(weight @ local) =
  stubs__prelu_kernel (globalize_gc_tensor self) (globalize_gc_tensor weight) |> with_tensor_gc

let _prelu_kernel_backward ~(grad_output @ local) (self @ local) ~(weight @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs__prelu_kernel_backward (CArray.start out__) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor weight);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _print ~s =
  stubs__print s

let _propagate_xla_data (input @ local) ~(output @ local) =
  stubs__propagate_xla_data (globalize_gc_tensor input) (globalize_gc_tensor output)

let _remove_batch_dim (self @ local) ~level ~batch_size ~out_dim =
  stubs__remove_batch_dim (globalize_gc_tensor self) (Int64.of_int level) (Int64.of_int batch_size) (Int64.of_int out_dim) |> with_tensor_gc

let _reshape_alias (self @ local) ~size ~stride =
  stubs__reshape_alias (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) |> with_tensor_gc

let _reshape_alias_copy (self @ local) ~size ~stride =
  stubs__reshape_alias_copy (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) |> with_tensor_gc

let _reshape_alias_copy_out ~(out @ local) (self @ local) ~size ~stride =
  stubs__reshape_alias_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) |> with_tensor_gc

let _reshape_copy (self @ local) ~size =
  stubs__reshape_copy (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let _reshape_from_tensor (self @ local) ~(shape @ local) =
  stubs__reshape_from_tensor (globalize_gc_tensor self) (globalize_gc_tensor shape) |> with_tensor_gc

let _resize_output (self @ local) ~size ~device =
  stubs__resize_output (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Device.to_int device) |> with_tensor_gc

let _resize_output_ (self @ local) ~size ~device =
  stubs__resize_output_ (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Device.to_int device) |> with_tensor_gc

let _resize_output_out ~(out @ local) (self @ local) ~size ~device =
  stubs__resize_output_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Device.to_int device) |> with_tensor_gc

let _rowwise_prune ~(weight @ local) ~(mask @ local) ~compressed_indices_dtype =
  let out__ = CArray.make raw_tensor 2 in
  stubs__rowwise_prune (CArray.start out__) (globalize_gc_tensor weight) (globalize_gc_tensor mask) (Kind.packed_to_int compressed_indices_dtype);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _safe_softmax (self @ local) ~dim ~dtype =
  stubs__safe_softmax (globalize_gc_tensor self) (Int64.of_int dim) (Kind.packed_to_int dtype) |> with_tensor_gc

let _sample_dirichlet (self @ local) =
  stubs__sample_dirichlet (globalize_gc_tensor self) |> with_tensor_gc

let _sample_dirichlet_out ~(out @ local) (self @ local) =
  stubs__sample_dirichlet_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let _saturate_weight_to_fp16 ~(weight @ local) =
  stubs__saturate_weight_to_fp16 (globalize_gc_tensor weight) |> with_tensor_gc

let _scaled_dot_product_attention_math ~(query @ local) ~(key @ local) ~(value @ local) ~(attn_mask @ local) ~dropout_p ~is_causal ~(dropout_mask @ local) ~scale ~enable_gqa =
  let out__ = CArray.make raw_tensor 2 in
  stubs__scaled_dot_product_attention_math (CArray.start out__) (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) (match (Base.Option.globalize globalize_gc_tensor attn_mask) with | Some v -> v | None -> none_gc_tensor) dropout_p (if is_causal then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor dropout_mask) with | Some v -> v | None -> none_gc_tensor)  (Option.value scale ~default:0.0) (match scale with | Some _ -> 0 | None -> 1)  (if enable_gqa then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _scaled_dot_product_attention_math_for_mps ~(query @ local) ~(key @ local) ~(value @ local) ~(attn_mask @ local) ~dropout_p ~is_causal ~(dropout_mask @ local) ~scale ~enable_gqa =
  let out__ = CArray.make raw_tensor 2 in
  stubs__scaled_dot_product_attention_math_for_mps (CArray.start out__) (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) (match (Base.Option.globalize globalize_gc_tensor attn_mask) with | Some v -> v | None -> none_gc_tensor) dropout_p (if is_causal then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor dropout_mask) with | Some v -> v | None -> none_gc_tensor)  (Option.value scale ~default:0.0) (match scale with | Some _ -> 0 | None -> 1)  (if enable_gqa then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _scaled_dot_product_cudnn_attention_backward ~(grad_out @ local) ~(query @ local) ~(key @ local) ~(value @ local) ~(out @ local) ~(logsumexp @ local) ~(philox_seed @ local) ~(philox_offset @ local) ~(attn_bias @ local) ~(cum_seq_q @ local) ~(cum_seq_k @ local) ~max_q ~max_k ~dropout_p ~is_causal ~scale =
  let out__ = CArray.make raw_tensor 3 in
  stubs__scaled_dot_product_cudnn_attention_backward (CArray.start out__) (globalize_gc_tensor grad_out) (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) (globalize_gc_tensor out) (globalize_gc_tensor logsumexp) (globalize_gc_tensor philox_seed) (globalize_gc_tensor philox_offset) (globalize_gc_tensor attn_bias) (globalize_gc_tensor cum_seq_q) (globalize_gc_tensor cum_seq_k) (Int64.of_int max_q) (Int64.of_int max_k) dropout_p (if is_causal then 1 else 0)  (Option.value scale ~default:0.0) (match scale with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _scaled_dot_product_efficient_attention ~(query @ local) ~(key @ local) ~(value @ local) ~(attn_bias @ local) ~compute_log_sumexp ~dropout_p ~is_causal ~scale =
  let out__ = CArray.make raw_tensor 4 in
  stubs__scaled_dot_product_efficient_attention (CArray.start out__) (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) (match (Base.Option.globalize globalize_gc_tensor attn_bias) with | Some v -> v | None -> none_gc_tensor) (if compute_log_sumexp then 1 else 0) dropout_p (if is_causal then 1 else 0)  (Option.value scale ~default:0.0) (match scale with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let _scaled_dot_product_flash_attention_backward ~(grad_out @ local) ~(query @ local) ~(key @ local) ~(value @ local) ~(out @ local) ~(logsumexp @ local) ~(cum_seq_q @ local) ~(cum_seq_k @ local) ~max_q ~max_k ~dropout_p ~is_causal ~(philox_seed @ local) ~(philox_offset @ local) ~scale =
  let out__ = CArray.make raw_tensor 3 in
  stubs__scaled_dot_product_flash_attention_backward (CArray.start out__) (globalize_gc_tensor grad_out) (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) (globalize_gc_tensor out) (globalize_gc_tensor logsumexp) (globalize_gc_tensor cum_seq_q) (globalize_gc_tensor cum_seq_k) (Int64.of_int max_q) (Int64.of_int max_k) dropout_p (if is_causal then 1 else 0) (globalize_gc_tensor philox_seed) (globalize_gc_tensor philox_offset)  (Option.value scale ~default:0.0) (match scale with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _scaled_dot_product_flash_attention_for_cpu ~(query @ local) ~(key @ local) ~(value @ local) ~dropout_p ~is_causal ~(attn_mask @ local) ~scale =
  let out__ = CArray.make raw_tensor 2 in
  stubs__scaled_dot_product_flash_attention_for_cpu (CArray.start out__) (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) dropout_p (if is_causal then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor attn_mask) with | Some v -> v | None -> none_gc_tensor)  (Option.value scale ~default:0.0) (match scale with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _scaled_dot_product_flash_attention_for_cpu_backward ~(grad_out @ local) ~(query @ local) ~(key @ local) ~(value @ local) ~(out @ local) ~(logsumexp @ local) ~dropout_p ~is_causal ~(attn_mask @ local) ~scale =
  let out__ = CArray.make raw_tensor 3 in
  stubs__scaled_dot_product_flash_attention_for_cpu_backward (CArray.start out__) (globalize_gc_tensor grad_out) (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) (globalize_gc_tensor out) (globalize_gc_tensor logsumexp) dropout_p (if is_causal then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor attn_mask) with | Some v -> v | None -> none_gc_tensor)  (Option.value scale ~default:0.0) (match scale with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _scaled_grouped_mm (self @ local) ~(mat2 @ local) ~(scale_a @ local) ~(scale_b @ local) ~(offs @ local) ~(bias @ local) ~(scale_result @ local) ~out_dtype ~use_fast_accum =
  stubs__scaled_grouped_mm (globalize_gc_tensor self) (globalize_gc_tensor mat2) (globalize_gc_tensor scale_a) (globalize_gc_tensor scale_b) (match (Base.Option.globalize globalize_gc_tensor offs) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor scale_result) with | Some v -> v | None -> none_gc_tensor) (Kind.packed_to_int out_dtype) (if use_fast_accum then 1 else 0) |> with_tensor_gc

let _scaled_grouped_mm_v2 (self @ local) ~(mat2 @ local) ~(scale_a @ local) ~recipe_a ~swizzle_a ~(scale_b @ local) ~recipe_b ~swizzle_b ~(offs @ local) ~(bias @ local) ~out_dtype ~contraction_dim ~use_fast_accum =
  stubs__scaled_grouped_mm_v2 (globalize_gc_tensor self) (globalize_gc_tensor mat2) (CArray.of_list gc_tensor (globalize_gc_tensor_list scale_a) |> CArray.start) (Base.List.length scale_a) (List.map Int64.of_int recipe_a |> CArray.of_list int64_t |> CArray.start) (List.length recipe_a) (List.map Int64.of_int swizzle_a |> CArray.of_list int64_t |> CArray.start) (List.length swizzle_a) (CArray.of_list gc_tensor (globalize_gc_tensor_list scale_b) |> CArray.start) (Base.List.length scale_b) (List.map Int64.of_int recipe_b |> CArray.of_list int64_t |> CArray.start) (List.length recipe_b) (List.map Int64.of_int swizzle_b |> CArray.of_list int64_t |> CArray.start) (List.length swizzle_b) (match (Base.Option.globalize globalize_gc_tensor offs) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (Kind.packed_to_int out_dtype) (List.map Int64.of_int contraction_dim |> CArray.of_list int64_t |> CArray.start) (List.length contraction_dim) (if use_fast_accum then 1 else 0) |> with_tensor_gc

let _scaled_mm (self @ local) ~(mat2 @ local) ~(scale_a @ local) ~(scale_b @ local) ~(bias @ local) ~(scale_result @ local) ~out_dtype ~use_fast_accum =
  stubs__scaled_mm (globalize_gc_tensor self) (globalize_gc_tensor mat2) (globalize_gc_tensor scale_a) (globalize_gc_tensor scale_b) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor scale_result) with | Some v -> v | None -> none_gc_tensor) (Kind.packed_to_int out_dtype) (if use_fast_accum then 1 else 0) |> with_tensor_gc

let _scaled_mm_out ~(out @ local) (self @ local) ~(mat2 @ local) ~(scale_a @ local) ~(scale_b @ local) ~(bias @ local) ~(scale_result @ local) ~out_dtype ~use_fast_accum =
  stubs__scaled_mm_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mat2) (globalize_gc_tensor scale_a) (globalize_gc_tensor scale_b) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor scale_result) with | Some v -> v | None -> none_gc_tensor) (Kind.packed_to_int out_dtype) (if use_fast_accum then 1 else 0) |> with_tensor_gc

let _scaled_mm_v2 (self @ local) ~(mat2 @ local) ~(scale_a @ local) ~recipe_a ~swizzle_a ~(scale_b @ local) ~recipe_b ~swizzle_b ~(bias @ local) ~out_dtype ~contraction_dim ~use_fast_accum =
  stubs__scaled_mm_v2 (globalize_gc_tensor self) (globalize_gc_tensor mat2) (CArray.of_list gc_tensor (globalize_gc_tensor_list scale_a) |> CArray.start) (Base.List.length scale_a) (List.map Int64.of_int recipe_a |> CArray.of_list int64_t |> CArray.start) (List.length recipe_a) (List.map Int64.of_int swizzle_a |> CArray.of_list int64_t |> CArray.start) (List.length swizzle_a) (CArray.of_list gc_tensor (globalize_gc_tensor_list scale_b) |> CArray.start) (Base.List.length scale_b) (List.map Int64.of_int recipe_b |> CArray.of_list int64_t |> CArray.start) (List.length recipe_b) (List.map Int64.of_int swizzle_b |> CArray.of_list int64_t |> CArray.start) (List.length swizzle_b) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (Kind.packed_to_int out_dtype) (List.map Int64.of_int contraction_dim |> CArray.of_list int64_t |> CArray.start) (List.length contraction_dim) (if use_fast_accum then 1 else 0) |> with_tensor_gc

let _scaled_mm_v2_out ~(out @ local) (self @ local) ~(mat2 @ local) ~(scale_a @ local) ~recipe_a ~swizzle_a ~(scale_b @ local) ~recipe_b ~swizzle_b ~(bias @ local) ~out_dtype ~contraction_dim ~use_fast_accum =
  stubs__scaled_mm_v2_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mat2) (CArray.of_list gc_tensor (globalize_gc_tensor_list scale_a) |> CArray.start) (Base.List.length scale_a) (List.map Int64.of_int recipe_a |> CArray.of_list int64_t |> CArray.start) (List.length recipe_a) (List.map Int64.of_int swizzle_a |> CArray.of_list int64_t |> CArray.start) (List.length swizzle_a) (CArray.of_list gc_tensor (globalize_gc_tensor_list scale_b) |> CArray.start) (Base.List.length scale_b) (List.map Int64.of_int recipe_b |> CArray.of_list int64_t |> CArray.start) (List.length recipe_b) (List.map Int64.of_int swizzle_b |> CArray.of_list int64_t |> CArray.start) (List.length swizzle_b) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (Kind.packed_to_int out_dtype) (List.map Int64.of_int contraction_dim |> CArray.of_list int64_t |> CArray.start) (List.length contraction_dim) (if use_fast_accum then 1 else 0) |> with_tensor_gc

let _scatter_reduce (self @ local) ~dim ~(index @ local) ~(src @ local) ~reduce ~include_self =
  stubs__scatter_reduce (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor src) reduce (if include_self then 1 else 0) |> with_tensor_gc

let _scatter_reduce_ (self @ local) ~dim ~(index @ local) ~(src @ local) ~reduce ~include_self =
  stubs__scatter_reduce_ (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor src) reduce (if include_self then 1 else 0) |> with_tensor_gc

let _scatter_reduce_two_out ~(out @ local) (self @ local) ~dim ~(index @ local) ~(src @ local) ~reduce ~include_self =
  stubs__scatter_reduce_two_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor src) reduce (if include_self then 1 else 0) |> with_tensor_gc

let _segment_reduce_backward ~(grad @ local) ~(output @ local) ~(data @ local) ~reduce ~(lengths @ local) ~(offsets @ local) ~axis ~initial =
  stubs__segment_reduce_backward (globalize_gc_tensor grad) (globalize_gc_tensor output) (globalize_gc_tensor data) reduce (match (Base.Option.globalize globalize_gc_tensor lengths) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor offsets) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int axis) initial |> with_tensor_gc

let _segment_reduce_backward_out ~(out @ local) ~(grad @ local) ~(output @ local) ~(data @ local) ~reduce ~(lengths @ local) ~(offsets @ local) ~axis ~initial =
  stubs__segment_reduce_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad) (globalize_gc_tensor output) (globalize_gc_tensor data) reduce (match (Base.Option.globalize globalize_gc_tensor lengths) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor offsets) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int axis) initial |> with_tensor_gc

let _shape_as_tensor (self @ local) =
  stubs__shape_as_tensor (globalize_gc_tensor self) |> with_tensor_gc

let _slow_conv2d_backward ~(grad_input @ local) ~(grad_weight @ local) ~(grad_bias @ local) ~(grad_output @ local) (self @ local) ~(weight @ local) ~kernel_size ~stride ~padding =
  let out__ = CArray.make raw_tensor 3 in
  stubs__slow_conv2d_backward (CArray.start out__) (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_weight) (globalize_gc_tensor grad_bias) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _sobol_engine_draw ~(quasi @ local) ~n ~(sobolstate @ local) ~dimension ~num_generated ~dtype =
  let out__ = CArray.make raw_tensor 2 in
  stubs__sobol_engine_draw (CArray.start out__) (globalize_gc_tensor quasi) (Int64.of_int n) (globalize_gc_tensor sobolstate) (Int64.of_int dimension) (Int64.of_int num_generated) (Kind.packed_to_int dtype);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _sobol_engine_ff_ (self @ local) ~n ~(sobolstate @ local) ~dimension ~num_generated =
  stubs__sobol_engine_ff_ (globalize_gc_tensor self) (Int64.of_int n) (globalize_gc_tensor sobolstate) (Int64.of_int dimension) (Int64.of_int num_generated) |> with_tensor_gc

let _sobol_engine_initialize_state_ (self @ local) ~dimension =
  stubs__sobol_engine_initialize_state_ (globalize_gc_tensor self) (Int64.of_int dimension) |> with_tensor_gc

let _sobol_engine_scramble_ (self @ local) ~(ltm @ local) ~dimension =
  stubs__sobol_engine_scramble_ (globalize_gc_tensor self) (globalize_gc_tensor ltm) (Int64.of_int dimension) |> with_tensor_gc

let _softmax (self @ local) ~dim ~half_to_float =
  stubs__softmax (globalize_gc_tensor self) (Int64.of_int dim) (if half_to_float then 1 else 0) |> with_tensor_gc

let _softmax_backward_data ~(grad_output @ local) ~(output @ local) ~dim ~input_dtype =
  stubs__softmax_backward_data (globalize_gc_tensor grad_output) (globalize_gc_tensor output) (Int64.of_int dim) (Kind.packed_to_int input_dtype) |> with_tensor_gc

let _softmax_backward_data_out ~(grad_input @ local) ~(grad_output @ local) ~(output @ local) ~dim ~input_dtype =
  stubs__softmax_backward_data_out (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor output) (Int64.of_int dim) (Kind.packed_to_int input_dtype) |> with_tensor_gc

let _softmax_out ~(out @ local) (self @ local) ~dim ~half_to_float =
  stubs__softmax_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (if half_to_float then 1 else 0) |> with_tensor_gc

let _sparse_addmm ?beta ?alpha (self @ local) ~(mat1 @ local) ~(mat2 @ local) =
  stubs__sparse_addmm (globalize_gc_tensor self) (globalize_gc_tensor mat1) (globalize_gc_tensor mat2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let _sparse_addmm_out ?beta ?alpha ~(out @ local) (self @ local) ~(mat1 @ local) ~(mat2 @ local) =
  stubs__sparse_addmm_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mat1) (globalize_gc_tensor mat2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let _sparse_broadcast_to (self @ local) ~size =
  stubs__sparse_broadcast_to (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let _sparse_broadcast_to_copy (self @ local) ~size =
  stubs__sparse_broadcast_to_copy (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let _sparse_broadcast_to_copy_out ~(out @ local) (self @ local) ~size =
  stubs__sparse_broadcast_to_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let _sparse_bsc_tensor_unsafe ~(ccol_indices @ local) ~(row_indices @ local) ~(values @ local) ~size ~options =
  stubs__sparse_bsc_tensor_unsafe (globalize_gc_tensor ccol_indices) (globalize_gc_tensor row_indices) (globalize_gc_tensor values) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let _sparse_bsr_tensor_unsafe ~(crow_indices @ local) ~(col_indices @ local) ~(values @ local) ~size ~options =
  stubs__sparse_bsr_tensor_unsafe (globalize_gc_tensor crow_indices) (globalize_gc_tensor col_indices) (globalize_gc_tensor values) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let _sparse_compressed_tensor_unsafe ~(compressed_indices @ local) ~(plain_indices @ local) ~(values @ local) ~size ~options =
  stubs__sparse_compressed_tensor_unsafe (globalize_gc_tensor compressed_indices) (globalize_gc_tensor plain_indices) (globalize_gc_tensor values) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let _sparse_compressed_tensor_with_dims ~nnz ~dense_dim ~size ~blocksize ~index_dtype ~options =
  stubs__sparse_compressed_tensor_with_dims (Int64.of_int nnz) (Int64.of_int dense_dim) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int blocksize |> CArray.of_list int64_t |> CArray.start) (List.length blocksize) (Kind.packed_to_int index_dtype) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let _sparse_coo_tensor_unsafe ~(indices @ local) ~(values @ local) ~size ~options ~is_coalesced =
  stubs__sparse_coo_tensor_unsafe (globalize_gc_tensor indices) (globalize_gc_tensor values) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) (if is_coalesced then 1 else 0) |> with_tensor_gc

let _sparse_coo_tensor_with_dims ~sparse_dim ~dense_dim ~size ~options =
  stubs__sparse_coo_tensor_with_dims (Int64.of_int sparse_dim) (Int64.of_int dense_dim) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let _sparse_coo_tensor_with_dims_and_tensors ~sparse_dim ~dense_dim ~size ~(indices @ local) ~(values @ local) ~options ~is_coalesced =
  stubs__sparse_coo_tensor_with_dims_and_tensors (Int64.of_int sparse_dim) (Int64.of_int dense_dim) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (globalize_gc_tensor indices) (globalize_gc_tensor values) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) (if is_coalesced then 1 else 0) |> with_tensor_gc

let _sparse_coo_tensor_with_dims_and_tensors_out ~(out @ local) ~sparse_dim ~dense_dim ~size ~(indices @ local) ~(values @ local) ~is_coalesced =
  stubs__sparse_coo_tensor_with_dims_and_tensors_out (globalize_gc_tensor out) (Int64.of_int sparse_dim) (Int64.of_int dense_dim) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (globalize_gc_tensor indices) (globalize_gc_tensor values) (if is_coalesced then 1 else 0) |> with_tensor_gc

let _sparse_coo_tensor_with_dims_out ~(out @ local) ~sparse_dim ~dense_dim ~size =
  stubs__sparse_coo_tensor_with_dims_out (globalize_gc_tensor out) (Int64.of_int sparse_dim) (Int64.of_int dense_dim) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let _sparse_csc_tensor_unsafe ~(ccol_indices @ local) ~(row_indices @ local) ~(values @ local) ~size ~options =
  stubs__sparse_csc_tensor_unsafe (globalize_gc_tensor ccol_indices) (globalize_gc_tensor row_indices) (globalize_gc_tensor values) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let _sparse_csr_prod (self @ local) ~dim ~keepdim ~dtype =
  stubs__sparse_csr_prod (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let _sparse_csr_prod_dim_dtype_out ~(out @ local) (self @ local) ~dim ~keepdim ~dtype =
  stubs__sparse_csr_prod_dim_dtype_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let _sparse_csr_sum (self @ local) ~dim ~keepdim ~dtype =
  stubs__sparse_csr_sum (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let _sparse_csr_sum_dim_dtype_out ~(out @ local) (self @ local) ~dim ~keepdim ~dtype =
  stubs__sparse_csr_sum_dim_dtype_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let _sparse_csr_tensor_unsafe ~(crow_indices @ local) ~(col_indices @ local) ~(values @ local) ~size ~options =
  stubs__sparse_csr_tensor_unsafe (globalize_gc_tensor crow_indices) (globalize_gc_tensor col_indices) (globalize_gc_tensor values) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let _sparse_log_softmax (self @ local) ~dim ~half_to_float =
  stubs__sparse_log_softmax (globalize_gc_tensor self) (Int64.of_int dim) (if half_to_float then 1 else 0) |> with_tensor_gc

let _sparse_log_softmax_backward_data ~(grad_output @ local) ~(output @ local) ~dim (self @ local) =
  stubs__sparse_log_softmax_backward_data (globalize_gc_tensor grad_output) (globalize_gc_tensor output) (Int64.of_int dim) (globalize_gc_tensor self) |> with_tensor_gc

let _sparse_log_softmax_backward_data_out ~(out @ local) ~(grad_output @ local) ~(output @ local) ~dim (self @ local) =
  stubs__sparse_log_softmax_backward_data_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (globalize_gc_tensor output) (Int64.of_int dim) (globalize_gc_tensor self) |> with_tensor_gc

let _sparse_log_softmax_int (self @ local) ~dim ~dtype =
  stubs__sparse_log_softmax_int (globalize_gc_tensor self) (Int64.of_int dim) (Kind.packed_to_int dtype) |> with_tensor_gc

let _sparse_log_softmax_out ~(out @ local) (self @ local) ~dim ~half_to_float =
  stubs__sparse_log_softmax_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (if half_to_float then 1 else 0) |> with_tensor_gc

let _sparse_mask_projection (self @ local) ~(mask @ local) ~accumulate_matches =
  stubs__sparse_mask_projection (globalize_gc_tensor self) (globalize_gc_tensor mask) (if accumulate_matches then 1 else 0) |> with_tensor_gc

let _sparse_mask_projection_out ~(out @ local) (self @ local) ~(mask @ local) ~accumulate_matches =
  stubs__sparse_mask_projection_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mask) (if accumulate_matches then 1 else 0) |> with_tensor_gc

let _sparse_mm ~(sparse @ local) ~(dense @ local) =
  stubs__sparse_mm (globalize_gc_tensor sparse) (globalize_gc_tensor dense) |> with_tensor_gc

let _sparse_mm_reduce ~(sparse @ local) ~(dense @ local) ~reduce =
  stubs__sparse_mm_reduce (globalize_gc_tensor sparse) (globalize_gc_tensor dense) reduce |> with_tensor_gc

let _sparse_mm_reduce_impl (self @ local) (other @ local) ~reduce =
  let out__ = CArray.make raw_tensor 2 in
  stubs__sparse_mm_reduce_impl (CArray.start out__) (globalize_gc_tensor self) (globalize_gc_tensor other) reduce;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _sparse_semi_structured_apply (input @ local) ~(thread_masks @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs__sparse_semi_structured_apply (CArray.start out__) (globalize_gc_tensor input) (globalize_gc_tensor thread_masks);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _sparse_semi_structured_apply_dense (input @ local) ~(thread_masks @ local) =
  stubs__sparse_semi_structured_apply_dense (globalize_gc_tensor input) (globalize_gc_tensor thread_masks) |> with_tensor_gc

let _sparse_semi_structured_linear (input @ local) ~(weight @ local) ~(meta @ local) ~(bias @ local) ~activation ~out_dtype =
  stubs__sparse_semi_structured_linear (globalize_gc_tensor input) (globalize_gc_tensor weight) (globalize_gc_tensor meta) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor)  (Option.value activation ~default:"") (match activation with | Some _ -> 0 | None -> 1)  (Kind.packed_to_int out_dtype) |> with_tensor_gc

let _sparse_semi_structured_mm ~(mat1 @ local) ~(mat1_meta @ local) ~(mat2 @ local) ~out_dtype =
  stubs__sparse_semi_structured_mm (globalize_gc_tensor mat1) (globalize_gc_tensor mat1_meta) (globalize_gc_tensor mat2) (Kind.packed_to_int out_dtype) |> with_tensor_gc

let _sparse_semi_structured_tile (input @ local) ~algorithm ~use_cutlass =
  let out__ = CArray.make raw_tensor 5 in
  stubs__sparse_semi_structured_tile (CArray.start out__) (globalize_gc_tensor input) algorithm (if use_cutlass then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  t0, t1, t2, t3, t4

let _sparse_softmax (self @ local) ~dim ~half_to_float =
  stubs__sparse_softmax (globalize_gc_tensor self) (Int64.of_int dim) (if half_to_float then 1 else 0) |> with_tensor_gc

let _sparse_softmax_backward_data ~(grad_output @ local) ~(output @ local) ~dim (self @ local) =
  stubs__sparse_softmax_backward_data (globalize_gc_tensor grad_output) (globalize_gc_tensor output) (Int64.of_int dim) (globalize_gc_tensor self) |> with_tensor_gc

let _sparse_softmax_backward_data_out ~(out @ local) ~(grad_output @ local) ~(output @ local) ~dim (self @ local) =
  stubs__sparse_softmax_backward_data_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (globalize_gc_tensor output) (Int64.of_int dim) (globalize_gc_tensor self) |> with_tensor_gc

let _sparse_softmax_int (self @ local) ~dim ~dtype =
  stubs__sparse_softmax_int (globalize_gc_tensor self) (Int64.of_int dim) (Kind.packed_to_int dtype) |> with_tensor_gc

let _sparse_softmax_out ~(out @ local) (self @ local) ~dim ~half_to_float =
  stubs__sparse_softmax_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (if half_to_float then 1 else 0) |> with_tensor_gc

let _sparse_sparse_matmul (self @ local) (other @ local) =
  stubs__sparse_sparse_matmul (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let _sparse_sparse_matmul_out ~(out @ local) (self @ local) (other @ local) =
  stubs__sparse_sparse_matmul_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let _sparse_sum (self @ local) =
  stubs__sparse_sum (globalize_gc_tensor self) |> with_tensor_gc

let _sparse_sum_backward ~(grad @ local) (self @ local) ~dim =
  stubs__sparse_sum_backward (globalize_gc_tensor grad) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) |> with_tensor_gc

let _sparse_sum_backward_out ~(out @ local) ~(grad @ local) (self @ local) ~dim =
  stubs__sparse_sum_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) |> with_tensor_gc

let _sparse_sum_dim (self @ local) ~dim =
  stubs__sparse_sum_dim (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) |> with_tensor_gc

let _sparse_sum_dim_dtype (self @ local) ~dim ~dtype =
  stubs__sparse_sum_dim_dtype (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (Kind.packed_to_int dtype) |> with_tensor_gc

let _sparse_sum_dim_out ~(out @ local) (self @ local) ~dim =
  stubs__sparse_sum_dim_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) |> with_tensor_gc

let _sparse_sum_dtype (self @ local) ~dtype =
  stubs__sparse_sum_dtype (globalize_gc_tensor self) (Kind.packed_to_int dtype) |> with_tensor_gc

let _spdiags ~(diagonals @ local) ~(offsets @ local) ~shape =
  stubs__spdiags (globalize_gc_tensor diagonals) (globalize_gc_tensor offsets) (List.map Int64.of_int shape |> CArray.of_list int64_t |> CArray.start) (List.length shape) |> with_tensor_gc

let _spdiags_out ~(out @ local) ~(diagonals @ local) ~(offsets @ local) ~shape =
  stubs__spdiags_out (globalize_gc_tensor out) (globalize_gc_tensor diagonals) (globalize_gc_tensor offsets) (List.map Int64.of_int shape |> CArray.of_list int64_t |> CArray.start) (List.length shape) |> with_tensor_gc

let _spsolve ~(a @ local) ~(b @ local) ~left =
  stubs__spsolve (globalize_gc_tensor a) (globalize_gc_tensor b) (if left then 1 else 0) |> with_tensor_gc

let _stack (tensors @ local) ~dim =
  stubs__stack (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) (Int64.of_int dim) |> with_tensor_gc

let _stack_out ~(out @ local) (tensors @ local) ~dim =
  stubs__stack_out (globalize_gc_tensor out) (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) (Int64.of_int dim) |> with_tensor_gc

let _standard_gamma (self @ local) =
  stubs__standard_gamma (globalize_gc_tensor self) |> with_tensor_gc

let _standard_gamma_grad (self @ local) ~(output @ local) =
  stubs__standard_gamma_grad (globalize_gc_tensor self) (globalize_gc_tensor output) |> with_tensor_gc

let _standard_gamma_grad_out ~(out @ local) (self @ local) ~(output @ local) =
  stubs__standard_gamma_grad_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor output) |> with_tensor_gc

let _standard_gamma_out ~(out @ local) (self @ local) =
  stubs__standard_gamma_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let _test_ambiguous_defaults ~(dummy @ local) ~a ~b =
  stubs__test_ambiguous_defaults (globalize_gc_tensor dummy) (Int64.of_int a) (Int64.of_int b) |> with_tensor_gc

let _test_ambiguous_defaults_b ~(dummy @ local) ~a ~b =
  stubs__test_ambiguous_defaults_b (globalize_gc_tensor dummy) (Int64.of_int a) b |> with_tensor_gc

let _test_autograd_multiple_dispatch (self @ local) =
  stubs__test_autograd_multiple_dispatch (globalize_gc_tensor self) |> with_tensor_gc

let _test_autograd_multiple_dispatch_fullcoverage_out ~(out @ local) (self @ local) =
  stubs__test_autograd_multiple_dispatch_fullcoverage_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let _test_autograd_multiple_dispatch_ntonly (self @ local) ~b =
  stubs__test_autograd_multiple_dispatch_ntonly (globalize_gc_tensor self) (if b then 1 else 0) |> with_tensor_gc

let _test_autograd_multiple_dispatch_view (self @ local) =
  stubs__test_autograd_multiple_dispatch_view (globalize_gc_tensor self) |> with_tensor_gc

let _test_autograd_multiple_dispatch_view_copy (self @ local) =
  stubs__test_autograd_multiple_dispatch_view_copy (globalize_gc_tensor self) |> with_tensor_gc

let _test_autograd_multiple_dispatch_view_copy_out ~(out @ local) (self @ local) =
  stubs__test_autograd_multiple_dispatch_view_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let _test_check_tensor (self @ local) =
  stubs__test_check_tensor (globalize_gc_tensor self) |> with_tensor_gc

let _test_functorch_fallback (self @ local) (other @ local) =
  stubs__test_functorch_fallback (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let _test_functorch_fallback_out ~(out @ local) (self @ local) (other @ local) =
  stubs__test_functorch_fallback_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let _test_optional_filled_intlist ~(values @ local) ~addends =
  stubs__test_optional_filled_intlist (globalize_gc_tensor values) (match addends with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match addends with | None -> -1 | Some v -> List.length v) |> with_tensor_gc

let _test_optional_filled_intlist_out ~(out @ local) ~(values @ local) ~addends =
  stubs__test_optional_filled_intlist_out (globalize_gc_tensor out) (globalize_gc_tensor values) (match addends with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match addends with | None -> -1 | Some v -> List.length v) |> with_tensor_gc

let _test_optional_floatlist ~(values @ local) ~addends =
  stubs__test_optional_floatlist (globalize_gc_tensor values) (addends |> CArray.of_list double |> CArray.start) (List.length addends) |> with_tensor_gc

let _test_optional_floatlist_out ~(out @ local) ~(values @ local) ~addends =
  stubs__test_optional_floatlist_out (globalize_gc_tensor out) (globalize_gc_tensor values) (addends |> CArray.of_list double |> CArray.start) (List.length addends) |> with_tensor_gc

let _test_optional_intlist ~(values @ local) ~addends =
  stubs__test_optional_intlist (globalize_gc_tensor values) (match addends with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match addends with | None -> -1 | Some v -> List.length v) |> with_tensor_gc

let _test_optional_intlist_out ~(out @ local) ~(values @ local) ~addends =
  stubs__test_optional_intlist_out (globalize_gc_tensor out) (globalize_gc_tensor values) (match addends with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match addends with | None -> -1 | Some v -> List.length v) |> with_tensor_gc

let _test_parallel_materialize (self @ local) ~num_parallel ~skip_first =
  stubs__test_parallel_materialize (globalize_gc_tensor self) (Int64.of_int num_parallel) (if skip_first then 1 else 0) |> with_tensor_gc

let _test_serialization_subcmul ?alpha (self @ local) (other @ local) =
  stubs__test_serialization_subcmul (globalize_gc_tensor self) (globalize_gc_tensor other) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let _test_string_default ~(dummy @ local) ~a ~b =
  stubs__test_string_default (globalize_gc_tensor dummy) a b |> with_tensor_gc

let _test_warn_in_autograd (self @ local) =
  stubs__test_warn_in_autograd (globalize_gc_tensor self) |> with_tensor_gc

let _test_warn_in_autograd_out ~(out @ local) (self @ local) =
  stubs__test_warn_in_autograd_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let _thnn_differentiable_gru_cell_backward ~(grad_hy @ local) ~(input_gates @ local) ~(hidden_gates @ local) ~(hx @ local) ~(input_bias @ local) ~(hidden_bias @ local) =
  let out__ = CArray.make raw_tensor 5 in
  stubs__thnn_differentiable_gru_cell_backward (CArray.start out__) (globalize_gc_tensor grad_hy) (globalize_gc_tensor input_gates) (globalize_gc_tensor hidden_gates) (globalize_gc_tensor hx) (match (Base.Option.globalize globalize_gc_tensor input_bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor hidden_bias) with | Some v -> v | None -> none_gc_tensor);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  t0, t1, t2, t3, t4

let _thnn_differentiable_lstm_cell_backward ~(grad_hy @ local) ~(grad_cy @ local) ~(input_gates @ local) ~(hidden_gates @ local) ~(input_bias @ local) ~(hidden_bias @ local) ~(cx @ local) ~(cy @ local) =
  let out__ = CArray.make raw_tensor 5 in
  stubs__thnn_differentiable_lstm_cell_backward (CArray.start out__) (match (Base.Option.globalize globalize_gc_tensor grad_hy) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor grad_cy) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor input_gates) (globalize_gc_tensor hidden_gates) (match (Base.Option.globalize globalize_gc_tensor input_bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor hidden_bias) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor cx) (globalize_gc_tensor cy);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  t0, t1, t2, t3, t4

let _thnn_fused_gru_cell ~(input_gates @ local) ~(hidden_gates @ local) ~(hx @ local) ~(input_bias @ local) ~(hidden_bias @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs__thnn_fused_gru_cell (CArray.start out__) (globalize_gc_tensor input_gates) (globalize_gc_tensor hidden_gates) (globalize_gc_tensor hx) (match (Base.Option.globalize globalize_gc_tensor input_bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor hidden_bias) with | Some v -> v | None -> none_gc_tensor);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _thnn_fused_gru_cell_backward ~(grad_hy @ local) ~(workspace @ local) ~has_bias =
  let out__ = CArray.make raw_tensor 5 in
  stubs__thnn_fused_gru_cell_backward (CArray.start out__) (globalize_gc_tensor grad_hy) (globalize_gc_tensor workspace) (if has_bias then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  t0, t1, t2, t3, t4

let _thnn_fused_gru_cell_backward_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) ~(out3 @ local) ~(out4 @ local) ~(grad_hy @ local) ~(workspace @ local) ~has_bias =
  let out__ = CArray.make raw_tensor 5 in
  stubs__thnn_fused_gru_cell_backward_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor out3) (globalize_gc_tensor out4) (globalize_gc_tensor grad_hy) (globalize_gc_tensor workspace) (if has_bias then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  t0, t1, t2, t3, t4

let _thnn_fused_gru_cell_out ~(out0 @ local) ~(out1 @ local) ~(input_gates @ local) ~(hidden_gates @ local) ~(hx @ local) ~(input_bias @ local) ~(hidden_bias @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs__thnn_fused_gru_cell_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor input_gates) (globalize_gc_tensor hidden_gates) (globalize_gc_tensor hx) (match (Base.Option.globalize globalize_gc_tensor input_bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor hidden_bias) with | Some v -> v | None -> none_gc_tensor);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _thnn_fused_lstm_cell ~(input_gates @ local) ~(hidden_gates @ local) ~(cx @ local) ~(input_bias @ local) ~(hidden_bias @ local) =
  let out__ = CArray.make raw_tensor 3 in
  stubs__thnn_fused_lstm_cell (CArray.start out__) (globalize_gc_tensor input_gates) (globalize_gc_tensor hidden_gates) (globalize_gc_tensor cx) (match (Base.Option.globalize globalize_gc_tensor input_bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor hidden_bias) with | Some v -> v | None -> none_gc_tensor);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _thnn_fused_lstm_cell_backward ~(grad_hy @ local) ~(grad_cy @ local) ~(cx @ local) ~(cy @ local) ~(workspace @ local) ~has_bias =
  let out__ = CArray.make raw_tensor 5 in
  stubs__thnn_fused_lstm_cell_backward (CArray.start out__) (match (Base.Option.globalize globalize_gc_tensor grad_hy) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor grad_cy) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor cx) (globalize_gc_tensor cy) (globalize_gc_tensor workspace) (if has_bias then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  t0, t1, t2, t3, t4

let _thnn_fused_lstm_cell_backward_impl ~(grad_hy @ local) ~(grad_cy @ local) ~(cx @ local) ~(cy @ local) ~(workspace @ local) ~has_bias =
  let out__ = CArray.make raw_tensor 3 in
  stubs__thnn_fused_lstm_cell_backward_impl (CArray.start out__) (match (Base.Option.globalize globalize_gc_tensor grad_hy) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor grad_cy) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor cx) (globalize_gc_tensor cy) (globalize_gc_tensor workspace) (if has_bias then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _thnn_fused_lstm_cell_backward_impl_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) ~(grad_hy @ local) ~(grad_cy @ local) ~(cx @ local) ~(cy @ local) ~(workspace @ local) ~has_bias =
  let out__ = CArray.make raw_tensor 3 in
  stubs__thnn_fused_lstm_cell_backward_impl_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (match (Base.Option.globalize globalize_gc_tensor grad_hy) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor grad_cy) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor cx) (globalize_gc_tensor cy) (globalize_gc_tensor workspace) (if has_bias then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _thnn_fused_lstm_cell_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) ~(input_gates @ local) ~(hidden_gates @ local) ~(cx @ local) ~(input_bias @ local) ~(hidden_bias @ local) =
  let out__ = CArray.make raw_tensor 3 in
  stubs__thnn_fused_lstm_cell_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor input_gates) (globalize_gc_tensor hidden_gates) (globalize_gc_tensor cx) (match (Base.Option.globalize globalize_gc_tensor input_bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor hidden_bias) with | Some v -> v | None -> none_gc_tensor);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _to_copy (self @ local) ~options ~non_blocking =
  stubs__to_copy (globalize_gc_tensor self) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) (if non_blocking then 1 else 0) |> with_tensor_gc

let _to_copy_out ~(out @ local) (self @ local) ~non_blocking =
  stubs__to_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (if non_blocking then 1 else 0) |> with_tensor_gc

let _to_cpu (tensors @ local) =
  stubs__to_cpu (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> to_tensor_list

let _to_dense (self @ local) ~dtype ~masked_grad =
  stubs__to_dense (globalize_gc_tensor self) (Kind.packed_to_int dtype) (if masked_grad then 1 else 0) |> with_tensor_gc

let _to_dense_out ~(out @ local) (self @ local) ~dtype ~masked_grad =
  stubs__to_dense_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Kind.packed_to_int dtype) (if masked_grad then 1 else 0) |> with_tensor_gc

let _to_sparse_bsc (self @ local) ~blocksize ~dense_dim =
  stubs__to_sparse_bsc (globalize_gc_tensor self) (List.map Int64.of_int blocksize |> CArray.of_list int64_t |> CArray.start) (List.length blocksize)  (match dense_dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dense_dim with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _to_sparse_bsc_out ~(out @ local) (self @ local) ~blocksize ~dense_dim =
  stubs__to_sparse_bsc_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int blocksize |> CArray.of_list int64_t |> CArray.start) (List.length blocksize)  (match dense_dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dense_dim with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _to_sparse_bsr (self @ local) ~blocksize ~dense_dim =
  stubs__to_sparse_bsr (globalize_gc_tensor self) (List.map Int64.of_int blocksize |> CArray.of_list int64_t |> CArray.start) (List.length blocksize)  (match dense_dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dense_dim with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _to_sparse_bsr_out ~(out @ local) (self @ local) ~blocksize ~dense_dim =
  stubs__to_sparse_bsr_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int blocksize |> CArray.of_list int64_t |> CArray.start) (List.length blocksize)  (match dense_dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dense_dim with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _to_sparse_csc (self @ local) ~dense_dim =
  stubs__to_sparse_csc (globalize_gc_tensor self)  (match dense_dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dense_dim with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _to_sparse_csc_out ~(out @ local) (self @ local) ~dense_dim =
  stubs__to_sparse_csc_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (match dense_dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dense_dim with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _to_sparse_csr (self @ local) ~dense_dim =
  stubs__to_sparse_csr (globalize_gc_tensor self)  (match dense_dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dense_dim with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _to_sparse_csr_out ~(out @ local) (self @ local) ~dense_dim =
  stubs__to_sparse_csr_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (match dense_dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dense_dim with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _to_sparse_semi_structured ~(dense @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs__to_sparse_semi_structured (CArray.start out__) (globalize_gc_tensor dense);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _transform_bias_rescale_qkv ~(qkv @ local) ~(qkv_bias @ local) ~num_heads =
  let out__ = CArray.make raw_tensor 3 in
  stubs__transform_bias_rescale_qkv (CArray.start out__) (globalize_gc_tensor qkv) (globalize_gc_tensor qkv_bias) (Int64.of_int num_heads);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _transform_bias_rescale_qkv_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) ~(qkv @ local) ~(qkv_bias @ local) ~num_heads =
  let out__ = CArray.make raw_tensor 3 in
  stubs__transform_bias_rescale_qkv_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor qkv) (globalize_gc_tensor qkv_bias) (Int64.of_int num_heads);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _transformer_encoder_layer_fwd ~(src @ local) ~embed_dim ~num_heads ~(qkv_weight @ local) ~(qkv_bias @ local) ~(proj_weight @ local) ~(proj_bias @ local) ~use_gelu ~norm_first ~eps ~(norm_weight_1 @ local) ~(norm_bias_1 @ local) ~(norm_weight_2 @ local) ~(norm_bias_2 @ local) ~(ffn_weight_1 @ local) ~(ffn_bias_1 @ local) ~(ffn_weight_2 @ local) ~(ffn_bias_2 @ local) ~(mask @ local) ~mask_type =
  stubs__transformer_encoder_layer_fwd (globalize_gc_tensor src) (Int64.of_int embed_dim) (Int64.of_int num_heads) (globalize_gc_tensor qkv_weight) (globalize_gc_tensor qkv_bias) (globalize_gc_tensor proj_weight) (globalize_gc_tensor proj_bias) (if use_gelu then 1 else 0) (if norm_first then 1 else 0) eps (globalize_gc_tensor norm_weight_1) (globalize_gc_tensor norm_bias_1) (globalize_gc_tensor norm_weight_2) (globalize_gc_tensor norm_bias_2) (globalize_gc_tensor ffn_weight_1) (globalize_gc_tensor ffn_bias_1) (globalize_gc_tensor ffn_weight_2) (globalize_gc_tensor ffn_bias_2) (match (Base.Option.globalize globalize_gc_tensor mask) with | Some v -> v | None -> none_gc_tensor)  (match mask_type with | None -> Int64.zero | Some v -> Int64.of_int v) (match mask_type with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _transformer_encoder_layer_fwd_out ~(out @ local) ~(src @ local) ~embed_dim ~num_heads ~(qkv_weight @ local) ~(qkv_bias @ local) ~(proj_weight @ local) ~(proj_bias @ local) ~use_gelu ~norm_first ~eps ~(norm_weight_1 @ local) ~(norm_bias_1 @ local) ~(norm_weight_2 @ local) ~(norm_bias_2 @ local) ~(ffn_weight_1 @ local) ~(ffn_bias_1 @ local) ~(ffn_weight_2 @ local) ~(ffn_bias_2 @ local) ~(mask @ local) ~mask_type =
  stubs__transformer_encoder_layer_fwd_out (globalize_gc_tensor out) (globalize_gc_tensor src) (Int64.of_int embed_dim) (Int64.of_int num_heads) (globalize_gc_tensor qkv_weight) (globalize_gc_tensor qkv_bias) (globalize_gc_tensor proj_weight) (globalize_gc_tensor proj_bias) (if use_gelu then 1 else 0) (if norm_first then 1 else 0) eps (globalize_gc_tensor norm_weight_1) (globalize_gc_tensor norm_bias_1) (globalize_gc_tensor norm_weight_2) (globalize_gc_tensor norm_bias_2) (globalize_gc_tensor ffn_weight_1) (globalize_gc_tensor ffn_bias_1) (globalize_gc_tensor ffn_weight_2) (globalize_gc_tensor ffn_bias_2) (match (Base.Option.globalize globalize_gc_tensor mask) with | Some v -> v | None -> none_gc_tensor)  (match mask_type with | None -> Int64.zero | Some v -> Int64.of_int v) (match mask_type with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _trilinear ~(i1 @ local) ~(i2 @ local) ~(i3 @ local) ~expand1 ~expand2 ~expand3 ~sumdim ~unroll_dim =
  stubs__trilinear (globalize_gc_tensor i1) (globalize_gc_tensor i2) (globalize_gc_tensor i3) (List.map Int64.of_int expand1 |> CArray.of_list int64_t |> CArray.start) (List.length expand1) (List.map Int64.of_int expand2 |> CArray.of_list int64_t |> CArray.start) (List.length expand2) (List.map Int64.of_int expand3 |> CArray.of_list int64_t |> CArray.start) (List.length expand3) (List.map Int64.of_int sumdim |> CArray.of_list int64_t |> CArray.start) (List.length sumdim) (Int64.of_int unroll_dim) |> with_tensor_gc

let _trilinear_out ~(out @ local) ~(i1 @ local) ~(i2 @ local) ~(i3 @ local) ~expand1 ~expand2 ~expand3 ~sumdim ~unroll_dim =
  stubs__trilinear_out (globalize_gc_tensor out) (globalize_gc_tensor i1) (globalize_gc_tensor i2) (globalize_gc_tensor i3) (List.map Int64.of_int expand1 |> CArray.of_list int64_t |> CArray.start) (List.length expand1) (List.map Int64.of_int expand2 |> CArray.of_list int64_t |> CArray.start) (List.length expand2) (List.map Int64.of_int expand3 |> CArray.of_list int64_t |> CArray.start) (List.length expand3) (List.map Int64.of_int sumdim |> CArray.of_list int64_t |> CArray.start) (List.length sumdim) (Int64.of_int unroll_dim) |> with_tensor_gc

let _triton_multi_head_attention ~(query @ local) ~(key @ local) ~(value @ local) ~embed_dim ~num_head ~(qkv_weight @ local) ~(qkv_bias @ local) ~(proj_weight @ local) ~(proj_bias @ local) ~(mask @ local) =
  stubs__triton_multi_head_attention (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) (Int64.of_int embed_dim) (Int64.of_int num_head) (globalize_gc_tensor qkv_weight) (globalize_gc_tensor qkv_bias) (globalize_gc_tensor proj_weight) (globalize_gc_tensor proj_bias) (match (Base.Option.globalize globalize_gc_tensor mask) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let _triton_multi_head_attention_out ~(out @ local) ~(query @ local) ~(key @ local) ~(value @ local) ~embed_dim ~num_head ~(qkv_weight @ local) ~(qkv_bias @ local) ~(proj_weight @ local) ~(proj_bias @ local) ~(mask @ local) =
  stubs__triton_multi_head_attention_out (globalize_gc_tensor out) (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) (Int64.of_int embed_dim) (Int64.of_int num_head) (globalize_gc_tensor qkv_weight) (globalize_gc_tensor qkv_bias) (globalize_gc_tensor proj_weight) (globalize_gc_tensor proj_bias) (match (Base.Option.globalize globalize_gc_tensor mask) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let _triton_scaled_dot_attention ~(q @ local) ~(k @ local) ~(v @ local) ~dropout_p =
  stubs__triton_scaled_dot_attention (globalize_gc_tensor q) (globalize_gc_tensor k) (globalize_gc_tensor v) dropout_p |> with_tensor_gc

let _triton_scaled_dot_attention_out ~(out @ local) ~(q @ local) ~(k @ local) ~(v @ local) ~dropout_p =
  stubs__triton_scaled_dot_attention_out (globalize_gc_tensor out) (globalize_gc_tensor q) (globalize_gc_tensor k) (globalize_gc_tensor v) dropout_p |> with_tensor_gc

let _unique (self @ local) ~sorted ~return_inverse =
  let out__ = CArray.make raw_tensor 2 in
  stubs__unique (CArray.start out__) (globalize_gc_tensor self) (if sorted then 1 else 0) (if return_inverse then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _unique2 (self @ local) ~sorted ~return_inverse ~return_counts =
  let out__ = CArray.make raw_tensor 3 in
  stubs__unique2 (CArray.start out__) (globalize_gc_tensor self) (if sorted then 1 else 0) (if return_inverse then 1 else 0) (if return_counts then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _unique2_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) (self @ local) ~sorted ~return_inverse ~return_counts =
  let out__ = CArray.make raw_tensor 3 in
  stubs__unique2_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor self) (if sorted then 1 else 0) (if return_inverse then 1 else 0) (if return_counts then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let _unique_out ~(out0 @ local) ~(out1 @ local) (self @ local) ~sorted ~return_inverse =
  let out__ = CArray.make raw_tensor 2 in
  stubs__unique_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor self) (if sorted then 1 else 0) (if return_inverse then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _unpack_dual ~(dual @ local) ~level =
  let out__ = CArray.make raw_tensor 2 in
  stubs__unpack_dual (CArray.start out__) (globalize_gc_tensor dual) (Int64.of_int level);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _unsafe_view (self @ local) ~size =
  stubs__unsafe_view (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let _unsafe_view_out ~(out @ local) (self @ local) ~size =
  stubs__unsafe_view_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let _upsample_bicubic2d_aa (self @ local) ~output_size ~align_corners ~scales_h ~scales_w =
  stubs__upsample_bicubic2d_aa (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_bicubic2d_aa_backward ~(grad_output @ local) ~output_size ~input_size ~align_corners ~scales_h ~scales_w =
  stubs__upsample_bicubic2d_aa_backward (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_bicubic2d_aa_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) ~output_size ~input_size ~align_corners ~scales_h ~scales_w =
  stubs__upsample_bicubic2d_aa_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_bicubic2d_aa_out ~(out @ local) (self @ local) ~output_size ~align_corners ~scales_h ~scales_w =
  stubs__upsample_bicubic2d_aa_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_bicubic2d_aa_vec (input @ local) ~output_size ~align_corners ~scale_factors =
  stubs__upsample_bicubic2d_aa_vec (globalize_gc_tensor input) (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) (if align_corners then 1 else 0) (scale_factors |> CArray.of_list double |> CArray.start) (List.length scale_factors) |> with_tensor_gc

let _upsample_bilinear2d_aa (self @ local) ~output_size ~align_corners ~scales_h ~scales_w =
  stubs__upsample_bilinear2d_aa (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_bilinear2d_aa_backward ~(grad_output @ local) ~output_size ~input_size ~align_corners ~scales_h ~scales_w =
  stubs__upsample_bilinear2d_aa_backward (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_bilinear2d_aa_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) ~output_size ~input_size ~align_corners ~scales_h ~scales_w =
  stubs__upsample_bilinear2d_aa_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_bilinear2d_aa_out ~(out @ local) (self @ local) ~output_size ~align_corners ~scales_h ~scales_w =
  stubs__upsample_bilinear2d_aa_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_bilinear2d_aa_vec (input @ local) ~output_size ~align_corners ~scale_factors =
  stubs__upsample_bilinear2d_aa_vec (globalize_gc_tensor input) (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) (if align_corners then 1 else 0) (scale_factors |> CArray.of_list double |> CArray.start) (List.length scale_factors) |> with_tensor_gc

let _upsample_lanczos2d_aa (self @ local) ~output_size ~align_corners ~scales_h ~scales_w =
  stubs__upsample_lanczos2d_aa (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_lanczos2d_aa_backward ~(grad_output @ local) ~output_size ~input_size ~align_corners ~scales_h ~scales_w =
  stubs__upsample_lanczos2d_aa_backward (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_lanczos2d_aa_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) ~output_size ~input_size ~align_corners ~scales_h ~scales_w =
  stubs__upsample_lanczos2d_aa_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_lanczos2d_aa_out ~(out @ local) (self @ local) ~output_size ~align_corners ~scales_h ~scales_w =
  stubs__upsample_lanczos2d_aa_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_lanczos2d_aa_vec (input @ local) ~output_size ~align_corners ~scale_factors =
  stubs__upsample_lanczos2d_aa_vec (globalize_gc_tensor input) (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) (if align_corners then 1 else 0) (scale_factors |> CArray.of_list double |> CArray.start) (List.length scale_factors) |> with_tensor_gc

let _upsample_nearest_exact1d (self @ local) ~output_size ~scales =
  stubs__upsample_nearest_exact1d (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size)  (Option.value scales ~default:0.0) (match scales with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_nearest_exact1d_backward ~(grad_output @ local) ~output_size ~input_size ~scales =
  stubs__upsample_nearest_exact1d_backward (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size)  (Option.value scales ~default:0.0) (match scales with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_nearest_exact1d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) ~output_size ~input_size ~scales =
  stubs__upsample_nearest_exact1d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size)  (Option.value scales ~default:0.0) (match scales with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_nearest_exact1d_out ~(out @ local) (self @ local) ~output_size ~scales =
  stubs__upsample_nearest_exact1d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size)  (Option.value scales ~default:0.0) (match scales with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_nearest_exact1d_vec (input @ local) ~output_size ~scale_factors =
  stubs__upsample_nearest_exact1d_vec (globalize_gc_tensor input) (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) (scale_factors |> CArray.of_list double |> CArray.start) (List.length scale_factors) |> with_tensor_gc

let _upsample_nearest_exact2d (self @ local) ~output_size ~scales_h ~scales_w =
  stubs__upsample_nearest_exact2d (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_nearest_exact2d_backward ~(grad_output @ local) ~output_size ~input_size ~scales_h ~scales_w =
  stubs__upsample_nearest_exact2d_backward (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_nearest_exact2d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) ~output_size ~input_size ~scales_h ~scales_w =
  stubs__upsample_nearest_exact2d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_nearest_exact2d_out ~(out @ local) (self @ local) ~output_size ~scales_h ~scales_w =
  stubs__upsample_nearest_exact2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_nearest_exact2d_vec (input @ local) ~output_size ~scale_factors =
  stubs__upsample_nearest_exact2d_vec (globalize_gc_tensor input) (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) (scale_factors |> CArray.of_list double |> CArray.start) (List.length scale_factors) |> with_tensor_gc

let _upsample_nearest_exact3d (self @ local) ~output_size ~scales_d ~scales_h ~scales_w =
  stubs__upsample_nearest_exact3d (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size)  (Option.value scales_d ~default:0.0) (match scales_d with | Some _ -> 0 | None -> 1)   (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_nearest_exact3d_backward ~(grad_output @ local) ~output_size ~input_size ~scales_d ~scales_h ~scales_w =
  stubs__upsample_nearest_exact3d_backward (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size)  (Option.value scales_d ~default:0.0) (match scales_d with | Some _ -> 0 | None -> 1)   (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_nearest_exact3d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) ~output_size ~input_size ~scales_d ~scales_h ~scales_w =
  stubs__upsample_nearest_exact3d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size)  (Option.value scales_d ~default:0.0) (match scales_d with | Some _ -> 0 | None -> 1)   (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_nearest_exact3d_out ~(out @ local) (self @ local) ~output_size ~scales_d ~scales_h ~scales_w =
  stubs__upsample_nearest_exact3d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size)  (Option.value scales_d ~default:0.0) (match scales_d with | Some _ -> 0 | None -> 1)   (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let _upsample_nearest_exact3d_vec (input @ local) ~output_size ~scale_factors =
  stubs__upsample_nearest_exact3d_vec (globalize_gc_tensor input) (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) (scale_factors |> CArray.of_list double |> CArray.start) (List.length scale_factors) |> with_tensor_gc

let _use_cudnn_ctc_loss ~(log_probs @ local) ~(targets @ local) ~input_lengths ~target_lengths ~blank =
  stubs__use_cudnn_ctc_loss (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (List.map Int64.of_int input_lengths |> CArray.of_list int64_t |> CArray.start) (List.length input_lengths) (List.map Int64.of_int target_lengths |> CArray.of_list int64_t |> CArray.start) (List.length target_lengths) (Int64.of_int blank)

let _use_cudnn_ctc_loss_tensor ~(log_probs @ local) ~(targets @ local) ~(input_lengths @ local) ~(target_lengths @ local) ~blank =
  stubs__use_cudnn_ctc_loss_tensor (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (globalize_gc_tensor input_lengths) (globalize_gc_tensor target_lengths) (Int64.of_int blank)

let _use_cudnn_rnn_flatten_weight  =
  stubs__use_cudnn_rnn_flatten_weight 

let _use_miopen_ctc_loss ~(log_probs @ local) ~(targets @ local) ~input_lengths ~target_lengths ~blank =
  stubs__use_miopen_ctc_loss (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (List.map Int64.of_int input_lengths |> CArray.of_list int64_t |> CArray.start) (List.length input_lengths) (List.map Int64.of_int target_lengths |> CArray.of_list int64_t |> CArray.start) (List.length target_lengths) (Int64.of_int blank)

let _use_miopen_ctc_loss_tensor ~(log_probs @ local) ~(targets @ local) ~(input_lengths @ local) ~(target_lengths @ local) ~blank =
  stubs__use_miopen_ctc_loss_tensor (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (globalize_gc_tensor input_lengths) (globalize_gc_tensor target_lengths) (Int64.of_int blank)

let _validate_compressed_sparse_indices ~is_crow ~(compressed_idx @ local) ~(plain_idx @ local) ~cdim ~dim ~nnz =
  stubs__validate_compressed_sparse_indices (if is_crow then 1 else 0) (globalize_gc_tensor compressed_idx) (globalize_gc_tensor plain_idx) (Int64.of_int cdim) (Int64.of_int dim) (Int64.of_int nnz)

let _validate_sparse_bsc_tensor_args ~(ccol_indices @ local) ~(row_indices @ local) ~(values @ local) ~size ~check_pinning =
  stubs__validate_sparse_bsc_tensor_args (globalize_gc_tensor ccol_indices) (globalize_gc_tensor row_indices) (globalize_gc_tensor values) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (if check_pinning then 1 else 0)

let _validate_sparse_bsr_tensor_args ~(crow_indices @ local) ~(col_indices @ local) ~(values @ local) ~size ~check_pinning =
  stubs__validate_sparse_bsr_tensor_args (globalize_gc_tensor crow_indices) (globalize_gc_tensor col_indices) (globalize_gc_tensor values) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (if check_pinning then 1 else 0)

let _validate_sparse_csc_tensor_args ~(ccol_indices @ local) ~(row_indices @ local) ~(values @ local) ~size ~check_pinning =
  stubs__validate_sparse_csc_tensor_args (globalize_gc_tensor ccol_indices) (globalize_gc_tensor row_indices) (globalize_gc_tensor values) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (if check_pinning then 1 else 0)

let _values (self @ local) =
  stubs__values (globalize_gc_tensor self) |> with_tensor_gc

let _values_copy (self @ local) =
  stubs__values_copy (globalize_gc_tensor self) |> with_tensor_gc

let _values_copy_out ~(out @ local) (self @ local) =
  stubs__values_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let _version (self @ local) =
  stubs__version (globalize_gc_tensor self)

let _weight_int4pack_mm (self @ local) ~(mat2 @ local) ~qgroupsize ~(qscaleandzeros @ local) =
  stubs__weight_int4pack_mm (globalize_gc_tensor self) (globalize_gc_tensor mat2) (Int64.of_int qgroupsize) (globalize_gc_tensor qscaleandzeros) |> with_tensor_gc

let _weight_int4pack_mm_for_cpu (self @ local) ~(mat2 @ local) ~qgroupsize ~(qscaleandzeros @ local) =
  stubs__weight_int4pack_mm_for_cpu (globalize_gc_tensor self) (globalize_gc_tensor mat2) (Int64.of_int qgroupsize) (globalize_gc_tensor qscaleandzeros) |> with_tensor_gc

let _weight_int4pack_mm_with_scales_and_zeros (self @ local) ~(mat2 @ local) ~qgroupsize ~(qscale @ local) ~(qzeros @ local) =
  stubs__weight_int4pack_mm_with_scales_and_zeros (globalize_gc_tensor self) (globalize_gc_tensor mat2) (Int64.of_int qgroupsize) (globalize_gc_tensor qscale) (globalize_gc_tensor qzeros) |> with_tensor_gc

let _weight_int8pack_mm (self @ local) ~(mat2 @ local) ~(scales @ local) =
  stubs__weight_int8pack_mm (globalize_gc_tensor self) (globalize_gc_tensor mat2) (globalize_gc_tensor scales) |> with_tensor_gc

let _weight_norm ~(v @ local) ~(g @ local) ~dim =
  stubs__weight_norm (globalize_gc_tensor v) (globalize_gc_tensor g) (Int64.of_int dim) |> with_tensor_gc

let _weight_norm_differentiable_backward ~(grad_w @ local) ~(saved_v @ local) ~(saved_g @ local) ~(saved_norms @ local) ~dim =
  let out__ = CArray.make raw_tensor 2 in
  stubs__weight_norm_differentiable_backward (CArray.start out__) (globalize_gc_tensor grad_w) (globalize_gc_tensor saved_v) (globalize_gc_tensor saved_g) (globalize_gc_tensor saved_norms) (Int64.of_int dim);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _weight_norm_interface ~(v @ local) ~(g @ local) ~dim =
  let out__ = CArray.make raw_tensor 2 in
  stubs__weight_norm_interface (CArray.start out__) (globalize_gc_tensor v) (globalize_gc_tensor g) (Int64.of_int dim);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _weight_norm_interface_backward ~(grad_w @ local) ~(saved_v @ local) ~(saved_g @ local) ~(saved_norms @ local) ~dim =
  let out__ = CArray.make raw_tensor 2 in
  stubs__weight_norm_interface_backward (CArray.start out__) (globalize_gc_tensor grad_w) (globalize_gc_tensor saved_v) (globalize_gc_tensor saved_g) (globalize_gc_tensor saved_norms) (Int64.of_int dim);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _weight_norm_interface_backward_out ~(out0 @ local) ~(out1 @ local) ~(grad_w @ local) ~(saved_v @ local) ~(saved_g @ local) ~(saved_norms @ local) ~dim =
  let out__ = CArray.make raw_tensor 2 in
  stubs__weight_norm_interface_backward_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor grad_w) (globalize_gc_tensor saved_v) (globalize_gc_tensor saved_g) (globalize_gc_tensor saved_norms) (Int64.of_int dim);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _weight_norm_interface_out ~(out0 @ local) ~(out1 @ local) ~(v @ local) ~(g @ local) ~dim =
  let out__ = CArray.make raw_tensor 2 in
  stubs__weight_norm_interface_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor v) (globalize_gc_tensor g) (Int64.of_int dim);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let _wrapped_linear_prepack ~(weight @ local) ~(weight_scale @ local) ~(weight_zero_point @ local) ~(bias @ local) =
  stubs__wrapped_linear_prepack (globalize_gc_tensor weight) (globalize_gc_tensor weight_scale) (globalize_gc_tensor weight_zero_point) (globalize_gc_tensor bias) |> with_tensor_gc

let _wrapped_quantized_linear_prepacked (input @ local) ~(input_scale @ local) ~(input_zero_point @ local) ~(packed_weight @ local) ~(output_scale @ local) ~(output_zero_point @ local) ~out_channel =
  stubs__wrapped_quantized_linear_prepacked (globalize_gc_tensor input) (globalize_gc_tensor input_scale) (globalize_gc_tensor input_zero_point) (globalize_gc_tensor packed_weight) (globalize_gc_tensor output_scale) (globalize_gc_tensor output_zero_point) (Int64.of_int out_channel) |> with_tensor_gc

let abs (self @ local) =
  stubs_abs (globalize_gc_tensor self) |> with_tensor_gc

let abs_ (self @ local) =
  stubs_abs_ (globalize_gc_tensor self) |> with_tensor_gc

let abs_out ~(out @ local) (self @ local) =
  stubs_abs_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let absolute (self @ local) =
  stubs_absolute (globalize_gc_tensor self) |> with_tensor_gc

let absolute_ (self @ local) =
  stubs_absolute_ (globalize_gc_tensor self) |> with_tensor_gc

let absolute_out ~(out @ local) (self @ local) =
  stubs_absolute_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let acos (self @ local) =
  stubs_acos (globalize_gc_tensor self) |> with_tensor_gc

let acos_ (self @ local) =
  stubs_acos_ (globalize_gc_tensor self) |> with_tensor_gc

let acos_out ~(out @ local) (self @ local) =
  stubs_acos_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let acosh (self @ local) =
  stubs_acosh (globalize_gc_tensor self) |> with_tensor_gc

let acosh_ (self @ local) =
  stubs_acosh_ (globalize_gc_tensor self) |> with_tensor_gc

let acosh_out ~(out @ local) (self @ local) =
  stubs_acosh_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let adaptive_avg_pool1d (self @ local) ~output_size =
  stubs_adaptive_avg_pool1d (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) |> with_tensor_gc

let adaptive_avg_pool1d_out ~(out @ local) (self @ local) ~output_size =
  stubs_adaptive_avg_pool1d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) |> with_tensor_gc

let adaptive_avg_pool2d (self @ local) ~output_size =
  stubs_adaptive_avg_pool2d (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) |> with_tensor_gc

let adaptive_avg_pool2d_out ~(out @ local) (self @ local) ~output_size =
  stubs_adaptive_avg_pool2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) |> with_tensor_gc

let adaptive_avg_pool3d (self @ local) ~output_size =
  stubs_adaptive_avg_pool3d (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) |> with_tensor_gc

let adaptive_avg_pool3d_backward ~(grad_input @ local) ~(grad_output @ local) (self @ local) =
  stubs_adaptive_avg_pool3d_backward (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) |> with_tensor_gc

let adaptive_avg_pool3d_out ~(out @ local) (self @ local) ~output_size =
  stubs_adaptive_avg_pool3d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) |> with_tensor_gc

let adaptive_max_pool1d (self @ local) ~output_size =
  let out__ = CArray.make raw_tensor 2 in
  stubs_adaptive_max_pool1d (CArray.start out__) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let adaptive_max_pool2d (self @ local) ~output_size =
  let out__ = CArray.make raw_tensor 2 in
  stubs_adaptive_max_pool2d (CArray.start out__) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let adaptive_max_pool2d_backward ~(grad_output @ local) (self @ local) ~(indices @ local) =
  stubs_adaptive_max_pool2d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor indices) |> with_tensor_gc

let adaptive_max_pool2d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~(indices @ local) =
  stubs_adaptive_max_pool2d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor indices) |> with_tensor_gc

let adaptive_max_pool2d_out ~(out @ local) ~(indices @ local) (self @ local) ~output_size =
  let out__ = CArray.make raw_tensor 2 in
  stubs_adaptive_max_pool2d_out (CArray.start out__) (globalize_gc_tensor out) (globalize_gc_tensor indices) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let adaptive_max_pool3d (self @ local) ~output_size =
  let out__ = CArray.make raw_tensor 2 in
  stubs_adaptive_max_pool3d (CArray.start out__) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let adaptive_max_pool3d_backward ~(grad_output @ local) (self @ local) ~(indices @ local) =
  stubs_adaptive_max_pool3d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor indices) |> with_tensor_gc

let adaptive_max_pool3d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~(indices @ local) =
  stubs_adaptive_max_pool3d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor indices) |> with_tensor_gc

let adaptive_max_pool3d_out ~(out @ local) ~(indices @ local) (self @ local) ~output_size =
  let out__ = CArray.make raw_tensor 2 in
  stubs_adaptive_max_pool3d_out (CArray.start out__) (globalize_gc_tensor out) (globalize_gc_tensor indices) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let add ?alpha (self @ local) (other @ local) =
  stubs_add (globalize_gc_tensor self) (globalize_gc_tensor other) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let add_ ?alpha (self @ local) (other @ local) =
  stubs_add_ (globalize_gc_tensor self) (globalize_gc_tensor other) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let add_out ?alpha ~(out @ local) (self @ local) (other @ local) =
  stubs_add_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let add_scalar ?alpha (self @ local) other =
  stubs_add_scalar (globalize_gc_tensor self) other (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let add_scalar_ ?alpha (self @ local) other =
  stubs_add_scalar_ (globalize_gc_tensor self) other (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let add_scalar_out ?alpha ~(out @ local) (self @ local) other =
  stubs_add_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addbmm ?beta ?alpha (self @ local) ~(batch1 @ local) ~(batch2 @ local) =
  stubs_addbmm (globalize_gc_tensor self) (globalize_gc_tensor batch1) (globalize_gc_tensor batch2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addbmm_ ?beta ?alpha (self @ local) ~(batch1 @ local) ~(batch2 @ local) =
  stubs_addbmm_ (globalize_gc_tensor self) (globalize_gc_tensor batch1) (globalize_gc_tensor batch2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addbmm_out ?beta ?alpha ~(out @ local) (self @ local) ~(batch1 @ local) ~(batch2 @ local) =
  stubs_addbmm_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor batch1) (globalize_gc_tensor batch2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addcdiv ?value (self @ local) ~(tensor1 @ local) ~(tensor2 @ local) =
  stubs_addcdiv (globalize_gc_tensor self) (globalize_gc_tensor tensor1) (globalize_gc_tensor tensor2) (match value with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addcdiv_ ?value (self @ local) ~(tensor1 @ local) ~(tensor2 @ local) =
  stubs_addcdiv_ (globalize_gc_tensor self) (globalize_gc_tensor tensor1) (globalize_gc_tensor tensor2) (match value with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addcdiv_out ?value ~(out @ local) (self @ local) ~(tensor1 @ local) ~(tensor2 @ local) =
  stubs_addcdiv_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor tensor1) (globalize_gc_tensor tensor2) (match value with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addcmul ?value (self @ local) ~(tensor1 @ local) ~(tensor2 @ local) =
  stubs_addcmul (globalize_gc_tensor self) (globalize_gc_tensor tensor1) (globalize_gc_tensor tensor2) (match value with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addcmul_ ?value (self @ local) ~(tensor1 @ local) ~(tensor2 @ local) =
  stubs_addcmul_ (globalize_gc_tensor self) (globalize_gc_tensor tensor1) (globalize_gc_tensor tensor2) (match value with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addcmul_out ?value ~(out @ local) (self @ local) ~(tensor1 @ local) ~(tensor2 @ local) =
  stubs_addcmul_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor tensor1) (globalize_gc_tensor tensor2) (match value with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addmm ?beta ?alpha (self @ local) ~(mat1 @ local) ~(mat2 @ local) =
  stubs_addmm (globalize_gc_tensor self) (globalize_gc_tensor mat1) (globalize_gc_tensor mat2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addmm_ ?beta ?alpha (self @ local) ~(mat1 @ local) ~(mat2 @ local) =
  stubs_addmm_ (globalize_gc_tensor self) (globalize_gc_tensor mat1) (globalize_gc_tensor mat2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addmm_dtype ?beta ?alpha (self @ local) ~(mat1 @ local) ~(mat2 @ local) ~out_dtype =
  stubs_addmm_dtype (globalize_gc_tensor self) (globalize_gc_tensor mat1) (globalize_gc_tensor mat2) (Kind.packed_to_int out_dtype) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addmm_dtype_out ?beta ?alpha ~(out @ local) (self @ local) ~(mat1 @ local) ~(mat2 @ local) ~out_dtype =
  stubs_addmm_dtype_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mat1) (globalize_gc_tensor mat2) (Kind.packed_to_int out_dtype) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addmm_out ?beta ?alpha ~(out @ local) (self @ local) ~(mat1 @ local) ~(mat2 @ local) =
  stubs_addmm_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mat1) (globalize_gc_tensor mat2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addmv ?beta ?alpha (self @ local) ~(mat @ local) ~(vec @ local) =
  stubs_addmv (globalize_gc_tensor self) (globalize_gc_tensor mat) (globalize_gc_tensor vec) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addmv_ ?beta ?alpha (self @ local) ~(mat @ local) ~(vec @ local) =
  stubs_addmv_ (globalize_gc_tensor self) (globalize_gc_tensor mat) (globalize_gc_tensor vec) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addmv_out ?beta ?alpha ~(out @ local) (self @ local) ~(mat @ local) ~(vec @ local) =
  stubs_addmv_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mat) (globalize_gc_tensor vec) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addr ?beta ?alpha (self @ local) ~(vec1 @ local) ~(vec2 @ local) =
  stubs_addr (globalize_gc_tensor self) (globalize_gc_tensor vec1) (globalize_gc_tensor vec2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addr_ ?beta ?alpha (self @ local) ~(vec1 @ local) ~(vec2 @ local) =
  stubs_addr_ (globalize_gc_tensor self) (globalize_gc_tensor vec1) (globalize_gc_tensor vec2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let addr_out ?beta ?alpha ~(out @ local) (self @ local) ~(vec1 @ local) ~(vec2 @ local) =
  stubs_addr_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor vec1) (globalize_gc_tensor vec2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let adjoint (self @ local) =
  stubs_adjoint (globalize_gc_tensor self) |> with_tensor_gc

let affine_grid_generator ~(theta @ local) ~size ~align_corners =
  stubs_affine_grid_generator (globalize_gc_tensor theta) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (if align_corners then 1 else 0) |> with_tensor_gc

let affine_grid_generator_backward ~(grad @ local) ~size ~align_corners =
  stubs_affine_grid_generator_backward (globalize_gc_tensor grad) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (if align_corners then 1 else 0) |> with_tensor_gc

let affine_grid_generator_out ~(out @ local) ~(theta @ local) ~size ~align_corners =
  stubs_affine_grid_generator_out (globalize_gc_tensor out) (globalize_gc_tensor theta) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (if align_corners then 1 else 0) |> with_tensor_gc

let alias (self @ local) =
  stubs_alias (globalize_gc_tensor self) |> with_tensor_gc

let alias_copy (self @ local) =
  stubs_alias_copy (globalize_gc_tensor self) |> with_tensor_gc

let alias_copy_out ~(out @ local) (self @ local) =
  stubs_alias_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let align_as (self @ local) (other @ local) =
  stubs_align_as (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let align_tensors (tensors @ local) =
  stubs_align_tensors (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> to_tensor_list

let all (self @ local) =
  stubs_all (globalize_gc_tensor self) |> with_tensor_gc

let all_all_out ~(out @ local) (self @ local) =
  stubs_all_all_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let all_dim (self @ local) ~dim ~keepdim =
  stubs_all_dim (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0) |> with_tensor_gc

let all_dims (self @ local) ~dim ~keepdim =
  stubs_all_dims (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if keepdim then 1 else 0) |> with_tensor_gc

let all_dims_out ~(out @ local) (self @ local) ~dim ~keepdim =
  stubs_all_dims_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if keepdim then 1 else 0) |> with_tensor_gc

let all_out ~(out @ local) (self @ local) ~dim ~keepdim =
  stubs_all_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0) |> with_tensor_gc

let allclose (self @ local) (other @ local) ~rtol ~atol ~equal_nan =
  stubs_allclose (globalize_gc_tensor self) (globalize_gc_tensor other) rtol atol (if equal_nan then 1 else 0)

let alpha_dropout (input @ local) ~p ~train =
  stubs_alpha_dropout (globalize_gc_tensor input) p (if train then 1 else 0) |> with_tensor_gc

let alpha_dropout_ (self @ local) ~p ~train =
  stubs_alpha_dropout_ (globalize_gc_tensor self) p (if train then 1 else 0) |> with_tensor_gc

let amax (self @ local) ~dim ~keepdim =
  stubs_amax (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) |> with_tensor_gc

let amax_out ~(out @ local) (self @ local) ~dim ~keepdim =
  stubs_amax_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) |> with_tensor_gc

let amin (self @ local) ~dim ~keepdim =
  stubs_amin (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) |> with_tensor_gc

let amin_out ~(out @ local) (self @ local) ~dim ~keepdim =
  stubs_amin_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) |> with_tensor_gc

let aminmax (self @ local) ~dim ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_aminmax (CArray.start out__) (globalize_gc_tensor self)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let aminmax_out ~(min @ local) ~(max @ local) (self @ local) ~dim ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_aminmax_out (CArray.start out__) (globalize_gc_tensor min) (globalize_gc_tensor max) (globalize_gc_tensor self)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let angle (self @ local) =
  stubs_angle (globalize_gc_tensor self) |> with_tensor_gc

let angle_out ~(out @ local) (self @ local) =
  stubs_angle_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let any (self @ local) =
  stubs_any (globalize_gc_tensor self) |> with_tensor_gc

let any_all_out ~(out @ local) (self @ local) =
  stubs_any_all_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let any_dim (self @ local) ~dim ~keepdim =
  stubs_any_dim (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0) |> with_tensor_gc

let any_dims (self @ local) ~dim ~keepdim =
  stubs_any_dims (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if keepdim then 1 else 0) |> with_tensor_gc

let any_dims_out ~(out @ local) (self @ local) ~dim ~keepdim =
  stubs_any_dims_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if keepdim then 1 else 0) |> with_tensor_gc

let any_out ~(out @ local) (self @ local) ~dim ~keepdim =
  stubs_any_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0) |> with_tensor_gc

let arange ~end_ ~options =
  stubs_arange end_ (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let arange_start ~start ~end_ ~options =
  stubs_arange_start start end_ (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let arange_start_step ?step ~start ~end_ ~options () =
  stubs_arange_start_step start end_ (match step with | Some v -> v | None -> none_scalar) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let arccos (self @ local) =
  stubs_arccos (globalize_gc_tensor self) |> with_tensor_gc

let arccos_ (self @ local) =
  stubs_arccos_ (globalize_gc_tensor self) |> with_tensor_gc

let arccos_out ~(out @ local) (self @ local) =
  stubs_arccos_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let arccosh (self @ local) =
  stubs_arccosh (globalize_gc_tensor self) |> with_tensor_gc

let arccosh_ (self @ local) =
  stubs_arccosh_ (globalize_gc_tensor self) |> with_tensor_gc

let arccosh_out ~(out @ local) (self @ local) =
  stubs_arccosh_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let arcsin (self @ local) =
  stubs_arcsin (globalize_gc_tensor self) |> with_tensor_gc

let arcsin_ (self @ local) =
  stubs_arcsin_ (globalize_gc_tensor self) |> with_tensor_gc

let arcsin_out ~(out @ local) (self @ local) =
  stubs_arcsin_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let arcsinh (self @ local) =
  stubs_arcsinh (globalize_gc_tensor self) |> with_tensor_gc

let arcsinh_ (self @ local) =
  stubs_arcsinh_ (globalize_gc_tensor self) |> with_tensor_gc

let arcsinh_out ~(out @ local) (self @ local) =
  stubs_arcsinh_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let arctan (self @ local) =
  stubs_arctan (globalize_gc_tensor self) |> with_tensor_gc

let arctan2 (self @ local) (other @ local) =
  stubs_arctan2 (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let arctan2_ (self @ local) (other @ local) =
  stubs_arctan2_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let arctan2_out ~(out @ local) (self @ local) (other @ local) =
  stubs_arctan2_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let arctan_ (self @ local) =
  stubs_arctan_ (globalize_gc_tensor self) |> with_tensor_gc

let arctan_out ~(out @ local) (self @ local) =
  stubs_arctan_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let arctanh (self @ local) =
  stubs_arctanh (globalize_gc_tensor self) |> with_tensor_gc

let arctanh_ (self @ local) =
  stubs_arctanh_ (globalize_gc_tensor self) |> with_tensor_gc

let arctanh_out ~(out @ local) (self @ local) =
  stubs_arctanh_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let argmax (self @ local) ~dim ~keepdim =
  stubs_argmax (globalize_gc_tensor self)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  (if keepdim then 1 else 0) |> with_tensor_gc

let argmax_out ~(out @ local) (self @ local) ~dim ~keepdim =
  stubs_argmax_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  (if keepdim then 1 else 0) |> with_tensor_gc

let argmin (self @ local) ~dim ~keepdim =
  stubs_argmin (globalize_gc_tensor self)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  (if keepdim then 1 else 0) |> with_tensor_gc

let argmin_out ~(out @ local) (self @ local) ~dim ~keepdim =
  stubs_argmin_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  (if keepdim then 1 else 0) |> with_tensor_gc

let argsort (self @ local) ~dim ~descending =
  stubs_argsort (globalize_gc_tensor self) (Int64.of_int dim) (if descending then 1 else 0) |> with_tensor_gc

let argsort_stable (self @ local) ~stable ~dim ~descending =
  stubs_argsort_stable (globalize_gc_tensor self) (if stable then 1 else 0) (Int64.of_int dim) (if descending then 1 else 0) |> with_tensor_gc

let argsort_stable_out ~(out @ local) (self @ local) ~stable ~dim ~descending =
  stubs_argsort_stable_out (globalize_gc_tensor out) (globalize_gc_tensor self) (if stable then 1 else 0) (Int64.of_int dim) (if descending then 1 else 0) |> with_tensor_gc

let argwhere (self @ local) =
  stubs_argwhere (globalize_gc_tensor self) |> with_tensor_gc

let as_strided (self @ local) ~size ~stride ~storage_offset =
  stubs_as_strided (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride)  (match storage_offset with | None -> Int64.zero | Some v -> Int64.of_int v) (match storage_offset with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let as_strided_ (self @ local) ~size ~stride ~storage_offset =
  stubs_as_strided_ (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride)  (match storage_offset with | None -> Int64.zero | Some v -> Int64.of_int v) (match storage_offset with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let as_strided_copy (self @ local) ~size ~stride ~storage_offset =
  stubs_as_strided_copy (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride)  (match storage_offset with | None -> Int64.zero | Some v -> Int64.of_int v) (match storage_offset with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let as_strided_copy_out ~(out @ local) (self @ local) ~size ~stride ~storage_offset =
  stubs_as_strided_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride)  (match storage_offset with | None -> Int64.zero | Some v -> Int64.of_int v) (match storage_offset with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let as_strided_scatter (self @ local) ~(src @ local) ~size ~stride ~storage_offset =
  stubs_as_strided_scatter (globalize_gc_tensor self) (globalize_gc_tensor src) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride)  (match storage_offset with | None -> Int64.zero | Some v -> Int64.of_int v) (match storage_offset with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let as_strided_scatter_out ~(out @ local) (self @ local) ~(src @ local) ~size ~stride ~storage_offset =
  stubs_as_strided_scatter_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor src) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride)  (match storage_offset with | None -> Int64.zero | Some v -> Int64.of_int v) (match storage_offset with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let asin (self @ local) =
  stubs_asin (globalize_gc_tensor self) |> with_tensor_gc

let asin_ (self @ local) =
  stubs_asin_ (globalize_gc_tensor self) |> with_tensor_gc

let asin_out ~(out @ local) (self @ local) =
  stubs_asin_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let asinh (self @ local) =
  stubs_asinh (globalize_gc_tensor self) |> with_tensor_gc

let asinh_ (self @ local) =
  stubs_asinh_ (globalize_gc_tensor self) |> with_tensor_gc

let asinh_out ~(out @ local) (self @ local) =
  stubs_asinh_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let atan (self @ local) =
  stubs_atan (globalize_gc_tensor self) |> with_tensor_gc

let atan2 (self @ local) (other @ local) =
  stubs_atan2 (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let atan2_ (self @ local) (other @ local) =
  stubs_atan2_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let atan2_out ~(out @ local) (self @ local) (other @ local) =
  stubs_atan2_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let atan_ (self @ local) =
  stubs_atan_ (globalize_gc_tensor self) |> with_tensor_gc

let atan_out ~(out @ local) (self @ local) =
  stubs_atan_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let atanh (self @ local) =
  stubs_atanh (globalize_gc_tensor self) |> with_tensor_gc

let atanh_ (self @ local) =
  stubs_atanh_ (globalize_gc_tensor self) |> with_tensor_gc

let atanh_out ~(out @ local) (self @ local) =
  stubs_atanh_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let atleast_1d (self @ local) =
  stubs_atleast_1d (globalize_gc_tensor self) |> with_tensor_gc

let atleast_1d_sequence (tensors @ local) =
  stubs_atleast_1d_sequence (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> to_tensor_list

let atleast_2d (self @ local) =
  stubs_atleast_2d (globalize_gc_tensor self) |> with_tensor_gc

let atleast_2d_sequence (tensors @ local) =
  stubs_atleast_2d_sequence (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> to_tensor_list

let atleast_3d (self @ local) =
  stubs_atleast_3d (globalize_gc_tensor self) |> with_tensor_gc

let atleast_3d_sequence (tensors @ local) =
  stubs_atleast_3d_sequence (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> to_tensor_list

let avg_pool1d (self @ local) ~kernel_size ~stride ~padding ~ceil_mode ~count_include_pad =
  stubs_avg_pool1d (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (if ceil_mode then 1 else 0) (if count_include_pad then 1 else 0) |> with_tensor_gc

let avg_pool1d_out ~(out @ local) (self @ local) ~kernel_size ~stride ~padding ~ceil_mode ~count_include_pad =
  stubs_avg_pool1d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (if ceil_mode then 1 else 0) (if count_include_pad then 1 else 0) |> with_tensor_gc

let avg_pool2d (self @ local) ~kernel_size ~stride ~padding ~ceil_mode ~count_include_pad ~divisor_override =
  stubs_avg_pool2d (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (if ceil_mode then 1 else 0) (if count_include_pad then 1 else 0)  (match divisor_override with | None -> Int64.zero | Some v -> Int64.of_int v) (match divisor_override with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let avg_pool2d_backward ~(grad_output @ local) (self @ local) ~kernel_size ~stride ~padding ~ceil_mode ~count_include_pad ~divisor_override =
  stubs_avg_pool2d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (if ceil_mode then 1 else 0) (if count_include_pad then 1 else 0)  (match divisor_override with | None -> Int64.zero | Some v -> Int64.of_int v) (match divisor_override with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let avg_pool2d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~kernel_size ~stride ~padding ~ceil_mode ~count_include_pad ~divisor_override =
  stubs_avg_pool2d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (if ceil_mode then 1 else 0) (if count_include_pad then 1 else 0)  (match divisor_override with | None -> Int64.zero | Some v -> Int64.of_int v) (match divisor_override with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let avg_pool2d_out ~(out @ local) (self @ local) ~kernel_size ~stride ~padding ~ceil_mode ~count_include_pad ~divisor_override =
  stubs_avg_pool2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (if ceil_mode then 1 else 0) (if count_include_pad then 1 else 0)  (match divisor_override with | None -> Int64.zero | Some v -> Int64.of_int v) (match divisor_override with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let avg_pool3d (self @ local) ~kernel_size ~stride ~padding ~ceil_mode ~count_include_pad ~divisor_override =
  stubs_avg_pool3d (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (if ceil_mode then 1 else 0) (if count_include_pad then 1 else 0)  (match divisor_override with | None -> Int64.zero | Some v -> Int64.of_int v) (match divisor_override with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let avg_pool3d_backward ~(grad_output @ local) (self @ local) ~kernel_size ~stride ~padding ~ceil_mode ~count_include_pad ~divisor_override =
  stubs_avg_pool3d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (if ceil_mode then 1 else 0) (if count_include_pad then 1 else 0)  (match divisor_override with | None -> Int64.zero | Some v -> Int64.of_int v) (match divisor_override with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let avg_pool3d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~kernel_size ~stride ~padding ~ceil_mode ~count_include_pad ~divisor_override =
  stubs_avg_pool3d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (if ceil_mode then 1 else 0) (if count_include_pad then 1 else 0)  (match divisor_override with | None -> Int64.zero | Some v -> Int64.of_int v) (match divisor_override with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let avg_pool3d_out ~(out @ local) (self @ local) ~kernel_size ~stride ~padding ~ceil_mode ~count_include_pad ~divisor_override =
  stubs_avg_pool3d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (if ceil_mode then 1 else 0) (if count_include_pad then 1 else 0)  (match divisor_override with | None -> Int64.zero | Some v -> Int64.of_int v) (match divisor_override with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let baddbmm ?beta ?alpha (self @ local) ~(batch1 @ local) ~(batch2 @ local) =
  stubs_baddbmm (globalize_gc_tensor self) (globalize_gc_tensor batch1) (globalize_gc_tensor batch2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let baddbmm_ ?beta ?alpha (self @ local) ~(batch1 @ local) ~(batch2 @ local) =
  stubs_baddbmm_ (globalize_gc_tensor self) (globalize_gc_tensor batch1) (globalize_gc_tensor batch2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let baddbmm_dtype ?beta ?alpha (self @ local) ~(batch1 @ local) ~(batch2 @ local) ~out_dtype =
  stubs_baddbmm_dtype (globalize_gc_tensor self) (globalize_gc_tensor batch1) (globalize_gc_tensor batch2) (Kind.packed_to_int out_dtype) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let baddbmm_dtype_out ?beta ?alpha ~(out @ local) (self @ local) ~(batch1 @ local) ~(batch2 @ local) ~out_dtype =
  stubs_baddbmm_dtype_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor batch1) (globalize_gc_tensor batch2) (Kind.packed_to_int out_dtype) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let baddbmm_out ?beta ?alpha ~(out @ local) (self @ local) ~(batch1 @ local) ~(batch2 @ local) =
  stubs_baddbmm_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor batch1) (globalize_gc_tensor batch2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let bartlett_window ~window_length ~options =
  stubs_bartlett_window (Int64.of_int window_length) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let bartlett_window_out ~(out @ local) ~window_length =
  stubs_bartlett_window_out (globalize_gc_tensor out) (Int64.of_int window_length) |> with_tensor_gc

let bartlett_window_periodic ~window_length ~periodic ~options =
  stubs_bartlett_window_periodic (Int64.of_int window_length) (if periodic then 1 else 0) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let bartlett_window_periodic_out ~(out @ local) ~window_length ~periodic =
  stubs_bartlett_window_periodic_out (globalize_gc_tensor out) (Int64.of_int window_length) (if periodic then 1 else 0) |> with_tensor_gc

let batch_norm (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~training ~momentum ~eps ~cudnn_enabled =
  stubs_batch_norm (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) (if training then 1 else 0) momentum eps (if cudnn_enabled then 1 else 0) |> with_tensor_gc

let batch_norm_backward_elemt ~(grad_out @ local) (input @ local) ~(mean @ local) ~(invstd @ local) ~(weight @ local) ~(sum_dy @ local) ~(sum_dy_xmu @ local) ~(count @ local) =
  stubs_batch_norm_backward_elemt (globalize_gc_tensor grad_out) (globalize_gc_tensor input) (globalize_gc_tensor mean) (globalize_gc_tensor invstd) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor sum_dy) (globalize_gc_tensor sum_dy_xmu) (globalize_gc_tensor count) |> with_tensor_gc

let batch_norm_backward_elemt_out ~(out @ local) ~(grad_out @ local) (input @ local) ~(mean @ local) ~(invstd @ local) ~(weight @ local) ~(sum_dy @ local) ~(sum_dy_xmu @ local) ~(count @ local) =
  stubs_batch_norm_backward_elemt_out (globalize_gc_tensor out) (globalize_gc_tensor grad_out) (globalize_gc_tensor input) (globalize_gc_tensor mean) (globalize_gc_tensor invstd) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor sum_dy) (globalize_gc_tensor sum_dy_xmu) (globalize_gc_tensor count) |> with_tensor_gc

let batch_norm_backward_reduce ~(grad_out @ local) (input @ local) ~(mean @ local) ~(invstd @ local) ~(weight @ local) ~input_g ~weight_g ~bias_g =
  let out__ = CArray.make raw_tensor 4 in
  stubs_batch_norm_backward_reduce (CArray.start out__) (globalize_gc_tensor grad_out) (globalize_gc_tensor input) (globalize_gc_tensor mean) (globalize_gc_tensor invstd) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (if input_g then 1 else 0) (if weight_g then 1 else 0) (if bias_g then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let batch_norm_backward_reduce_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) ~(out3 @ local) ~(grad_out @ local) (input @ local) ~(mean @ local) ~(invstd @ local) ~(weight @ local) ~input_g ~weight_g ~bias_g =
  let out__ = CArray.make raw_tensor 4 in
  stubs_batch_norm_backward_reduce_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor out3) (globalize_gc_tensor grad_out) (globalize_gc_tensor input) (globalize_gc_tensor mean) (globalize_gc_tensor invstd) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (if input_g then 1 else 0) (if weight_g then 1 else 0) (if bias_g then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let batch_norm_elemt (input @ local) ~(weight @ local) ~(bias @ local) ~(mean @ local) ~(invstd @ local) ~eps =
  stubs_batch_norm_elemt (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor mean) (globalize_gc_tensor invstd) eps |> with_tensor_gc

let batch_norm_elemt_out ~(out @ local) (input @ local) ~(weight @ local) ~(bias @ local) ~(mean @ local) ~(invstd @ local) ~eps =
  stubs_batch_norm_elemt_out (globalize_gc_tensor out) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor mean) (globalize_gc_tensor invstd) eps |> with_tensor_gc

let batch_norm_gather_stats (input @ local) ~(mean @ local) ~(invstd @ local) ~(running_mean @ local) ~(running_var @ local) ~momentum ~eps ~count =
  let out__ = CArray.make raw_tensor 2 in
  stubs_batch_norm_gather_stats (CArray.start out__) (globalize_gc_tensor input) (globalize_gc_tensor mean) (globalize_gc_tensor invstd) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) momentum eps (Int64.of_int count);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let batch_norm_gather_stats_out ~(out0 @ local) ~(out1 @ local) (input @ local) ~(mean @ local) ~(invstd @ local) ~(running_mean @ local) ~(running_var @ local) ~momentum ~eps ~count =
  let out__ = CArray.make raw_tensor 2 in
  stubs_batch_norm_gather_stats_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor input) (globalize_gc_tensor mean) (globalize_gc_tensor invstd) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) momentum eps (Int64.of_int count);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let batch_norm_gather_stats_with_counts (input @ local) ~(mean @ local) ~(invstd @ local) ~(running_mean @ local) ~(running_var @ local) ~momentum ~eps ~(counts @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_batch_norm_gather_stats_with_counts (CArray.start out__) (globalize_gc_tensor input) (globalize_gc_tensor mean) (globalize_gc_tensor invstd) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) momentum eps (globalize_gc_tensor counts);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let batch_norm_gather_stats_with_counts_out ~(out0 @ local) ~(out1 @ local) (input @ local) ~(mean @ local) ~(invstd @ local) ~(running_mean @ local) ~(running_var @ local) ~momentum ~eps ~(counts @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_batch_norm_gather_stats_with_counts_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor input) (globalize_gc_tensor mean) (globalize_gc_tensor invstd) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) momentum eps (globalize_gc_tensor counts);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let batch_norm_stats (input @ local) ~eps =
  let out__ = CArray.make raw_tensor 2 in
  stubs_batch_norm_stats (CArray.start out__) (globalize_gc_tensor input) eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let batch_norm_stats_out ~(out0 @ local) ~(out1 @ local) (input @ local) ~eps =
  let out__ = CArray.make raw_tensor 2 in
  stubs_batch_norm_stats_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor input) eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let batch_norm_update_stats (input @ local) ~(running_mean @ local) ~(running_var @ local) ~momentum =
  let out__ = CArray.make raw_tensor 2 in
  stubs_batch_norm_update_stats (CArray.start out__) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) momentum;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let batch_norm_update_stats_out ~(out0 @ local) ~(out1 @ local) (input @ local) ~(running_mean @ local) ~(running_var @ local) ~momentum =
  let out__ = CArray.make raw_tensor 2 in
  stubs_batch_norm_update_stats_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) momentum;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let bernoulli (self @ local) =
  stubs_bernoulli (globalize_gc_tensor self) |> with_tensor_gc

let bernoulli_ (self @ local) ~(p @ local) =
  stubs_bernoulli_ (globalize_gc_tensor self) (globalize_gc_tensor p) |> with_tensor_gc

let bernoulli_float_ (self @ local) ~p =
  stubs_bernoulli_float_ (globalize_gc_tensor self) p |> with_tensor_gc

let bernoulli_p (self @ local) ~p =
  stubs_bernoulli_p (globalize_gc_tensor self) p |> with_tensor_gc

let bernoulli_tensor (self @ local) ~(p @ local) =
  stubs_bernoulli_tensor (globalize_gc_tensor self) (globalize_gc_tensor p) |> with_tensor_gc

let bilinear ~(input1 @ local) ~(input2 @ local) ~(weight @ local) ~(bias @ local) =
  stubs_bilinear (globalize_gc_tensor input1) (globalize_gc_tensor input2) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let binary_cross_entropy (self @ local) ~(target @ local) ~(weight @ local) ~reduction =
  stubs_binary_cross_entropy (globalize_gc_tensor self) (globalize_gc_tensor target) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let binary_cross_entropy_backward ~(grad_output @ local) (self @ local) ~(target @ local) ~(weight @ local) ~reduction =
  stubs_binary_cross_entropy_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let binary_cross_entropy_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~(target @ local) ~(weight @ local) ~reduction =
  stubs_binary_cross_entropy_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let binary_cross_entropy_out ~(out @ local) (self @ local) ~(target @ local) ~(weight @ local) ~reduction =
  stubs_binary_cross_entropy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor target) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let binary_cross_entropy_with_logits (self @ local) ~(target @ local) ~(weight @ local) ~(pos_weight @ local) ~reduction =
  stubs_binary_cross_entropy_with_logits (globalize_gc_tensor self) (globalize_gc_tensor target) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor pos_weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let binary_cross_entropy_with_logits_out ~(out @ local) (self @ local) ~(target @ local) ~(weight @ local) ~(pos_weight @ local) ~reduction =
  stubs_binary_cross_entropy_with_logits_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor target) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor pos_weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let bincount (self @ local) ~(weights @ local) ~minlength =
  stubs_bincount (globalize_gc_tensor self) (match (Base.Option.globalize globalize_gc_tensor weights) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int minlength) |> with_tensor_gc

let bincount_out ~(out @ local) (self @ local) ~(weights @ local) ~minlength =
  stubs_bincount_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match (Base.Option.globalize globalize_gc_tensor weights) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int minlength) |> with_tensor_gc

let binomial ~(count @ local) ~(prob @ local) =
  stubs_binomial (globalize_gc_tensor count) (globalize_gc_tensor prob) |> with_tensor_gc

let binomial_out ~(out @ local) ~(count @ local) ~(prob @ local) =
  stubs_binomial_out (globalize_gc_tensor out) (globalize_gc_tensor count) (globalize_gc_tensor prob) |> with_tensor_gc

let bitwise_and (self @ local) other =
  stubs_bitwise_and (globalize_gc_tensor self) other |> with_tensor_gc

let bitwise_and_ (self @ local) other =
  stubs_bitwise_and_ (globalize_gc_tensor self) other |> with_tensor_gc

let bitwise_and_scalar_out ~(out @ local) (self @ local) other =
  stubs_bitwise_and_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let bitwise_and_scalar_tensor self (other @ local) =
  stubs_bitwise_and_scalar_tensor self (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_and_scalar_tensor_out ~(out @ local) self (other @ local) =
  stubs_bitwise_and_scalar_tensor_out (globalize_gc_tensor out) self (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_and_tensor (self @ local) (other @ local) =
  stubs_bitwise_and_tensor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_and_tensor_ (self @ local) (other @ local) =
  stubs_bitwise_and_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_and_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_bitwise_and_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_left_shift (self @ local) (other @ local) =
  stubs_bitwise_left_shift (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_left_shift_ (self @ local) (other @ local) =
  stubs_bitwise_left_shift_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_left_shift_scalar_tensor self (other @ local) =
  stubs_bitwise_left_shift_scalar_tensor self (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_left_shift_scalar_tensor_out ~(out @ local) self (other @ local) =
  stubs_bitwise_left_shift_scalar_tensor_out (globalize_gc_tensor out) self (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_left_shift_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_bitwise_left_shift_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_left_shift_tensor_scalar (self @ local) other =
  stubs_bitwise_left_shift_tensor_scalar (globalize_gc_tensor self) other |> with_tensor_gc

let bitwise_left_shift_tensor_scalar_ (self @ local) other =
  stubs_bitwise_left_shift_tensor_scalar_ (globalize_gc_tensor self) other |> with_tensor_gc

let bitwise_left_shift_tensor_scalar_out ~(out @ local) (self @ local) other =
  stubs_bitwise_left_shift_tensor_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let bitwise_not (self @ local) =
  stubs_bitwise_not (globalize_gc_tensor self) |> with_tensor_gc

let bitwise_not_ (self @ local) =
  stubs_bitwise_not_ (globalize_gc_tensor self) |> with_tensor_gc

let bitwise_not_out ~(out @ local) (self @ local) =
  stubs_bitwise_not_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let bitwise_or (self @ local) other =
  stubs_bitwise_or (globalize_gc_tensor self) other |> with_tensor_gc

let bitwise_or_ (self @ local) other =
  stubs_bitwise_or_ (globalize_gc_tensor self) other |> with_tensor_gc

let bitwise_or_scalar_out ~(out @ local) (self @ local) other =
  stubs_bitwise_or_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let bitwise_or_scalar_tensor self (other @ local) =
  stubs_bitwise_or_scalar_tensor self (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_or_scalar_tensor_out ~(out @ local) self (other @ local) =
  stubs_bitwise_or_scalar_tensor_out (globalize_gc_tensor out) self (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_or_tensor (self @ local) (other @ local) =
  stubs_bitwise_or_tensor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_or_tensor_ (self @ local) (other @ local) =
  stubs_bitwise_or_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_or_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_bitwise_or_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_right_shift (self @ local) (other @ local) =
  stubs_bitwise_right_shift (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_right_shift_ (self @ local) (other @ local) =
  stubs_bitwise_right_shift_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_right_shift_scalar_tensor self (other @ local) =
  stubs_bitwise_right_shift_scalar_tensor self (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_right_shift_scalar_tensor_out ~(out @ local) self (other @ local) =
  stubs_bitwise_right_shift_scalar_tensor_out (globalize_gc_tensor out) self (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_right_shift_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_bitwise_right_shift_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_right_shift_tensor_scalar (self @ local) other =
  stubs_bitwise_right_shift_tensor_scalar (globalize_gc_tensor self) other |> with_tensor_gc

let bitwise_right_shift_tensor_scalar_ (self @ local) other =
  stubs_bitwise_right_shift_tensor_scalar_ (globalize_gc_tensor self) other |> with_tensor_gc

let bitwise_right_shift_tensor_scalar_out ~(out @ local) (self @ local) other =
  stubs_bitwise_right_shift_tensor_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let bitwise_xor (self @ local) other =
  stubs_bitwise_xor (globalize_gc_tensor self) other |> with_tensor_gc

let bitwise_xor_ (self @ local) other =
  stubs_bitwise_xor_ (globalize_gc_tensor self) other |> with_tensor_gc

let bitwise_xor_scalar_out ~(out @ local) (self @ local) other =
  stubs_bitwise_xor_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let bitwise_xor_scalar_tensor self (other @ local) =
  stubs_bitwise_xor_scalar_tensor self (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_xor_scalar_tensor_out ~(out @ local) self (other @ local) =
  stubs_bitwise_xor_scalar_tensor_out (globalize_gc_tensor out) self (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_xor_tensor (self @ local) (other @ local) =
  stubs_bitwise_xor_tensor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_xor_tensor_ (self @ local) (other @ local) =
  stubs_bitwise_xor_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let bitwise_xor_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_bitwise_xor_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let blackman_window ~window_length ~options =
  stubs_blackman_window (Int64.of_int window_length) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let blackman_window_out ~(out @ local) ~window_length =
  stubs_blackman_window_out (globalize_gc_tensor out) (Int64.of_int window_length) |> with_tensor_gc

let blackman_window_periodic ~window_length ~periodic ~options =
  stubs_blackman_window_periodic (Int64.of_int window_length) (if periodic then 1 else 0) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let blackman_window_periodic_out ~(out @ local) ~window_length ~periodic =
  stubs_blackman_window_periodic_out (globalize_gc_tensor out) (Int64.of_int window_length) (if periodic then 1 else 0) |> with_tensor_gc

let block_diag (tensors @ local) =
  stubs_block_diag (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> with_tensor_gc

let block_diag_out ~(out @ local) (tensors @ local) =
  stubs_block_diag_out (globalize_gc_tensor out) (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> with_tensor_gc

let bmm (self @ local) ~(mat2 @ local) =
  stubs_bmm (globalize_gc_tensor self) (globalize_gc_tensor mat2) |> with_tensor_gc

let bmm_dtype (self @ local) ~(mat2 @ local) ~out_dtype =
  stubs_bmm_dtype (globalize_gc_tensor self) (globalize_gc_tensor mat2) (Kind.packed_to_int out_dtype) |> with_tensor_gc

let bmm_dtype_out ~(out @ local) (self @ local) ~(mat2 @ local) ~out_dtype =
  stubs_bmm_dtype_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mat2) (Kind.packed_to_int out_dtype) |> with_tensor_gc

let bmm_out ~(out @ local) (self @ local) ~(mat2 @ local) =
  stubs_bmm_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mat2) |> with_tensor_gc

let broadcast_tensors (tensors @ local) =
  stubs_broadcast_tensors (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> to_tensor_list

let broadcast_to (self @ local) ~size =
  stubs_broadcast_to (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let bucketize (self @ local) ~(boundaries @ local) ~out_int32 ~right =
  stubs_bucketize (globalize_gc_tensor self) (globalize_gc_tensor boundaries) (if out_int32 then 1 else 0) (if right then 1 else 0) |> with_tensor_gc

let bucketize_scalar self ~(boundaries @ local) ~out_int32 ~right =
  stubs_bucketize_scalar self (globalize_gc_tensor boundaries) (if out_int32 then 1 else 0) (if right then 1 else 0) |> with_tensor_gc

let bucketize_scalar_out ~(out @ local) self ~(boundaries @ local) ~out_int32 ~right =
  stubs_bucketize_scalar_out (globalize_gc_tensor out) self (globalize_gc_tensor boundaries) (if out_int32 then 1 else 0) (if right then 1 else 0) |> with_tensor_gc

let bucketize_tensor_out ~(out @ local) (self @ local) ~(boundaries @ local) ~out_int32 ~right =
  stubs_bucketize_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor boundaries) (if out_int32 then 1 else 0) (if right then 1 else 0) |> with_tensor_gc

let can_cast ~from_ ~to_ =
  stubs_can_cast (Kind.packed_to_int from_) (Kind.packed_to_int to_)

let cartesian_prod (tensors @ local) =
  stubs_cartesian_prod (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> with_tensor_gc

let cat (tensors @ local) ~dim =
  stubs_cat (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) (Int64.of_int dim) |> with_tensor_gc

let cat_out ~(out @ local) (tensors @ local) ~dim =
  stubs_cat_out (globalize_gc_tensor out) (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) (Int64.of_int dim) |> with_tensor_gc

let cauchy (self @ local) ~median ~sigma =
  stubs_cauchy (globalize_gc_tensor self) median sigma |> with_tensor_gc

let cauchy_ (self @ local) ~median ~sigma =
  stubs_cauchy_ (globalize_gc_tensor self) median sigma |> with_tensor_gc

let cauchy_out ~(out @ local) (self @ local) ~median ~sigma =
  stubs_cauchy_out (globalize_gc_tensor out) (globalize_gc_tensor self) median sigma |> with_tensor_gc

let ccol_indices (self @ local) =
  stubs_ccol_indices (globalize_gc_tensor self) |> with_tensor_gc

let ccol_indices_copy (self @ local) =
  stubs_ccol_indices_copy (globalize_gc_tensor self) |> with_tensor_gc

let ccol_indices_copy_out ~(out @ local) (self @ local) =
  stubs_ccol_indices_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let cdist ~(x1 @ local) ~(x2 @ local) ~p ~compute_mode =
  stubs_cdist (globalize_gc_tensor x1) (globalize_gc_tensor x2) p  (match compute_mode with | None -> Int64.zero | Some v -> Int64.of_int v) (match compute_mode with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let ceil (self @ local) =
  stubs_ceil (globalize_gc_tensor self) |> with_tensor_gc

let ceil_ (self @ local) =
  stubs_ceil_ (globalize_gc_tensor self) |> with_tensor_gc

let ceil_out ~(out @ local) (self @ local) =
  stubs_ceil_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let celu ?alpha (self @ local) =
  stubs_celu (globalize_gc_tensor self) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let celu_ ?alpha (self @ local) =
  stubs_celu_ (globalize_gc_tensor self) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let celu_out ?alpha ~(out @ local) (self @ local) =
  stubs_celu_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let chain_matmul ~(matrices @ local) =
  stubs_chain_matmul (CArray.of_list gc_tensor (globalize_gc_tensor_list matrices) |> CArray.start) (Base.List.length matrices) |> with_tensor_gc

let chain_matmul_out ~(out @ local) ~(matrices @ local) =
  stubs_chain_matmul_out (globalize_gc_tensor out) (CArray.of_list gc_tensor (globalize_gc_tensor_list matrices) |> CArray.start) (Base.List.length matrices) |> with_tensor_gc

let chalf (self @ local) =
  stubs_chalf (globalize_gc_tensor self) |> with_tensor_gc

let channel_shuffle (self @ local) ~groups =
  stubs_channel_shuffle (globalize_gc_tensor self) (Int64.of_int groups) |> with_tensor_gc

let channel_shuffle_out ~(out @ local) (self @ local) ~groups =
  stubs_channel_shuffle_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int groups) |> with_tensor_gc

let cholesky (self @ local) ~upper =
  stubs_cholesky (globalize_gc_tensor self) (if upper then 1 else 0) |> with_tensor_gc

let cholesky_inverse (self @ local) ~upper =
  stubs_cholesky_inverse (globalize_gc_tensor self) (if upper then 1 else 0) |> with_tensor_gc

let cholesky_inverse_out ~(out @ local) (self @ local) ~upper =
  stubs_cholesky_inverse_out (globalize_gc_tensor out) (globalize_gc_tensor self) (if upper then 1 else 0) |> with_tensor_gc

let cholesky_out ~(out @ local) (self @ local) ~upper =
  stubs_cholesky_out (globalize_gc_tensor out) (globalize_gc_tensor self) (if upper then 1 else 0) |> with_tensor_gc

let cholesky_solve (self @ local) ~(input2 @ local) ~upper =
  stubs_cholesky_solve (globalize_gc_tensor self) (globalize_gc_tensor input2) (if upper then 1 else 0) |> with_tensor_gc

let cholesky_solve_out ~(out @ local) (self @ local) ~(input2 @ local) ~upper =
  stubs_cholesky_solve_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor input2) (if upper then 1 else 0) |> with_tensor_gc

let choose_qparams_optimized (input @ local) ~numel ~n_bins ~ratio ~bit_width =
  let out__ = CArray.make raw_tensor 2 in
  stubs_choose_qparams_optimized (CArray.start out__) (globalize_gc_tensor input) (Int64.of_int numel) (Int64.of_int n_bins) ratio (Int64.of_int bit_width);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let chunk (self @ local) ~chunks ~dim =
  stubs_chunk (globalize_gc_tensor self) (Int64.of_int chunks) (Int64.of_int dim) |> to_tensor_list

let clamp (self @ local) ~min ~max =
  stubs_clamp (globalize_gc_tensor self) min max |> with_tensor_gc

let clamp_ (self @ local) ~min ~max =
  stubs_clamp_ (globalize_gc_tensor self) min max |> with_tensor_gc

let clamp_max (self @ local) ~max =
  stubs_clamp_max (globalize_gc_tensor self) max |> with_tensor_gc

let clamp_max_ (self @ local) ~max =
  stubs_clamp_max_ (globalize_gc_tensor self) max |> with_tensor_gc

let clamp_max_out ~(out @ local) (self @ local) ~max =
  stubs_clamp_max_out (globalize_gc_tensor out) (globalize_gc_tensor self) max |> with_tensor_gc

let clamp_max_tensor (self @ local) ~(max @ local) =
  stubs_clamp_max_tensor (globalize_gc_tensor self) (globalize_gc_tensor max) |> with_tensor_gc

let clamp_max_tensor_ (self @ local) ~(max @ local) =
  stubs_clamp_max_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor max) |> with_tensor_gc

let clamp_max_tensor_out ~(out @ local) (self @ local) ~(max @ local) =
  stubs_clamp_max_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor max) |> with_tensor_gc

let clamp_min (self @ local) ~min =
  stubs_clamp_min (globalize_gc_tensor self) min |> with_tensor_gc

let clamp_min_ (self @ local) ~min =
  stubs_clamp_min_ (globalize_gc_tensor self) min |> with_tensor_gc

let clamp_min_out ~(out @ local) (self @ local) ~min =
  stubs_clamp_min_out (globalize_gc_tensor out) (globalize_gc_tensor self) min |> with_tensor_gc

let clamp_min_tensor (self @ local) ~(min @ local) =
  stubs_clamp_min_tensor (globalize_gc_tensor self) (globalize_gc_tensor min) |> with_tensor_gc

let clamp_min_tensor_ (self @ local) ~(min @ local) =
  stubs_clamp_min_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor min) |> with_tensor_gc

let clamp_min_tensor_out ~(out @ local) (self @ local) ~(min @ local) =
  stubs_clamp_min_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor min) |> with_tensor_gc

let clamp_out ~(out @ local) (self @ local) ~min ~max =
  stubs_clamp_out (globalize_gc_tensor out) (globalize_gc_tensor self) min max |> with_tensor_gc

let clamp_tensor (self @ local) ~(min @ local) ~(max @ local) =
  stubs_clamp_tensor (globalize_gc_tensor self) (match (Base.Option.globalize globalize_gc_tensor min) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor max) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let clamp_tensor_ (self @ local) ~(min @ local) ~(max @ local) =
  stubs_clamp_tensor_ (globalize_gc_tensor self) (match (Base.Option.globalize globalize_gc_tensor min) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor max) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let clamp_tensor_out ~(out @ local) (self @ local) ~(min @ local) ~(max @ local) =
  stubs_clamp_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match (Base.Option.globalize globalize_gc_tensor min) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor max) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let clip (self @ local) ~min ~max =
  stubs_clip (globalize_gc_tensor self) min max |> with_tensor_gc

let clip_ (self @ local) ~min ~max =
  stubs_clip_ (globalize_gc_tensor self) min max |> with_tensor_gc

let clip_out ~(out @ local) (self @ local) ~min ~max =
  stubs_clip_out (globalize_gc_tensor out) (globalize_gc_tensor self) min max |> with_tensor_gc

let clip_tensor (self @ local) ~(min @ local) ~(max @ local) =
  stubs_clip_tensor (globalize_gc_tensor self) (match (Base.Option.globalize globalize_gc_tensor min) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor max) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let clip_tensor_ (self @ local) ~(min @ local) ~(max @ local) =
  stubs_clip_tensor_ (globalize_gc_tensor self) (match (Base.Option.globalize globalize_gc_tensor min) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor max) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let clip_tensor_out ~(out @ local) (self @ local) ~(min @ local) ~(max @ local) =
  stubs_clip_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match (Base.Option.globalize globalize_gc_tensor min) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor max) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let clone (self @ local) =
  stubs_clone (globalize_gc_tensor self) |> with_tensor_gc

let clone_out ~(out @ local) (self @ local) =
  stubs_clone_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let coalesce (self @ local) =
  stubs_coalesce (globalize_gc_tensor self) |> with_tensor_gc

let col2im (self @ local) ~output_size ~kernel_size ~dilation ~padding ~stride =
  stubs_col2im (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) |> with_tensor_gc

let col2im_out ~(out @ local) (self @ local) ~output_size ~kernel_size ~dilation ~padding ~stride =
  stubs_col2im_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) |> with_tensor_gc

let col_indices (self @ local) =
  stubs_col_indices (globalize_gc_tensor self) |> with_tensor_gc

let col_indices_copy (self @ local) =
  stubs_col_indices_copy (globalize_gc_tensor self) |> with_tensor_gc

let col_indices_copy_out ~(out @ local) (self @ local) =
  stubs_col_indices_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let column_stack (tensors @ local) =
  stubs_column_stack (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> with_tensor_gc

let column_stack_out ~(out @ local) (tensors @ local) =
  stubs_column_stack_out (globalize_gc_tensor out) (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> with_tensor_gc

let combinations (self @ local) ~r ~with_replacement =
  stubs_combinations (globalize_gc_tensor self) (Int64.of_int r) (if with_replacement then 1 else 0) |> with_tensor_gc

let complex ~(real @ local) ~(imag @ local) =
  stubs_complex (globalize_gc_tensor real) (globalize_gc_tensor imag) |> with_tensor_gc

let complex_out ~(out @ local) ~(real @ local) ~(imag @ local) =
  stubs_complex_out (globalize_gc_tensor out) (globalize_gc_tensor real) (globalize_gc_tensor imag) |> with_tensor_gc

let concat (tensors @ local) ~dim =
  stubs_concat (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) (Int64.of_int dim) |> with_tensor_gc

let concat_out ~(out @ local) (tensors @ local) ~dim =
  stubs_concat_out (globalize_gc_tensor out) (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) (Int64.of_int dim) |> with_tensor_gc

let concatenate (tensors @ local) ~dim =
  stubs_concatenate (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) (Int64.of_int dim) |> with_tensor_gc

let concatenate_out ~(out @ local) (tensors @ local) ~dim =
  stubs_concatenate_out (globalize_gc_tensor out) (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) (Int64.of_int dim) |> with_tensor_gc

let conj (self @ local) =
  stubs_conj (globalize_gc_tensor self) |> with_tensor_gc

let conj_physical (self @ local) =
  stubs_conj_physical (globalize_gc_tensor self) |> with_tensor_gc

let conj_physical_ (self @ local) =
  stubs_conj_physical_ (globalize_gc_tensor self) |> with_tensor_gc

let conj_physical_out ~(out @ local) (self @ local) =
  stubs_conj_physical_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let constant_pad_nd ?value (self @ local) ~pad =
  stubs_constant_pad_nd (globalize_gc_tensor self) (List.map Int64.of_int pad |> CArray.of_list int64_t |> CArray.start) (List.length pad) (match value with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let constant_pad_nd_out ?value ~(out @ local) (self @ local) ~pad =
  stubs_constant_pad_nd_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int pad |> CArray.of_list int64_t |> CArray.start) (List.length pad) (match value with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let contiguous (self @ local) =
  stubs_contiguous (globalize_gc_tensor self) |> with_tensor_gc

let conv1d (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~groups =
  stubs_conv1d (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let conv1d_padding (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~groups =
  stubs_conv1d_padding (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) padding (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let conv2d (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~groups =
  stubs_conv2d (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let conv2d_padding (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~groups =
  stubs_conv2d_padding (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) padding (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let conv3d (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~groups =
  stubs_conv3d (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let conv3d_padding (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~groups =
  stubs_conv3d_padding (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) padding (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let conv_depthwise3d (self @ local) ~(weight @ local) ~kernel_size ~(bias @ local) ~stride ~padding ~dilation =
  stubs_conv_depthwise3d (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) |> with_tensor_gc

let conv_depthwise3d_out ~(out @ local) (self @ local) ~(weight @ local) ~kernel_size ~(bias @ local) ~stride ~padding ~dilation =
  stubs_conv_depthwise3d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) |> with_tensor_gc

let conv_tbc (self @ local) ~(weight @ local) ~(bias @ local) ~pad =
  stubs_conv_tbc (globalize_gc_tensor self) (globalize_gc_tensor weight) (globalize_gc_tensor bias) (Int64.of_int pad) |> with_tensor_gc

let conv_tbc_backward (self @ local) (input @ local) ~(weight @ local) ~(bias @ local) ~pad =
  let out__ = CArray.make raw_tensor 3 in
  stubs_conv_tbc_backward (CArray.start out__) (globalize_gc_tensor self) (globalize_gc_tensor input) (globalize_gc_tensor weight) (globalize_gc_tensor bias) (Int64.of_int pad);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let conv_tbc_out ~(out @ local) (self @ local) ~(weight @ local) ~(bias @ local) ~pad =
  stubs_conv_tbc_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (globalize_gc_tensor bias) (Int64.of_int pad) |> with_tensor_gc

let conv_transpose1d (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~output_padding ~groups ~dilation =
  stubs_conv_transpose1d (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (Int64.of_int groups) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) |> with_tensor_gc

let conv_transpose2d (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~output_padding ~groups ~dilation =
  stubs_conv_transpose2d (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (Int64.of_int groups) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) |> with_tensor_gc

let conv_transpose3d (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~output_padding ~groups ~dilation =
  stubs_conv_transpose3d (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (Int64.of_int groups) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) |> with_tensor_gc

let convolution (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~transposed ~output_padding ~groups =
  stubs_convolution (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if transposed then 1 else 0) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (Int64.of_int groups) |> with_tensor_gc

let convolution_out ~(out @ local) (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~transposed ~output_padding ~groups =
  stubs_convolution_out (globalize_gc_tensor out) (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if transposed then 1 else 0) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (Int64.of_int groups) |> with_tensor_gc

let convolution_overrideable (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~transposed ~output_padding ~groups =
  stubs_convolution_overrideable (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if transposed then 1 else 0) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (Int64.of_int groups) |> with_tensor_gc

let convolution_overrideable_out ~(out @ local) (input @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~transposed ~output_padding ~groups =
  stubs_convolution_overrideable_out (globalize_gc_tensor out) (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if transposed then 1 else 0) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (Int64.of_int groups) |> with_tensor_gc

let copy (self @ local) ~(src @ local) ~non_blocking =
  stubs_copy (globalize_gc_tensor self) (globalize_gc_tensor src) (if non_blocking then 1 else 0) |> with_tensor_gc

let copy_out ~(out @ local) (self @ local) ~(src @ local) ~non_blocking =
  stubs_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor src) (if non_blocking then 1 else 0) |> with_tensor_gc

let copy_sparse_to_sparse (self @ local) ~(src @ local) ~non_blocking =
  stubs_copy_sparse_to_sparse (globalize_gc_tensor self) (globalize_gc_tensor src) (if non_blocking then 1 else 0) |> with_tensor_gc

let copy_sparse_to_sparse_ (self @ local) ~(src @ local) ~non_blocking =
  stubs_copy_sparse_to_sparse_ (globalize_gc_tensor self) (globalize_gc_tensor src) (if non_blocking then 1 else 0) |> with_tensor_gc

let copy_sparse_to_sparse_out ~(out @ local) (self @ local) ~(src @ local) ~non_blocking =
  stubs_copy_sparse_to_sparse_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor src) (if non_blocking then 1 else 0) |> with_tensor_gc

let copysign (self @ local) (other @ local) =
  stubs_copysign (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let copysign_ (self @ local) (other @ local) =
  stubs_copysign_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let copysign_out ~(out @ local) (self @ local) (other @ local) =
  stubs_copysign_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let copysign_scalar (self @ local) other =
  stubs_copysign_scalar (globalize_gc_tensor self) other |> with_tensor_gc

let copysign_scalar_ (self @ local) other =
  stubs_copysign_scalar_ (globalize_gc_tensor self) other |> with_tensor_gc

let copysign_scalar_out ~(out @ local) (self @ local) other =
  stubs_copysign_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let corrcoef (self @ local) =
  stubs_corrcoef (globalize_gc_tensor self) |> with_tensor_gc

let cos (self @ local) =
  stubs_cos (globalize_gc_tensor self) |> with_tensor_gc

let cos_ (self @ local) =
  stubs_cos_ (globalize_gc_tensor self) |> with_tensor_gc

let cos_out ~(out @ local) (self @ local) =
  stubs_cos_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let cosh (self @ local) =
  stubs_cosh (globalize_gc_tensor self) |> with_tensor_gc

let cosh_ (self @ local) =
  stubs_cosh_ (globalize_gc_tensor self) |> with_tensor_gc

let cosh_out ~(out @ local) (self @ local) =
  stubs_cosh_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let cosine_embedding_loss ~(input1 @ local) ~(input2 @ local) ~(target @ local) ~margin ~reduction =
  stubs_cosine_embedding_loss (globalize_gc_tensor input1) (globalize_gc_tensor input2) (globalize_gc_tensor target) margin (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let cosine_similarity ~(x1 @ local) ~(x2 @ local) ~dim ~eps =
  stubs_cosine_similarity (globalize_gc_tensor x1) (globalize_gc_tensor x2) (Int64.of_int dim) eps |> with_tensor_gc

let count_nonzero ~(out @ local) (self @ local) ~dim =
  stubs_count_nonzero (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) |> with_tensor_gc

let count_nonzero_out ~(out @ local) (self @ local) ~dim =
  stubs_count_nonzero_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let cov (self @ local) ~correction ~(fweights @ local) ~(aweights @ local) =
  stubs_cov (globalize_gc_tensor self) (Int64.of_int correction) (match (Base.Option.globalize globalize_gc_tensor fweights) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor aweights) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let cross (self @ local) (other @ local) ~dim =
  stubs_cross (globalize_gc_tensor self) (globalize_gc_tensor other)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let cross_entropy_loss (self @ local) ~(target @ local) ~(weight @ local) ~reduction ~ignore_index ~label_smoothing =
  stubs_cross_entropy_loss (globalize_gc_tensor self) (globalize_gc_tensor target) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) (Int64.of_int ignore_index) label_smoothing |> with_tensor_gc

let cross_out ~(out @ local) (self @ local) (other @ local) ~dim =
  stubs_cross_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let crow_indices (self @ local) =
  stubs_crow_indices (globalize_gc_tensor self) |> with_tensor_gc

let crow_indices_copy (self @ local) =
  stubs_crow_indices_copy (globalize_gc_tensor self) |> with_tensor_gc

let crow_indices_copy_out ~(out @ local) (self @ local) =
  stubs_crow_indices_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let ctc_loss ~(log_probs @ local) ~(targets @ local) ~input_lengths ~target_lengths ~blank ~reduction ~zero_infinity =
  stubs_ctc_loss (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (List.map Int64.of_int input_lengths |> CArray.of_list int64_t |> CArray.start) (List.length input_lengths) (List.map Int64.of_int target_lengths |> CArray.of_list int64_t |> CArray.start) (List.length target_lengths) (Int64.of_int blank) (Reduction.to_int reduction |> Int64.of_int) (if zero_infinity then 1 else 0) |> with_tensor_gc

let ctc_loss_tensor ~(log_probs @ local) ~(targets @ local) ~(input_lengths @ local) ~(target_lengths @ local) ~blank ~reduction ~zero_infinity =
  stubs_ctc_loss_tensor (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (globalize_gc_tensor input_lengths) (globalize_gc_tensor target_lengths) (Int64.of_int blank) (Reduction.to_int reduction |> Int64.of_int) (if zero_infinity then 1 else 0) |> with_tensor_gc

let cudnn_affine_grid_generator ~(theta @ local) ~n ~c ~h ~w =
  stubs_cudnn_affine_grid_generator (globalize_gc_tensor theta) (Int64.of_int n) (Int64.of_int c) (Int64.of_int h) (Int64.of_int w) |> with_tensor_gc

let cudnn_affine_grid_generator_backward ~(grad @ local) ~n ~c ~h ~w =
  stubs_cudnn_affine_grid_generator_backward (globalize_gc_tensor grad) (Int64.of_int n) (Int64.of_int c) (Int64.of_int h) (Int64.of_int w) |> with_tensor_gc

let cudnn_affine_grid_generator_backward_out ~(out @ local) ~(grad @ local) ~n ~c ~h ~w =
  stubs_cudnn_affine_grid_generator_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad) (Int64.of_int n) (Int64.of_int c) (Int64.of_int h) (Int64.of_int w) |> with_tensor_gc

let cudnn_affine_grid_generator_out ~(out @ local) ~(theta @ local) ~n ~c ~h ~w =
  stubs_cudnn_affine_grid_generator_out (globalize_gc_tensor out) (globalize_gc_tensor theta) (Int64.of_int n) (Int64.of_int c) (Int64.of_int h) (Int64.of_int w) |> with_tensor_gc

let cudnn_batch_norm (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~training ~exponential_average_factor ~epsilon =
  let out__ = CArray.make raw_tensor 4 in
  stubs_cudnn_batch_norm (CArray.start out__) (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) (if training then 1 else 0) exponential_average_factor epsilon;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let cudnn_batch_norm_backward (input @ local) ~(grad_output @ local) ~(weight @ local) ~(running_mean @ local) ~(running_var @ local) ~(save_mean @ local) ~(save_var @ local) ~epsilon ~(reservespace @ local) =
  let out__ = CArray.make raw_tensor 3 in
  stubs_cudnn_batch_norm_backward (CArray.start out__) (globalize_gc_tensor input) (globalize_gc_tensor grad_output) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor save_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor save_var) with | Some v -> v | None -> none_gc_tensor) epsilon (globalize_gc_tensor reservespace);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let cudnn_batch_norm_backward_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) (input @ local) ~(grad_output @ local) ~(weight @ local) ~(running_mean @ local) ~(running_var @ local) ~(save_mean @ local) ~(save_var @ local) ~epsilon ~(reservespace @ local) =
  let out__ = CArray.make raw_tensor 3 in
  stubs_cudnn_batch_norm_backward_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor input) (globalize_gc_tensor grad_output) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor save_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor save_var) with | Some v -> v | None -> none_gc_tensor) epsilon (globalize_gc_tensor reservespace);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let cudnn_batch_norm_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) ~(out3 @ local) (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~training ~exponential_average_factor ~epsilon =
  let out__ = CArray.make raw_tensor 4 in
  stubs_cudnn_batch_norm_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor out3) (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) (if training then 1 else 0) exponential_average_factor epsilon;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let cudnn_convolution (self @ local) ~(weight @ local) ~padding ~stride ~dilation ~groups ~benchmark ~deterministic ~allow_tf32 =
  stubs_cudnn_convolution (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) (if benchmark then 1 else 0) (if deterministic then 1 else 0) (if allow_tf32 then 1 else 0) |> with_tensor_gc

let cudnn_convolution_add_relu (self @ local) ~(weight @ local) ~(z @ local) ~alpha ~(bias @ local) ~stride ~padding ~dilation ~groups =
  stubs_cudnn_convolution_add_relu (globalize_gc_tensor self) (globalize_gc_tensor weight) (globalize_gc_tensor z) alpha (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let cudnn_convolution_add_relu_out ~(out @ local) (self @ local) ~(weight @ local) ~(z @ local) ~alpha ~(bias @ local) ~stride ~padding ~dilation ~groups =
  stubs_cudnn_convolution_add_relu_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (globalize_gc_tensor z) alpha (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let cudnn_convolution_out ~(out @ local) (self @ local) ~(weight @ local) ~padding ~stride ~dilation ~groups ~benchmark ~deterministic ~allow_tf32 =
  stubs_cudnn_convolution_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) (if benchmark then 1 else 0) (if deterministic then 1 else 0) (if allow_tf32 then 1 else 0) |> with_tensor_gc

let cudnn_convolution_relu (self @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~groups =
  stubs_cudnn_convolution_relu (globalize_gc_tensor self) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let cudnn_convolution_relu_out ~(out @ local) (self @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~groups =
  stubs_cudnn_convolution_relu_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let cudnn_convolution_transpose (self @ local) ~(weight @ local) ~padding ~output_padding ~stride ~dilation ~groups ~benchmark ~deterministic ~allow_tf32 =
  stubs_cudnn_convolution_transpose (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) (if benchmark then 1 else 0) (if deterministic then 1 else 0) (if allow_tf32 then 1 else 0) |> with_tensor_gc

let cudnn_convolution_transpose_out ~(out @ local) (self @ local) ~(weight @ local) ~padding ~output_padding ~stride ~dilation ~groups ~benchmark ~deterministic ~allow_tf32 =
  stubs_cudnn_convolution_transpose_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) (if benchmark then 1 else 0) (if deterministic then 1 else 0) (if allow_tf32 then 1 else 0) |> with_tensor_gc

let cudnn_grid_sampler (self @ local) ~(grid @ local) =
  stubs_cudnn_grid_sampler (globalize_gc_tensor self) (globalize_gc_tensor grid) |> with_tensor_gc

let cudnn_grid_sampler_backward (self @ local) ~(grid @ local) ~(grad_output @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_cudnn_grid_sampler_backward (CArray.start out__) (globalize_gc_tensor self) (globalize_gc_tensor grid) (globalize_gc_tensor grad_output);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let cudnn_grid_sampler_backward_out ~(out0 @ local) ~(out1 @ local) (self @ local) ~(grid @ local) ~(grad_output @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_cudnn_grid_sampler_backward_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor self) (globalize_gc_tensor grid) (globalize_gc_tensor grad_output);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let cudnn_grid_sampler_out ~(out @ local) (self @ local) ~(grid @ local) =
  stubs_cudnn_grid_sampler_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor grid) |> with_tensor_gc

let cudnn_is_acceptable (self @ local) =
  stubs_cudnn_is_acceptable (globalize_gc_tensor self)

let cummax (self @ local) ~dim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_cummax (CArray.start out__) (globalize_gc_tensor self) (Int64.of_int dim);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let cummax_out ~(values @ local) ~(indices @ local) (self @ local) ~dim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_cummax_out (CArray.start out__) (globalize_gc_tensor values) (globalize_gc_tensor indices) (globalize_gc_tensor self) (Int64.of_int dim);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let cummaxmin_backward ~(grad @ local) (input @ local) ~(indices @ local) ~dim =
  stubs_cummaxmin_backward (globalize_gc_tensor grad) (globalize_gc_tensor input) (globalize_gc_tensor indices) (Int64.of_int dim) |> with_tensor_gc

let cummin (self @ local) ~dim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_cummin (CArray.start out__) (globalize_gc_tensor self) (Int64.of_int dim);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let cummin_out ~(values @ local) ~(indices @ local) (self @ local) ~dim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_cummin_out (CArray.start out__) (globalize_gc_tensor values) (globalize_gc_tensor indices) (globalize_gc_tensor self) (Int64.of_int dim);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let cumprod (self @ local) ~dim ~dtype =
  stubs_cumprod (globalize_gc_tensor self) (Int64.of_int dim) (Kind.packed_to_int dtype) |> with_tensor_gc

let cumprod_ (self @ local) ~dim ~dtype =
  stubs_cumprod_ (globalize_gc_tensor self) (Int64.of_int dim) (Kind.packed_to_int dtype) |> with_tensor_gc

let cumprod_backward ~(grad @ local) (input @ local) ~dim ~(output @ local) =
  stubs_cumprod_backward (globalize_gc_tensor grad) (globalize_gc_tensor input) (Int64.of_int dim) (globalize_gc_tensor output) |> with_tensor_gc

let cumprod_out ~(out @ local) (self @ local) ~dim ~dtype =
  stubs_cumprod_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (Kind.packed_to_int dtype) |> with_tensor_gc

let cumsum (self @ local) ~dim ~dtype =
  stubs_cumsum (globalize_gc_tensor self) (Int64.of_int dim) (Kind.packed_to_int dtype) |> with_tensor_gc

let cumsum_ (self @ local) ~dim ~dtype =
  stubs_cumsum_ (globalize_gc_tensor self) (Int64.of_int dim) (Kind.packed_to_int dtype) |> with_tensor_gc

let cumsum_out ~(out @ local) (self @ local) ~dim ~dtype =
  stubs_cumsum_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (Kind.packed_to_int dtype) |> with_tensor_gc

let cumulative_trapezoid ~(y @ local) ~(x @ local) ~dim =
  stubs_cumulative_trapezoid (globalize_gc_tensor y) (globalize_gc_tensor x) (Int64.of_int dim) |> with_tensor_gc

let cumulative_trapezoid_dx ?dx ~(y @ local) ~dim () =
  stubs_cumulative_trapezoid_dx (globalize_gc_tensor y) (match dx with | Some v -> v | None -> none_scalar) (Int64.of_int dim) |> with_tensor_gc

let data (self @ local) =
  stubs_data (globalize_gc_tensor self) |> with_tensor_gc

let deg2rad (self @ local) =
  stubs_deg2rad (globalize_gc_tensor self) |> with_tensor_gc

let deg2rad_ (self @ local) =
  stubs_deg2rad_ (globalize_gc_tensor self) |> with_tensor_gc

let deg2rad_out ~(out @ local) (self @ local) =
  stubs_deg2rad_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let dense_dim (self @ local) =
  stubs_dense_dim (globalize_gc_tensor self)

let dequantize (self @ local) =
  stubs_dequantize (globalize_gc_tensor self) |> with_tensor_gc

let dequantize_self_out ~(out @ local) (self @ local) =
  stubs_dequantize_self_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let dequantize_tensors (tensors @ local) =
  stubs_dequantize_tensors (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> to_tensor_list

let dequantize_tensors_out ~(out @ local) (tensors @ local) =
  stubs_dequantize_tensors_out (CArray.of_list gc_tensor (globalize_gc_tensor_list out) |> CArray.start) (Base.List.length out) (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors)

let det (self @ local) =
  stubs_det (globalize_gc_tensor self) |> with_tensor_gc

let detach (self @ local) =
  stubs_detach (globalize_gc_tensor self) |> with_tensor_gc

let detach_ (self @ local) =
  stubs_detach_ (globalize_gc_tensor self) |> with_tensor_gc

let detach_copy (self @ local) =
  stubs_detach_copy (globalize_gc_tensor self) |> with_tensor_gc

let detach_copy_out ~(out @ local) (self @ local) =
  stubs_detach_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let diag (self @ local) ~diagonal =
  stubs_diag (globalize_gc_tensor self) (Int64.of_int diagonal) |> with_tensor_gc

let diag_embed (self @ local) ~offset ~dim1 ~dim2 =
  stubs_diag_embed (globalize_gc_tensor self) (Int64.of_int offset) (Int64.of_int dim1) (Int64.of_int dim2) |> with_tensor_gc

let diag_embed_out ~(out @ local) (self @ local) ~offset ~dim1 ~dim2 =
  stubs_diag_embed_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int offset) (Int64.of_int dim1) (Int64.of_int dim2) |> with_tensor_gc

let diag_out ~(out @ local) (self @ local) ~diagonal =
  stubs_diag_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int diagonal) |> with_tensor_gc

let diagflat (self @ local) ~offset =
  stubs_diagflat (globalize_gc_tensor self) (Int64.of_int offset) |> with_tensor_gc

let diagonal (self @ local) ~offset ~dim1 ~dim2 =
  stubs_diagonal (globalize_gc_tensor self) (Int64.of_int offset) (Int64.of_int dim1) (Int64.of_int dim2) |> with_tensor_gc

let diagonal_backward ~(grad_output @ local) ~input_sizes ~offset ~dim1 ~dim2 =
  stubs_diagonal_backward (globalize_gc_tensor grad_output) (List.map Int64.of_int input_sizes |> CArray.of_list int64_t |> CArray.start) (List.length input_sizes) (Int64.of_int offset) (Int64.of_int dim1) (Int64.of_int dim2) |> with_tensor_gc

let diagonal_backward_out ~(out @ local) ~(grad_output @ local) ~input_sizes ~offset ~dim1 ~dim2 =
  stubs_diagonal_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (List.map Int64.of_int input_sizes |> CArray.of_list int64_t |> CArray.start) (List.length input_sizes) (Int64.of_int offset) (Int64.of_int dim1) (Int64.of_int dim2) |> with_tensor_gc

let diagonal_copy (self @ local) ~offset ~dim1 ~dim2 =
  stubs_diagonal_copy (globalize_gc_tensor self) (Int64.of_int offset) (Int64.of_int dim1) (Int64.of_int dim2) |> with_tensor_gc

let diagonal_copy_out ~(out @ local) (self @ local) ~offset ~dim1 ~dim2 =
  stubs_diagonal_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int offset) (Int64.of_int dim1) (Int64.of_int dim2) |> with_tensor_gc

let diagonal_scatter (self @ local) ~(src @ local) ~offset ~dim1 ~dim2 =
  stubs_diagonal_scatter (globalize_gc_tensor self) (globalize_gc_tensor src) (Int64.of_int offset) (Int64.of_int dim1) (Int64.of_int dim2) |> with_tensor_gc

let diagonal_scatter_out ~(out @ local) (self @ local) ~(src @ local) ~offset ~dim1 ~dim2 =
  stubs_diagonal_scatter_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor src) (Int64.of_int offset) (Int64.of_int dim1) (Int64.of_int dim2) |> with_tensor_gc

let diff (self @ local) ~n ~dim ~(prepend @ local) ~(append @ local) =
  stubs_diff (globalize_gc_tensor self) (Int64.of_int n) (Int64.of_int dim) (match (Base.Option.globalize globalize_gc_tensor prepend) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor append) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let diff_out ~(out @ local) (self @ local) ~n ~dim ~(prepend @ local) ~(append @ local) =
  stubs_diff_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int n) (Int64.of_int dim) (match (Base.Option.globalize globalize_gc_tensor prepend) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor append) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let digamma (self @ local) =
  stubs_digamma (globalize_gc_tensor self) |> with_tensor_gc

let digamma_ (self @ local) =
  stubs_digamma_ (globalize_gc_tensor self) |> with_tensor_gc

let digamma_out ~(out @ local) (self @ local) =
  stubs_digamma_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let dim (self @ local) =
  stubs_dim (globalize_gc_tensor self)

let dist ?p (self @ local) (other @ local) =
  stubs_dist (globalize_gc_tensor self) (globalize_gc_tensor other) (match p with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let dist_out ?p ~(out @ local) (self @ local) (other @ local) =
  stubs_dist_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) (match p with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let div (self @ local) (other @ local) =
  stubs_div (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let div_ (self @ local) (other @ local) =
  stubs_div_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let div_out ~(out @ local) (self @ local) (other @ local) =
  stubs_div_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let div_out_mode ~(out @ local) (self @ local) (other @ local) ~rounding_mode =
  stubs_div_out_mode (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other)  (Option.value rounding_mode ~default:"") (match rounding_mode with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let div_scalar (self @ local) other =
  stubs_div_scalar (globalize_gc_tensor self) other |> with_tensor_gc

let div_scalar_ (self @ local) other =
  stubs_div_scalar_ (globalize_gc_tensor self) other |> with_tensor_gc

let div_scalar_mode (self @ local) other ~rounding_mode =
  stubs_div_scalar_mode (globalize_gc_tensor self) other  (Option.value rounding_mode ~default:"") (match rounding_mode with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let div_scalar_mode_ (self @ local) other ~rounding_mode =
  stubs_div_scalar_mode_ (globalize_gc_tensor self) other  (Option.value rounding_mode ~default:"") (match rounding_mode with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let div_scalar_mode_out ~(out @ local) (self @ local) other ~rounding_mode =
  stubs_div_scalar_mode_out (globalize_gc_tensor out) (globalize_gc_tensor self) other  (Option.value rounding_mode ~default:"") (match rounding_mode with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let div_scalar_out ~(out @ local) (self @ local) other =
  stubs_div_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let div_tensor_mode (self @ local) (other @ local) ~rounding_mode =
  stubs_div_tensor_mode (globalize_gc_tensor self) (globalize_gc_tensor other)  (Option.value rounding_mode ~default:"") (match rounding_mode with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let div_tensor_mode_ (self @ local) (other @ local) ~rounding_mode =
  stubs_div_tensor_mode_ (globalize_gc_tensor self) (globalize_gc_tensor other)  (Option.value rounding_mode ~default:"") (match rounding_mode with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let divide (self @ local) (other @ local) =
  stubs_divide (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let divide_ (self @ local) (other @ local) =
  stubs_divide_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let divide_out ~(out @ local) (self @ local) (other @ local) =
  stubs_divide_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let divide_out_mode ~(out @ local) (self @ local) (other @ local) ~rounding_mode =
  stubs_divide_out_mode (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other)  (Option.value rounding_mode ~default:"") (match rounding_mode with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let divide_scalar (self @ local) other =
  stubs_divide_scalar (globalize_gc_tensor self) other |> with_tensor_gc

let divide_scalar_ (self @ local) other =
  stubs_divide_scalar_ (globalize_gc_tensor self) other |> with_tensor_gc

let divide_scalar_mode (self @ local) other ~rounding_mode =
  stubs_divide_scalar_mode (globalize_gc_tensor self) other  (Option.value rounding_mode ~default:"") (match rounding_mode with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let divide_scalar_mode_ (self @ local) other ~rounding_mode =
  stubs_divide_scalar_mode_ (globalize_gc_tensor self) other  (Option.value rounding_mode ~default:"") (match rounding_mode with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let divide_tensor_mode (self @ local) (other @ local) ~rounding_mode =
  stubs_divide_tensor_mode (globalize_gc_tensor self) (globalize_gc_tensor other)  (Option.value rounding_mode ~default:"") (match rounding_mode with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let divide_tensor_mode_ (self @ local) (other @ local) ~rounding_mode =
  stubs_divide_tensor_mode_ (globalize_gc_tensor self) (globalize_gc_tensor other)  (Option.value rounding_mode ~default:"") (match rounding_mode with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let dot (self @ local) (tensor @ local) =
  stubs_dot (globalize_gc_tensor self) (globalize_gc_tensor tensor) |> with_tensor_gc

let dot_out ~(out @ local) (self @ local) (tensor @ local) =
  stubs_dot_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor tensor) |> with_tensor_gc

let dropout (input @ local) ~p ~train =
  stubs_dropout (globalize_gc_tensor input) p (if train then 1 else 0) |> with_tensor_gc

let dropout_ (self @ local) ~p ~train =
  stubs_dropout_ (globalize_gc_tensor self) p (if train then 1 else 0) |> with_tensor_gc

let dsplit (self @ local) ~sections =
  stubs_dsplit (globalize_gc_tensor self) (Int64.of_int sections) |> to_tensor_list

let dsplit_array (self @ local) ~indices =
  stubs_dsplit_array (globalize_gc_tensor self) (List.map Int64.of_int indices |> CArray.of_list int64_t |> CArray.start) (List.length indices) |> to_tensor_list

let dstack (tensors @ local) =
  stubs_dstack (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> with_tensor_gc

let dstack_out ~(out @ local) (tensors @ local) =
  stubs_dstack_out (globalize_gc_tensor out) (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> with_tensor_gc

let einsum ~equation (tensors @ local) ~path =
  stubs_einsum equation (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) (match path with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match path with | None -> -1 | Some v -> List.length v) |> with_tensor_gc

let elu ?alpha ?scale ?input_scale (self @ local) =
  stubs_elu (globalize_gc_tensor self) (match alpha with | Some v -> v | None -> none_scalar) (match scale with | Some v -> v | None -> none_scalar) (match input_scale with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let elu_ ?alpha ?scale ?input_scale (self @ local) =
  stubs_elu_ (globalize_gc_tensor self) (match alpha with | Some v -> v | None -> none_scalar) (match scale with | Some v -> v | None -> none_scalar) (match input_scale with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let elu_backward ~(grad_output @ local) ~alpha ~scale ~input_scale ~is_result ~(self_or_result @ local) =
  stubs_elu_backward (globalize_gc_tensor grad_output) alpha scale input_scale (if is_result then 1 else 0) (globalize_gc_tensor self_or_result) |> with_tensor_gc

let elu_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) ~alpha ~scale ~input_scale ~is_result ~(self_or_result @ local) =
  stubs_elu_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) alpha scale input_scale (if is_result then 1 else 0) (globalize_gc_tensor self_or_result) |> with_tensor_gc

let elu_out ?alpha ?scale ?input_scale ~(out @ local) (self @ local) =
  stubs_elu_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match alpha with | Some v -> v | None -> none_scalar) (match scale with | Some v -> v | None -> none_scalar) (match input_scale with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let embedding ~(weight @ local) ~(indices @ local) ~padding_idx ~scale_grad_by_freq ~sparse =
  stubs_embedding (globalize_gc_tensor weight) (globalize_gc_tensor indices) (Int64.of_int padding_idx) (if scale_grad_by_freq then 1 else 0) (if sparse then 1 else 0) |> with_tensor_gc

let embedding_backward ~(grad @ local) ~(indices @ local) ~num_weights ~padding_idx ~scale_grad_by_freq ~sparse =
  stubs_embedding_backward (globalize_gc_tensor grad) (globalize_gc_tensor indices) (Int64.of_int num_weights) (Int64.of_int padding_idx) (if scale_grad_by_freq then 1 else 0) (if sparse then 1 else 0) |> with_tensor_gc

let embedding_bag ~(weight @ local) ~(indices @ local) ~(offsets @ local) ~scale_grad_by_freq ~mode ~sparse ~(per_sample_weights @ local) ~include_last_offset =
  let out__ = CArray.make raw_tensor 4 in
  stubs_embedding_bag (CArray.start out__) (globalize_gc_tensor weight) (globalize_gc_tensor indices) (globalize_gc_tensor offsets) (if scale_grad_by_freq then 1 else 0) (Int64.of_int mode) (if sparse then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor per_sample_weights) with | Some v -> v | None -> none_gc_tensor) (if include_last_offset then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let embedding_bag_padding_idx ~(weight @ local) ~(indices @ local) ~(offsets @ local) ~scale_grad_by_freq ~mode ~sparse ~(per_sample_weights @ local) ~include_last_offset ~padding_idx =
  let out__ = CArray.make raw_tensor 4 in
  stubs_embedding_bag_padding_idx (CArray.start out__) (globalize_gc_tensor weight) (globalize_gc_tensor indices) (globalize_gc_tensor offsets) (if scale_grad_by_freq then 1 else 0) (Int64.of_int mode) (if sparse then 1 else 0) (match (Base.Option.globalize globalize_gc_tensor per_sample_weights) with | Some v -> v | None -> none_gc_tensor) (if include_last_offset then 1 else 0)  (match padding_idx with | None -> Int64.zero | Some v -> Int64.of_int v) (match padding_idx with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let embedding_dense_backward ~(grad_output @ local) ~(indices @ local) ~num_weights ~padding_idx ~scale_grad_by_freq =
  stubs_embedding_dense_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor indices) (Int64.of_int num_weights) (Int64.of_int padding_idx) (if scale_grad_by_freq then 1 else 0) |> with_tensor_gc

let embedding_dense_backward_out ~(out @ local) ~(grad_output @ local) ~(indices @ local) ~num_weights ~padding_idx ~scale_grad_by_freq =
  stubs_embedding_dense_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (globalize_gc_tensor indices) (Int64.of_int num_weights) (Int64.of_int padding_idx) (if scale_grad_by_freq then 1 else 0) |> with_tensor_gc

let embedding_out ~(out @ local) ~(weight @ local) ~(indices @ local) ~padding_idx ~scale_grad_by_freq ~sparse =
  stubs_embedding_out (globalize_gc_tensor out) (globalize_gc_tensor weight) (globalize_gc_tensor indices) (Int64.of_int padding_idx) (if scale_grad_by_freq then 1 else 0) (if sparse then 1 else 0) |> with_tensor_gc

let embedding_renorm (self @ local) ~(indices @ local) ~max_norm ~norm_type =
  stubs_embedding_renorm (globalize_gc_tensor self) (globalize_gc_tensor indices) max_norm norm_type |> with_tensor_gc

let embedding_renorm_ (self @ local) ~(indices @ local) ~max_norm ~norm_type =
  stubs_embedding_renorm_ (globalize_gc_tensor self) (globalize_gc_tensor indices) max_norm norm_type |> with_tensor_gc

let embedding_renorm_out ~(out @ local) (self @ local) ~(indices @ local) ~max_norm ~norm_type =
  stubs_embedding_renorm_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor indices) max_norm norm_type |> with_tensor_gc

let embedding_sparse_backward ~(grad @ local) ~(indices @ local) ~num_weights ~padding_idx ~scale_grad_by_freq =
  stubs_embedding_sparse_backward (globalize_gc_tensor grad) (globalize_gc_tensor indices) (Int64.of_int num_weights) (Int64.of_int padding_idx) (if scale_grad_by_freq then 1 else 0) |> with_tensor_gc

let empty ~size ~options =
  stubs_empty (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let empty_like (self @ local) =
  stubs_empty_like (globalize_gc_tensor self) |> with_tensor_gc

let empty_like_out ~(out @ local) (self @ local) =
  stubs_empty_like_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let empty_out ~(out @ local) ~size =
  stubs_empty_out (globalize_gc_tensor out) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let empty_permuted ~size ~physical_layout ~options =
  stubs_empty_permuted (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int physical_layout |> CArray.of_list int64_t |> CArray.start) (List.length physical_layout) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let empty_permuted_out ~(out @ local) ~size ~physical_layout =
  stubs_empty_permuted_out (globalize_gc_tensor out) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int physical_layout |> CArray.of_list int64_t |> CArray.start) (List.length physical_layout) |> with_tensor_gc

let empty_quantized ~size ~(qtensor @ local) ~options =
  stubs_empty_quantized (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (globalize_gc_tensor qtensor) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let empty_quantized_out ~(out @ local) ~size ~(qtensor @ local) =
  stubs_empty_quantized_out (globalize_gc_tensor out) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (globalize_gc_tensor qtensor) |> with_tensor_gc

let empty_strided ~size ~stride ~options =
  stubs_empty_strided (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let empty_strided_out ~(out @ local) ~size ~stride =
  stubs_empty_strided_out (globalize_gc_tensor out) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) |> with_tensor_gc

let eq (self @ local) other =
  stubs_eq (globalize_gc_tensor self) other |> with_tensor_gc

let eq_ (self @ local) other =
  stubs_eq_ (globalize_gc_tensor self) other |> with_tensor_gc

let eq_scalar_out ~(out @ local) (self @ local) other =
  stubs_eq_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let eq_tensor (self @ local) (other @ local) =
  stubs_eq_tensor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let eq_tensor_ (self @ local) (other @ local) =
  stubs_eq_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let eq_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_eq_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let equal (self @ local) (other @ local) =
  stubs_equal (globalize_gc_tensor self) (globalize_gc_tensor other)

let erf (self @ local) =
  stubs_erf (globalize_gc_tensor self) |> with_tensor_gc

let erf_ (self @ local) =
  stubs_erf_ (globalize_gc_tensor self) |> with_tensor_gc

let erf_out ~(out @ local) (self @ local) =
  stubs_erf_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let erfc (self @ local) =
  stubs_erfc (globalize_gc_tensor self) |> with_tensor_gc

let erfc_ (self @ local) =
  stubs_erfc_ (globalize_gc_tensor self) |> with_tensor_gc

let erfc_out ~(out @ local) (self @ local) =
  stubs_erfc_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let erfinv (self @ local) =
  stubs_erfinv (globalize_gc_tensor self) |> with_tensor_gc

let erfinv_ (self @ local) =
  stubs_erfinv_ (globalize_gc_tensor self) |> with_tensor_gc

let erfinv_out ~(out @ local) (self @ local) =
  stubs_erfinv_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let exp (self @ local) =
  stubs_exp (globalize_gc_tensor self) |> with_tensor_gc

let exp2 (self @ local) =
  stubs_exp2 (globalize_gc_tensor self) |> with_tensor_gc

let exp2_ (self @ local) =
  stubs_exp2_ (globalize_gc_tensor self) |> with_tensor_gc

let exp2_out ~(out @ local) (self @ local) =
  stubs_exp2_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let exp_ (self @ local) =
  stubs_exp_ (globalize_gc_tensor self) |> with_tensor_gc

let exp_out ~(out @ local) (self @ local) =
  stubs_exp_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let expand (self @ local) ~size ~implicit =
  stubs_expand (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (if implicit then 1 else 0) |> with_tensor_gc

let expand_as (self @ local) (other @ local) =
  stubs_expand_as (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let expand_copy (self @ local) ~size ~implicit =
  stubs_expand_copy (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (if implicit then 1 else 0) |> with_tensor_gc

let expand_copy_out ~(out @ local) (self @ local) ~size ~implicit =
  stubs_expand_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (if implicit then 1 else 0) |> with_tensor_gc

let expm1 (self @ local) =
  stubs_expm1 (globalize_gc_tensor self) |> with_tensor_gc

let expm1_ (self @ local) =
  stubs_expm1_ (globalize_gc_tensor self) |> with_tensor_gc

let expm1_out ~(out @ local) (self @ local) =
  stubs_expm1_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let exponential (self @ local) ~lambd =
  stubs_exponential (globalize_gc_tensor self) lambd |> with_tensor_gc

let exponential_ (self @ local) ~lambd =
  stubs_exponential_ (globalize_gc_tensor self) lambd |> with_tensor_gc

let exponential_out ~(out @ local) (self @ local) ~lambd =
  stubs_exponential_out (globalize_gc_tensor out) (globalize_gc_tensor self) lambd |> with_tensor_gc

let eye ~n ~options =
  stubs_eye (Int64.of_int n) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let eye_m ~n ~m ~options =
  stubs_eye_m (Int64.of_int n) (Int64.of_int m) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let eye_m_out ~(out @ local) ~n ~m =
  stubs_eye_m_out (globalize_gc_tensor out) (Int64.of_int n) (Int64.of_int m) |> with_tensor_gc

let eye_out ~(out @ local) ~n =
  stubs_eye_out (globalize_gc_tensor out) (Int64.of_int n) |> with_tensor_gc

let fake_quantize_per_channel_affine (self @ local) ~(scale @ local) ~(zero_point @ local) ~axis ~quant_min ~quant_max =
  stubs_fake_quantize_per_channel_affine (globalize_gc_tensor self) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) (Int64.of_int axis) (Int64.of_int quant_min) (Int64.of_int quant_max) |> with_tensor_gc

let fake_quantize_per_channel_affine_cachemask (self @ local) ~(scale @ local) ~(zero_point @ local) ~axis ~quant_min ~quant_max =
  let out__ = CArray.make raw_tensor 2 in
  stubs_fake_quantize_per_channel_affine_cachemask (CArray.start out__) (globalize_gc_tensor self) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) (Int64.of_int axis) (Int64.of_int quant_min) (Int64.of_int quant_max);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let fake_quantize_per_channel_affine_cachemask_backward ~(grad @ local) ~(mask @ local) =
  stubs_fake_quantize_per_channel_affine_cachemask_backward (globalize_gc_tensor grad) (globalize_gc_tensor mask) |> with_tensor_gc

let fake_quantize_per_channel_affine_cachemask_out ~(out0 @ local) ~(out1 @ local) (self @ local) ~(scale @ local) ~(zero_point @ local) ~axis ~quant_min ~quant_max =
  let out__ = CArray.make raw_tensor 2 in
  stubs_fake_quantize_per_channel_affine_cachemask_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor self) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) (Int64.of_int axis) (Int64.of_int quant_min) (Int64.of_int quant_max);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let fake_quantize_per_tensor_affine (self @ local) ~scale ~zero_point ~quant_min ~quant_max =
  stubs_fake_quantize_per_tensor_affine (globalize_gc_tensor self) scale (Int64.of_int zero_point) (Int64.of_int quant_min) (Int64.of_int quant_max) |> with_tensor_gc

let fake_quantize_per_tensor_affine_cachemask (self @ local) ~scale ~zero_point ~quant_min ~quant_max =
  let out__ = CArray.make raw_tensor 2 in
  stubs_fake_quantize_per_tensor_affine_cachemask (CArray.start out__) (globalize_gc_tensor self) scale (Int64.of_int zero_point) (Int64.of_int quant_min) (Int64.of_int quant_max);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let fake_quantize_per_tensor_affine_cachemask_backward ~(grad @ local) ~(mask @ local) =
  stubs_fake_quantize_per_tensor_affine_cachemask_backward (globalize_gc_tensor grad) (globalize_gc_tensor mask) |> with_tensor_gc

let fake_quantize_per_tensor_affine_cachemask_out ~(out0 @ local) ~(out1 @ local) (self @ local) ~scale ~zero_point ~quant_min ~quant_max =
  let out__ = CArray.make raw_tensor 2 in
  stubs_fake_quantize_per_tensor_affine_cachemask_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor self) scale (Int64.of_int zero_point) (Int64.of_int quant_min) (Int64.of_int quant_max);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let fake_quantize_per_tensor_affine_tensor_qparams (self @ local) ~(scale @ local) ~(zero_point @ local) ~quant_min ~quant_max =
  stubs_fake_quantize_per_tensor_affine_tensor_qparams (globalize_gc_tensor self) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) (Int64.of_int quant_min) (Int64.of_int quant_max) |> with_tensor_gc

let fbgemm_linear_fp16_weight (input @ local) ~(packed_weight @ local) ~(bias @ local) =
  stubs_fbgemm_linear_fp16_weight (globalize_gc_tensor input) (globalize_gc_tensor packed_weight) (globalize_gc_tensor bias) |> with_tensor_gc

let fbgemm_linear_fp16_weight_fp32_activation (input @ local) ~(packed_weight @ local) ~(bias @ local) =
  stubs_fbgemm_linear_fp16_weight_fp32_activation (globalize_gc_tensor input) (globalize_gc_tensor packed_weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let fbgemm_linear_fp16_weight_fp32_activation_out (input @ local) ~(packed_weight @ local) ~(bias @ local) ~(output @ local) =
  stubs_fbgemm_linear_fp16_weight_fp32_activation_out (globalize_gc_tensor input) (globalize_gc_tensor packed_weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor output) |> with_tensor_gc

let fbgemm_linear_fp16_weight_out (input @ local) ~(packed_weight @ local) ~(bias @ local) ~(output @ local) =
  stubs_fbgemm_linear_fp16_weight_out (globalize_gc_tensor input) (globalize_gc_tensor packed_weight) (globalize_gc_tensor bias) (globalize_gc_tensor output) |> with_tensor_gc

let fbgemm_linear_int8_weight (input @ local) ~(weight @ local) ~(packed @ local) ~(col_offsets @ local) ~weight_scale ~weight_zero_point ~(bias @ local) =
  stubs_fbgemm_linear_int8_weight (globalize_gc_tensor input) (globalize_gc_tensor weight) (globalize_gc_tensor packed) (globalize_gc_tensor col_offsets) weight_scale weight_zero_point (globalize_gc_tensor bias) |> with_tensor_gc

let fbgemm_linear_int8_weight_fp32_activation (input @ local) ~(weight @ local) ~(packed @ local) ~(col_offsets @ local) ~weight_scale ~weight_zero_point ~(bias @ local) =
  stubs_fbgemm_linear_int8_weight_fp32_activation (globalize_gc_tensor input) (globalize_gc_tensor weight) (globalize_gc_tensor packed) (globalize_gc_tensor col_offsets) weight_scale weight_zero_point (globalize_gc_tensor bias) |> with_tensor_gc

let fbgemm_pack_gemm_matrix_fp16 (input @ local) =
  stubs_fbgemm_pack_gemm_matrix_fp16 (globalize_gc_tensor input) |> with_tensor_gc

let fbgemm_pack_quantized_matrix (input @ local) =
  stubs_fbgemm_pack_quantized_matrix (globalize_gc_tensor input) |> with_tensor_gc

let fbgemm_pack_quantized_matrix_kn (input @ local) ~k ~n =
  stubs_fbgemm_pack_quantized_matrix_kn (globalize_gc_tensor input) (Int64.of_int k) (Int64.of_int n) |> with_tensor_gc

let feature_alpha_dropout (input @ local) ~p ~train =
  stubs_feature_alpha_dropout (globalize_gc_tensor input) p (if train then 1 else 0) |> with_tensor_gc

let feature_alpha_dropout_ (self @ local) ~p ~train =
  stubs_feature_alpha_dropout_ (globalize_gc_tensor self) p (if train then 1 else 0) |> with_tensor_gc

let feature_dropout (input @ local) ~p ~train =
  stubs_feature_dropout (globalize_gc_tensor input) p (if train then 1 else 0) |> with_tensor_gc

let feature_dropout_ (self @ local) ~p ~train =
  stubs_feature_dropout_ (globalize_gc_tensor self) p (if train then 1 else 0) |> with_tensor_gc

let fft_fft (self @ local) ~n ~dim ~norm =
  stubs_fft_fft (globalize_gc_tensor self)  (match n with | None -> Int64.zero | Some v -> Int64.of_int v) (match n with | Some _ -> 0 | None -> 1)  (Int64.of_int dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_fft2 (self @ local) ~s ~dim ~norm =
  stubs_fft_fft2 (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_fft2_out ~(out @ local) (self @ local) ~s ~dim ~norm =
  stubs_fft_fft2_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_fft_out ~(out @ local) (self @ local) ~n ~dim ~norm =
  stubs_fft_fft_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (match n with | None -> Int64.zero | Some v -> Int64.of_int v) (match n with | Some _ -> 0 | None -> 1)  (Int64.of_int dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_fftfreq ~n ~d ~options =
  stubs_fft_fftfreq (Int64.of_int n) d (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let fft_fftfreq_out ~(out @ local) ~n ~d =
  stubs_fft_fftfreq_out (globalize_gc_tensor out) (Int64.of_int n) d |> with_tensor_gc

let fft_fftn (self @ local) ~s ~dim ~norm =
  stubs_fft_fftn (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_fftn_out ~(out @ local) (self @ local) ~s ~dim ~norm =
  stubs_fft_fftn_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_fftshift (self @ local) ~dim =
  stubs_fft_fftshift (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) |> with_tensor_gc

let fft_hfft (self @ local) ~n ~dim ~norm =
  stubs_fft_hfft (globalize_gc_tensor self)  (match n with | None -> Int64.zero | Some v -> Int64.of_int v) (match n with | Some _ -> 0 | None -> 1)  (Int64.of_int dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_hfft2 (self @ local) ~s ~dim ~norm =
  stubs_fft_hfft2 (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_hfft2_out ~(out @ local) (self @ local) ~s ~dim ~norm =
  stubs_fft_hfft2_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_hfft_out ~(out @ local) (self @ local) ~n ~dim ~norm =
  stubs_fft_hfft_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (match n with | None -> Int64.zero | Some v -> Int64.of_int v) (match n with | Some _ -> 0 | None -> 1)  (Int64.of_int dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_hfftn (self @ local) ~s ~dim ~norm =
  stubs_fft_hfftn (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_hfftn_out ~(out @ local) (self @ local) ~s ~dim ~norm =
  stubs_fft_hfftn_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_ifft (self @ local) ~n ~dim ~norm =
  stubs_fft_ifft (globalize_gc_tensor self)  (match n with | None -> Int64.zero | Some v -> Int64.of_int v) (match n with | Some _ -> 0 | None -> 1)  (Int64.of_int dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_ifft2 (self @ local) ~s ~dim ~norm =
  stubs_fft_ifft2 (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_ifft2_out ~(out @ local) (self @ local) ~s ~dim ~norm =
  stubs_fft_ifft2_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_ifft_out ~(out @ local) (self @ local) ~n ~dim ~norm =
  stubs_fft_ifft_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (match n with | None -> Int64.zero | Some v -> Int64.of_int v) (match n with | Some _ -> 0 | None -> 1)  (Int64.of_int dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_ifftn (self @ local) ~s ~dim ~norm =
  stubs_fft_ifftn (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_ifftn_out ~(out @ local) (self @ local) ~s ~dim ~norm =
  stubs_fft_ifftn_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_ifftshift (self @ local) ~dim =
  stubs_fft_ifftshift (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) |> with_tensor_gc

let fft_ihfft (self @ local) ~n ~dim ~norm =
  stubs_fft_ihfft (globalize_gc_tensor self)  (match n with | None -> Int64.zero | Some v -> Int64.of_int v) (match n with | Some _ -> 0 | None -> 1)  (Int64.of_int dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_ihfft2 (self @ local) ~s ~dim ~norm =
  stubs_fft_ihfft2 (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_ihfft2_out ~(out @ local) (self @ local) ~s ~dim ~norm =
  stubs_fft_ihfft2_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_ihfft_out ~(out @ local) (self @ local) ~n ~dim ~norm =
  stubs_fft_ihfft_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (match n with | None -> Int64.zero | Some v -> Int64.of_int v) (match n with | Some _ -> 0 | None -> 1)  (Int64.of_int dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_ihfftn (self @ local) ~s ~dim ~norm =
  stubs_fft_ihfftn (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_ihfftn_out ~(out @ local) (self @ local) ~s ~dim ~norm =
  stubs_fft_ihfftn_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_irfft (self @ local) ~n ~dim ~norm =
  stubs_fft_irfft (globalize_gc_tensor self)  (match n with | None -> Int64.zero | Some v -> Int64.of_int v) (match n with | Some _ -> 0 | None -> 1)  (Int64.of_int dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_irfft2 (self @ local) ~s ~dim ~norm =
  stubs_fft_irfft2 (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_irfft2_out ~(out @ local) (self @ local) ~s ~dim ~norm =
  stubs_fft_irfft2_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_irfft_out ~(out @ local) (self @ local) ~n ~dim ~norm =
  stubs_fft_irfft_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (match n with | None -> Int64.zero | Some v -> Int64.of_int v) (match n with | Some _ -> 0 | None -> 1)  (Int64.of_int dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_irfftn (self @ local) ~s ~dim ~norm =
  stubs_fft_irfftn (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_irfftn_out ~(out @ local) (self @ local) ~s ~dim ~norm =
  stubs_fft_irfftn_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_rfft (self @ local) ~n ~dim ~norm =
  stubs_fft_rfft (globalize_gc_tensor self)  (match n with | None -> Int64.zero | Some v -> Int64.of_int v) (match n with | Some _ -> 0 | None -> 1)  (Int64.of_int dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_rfft2 (self @ local) ~s ~dim ~norm =
  stubs_fft_rfft2 (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_rfft2_out ~(out @ local) (self @ local) ~s ~dim ~norm =
  stubs_fft_rfft2_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_rfft_out ~(out @ local) (self @ local) ~n ~dim ~norm =
  stubs_fft_rfft_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (match n with | None -> Int64.zero | Some v -> Int64.of_int v) (match n with | Some _ -> 0 | None -> 1)  (Int64.of_int dim)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_rfftfreq ~n ~d ~options =
  stubs_fft_rfftfreq (Int64.of_int n) d (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let fft_rfftfreq_out ~(out @ local) ~n ~d =
  stubs_fft_rfftfreq_out (globalize_gc_tensor out) (Int64.of_int n) d |> with_tensor_gc

let fft_rfftn (self @ local) ~s ~dim ~norm =
  stubs_fft_rfftn (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fft_rfftn_out ~(out @ local) (self @ local) ~s ~dim ~norm =
  stubs_fft_rfftn_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match s with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match s with | None -> -1 | Some v -> List.length v) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v)  (Option.value norm ~default:"") (match norm with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let fill (self @ local) ~value =
  stubs_fill (globalize_gc_tensor self) value |> with_tensor_gc

let fill_ (self @ local) ~value =
  stubs_fill_ (globalize_gc_tensor self) value |> with_tensor_gc

let fill_diagonal_ (self @ local) ~fill_value ~wrap =
  stubs_fill_diagonal_ (globalize_gc_tensor self) fill_value (if wrap then 1 else 0) |> with_tensor_gc

let fill_scalar_out ~(out @ local) (self @ local) ~value =
  stubs_fill_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) value |> with_tensor_gc

let fill_tensor (self @ local) ~(value @ local) =
  stubs_fill_tensor (globalize_gc_tensor self) (globalize_gc_tensor value) |> with_tensor_gc

let fill_tensor_ (self @ local) ~(value @ local) =
  stubs_fill_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor value) |> with_tensor_gc

let fill_tensor_out ~(out @ local) (self @ local) ~(value @ local) =
  stubs_fill_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor value) |> with_tensor_gc

let fix (self @ local) =
  stubs_fix (globalize_gc_tensor self) |> with_tensor_gc

let fix_ (self @ local) =
  stubs_fix_ (globalize_gc_tensor self) |> with_tensor_gc

let fix_out ~(out @ local) (self @ local) =
  stubs_fix_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let flatten (self @ local) ~start_dim ~end_dim =
  stubs_flatten (globalize_gc_tensor self) (Int64.of_int start_dim) (Int64.of_int end_dim) |> with_tensor_gc

let flatten_dense_tensors (tensors @ local) =
  stubs_flatten_dense_tensors (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> with_tensor_gc

let flip (self @ local) ~dims =
  stubs_flip (globalize_gc_tensor self) (List.map Int64.of_int dims |> CArray.of_list int64_t |> CArray.start) (List.length dims) |> with_tensor_gc

let flip_out ~(out @ local) (self @ local) ~dims =
  stubs_flip_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dims |> CArray.of_list int64_t |> CArray.start) (List.length dims) |> with_tensor_gc

let fliplr (self @ local) =
  stubs_fliplr (globalize_gc_tensor self) |> with_tensor_gc

let flipud (self @ local) =
  stubs_flipud (globalize_gc_tensor self) |> with_tensor_gc

let float_power (self @ local) ~(exponent @ local) =
  stubs_float_power (globalize_gc_tensor self) (globalize_gc_tensor exponent) |> with_tensor_gc

let float_power_ (self @ local) ~exponent =
  stubs_float_power_ (globalize_gc_tensor self) exponent |> with_tensor_gc

let float_power_scalar self ~(exponent @ local) =
  stubs_float_power_scalar self (globalize_gc_tensor exponent) |> with_tensor_gc

let float_power_scalar_out ~(out @ local) self ~(exponent @ local) =
  stubs_float_power_scalar_out (globalize_gc_tensor out) self (globalize_gc_tensor exponent) |> with_tensor_gc

let float_power_tensor_ (self @ local) ~(exponent @ local) =
  stubs_float_power_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor exponent) |> with_tensor_gc

let float_power_tensor_scalar (self @ local) ~exponent =
  stubs_float_power_tensor_scalar (globalize_gc_tensor self) exponent |> with_tensor_gc

let float_power_tensor_scalar_out ~(out @ local) (self @ local) ~exponent =
  stubs_float_power_tensor_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) exponent |> with_tensor_gc

let float_power_tensor_tensor_out ~(out @ local) (self @ local) ~(exponent @ local) =
  stubs_float_power_tensor_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor exponent) |> with_tensor_gc

let floor (self @ local) =
  stubs_floor (globalize_gc_tensor self) |> with_tensor_gc

let floor_ (self @ local) =
  stubs_floor_ (globalize_gc_tensor self) |> with_tensor_gc

let floor_divide (self @ local) (other @ local) =
  stubs_floor_divide (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let floor_divide_ (self @ local) (other @ local) =
  stubs_floor_divide_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let floor_divide_out ~(out @ local) (self @ local) (other @ local) =
  stubs_floor_divide_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let floor_divide_scalar (self @ local) other =
  stubs_floor_divide_scalar (globalize_gc_tensor self) other |> with_tensor_gc

let floor_divide_scalar_ (self @ local) other =
  stubs_floor_divide_scalar_ (globalize_gc_tensor self) other |> with_tensor_gc

let floor_divide_scalar_out ~(out @ local) (self @ local) other =
  stubs_floor_divide_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let floor_out ~(out @ local) (self @ local) =
  stubs_floor_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let fmax (self @ local) (other @ local) =
  stubs_fmax (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let fmax_out ~(out @ local) (self @ local) (other @ local) =
  stubs_fmax_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let fmin (self @ local) (other @ local) =
  stubs_fmin (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let fmin_out ~(out @ local) (self @ local) (other @ local) =
  stubs_fmin_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let fmod (self @ local) other =
  stubs_fmod (globalize_gc_tensor self) other |> with_tensor_gc

let fmod_ (self @ local) other =
  stubs_fmod_ (globalize_gc_tensor self) other |> with_tensor_gc

let fmod_scalar_out ~(out @ local) (self @ local) other =
  stubs_fmod_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let fmod_tensor (self @ local) (other @ local) =
  stubs_fmod_tensor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let fmod_tensor_ (self @ local) (other @ local) =
  stubs_fmod_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let fmod_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_fmod_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let frac (self @ local) =
  stubs_frac (globalize_gc_tensor self) |> with_tensor_gc

let frac_ (self @ local) =
  stubs_frac_ (globalize_gc_tensor self) |> with_tensor_gc

let frac_out ~(out @ local) (self @ local) =
  stubs_frac_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let fractional_max_pool2d (self @ local) ~kernel_size ~output_size ~(random_samples @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_fractional_max_pool2d (CArray.start out__) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (globalize_gc_tensor random_samples);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let fractional_max_pool2d_backward ~(grad_output @ local) (self @ local) ~kernel_size ~output_size ~(indices @ local) =
  stubs_fractional_max_pool2d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (globalize_gc_tensor indices) |> with_tensor_gc

let fractional_max_pool2d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~kernel_size ~output_size ~(indices @ local) =
  stubs_fractional_max_pool2d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (globalize_gc_tensor indices) |> with_tensor_gc

let fractional_max_pool2d_output ~(output @ local) ~(indices @ local) (self @ local) ~kernel_size ~output_size ~(random_samples @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_fractional_max_pool2d_output (CArray.start out__) (globalize_gc_tensor output) (globalize_gc_tensor indices) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (globalize_gc_tensor random_samples);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let fractional_max_pool3d (self @ local) ~kernel_size ~output_size ~(random_samples @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_fractional_max_pool3d (CArray.start out__) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (globalize_gc_tensor random_samples);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let fractional_max_pool3d_backward ~(grad_output @ local) (self @ local) ~kernel_size ~output_size ~(indices @ local) =
  stubs_fractional_max_pool3d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (globalize_gc_tensor indices) |> with_tensor_gc

let fractional_max_pool3d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~kernel_size ~output_size ~(indices @ local) =
  stubs_fractional_max_pool3d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (globalize_gc_tensor indices) |> with_tensor_gc

let fractional_max_pool3d_output ~(output @ local) ~(indices @ local) (self @ local) ~kernel_size ~output_size ~(random_samples @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_fractional_max_pool3d_output (CArray.start out__) (globalize_gc_tensor output) (globalize_gc_tensor indices) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (globalize_gc_tensor random_samples);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let frexp (self @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_frexp (CArray.start out__) (globalize_gc_tensor self);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let frexp_tensor_out ~(mantissa @ local) ~(exponent @ local) (self @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_frexp_tensor_out (CArray.start out__) (globalize_gc_tensor mantissa) (globalize_gc_tensor exponent) (globalize_gc_tensor self);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let frobenius_norm (self @ local) ~dim ~keepdim =
  stubs_frobenius_norm (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) |> with_tensor_gc

let frobenius_norm_out ~(out @ local) (self @ local) ~dim ~keepdim =
  stubs_frobenius_norm_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) |> with_tensor_gc

let from_file ~filename ~shared ~size ~options =
  stubs_from_file filename (if shared then 1 else 0)  (match size with | None -> Int64.zero | Some v -> Int64.of_int v) (match size with | Some _ -> 0 | None -> 1)  (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let from_file_out ~(out @ local) ~filename ~shared ~size =
  stubs_from_file_out (globalize_gc_tensor out) filename (if shared then 1 else 0)  (match size with | None -> Int64.zero | Some v -> Int64.of_int v) (match size with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let full ~size ~fill_value ~options =
  stubs_full (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) fill_value (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let full_like (self @ local) ~fill_value =
  stubs_full_like (globalize_gc_tensor self) fill_value |> with_tensor_gc

let full_like_out ~(out @ local) (self @ local) ~fill_value =
  stubs_full_like_out (globalize_gc_tensor out) (globalize_gc_tensor self) fill_value |> with_tensor_gc

let full_out ~(out @ local) ~size ~fill_value =
  stubs_full_out (globalize_gc_tensor out) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) fill_value |> with_tensor_gc

let fused_moving_avg_obs_fake_quant (self @ local) ~(observer_on @ local) ~(fake_quant_on @ local) ~(running_min @ local) ~(running_max @ local) ~(scale @ local) ~(zero_point @ local) ~averaging_const ~quant_min ~quant_max ~ch_axis ~per_row_fake_quant ~symmetric_quant =
  stubs_fused_moving_avg_obs_fake_quant (globalize_gc_tensor self) (globalize_gc_tensor observer_on) (globalize_gc_tensor fake_quant_on) (globalize_gc_tensor running_min) (globalize_gc_tensor running_max) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) averaging_const (Int64.of_int quant_min) (Int64.of_int quant_max) (Int64.of_int ch_axis) (if per_row_fake_quant then 1 else 0) (if symmetric_quant then 1 else 0) |> with_tensor_gc

let gather (self @ local) ~dim ~(index @ local) ~sparse_grad =
  stubs_gather (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (if sparse_grad then 1 else 0) |> with_tensor_gc

let gather_backward ~(grad @ local) (self @ local) ~dim ~(index @ local) ~sparse_grad =
  stubs_gather_backward (globalize_gc_tensor grad) (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (if sparse_grad then 1 else 0) |> with_tensor_gc

let gather_out ~(out @ local) (self @ local) ~dim ~(index @ local) ~sparse_grad =
  stubs_gather_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (if sparse_grad then 1 else 0) |> with_tensor_gc

let gcd (self @ local) (other @ local) =
  stubs_gcd (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let gcd_ (self @ local) (other @ local) =
  stubs_gcd_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let gcd_out ~(out @ local) (self @ local) (other @ local) =
  stubs_gcd_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let ge (self @ local) other =
  stubs_ge (globalize_gc_tensor self) other |> with_tensor_gc

let ge_ (self @ local) other =
  stubs_ge_ (globalize_gc_tensor self) other |> with_tensor_gc

let ge_scalar_out ~(out @ local) (self @ local) other =
  stubs_ge_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let ge_tensor (self @ local) (other @ local) =
  stubs_ge_tensor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let ge_tensor_ (self @ local) (other @ local) =
  stubs_ge_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let ge_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_ge_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let gelu (self @ local) ~approximate =
  stubs_gelu (globalize_gc_tensor self) approximate |> with_tensor_gc

let gelu_ (self @ local) ~approximate =
  stubs_gelu_ (globalize_gc_tensor self) approximate |> with_tensor_gc

let gelu_backward ~(grad_output @ local) (self @ local) ~approximate =
  stubs_gelu_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) approximate |> with_tensor_gc

let gelu_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~approximate =
  stubs_gelu_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) approximate |> with_tensor_gc

let gelu_out ~(out @ local) (self @ local) ~approximate =
  stubs_gelu_out (globalize_gc_tensor out) (globalize_gc_tensor self) approximate |> with_tensor_gc

let geometric (self @ local) ~p =
  stubs_geometric (globalize_gc_tensor self) p |> with_tensor_gc

let geometric_ (self @ local) ~p =
  stubs_geometric_ (globalize_gc_tensor self) p |> with_tensor_gc

let geometric_out ~(out @ local) (self @ local) ~p =
  stubs_geometric_out (globalize_gc_tensor out) (globalize_gc_tensor self) p |> with_tensor_gc

let geqrf (self @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_geqrf (CArray.start out__) (globalize_gc_tensor self);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let geqrf_a ~(a @ local) ~(tau @ local) (self @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_geqrf_a (CArray.start out__) (globalize_gc_tensor a) (globalize_gc_tensor tau) (globalize_gc_tensor self);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let ger (self @ local) ~(vec2 @ local) =
  stubs_ger (globalize_gc_tensor self) (globalize_gc_tensor vec2) |> with_tensor_gc

let ger_out ~(out @ local) (self @ local) ~(vec2 @ local) =
  stubs_ger_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor vec2) |> with_tensor_gc

let get_device (self @ local) =
  stubs_get_device (globalize_gc_tensor self)

let glu (self @ local) ~dim =
  stubs_glu (globalize_gc_tensor self) (Int64.of_int dim) |> with_tensor_gc

let glu_backward ~(grad_output @ local) (self @ local) ~dim =
  stubs_glu_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (Int64.of_int dim) |> with_tensor_gc

let glu_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~dim =
  stubs_glu_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (Int64.of_int dim) |> with_tensor_gc

let glu_backward_jvp ~(grad_x @ local) ~(grad_glu @ local) ~(x @ local) ~(dgrad_glu @ local) ~(dx @ local) ~dim =
  stubs_glu_backward_jvp (globalize_gc_tensor grad_x) (globalize_gc_tensor grad_glu) (globalize_gc_tensor x) (globalize_gc_tensor dgrad_glu) (globalize_gc_tensor dx) (Int64.of_int dim) |> with_tensor_gc

let glu_backward_jvp_out ~(out @ local) ~(grad_x @ local) ~(grad_glu @ local) ~(x @ local) ~(dgrad_glu @ local) ~(dx @ local) ~dim =
  stubs_glu_backward_jvp_out (globalize_gc_tensor out) (globalize_gc_tensor grad_x) (globalize_gc_tensor grad_glu) (globalize_gc_tensor x) (globalize_gc_tensor dgrad_glu) (globalize_gc_tensor dx) (Int64.of_int dim) |> with_tensor_gc

let glu_jvp ~(glu @ local) ~(x @ local) ~(dx @ local) ~dim =
  stubs_glu_jvp (globalize_gc_tensor glu) (globalize_gc_tensor x) (globalize_gc_tensor dx) (Int64.of_int dim) |> with_tensor_gc

let glu_jvp_out ~(out @ local) ~(glu @ local) ~(x @ local) ~(dx @ local) ~dim =
  stubs_glu_jvp_out (globalize_gc_tensor out) (globalize_gc_tensor glu) (globalize_gc_tensor x) (globalize_gc_tensor dx) (Int64.of_int dim) |> with_tensor_gc

let glu_out ~(out @ local) (self @ local) ~dim =
  stubs_glu_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) |> with_tensor_gc

let grad (self @ local) =
  stubs_grad (globalize_gc_tensor self) |> with_tensor_gc

let greater (self @ local) other =
  stubs_greater (globalize_gc_tensor self) other |> with_tensor_gc

let greater_ (self @ local) other =
  stubs_greater_ (globalize_gc_tensor self) other |> with_tensor_gc

let greater_equal (self @ local) other =
  stubs_greater_equal (globalize_gc_tensor self) other |> with_tensor_gc

let greater_equal_ (self @ local) other =
  stubs_greater_equal_ (globalize_gc_tensor self) other |> with_tensor_gc

let greater_equal_scalar_out ~(out @ local) (self @ local) other =
  stubs_greater_equal_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let greater_equal_tensor (self @ local) (other @ local) =
  stubs_greater_equal_tensor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let greater_equal_tensor_ (self @ local) (other @ local) =
  stubs_greater_equal_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let greater_equal_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_greater_equal_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let greater_scalar_out ~(out @ local) (self @ local) other =
  stubs_greater_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let greater_tensor (self @ local) (other @ local) =
  stubs_greater_tensor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let greater_tensor_ (self @ local) (other @ local) =
  stubs_greater_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let greater_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_greater_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let grid_sampler (input @ local) ~(grid @ local) ~interpolation_mode ~padding_mode ~align_corners =
  stubs_grid_sampler (globalize_gc_tensor input) (globalize_gc_tensor grid) (Int64.of_int interpolation_mode) (Int64.of_int padding_mode) (if align_corners then 1 else 0) |> with_tensor_gc

let grid_sampler_2d (input @ local) ~(grid @ local) ~interpolation_mode ~padding_mode ~align_corners =
  stubs_grid_sampler_2d (globalize_gc_tensor input) (globalize_gc_tensor grid) (Int64.of_int interpolation_mode) (Int64.of_int padding_mode) (if align_corners then 1 else 0) |> with_tensor_gc

let grid_sampler_2d_out ~(out @ local) (input @ local) ~(grid @ local) ~interpolation_mode ~padding_mode ~align_corners =
  stubs_grid_sampler_2d_out (globalize_gc_tensor out) (globalize_gc_tensor input) (globalize_gc_tensor grid) (Int64.of_int interpolation_mode) (Int64.of_int padding_mode) (if align_corners then 1 else 0) |> with_tensor_gc

let grid_sampler_3d (input @ local) ~(grid @ local) ~interpolation_mode ~padding_mode ~align_corners =
  stubs_grid_sampler_3d (globalize_gc_tensor input) (globalize_gc_tensor grid) (Int64.of_int interpolation_mode) (Int64.of_int padding_mode) (if align_corners then 1 else 0) |> with_tensor_gc

let grid_sampler_3d_out ~(out @ local) (input @ local) ~(grid @ local) ~interpolation_mode ~padding_mode ~align_corners =
  stubs_grid_sampler_3d_out (globalize_gc_tensor out) (globalize_gc_tensor input) (globalize_gc_tensor grid) (Int64.of_int interpolation_mode) (Int64.of_int padding_mode) (if align_corners then 1 else 0) |> with_tensor_gc

let group_norm (input @ local) ~num_groups ~(weight @ local) ~(bias @ local) ~eps ~cudnn_enabled =
  stubs_group_norm (globalize_gc_tensor input) (Int64.of_int num_groups) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) eps (if cudnn_enabled then 1 else 0) |> with_tensor_gc

let gru (input @ local) ~(hx @ local) ~(params @ local) ~has_biases ~num_layers ~dropout ~train ~bidirectional ~batch_first =
  let out__ = CArray.make raw_tensor 2 in
  stubs_gru (CArray.start out__) (globalize_gc_tensor input) (globalize_gc_tensor hx) (CArray.of_list gc_tensor (globalize_gc_tensor_list params) |> CArray.start) (Base.List.length params) (if has_biases then 1 else 0) (Int64.of_int num_layers) dropout (if train then 1 else 0) (if bidirectional then 1 else 0) (if batch_first then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let gru_cell (input @ local) ~(hx @ local) ~(w_ih @ local) ~(w_hh @ local) ~(b_ih @ local) ~(b_hh @ local) =
  stubs_gru_cell (globalize_gc_tensor input) (globalize_gc_tensor hx) (globalize_gc_tensor w_ih) (globalize_gc_tensor w_hh) (match (Base.Option.globalize globalize_gc_tensor b_ih) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor b_hh) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let gru_data ~(data @ local) ~(batch_sizes @ local) ~(hx @ local) ~(params @ local) ~has_biases ~num_layers ~dropout ~train ~bidirectional =
  let out__ = CArray.make raw_tensor 2 in
  stubs_gru_data (CArray.start out__) (globalize_gc_tensor data) (globalize_gc_tensor batch_sizes) (globalize_gc_tensor hx) (CArray.of_list gc_tensor (globalize_gc_tensor_list params) |> CArray.start) (Base.List.length params) (if has_biases then 1 else 0) (Int64.of_int num_layers) dropout (if train then 1 else 0) (if bidirectional then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let gt (self @ local) other =
  stubs_gt (globalize_gc_tensor self) other |> with_tensor_gc

let gt_ (self @ local) other =
  stubs_gt_ (globalize_gc_tensor self) other |> with_tensor_gc

let gt_scalar_out ~(out @ local) (self @ local) other =
  stubs_gt_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let gt_tensor (self @ local) (other @ local) =
  stubs_gt_tensor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let gt_tensor_ (self @ local) (other @ local) =
  stubs_gt_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let gt_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_gt_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let hamming_window ~window_length ~options =
  stubs_hamming_window (Int64.of_int window_length) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let hamming_window_out ~(out @ local) ~window_length =
  stubs_hamming_window_out (globalize_gc_tensor out) (Int64.of_int window_length) |> with_tensor_gc

let hamming_window_periodic ~window_length ~periodic ~options =
  stubs_hamming_window_periodic (Int64.of_int window_length) (if periodic then 1 else 0) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let hamming_window_periodic_alpha ~window_length ~periodic ~alpha ~options =
  stubs_hamming_window_periodic_alpha (Int64.of_int window_length) (if periodic then 1 else 0) alpha (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let hamming_window_periodic_alpha_beta ~window_length ~periodic ~alpha ~beta ~options =
  stubs_hamming_window_periodic_alpha_beta (Int64.of_int window_length) (if periodic then 1 else 0) alpha beta (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let hamming_window_periodic_alpha_beta_out ~(out @ local) ~window_length ~periodic ~alpha ~beta =
  stubs_hamming_window_periodic_alpha_beta_out (globalize_gc_tensor out) (Int64.of_int window_length) (if periodic then 1 else 0) alpha beta |> with_tensor_gc

let hamming_window_periodic_alpha_out ~(out @ local) ~window_length ~periodic ~alpha =
  stubs_hamming_window_periodic_alpha_out (globalize_gc_tensor out) (Int64.of_int window_length) (if periodic then 1 else 0) alpha |> with_tensor_gc

let hamming_window_periodic_out ~(out @ local) ~window_length ~periodic =
  stubs_hamming_window_periodic_out (globalize_gc_tensor out) (Int64.of_int window_length) (if periodic then 1 else 0) |> with_tensor_gc

let hann_window ~window_length ~options =
  stubs_hann_window (Int64.of_int window_length) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let hann_window_out ~(out @ local) ~window_length =
  stubs_hann_window_out (globalize_gc_tensor out) (Int64.of_int window_length) |> with_tensor_gc

let hann_window_periodic ~window_length ~periodic ~options =
  stubs_hann_window_periodic (Int64.of_int window_length) (if periodic then 1 else 0) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let hann_window_periodic_out ~(out @ local) ~window_length ~periodic =
  stubs_hann_window_periodic_out (globalize_gc_tensor out) (Int64.of_int window_length) (if periodic then 1 else 0) |> with_tensor_gc

let hardshrink ?lambd (self @ local) =
  stubs_hardshrink (globalize_gc_tensor self) (match lambd with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let hardshrink_backward ~(grad_out @ local) (self @ local) ~lambd =
  stubs_hardshrink_backward (globalize_gc_tensor grad_out) (globalize_gc_tensor self) lambd |> with_tensor_gc

let hardshrink_backward_grad_input ~(grad_input @ local) ~(grad_out @ local) (self @ local) ~lambd =
  stubs_hardshrink_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_out) (globalize_gc_tensor self) lambd |> with_tensor_gc

let hardshrink_out ?lambd ~(out @ local) (self @ local) =
  stubs_hardshrink_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match lambd with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let hardsigmoid (self @ local) =
  stubs_hardsigmoid (globalize_gc_tensor self) |> with_tensor_gc

let hardsigmoid_ (self @ local) =
  stubs_hardsigmoid_ (globalize_gc_tensor self) |> with_tensor_gc

let hardsigmoid_backward ~(grad_output @ local) (self @ local) =
  stubs_hardsigmoid_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) |> with_tensor_gc

let hardsigmoid_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) =
  stubs_hardsigmoid_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) |> with_tensor_gc

let hardsigmoid_out ~(out @ local) (self @ local) =
  stubs_hardsigmoid_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let hardswish (self @ local) =
  stubs_hardswish (globalize_gc_tensor self) |> with_tensor_gc

let hardswish_ (self @ local) =
  stubs_hardswish_ (globalize_gc_tensor self) |> with_tensor_gc

let hardswish_backward ~(grad_output @ local) (self @ local) =
  stubs_hardswish_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) |> with_tensor_gc

let hardswish_backward_out ~(out @ local) ~(grad_output @ local) (self @ local) =
  stubs_hardswish_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) |> with_tensor_gc

let hardswish_out ~(out @ local) (self @ local) =
  stubs_hardswish_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let hardtanh ?min_val ?max_val (self @ local) =
  stubs_hardtanh (globalize_gc_tensor self) (match min_val with | Some v -> v | None -> none_scalar) (match max_val with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let hardtanh_ ?min_val ?max_val (self @ local) =
  stubs_hardtanh_ (globalize_gc_tensor self) (match min_val with | Some v -> v | None -> none_scalar) (match max_val with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let hardtanh_backward ~(grad_output @ local) (self @ local) ~min_val ~max_val =
  stubs_hardtanh_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) min_val max_val |> with_tensor_gc

let hardtanh_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~min_val ~max_val =
  stubs_hardtanh_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) min_val max_val |> with_tensor_gc

let hardtanh_out ?min_val ?max_val ~(out @ local) (self @ local) =
  stubs_hardtanh_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match min_val with | Some v -> v | None -> none_scalar) (match max_val with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let hash_tensor (self @ local) ~dim ~keepdim ~mode =
  stubs_hash_tensor (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) (Int64.of_int mode) |> with_tensor_gc

let hash_tensor_out ~(out @ local) (self @ local) ~dim ~keepdim ~mode =
  stubs_hash_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) (Int64.of_int mode) |> with_tensor_gc

let heaviside (self @ local) ~(values @ local) =
  stubs_heaviside (globalize_gc_tensor self) (globalize_gc_tensor values) |> with_tensor_gc

let heaviside_ (self @ local) ~(values @ local) =
  stubs_heaviside_ (globalize_gc_tensor self) (globalize_gc_tensor values) |> with_tensor_gc

let heaviside_out ~(out @ local) (self @ local) ~(values @ local) =
  stubs_heaviside_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor values) |> with_tensor_gc

let hinge_embedding_loss (self @ local) ~(target @ local) ~margin ~reduction =
  stubs_hinge_embedding_loss (globalize_gc_tensor self) (globalize_gc_tensor target) margin (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let histc ?min ?max (self @ local) ~bins =
  stubs_histc (globalize_gc_tensor self) (Int64.of_int bins) (match min with | Some v -> v | None -> none_scalar) (match max with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let histc_out ?min ?max ~(out @ local) (self @ local) ~bins =
  stubs_histc_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int bins) (match min with | Some v -> v | None -> none_scalar) (match max with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let hsplit (self @ local) ~sections =
  stubs_hsplit (globalize_gc_tensor self) (Int64.of_int sections) |> to_tensor_list

let hsplit_array (self @ local) ~indices =
  stubs_hsplit_array (globalize_gc_tensor self) (List.map Int64.of_int indices |> CArray.of_list int64_t |> CArray.start) (List.length indices) |> to_tensor_list

let hspmm ~(mat1 @ local) ~(mat2 @ local) =
  stubs_hspmm (globalize_gc_tensor mat1) (globalize_gc_tensor mat2) |> with_tensor_gc

let hspmm_out ~(out @ local) ~(mat1 @ local) ~(mat2 @ local) =
  stubs_hspmm_out (globalize_gc_tensor out) (globalize_gc_tensor mat1) (globalize_gc_tensor mat2) |> with_tensor_gc

let hstack (tensors @ local) =
  stubs_hstack (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> with_tensor_gc

let hstack_out ~(out @ local) (tensors @ local) =
  stubs_hstack_out (globalize_gc_tensor out) (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> with_tensor_gc

let huber_loss (self @ local) ~(target @ local) ~reduction ~delta =
  stubs_huber_loss (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) delta |> with_tensor_gc

let huber_loss_backward ~(grad_output @ local) (self @ local) ~(target @ local) ~reduction ~delta =
  stubs_huber_loss_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) delta |> with_tensor_gc

let huber_loss_backward_out ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~(target @ local) ~reduction ~delta =
  stubs_huber_loss_backward_out (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) delta |> with_tensor_gc

let huber_loss_out ~(out @ local) (self @ local) ~(target @ local) ~reduction ~delta =
  stubs_huber_loss_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) delta |> with_tensor_gc

let hypot (self @ local) (other @ local) =
  stubs_hypot (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let hypot_ (self @ local) (other @ local) =
  stubs_hypot_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let hypot_out ~(out @ local) (self @ local) (other @ local) =
  stubs_hypot_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let i0 (self @ local) =
  stubs_i0 (globalize_gc_tensor self) |> with_tensor_gc

let i0_ (self @ local) =
  stubs_i0_ (globalize_gc_tensor self) |> with_tensor_gc

let i0_out ~(out @ local) (self @ local) =
  stubs_i0_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let igamma (self @ local) (other @ local) =
  stubs_igamma (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let igamma_ (self @ local) (other @ local) =
  stubs_igamma_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let igamma_out ~(out @ local) (self @ local) (other @ local) =
  stubs_igamma_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let igammac (self @ local) (other @ local) =
  stubs_igammac (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let igammac_ (self @ local) (other @ local) =
  stubs_igammac_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let igammac_out ~(out @ local) (self @ local) (other @ local) =
  stubs_igammac_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let im2col (self @ local) ~kernel_size ~dilation ~padding ~stride =
  stubs_im2col (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) |> with_tensor_gc

let im2col_out ~(out @ local) (self @ local) ~kernel_size ~dilation ~padding ~stride =
  stubs_im2col_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) |> with_tensor_gc

let imag (self @ local) =
  stubs_imag (globalize_gc_tensor self) |> with_tensor_gc

let index_add ?alpha (self @ local) ~dim ~(index @ local) ~(source @ local) =
  stubs_index_add (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor source) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let index_add_ ?alpha (self @ local) ~dim ~(index @ local) ~(source @ local) =
  stubs_index_add_ (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor source) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let index_add_out ?alpha ~(out @ local) (self @ local) ~dim ~(index @ local) ~(source @ local) =
  stubs_index_add_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor source) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let index_copy (self @ local) ~dim ~(index @ local) ~(source @ local) =
  stubs_index_copy (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor source) |> with_tensor_gc

let index_copy_ (self @ local) ~dim ~(index @ local) ~(source @ local) =
  stubs_index_copy_ (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor source) |> with_tensor_gc

let index_copy_out ~(out @ local) (self @ local) ~dim ~(index @ local) ~(source @ local) =
  stubs_index_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor source) |> with_tensor_gc

let index_fill (self @ local) ~dim ~(index @ local) ~value =
  stubs_index_fill (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) value |> with_tensor_gc

let index_fill_ (self @ local) ~dim ~(index @ local) ~value =
  stubs_index_fill_ (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) value |> with_tensor_gc

let index_fill_int_scalar_out ~(out @ local) (self @ local) ~dim ~(index @ local) ~value =
  stubs_index_fill_int_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) value |> with_tensor_gc

let index_fill_int_tensor (self @ local) ~dim ~(index @ local) ~(value @ local) =
  stubs_index_fill_int_tensor (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor value) |> with_tensor_gc

let index_fill_int_tensor_ (self @ local) ~dim ~(index @ local) ~(value @ local) =
  stubs_index_fill_int_tensor_ (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor value) |> with_tensor_gc

let index_fill_int_tensor_out ~(out @ local) (self @ local) ~dim ~(index @ local) ~(value @ local) =
  stubs_index_fill_int_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor value) |> with_tensor_gc

let index_reduce (self @ local) ~dim ~(index @ local) ~(source @ local) ~reduce ~include_self =
  stubs_index_reduce (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor source) reduce (if include_self then 1 else 0) |> with_tensor_gc

let index_reduce_ (self @ local) ~dim ~(index @ local) ~(source @ local) ~reduce ~include_self =
  stubs_index_reduce_ (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor source) reduce (if include_self then 1 else 0) |> with_tensor_gc

let index_reduce_out ~(out @ local) (self @ local) ~dim ~(index @ local) ~(source @ local) ~reduce ~include_self =
  stubs_index_reduce_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor source) reduce (if include_self then 1 else 0) |> with_tensor_gc

let index_select (self @ local) ~dim ~(index @ local) =
  stubs_index_select (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) |> with_tensor_gc

let index_select_backward ~(grad @ local) ~self_sizes ~dim ~(index @ local) =
  stubs_index_select_backward (globalize_gc_tensor grad) (List.map Int64.of_int self_sizes |> CArray.of_list int64_t |> CArray.start) (List.length self_sizes) (Int64.of_int dim) (globalize_gc_tensor index) |> with_tensor_gc

let index_select_out ~(out @ local) (self @ local) ~dim ~(index @ local) =
  stubs_index_select_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) |> with_tensor_gc

let indices (self @ local) =
  stubs_indices (globalize_gc_tensor self) |> with_tensor_gc

let indices_copy (self @ local) =
  stubs_indices_copy (globalize_gc_tensor self) |> with_tensor_gc

let indices_copy_out ~(out @ local) (self @ local) =
  stubs_indices_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let infinitely_differentiable_gelu_backward ~(grad @ local) (self @ local) =
  stubs_infinitely_differentiable_gelu_backward (globalize_gc_tensor grad) (globalize_gc_tensor self) |> with_tensor_gc

let inner (self @ local) (other @ local) =
  stubs_inner (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let inner_out ~(out @ local) (self @ local) (other @ local) =
  stubs_inner_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let instance_norm (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~use_input_stats ~momentum ~eps ~cudnn_enabled =
  stubs_instance_norm (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) (if use_input_stats then 1 else 0) momentum eps (if cudnn_enabled then 1 else 0) |> with_tensor_gc

let int_repr (self @ local) =
  stubs_int_repr (globalize_gc_tensor self) |> with_tensor_gc

let int_repr_out ~(out @ local) (self @ local) =
  stubs_int_repr_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let inverse (self @ local) =
  stubs_inverse (globalize_gc_tensor self) |> with_tensor_gc

let inverse_out ~(out @ local) (self @ local) =
  stubs_inverse_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let is_coalesced (self @ local) =
  stubs_is_coalesced (globalize_gc_tensor self)

let is_complex (self @ local) =
  stubs_is_complex (globalize_gc_tensor self)

let is_conj (self @ local) =
  stubs_is_conj (globalize_gc_tensor self)

let is_contiguous (self @ local) =
  stubs_is_contiguous (globalize_gc_tensor self)

let is_distributed (self @ local) =
  stubs_is_distributed (globalize_gc_tensor self)

let is_floating_point (self @ local) =
  stubs_is_floating_point (globalize_gc_tensor self)

let is_inference (self @ local) =
  stubs_is_inference (globalize_gc_tensor self)

let is_leaf (self @ local) =
  stubs_is_leaf (globalize_gc_tensor self)

let is_neg (self @ local) =
  stubs_is_neg (globalize_gc_tensor self)

let is_nonzero (self @ local) =
  stubs_is_nonzero (globalize_gc_tensor self)

let is_pinned (self @ local) ~device =
  stubs_is_pinned (globalize_gc_tensor self)  (Device.option_to_int device) 

let is_same_size (self @ local) (other @ local) =
  stubs_is_same_size (globalize_gc_tensor self) (globalize_gc_tensor other)

let is_set_to (self @ local) (tensor @ local) =
  stubs_is_set_to (globalize_gc_tensor self) (globalize_gc_tensor tensor)

let is_signed (self @ local) =
  stubs_is_signed (globalize_gc_tensor self)

let is_vulkan_available  =
  stubs_is_vulkan_available 

let isclose (self @ local) (other @ local) ~rtol ~atol ~equal_nan =
  stubs_isclose (globalize_gc_tensor self) (globalize_gc_tensor other) rtol atol (if equal_nan then 1 else 0) |> with_tensor_gc

let isfinite (self @ local) =
  stubs_isfinite (globalize_gc_tensor self) |> with_tensor_gc

let isin ~(elements @ local) ~(test_elements @ local) ~assume_unique ~invert =
  stubs_isin (globalize_gc_tensor elements) (globalize_gc_tensor test_elements) (if assume_unique then 1 else 0) (if invert then 1 else 0) |> with_tensor_gc

let isin_scalar_tensor ~element ~(test_elements @ local) ~assume_unique ~invert =
  stubs_isin_scalar_tensor element (globalize_gc_tensor test_elements) (if assume_unique then 1 else 0) (if invert then 1 else 0) |> with_tensor_gc

let isin_scalar_tensor_out ~(out @ local) ~element ~(test_elements @ local) ~assume_unique ~invert =
  stubs_isin_scalar_tensor_out (globalize_gc_tensor out) element (globalize_gc_tensor test_elements) (if assume_unique then 1 else 0) (if invert then 1 else 0) |> with_tensor_gc

let isin_tensor_scalar ~(elements @ local) ~test_element ~assume_unique ~invert =
  stubs_isin_tensor_scalar (globalize_gc_tensor elements) test_element (if assume_unique then 1 else 0) (if invert then 1 else 0) |> with_tensor_gc

let isin_tensor_scalar_out ~(out @ local) ~(elements @ local) ~test_element ~assume_unique ~invert =
  stubs_isin_tensor_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor elements) test_element (if assume_unique then 1 else 0) (if invert then 1 else 0) |> with_tensor_gc

let isin_tensor_tensor_out ~(out @ local) ~(elements @ local) ~(test_elements @ local) ~assume_unique ~invert =
  stubs_isin_tensor_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor elements) (globalize_gc_tensor test_elements) (if assume_unique then 1 else 0) (if invert then 1 else 0) |> with_tensor_gc

let isinf (self @ local) =
  stubs_isinf (globalize_gc_tensor self) |> with_tensor_gc

let isinf_out ~(out @ local) (self @ local) =
  stubs_isinf_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let isnan (self @ local) =
  stubs_isnan (globalize_gc_tensor self) |> with_tensor_gc

let isnan_out ~(out @ local) (self @ local) =
  stubs_isnan_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let isneginf (self @ local) =
  stubs_isneginf (globalize_gc_tensor self) |> with_tensor_gc

let isneginf_out ~(out @ local) (self @ local) =
  stubs_isneginf_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let isposinf (self @ local) =
  stubs_isposinf (globalize_gc_tensor self) |> with_tensor_gc

let isposinf_out ~(out @ local) (self @ local) =
  stubs_isposinf_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let isreal (self @ local) =
  stubs_isreal (globalize_gc_tensor self) |> with_tensor_gc

let istft (self @ local) ~n_fft ~hop_length ~win_length ~(window @ local) ~center ~normalized ~onesided ~length ~return_complex =
  stubs_istft (globalize_gc_tensor self) (Int64.of_int n_fft)  (match hop_length with | None -> Int64.zero | Some v -> Int64.of_int v) (match hop_length with | Some _ -> 0 | None -> 1)   (match win_length with | None -> Int64.zero | Some v -> Int64.of_int v) (match win_length with | Some _ -> 0 | None -> 1)  (match (Base.Option.globalize globalize_gc_tensor window) with | Some v -> v | None -> none_gc_tensor) (if center then 1 else 0) (if normalized then 1 else 0) (if onesided then 1 else 0)  (match length with | None -> Int64.zero | Some v -> Int64.of_int v) (match length with | Some _ -> 0 | None -> 1)  (if return_complex then 1 else 0) |> with_tensor_gc

let kaiser_window ~window_length ~options =
  stubs_kaiser_window (Int64.of_int window_length) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let kaiser_window_beta ~window_length ~periodic ~beta ~options =
  stubs_kaiser_window_beta (Int64.of_int window_length) (if periodic then 1 else 0) beta (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let kaiser_window_beta_out ~(out @ local) ~window_length ~periodic ~beta =
  stubs_kaiser_window_beta_out (globalize_gc_tensor out) (Int64.of_int window_length) (if periodic then 1 else 0) beta |> with_tensor_gc

let kaiser_window_out ~(out @ local) ~window_length =
  stubs_kaiser_window_out (globalize_gc_tensor out) (Int64.of_int window_length) |> with_tensor_gc

let kaiser_window_periodic ~window_length ~periodic ~options =
  stubs_kaiser_window_periodic (Int64.of_int window_length) (if periodic then 1 else 0) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let kaiser_window_periodic_out ~(out @ local) ~window_length ~periodic =
  stubs_kaiser_window_periodic_out (globalize_gc_tensor out) (Int64.of_int window_length) (if periodic then 1 else 0) |> with_tensor_gc

let kl_div (self @ local) ~(target @ local) ~reduction ~log_target =
  stubs_kl_div (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) (if log_target then 1 else 0) |> with_tensor_gc

let kron (self @ local) (other @ local) =
  stubs_kron (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let kron_out ~(out @ local) (self @ local) (other @ local) =
  stubs_kron_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let kthvalue (self @ local) ~k ~dim ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_kthvalue (CArray.start out__) (globalize_gc_tensor self) (Int64.of_int k) (Int64.of_int dim) (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let kthvalue_values ~(values @ local) ~(indices @ local) (self @ local) ~k ~dim ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_kthvalue_values (CArray.start out__) (globalize_gc_tensor values) (globalize_gc_tensor indices) (globalize_gc_tensor self) (Int64.of_int k) (Int64.of_int dim) (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let l1_loss (self @ local) ~(target @ local) ~reduction =
  stubs_l1_loss (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let layer_norm (input @ local) ~normalized_shape ~(weight @ local) ~(bias @ local) ~eps ~cudnn_enable =
  stubs_layer_norm (globalize_gc_tensor input) (List.map Int64.of_int normalized_shape |> CArray.of_list int64_t |> CArray.start) (List.length normalized_shape) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) eps (if cudnn_enable then 1 else 0) |> with_tensor_gc

let lcm (self @ local) (other @ local) =
  stubs_lcm (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let lcm_ (self @ local) (other @ local) =
  stubs_lcm_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let lcm_out ~(out @ local) (self @ local) (other @ local) =
  stubs_lcm_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let ldexp (self @ local) (other @ local) =
  stubs_ldexp (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let ldexp_ (self @ local) (other @ local) =
  stubs_ldexp_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let ldexp_out ~(out @ local) (self @ local) (other @ local) =
  stubs_ldexp_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let le (self @ local) other =
  stubs_le (globalize_gc_tensor self) other |> with_tensor_gc

let le_ (self @ local) other =
  stubs_le_ (globalize_gc_tensor self) other |> with_tensor_gc

let le_scalar_out ~(out @ local) (self @ local) other =
  stubs_le_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let le_tensor (self @ local) (other @ local) =
  stubs_le_tensor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let le_tensor_ (self @ local) (other @ local) =
  stubs_le_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let le_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_le_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let leaky_relu ?negative_slope (self @ local) =
  stubs_leaky_relu (globalize_gc_tensor self) (match negative_slope with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let leaky_relu_ ?negative_slope (self @ local) =
  stubs_leaky_relu_ (globalize_gc_tensor self) (match negative_slope with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let leaky_relu_backward ~(grad_output @ local) (self @ local) ~negative_slope ~self_is_result =
  stubs_leaky_relu_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) negative_slope (if self_is_result then 1 else 0) |> with_tensor_gc

let leaky_relu_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~negative_slope ~self_is_result =
  stubs_leaky_relu_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) negative_slope (if self_is_result then 1 else 0) |> with_tensor_gc

let leaky_relu_out ?negative_slope ~(out @ local) (self @ local) =
  stubs_leaky_relu_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match negative_slope with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let lerp (self @ local) ~(end_ @ local) ~weight =
  stubs_lerp (globalize_gc_tensor self) (globalize_gc_tensor end_) weight |> with_tensor_gc

let lerp_ (self @ local) ~(end_ @ local) ~weight =
  stubs_lerp_ (globalize_gc_tensor self) (globalize_gc_tensor end_) weight |> with_tensor_gc

let lerp_scalar_out ~(out @ local) (self @ local) ~(end_ @ local) ~weight =
  stubs_lerp_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor end_) weight |> with_tensor_gc

let lerp_tensor (self @ local) ~(end_ @ local) ~(weight @ local) =
  stubs_lerp_tensor (globalize_gc_tensor self) (globalize_gc_tensor end_) (globalize_gc_tensor weight) |> with_tensor_gc

let lerp_tensor_ (self @ local) ~(end_ @ local) ~(weight @ local) =
  stubs_lerp_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor end_) (globalize_gc_tensor weight) |> with_tensor_gc

let lerp_tensor_out ~(out @ local) (self @ local) ~(end_ @ local) ~(weight @ local) =
  stubs_lerp_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor end_) (globalize_gc_tensor weight) |> with_tensor_gc

let less (self @ local) other =
  stubs_less (globalize_gc_tensor self) other |> with_tensor_gc

let less_ (self @ local) other =
  stubs_less_ (globalize_gc_tensor self) other |> with_tensor_gc

let less_equal (self @ local) other =
  stubs_less_equal (globalize_gc_tensor self) other |> with_tensor_gc

let less_equal_ (self @ local) other =
  stubs_less_equal_ (globalize_gc_tensor self) other |> with_tensor_gc

let less_equal_scalar_out ~(out @ local) (self @ local) other =
  stubs_less_equal_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let less_equal_tensor (self @ local) (other @ local) =
  stubs_less_equal_tensor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let less_equal_tensor_ (self @ local) (other @ local) =
  stubs_less_equal_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let less_equal_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_less_equal_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let less_scalar_out ~(out @ local) (self @ local) other =
  stubs_less_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let less_tensor (self @ local) (other @ local) =
  stubs_less_tensor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let less_tensor_ (self @ local) (other @ local) =
  stubs_less_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let less_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_less_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let lgamma (self @ local) =
  stubs_lgamma (globalize_gc_tensor self) |> with_tensor_gc

let lgamma_ (self @ local) =
  stubs_lgamma_ (globalize_gc_tensor self) |> with_tensor_gc

let lgamma_out ~(out @ local) (self @ local) =
  stubs_lgamma_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let lift (self @ local) =
  stubs_lift (globalize_gc_tensor self) |> with_tensor_gc

let lift_fresh (self @ local) =
  stubs_lift_fresh (globalize_gc_tensor self) |> with_tensor_gc

let lift_fresh_copy (self @ local) =
  stubs_lift_fresh_copy (globalize_gc_tensor self) |> with_tensor_gc

let lift_fresh_copy_out ~(out @ local) (self @ local) =
  stubs_lift_fresh_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let lift_out ~(out @ local) (self @ local) =
  stubs_lift_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let linalg__powsum ?ord (self @ local) ~dim ~keepdim ~dtype =
  stubs_linalg__powsum (globalize_gc_tensor self) (match ord with | Some v -> v | None -> none_scalar) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let linalg_cholesky (self @ local) ~upper =
  stubs_linalg_cholesky (globalize_gc_tensor self) (if upper then 1 else 0) |> with_tensor_gc

let linalg_cholesky_ex (self @ local) ~upper ~check_errors =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_cholesky_ex (CArray.start out__) (globalize_gc_tensor self) (if upper then 1 else 0) (if check_errors then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_cholesky_ex_l ~(l @ local) ~(info @ local) (self @ local) ~upper ~check_errors =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_cholesky_ex_l (CArray.start out__) (globalize_gc_tensor l) (globalize_gc_tensor info) (globalize_gc_tensor self) (if upper then 1 else 0) (if check_errors then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_cholesky_out ~(out @ local) (self @ local) ~upper =
  stubs_linalg_cholesky_out (globalize_gc_tensor out) (globalize_gc_tensor self) (if upper then 1 else 0) |> with_tensor_gc

let linalg_cond (self @ local) ~p =
  stubs_linalg_cond (globalize_gc_tensor self) p |> with_tensor_gc

let linalg_cond_out ~(out @ local) (self @ local) ~p =
  stubs_linalg_cond_out (globalize_gc_tensor out) (globalize_gc_tensor self) p |> with_tensor_gc

let linalg_cond_p_str (self @ local) ~p =
  stubs_linalg_cond_p_str (globalize_gc_tensor self) p |> with_tensor_gc

let linalg_cond_p_str_out ~(out @ local) (self @ local) ~p =
  stubs_linalg_cond_p_str_out (globalize_gc_tensor out) (globalize_gc_tensor self) p |> with_tensor_gc

let linalg_cross (self @ local) (other @ local) ~dim =
  stubs_linalg_cross (globalize_gc_tensor self) (globalize_gc_tensor other) (Int64.of_int dim) |> with_tensor_gc

let linalg_cross_out ~(out @ local) (self @ local) (other @ local) ~dim =
  stubs_linalg_cross_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) (Int64.of_int dim) |> with_tensor_gc

let linalg_det ~(a @ local) =
  stubs_linalg_det (globalize_gc_tensor a) |> with_tensor_gc

let linalg_det_out ~(out @ local) ~(a @ local) =
  stubs_linalg_det_out (globalize_gc_tensor out) (globalize_gc_tensor a) |> with_tensor_gc

let linalg_diagonal ~(a @ local) ~offset ~dim1 ~dim2 =
  stubs_linalg_diagonal (globalize_gc_tensor a) (Int64.of_int offset) (Int64.of_int dim1) (Int64.of_int dim2) |> with_tensor_gc

let linalg_eig (self @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_eig (CArray.start out__) (globalize_gc_tensor self);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_eig_out ~(eigenvalues @ local) ~(eigenvectors @ local) (self @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_eig_out (CArray.start out__) (globalize_gc_tensor eigenvalues) (globalize_gc_tensor eigenvectors) (globalize_gc_tensor self);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_eigh (self @ local) ~uplo =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_eigh (CArray.start out__) (globalize_gc_tensor self) uplo;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_eigh_eigvals ~(eigvals @ local) ~(eigvecs @ local) (self @ local) ~uplo =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_eigh_eigvals (CArray.start out__) (globalize_gc_tensor eigvals) (globalize_gc_tensor eigvecs) (globalize_gc_tensor self) uplo;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_eigvals (self @ local) =
  stubs_linalg_eigvals (globalize_gc_tensor self) |> with_tensor_gc

let linalg_eigvals_out ~(out @ local) (self @ local) =
  stubs_linalg_eigvals_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let linalg_eigvalsh (self @ local) ~uplo =
  stubs_linalg_eigvalsh (globalize_gc_tensor self) uplo |> with_tensor_gc

let linalg_eigvalsh_out ~(out @ local) (self @ local) ~uplo =
  stubs_linalg_eigvalsh_out (globalize_gc_tensor out) (globalize_gc_tensor self) uplo |> with_tensor_gc

let linalg_householder_product (input @ local) ~(tau @ local) =
  stubs_linalg_householder_product (globalize_gc_tensor input) (globalize_gc_tensor tau) |> with_tensor_gc

let linalg_householder_product_out ~(out @ local) (input @ local) ~(tau @ local) =
  stubs_linalg_householder_product_out (globalize_gc_tensor out) (globalize_gc_tensor input) (globalize_gc_tensor tau) |> with_tensor_gc

let linalg_inv ~(a @ local) =
  stubs_linalg_inv (globalize_gc_tensor a) |> with_tensor_gc

let linalg_inv_ex ~(a @ local) ~check_errors =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_inv_ex (CArray.start out__) (globalize_gc_tensor a) (if check_errors then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_inv_ex_inverse ~(inverse @ local) ~(info @ local) ~(a @ local) ~check_errors =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_inv_ex_inverse (CArray.start out__) (globalize_gc_tensor inverse) (globalize_gc_tensor info) (globalize_gc_tensor a) (if check_errors then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_inv_out ~(out @ local) ~(a @ local) =
  stubs_linalg_inv_out (globalize_gc_tensor out) (globalize_gc_tensor a) |> with_tensor_gc

let linalg_ldl_factor (self @ local) ~hermitian =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_ldl_factor (CArray.start out__) (globalize_gc_tensor self) (if hermitian then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_ldl_factor_ex (self @ local) ~hermitian ~check_errors =
  let out__ = CArray.make raw_tensor 3 in
  stubs_linalg_ldl_factor_ex (CArray.start out__) (globalize_gc_tensor self) (if hermitian then 1 else 0) (if check_errors then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let linalg_ldl_factor_ex_out ~(ld @ local) ~(pivots @ local) ~(info @ local) (self @ local) ~hermitian ~check_errors =
  let out__ = CArray.make raw_tensor 3 in
  stubs_linalg_ldl_factor_ex_out (CArray.start out__) (globalize_gc_tensor ld) (globalize_gc_tensor pivots) (globalize_gc_tensor info) (globalize_gc_tensor self) (if hermitian then 1 else 0) (if check_errors then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let linalg_ldl_factor_out ~(ld @ local) ~(pivots @ local) (self @ local) ~hermitian =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_ldl_factor_out (CArray.start out__) (globalize_gc_tensor ld) (globalize_gc_tensor pivots) (globalize_gc_tensor self) (if hermitian then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_ldl_solve ~(ld @ local) ~(pivots @ local) ~(b @ local) ~hermitian =
  stubs_linalg_ldl_solve (globalize_gc_tensor ld) (globalize_gc_tensor pivots) (globalize_gc_tensor b) (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_ldl_solve_out ~(out @ local) ~(ld @ local) ~(pivots @ local) ~(b @ local) ~hermitian =
  stubs_linalg_ldl_solve_out (globalize_gc_tensor out) (globalize_gc_tensor ld) (globalize_gc_tensor pivots) (globalize_gc_tensor b) (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_lstsq (self @ local) ~(b @ local) ~rcond ~driver =
  let out__ = CArray.make raw_tensor 4 in
  stubs_linalg_lstsq (CArray.start out__) (globalize_gc_tensor self) (globalize_gc_tensor b)  (Option.value rcond ~default:0.0) (match rcond with | Some _ -> 0 | None -> 1)   (Option.value driver ~default:"") (match driver with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let linalg_lstsq_out ~(solution @ local) ~(residuals @ local) ~(rank @ local) ~(singular_values @ local) (self @ local) ~(b @ local) ~rcond ~driver =
  let out__ = CArray.make raw_tensor 4 in
  stubs_linalg_lstsq_out (CArray.start out__) (globalize_gc_tensor solution) (globalize_gc_tensor residuals) (globalize_gc_tensor rank) (globalize_gc_tensor singular_values) (globalize_gc_tensor self) (globalize_gc_tensor b)  (Option.value rcond ~default:0.0) (match rcond with | Some _ -> 0 | None -> 1)   (Option.value driver ~default:"") (match driver with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let linalg_lu ~(a @ local) ~pivot =
  let out__ = CArray.make raw_tensor 3 in
  stubs_linalg_lu (CArray.start out__) (globalize_gc_tensor a) (if pivot then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let linalg_lu_factor ~(a @ local) ~pivot =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_lu_factor (CArray.start out__) (globalize_gc_tensor a) (if pivot then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_lu_factor_ex ~(a @ local) ~pivot ~check_errors =
  let out__ = CArray.make raw_tensor 3 in
  stubs_linalg_lu_factor_ex (CArray.start out__) (globalize_gc_tensor a) (if pivot then 1 else 0) (if check_errors then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let linalg_lu_factor_ex_out ~(lu @ local) ~(pivots @ local) ~(info @ local) ~(a @ local) ~pivot ~check_errors =
  let out__ = CArray.make raw_tensor 3 in
  stubs_linalg_lu_factor_ex_out (CArray.start out__) (globalize_gc_tensor lu) (globalize_gc_tensor pivots) (globalize_gc_tensor info) (globalize_gc_tensor a) (if pivot then 1 else 0) (if check_errors then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let linalg_lu_factor_out ~(lu @ local) ~(pivots @ local) ~(a @ local) ~pivot =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_lu_factor_out (CArray.start out__) (globalize_gc_tensor lu) (globalize_gc_tensor pivots) (globalize_gc_tensor a) (if pivot then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_lu_out ~(p @ local) ~(l @ local) ~(u @ local) ~(a @ local) ~pivot =
  let out__ = CArray.make raw_tensor 3 in
  stubs_linalg_lu_out (CArray.start out__) (globalize_gc_tensor p) (globalize_gc_tensor l) (globalize_gc_tensor u) (globalize_gc_tensor a) (if pivot then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let linalg_lu_solve ~(lu @ local) ~(pivots @ local) ~(b @ local) ~left ~adjoint =
  stubs_linalg_lu_solve (globalize_gc_tensor lu) (globalize_gc_tensor pivots) (globalize_gc_tensor b) (if left then 1 else 0) (if adjoint then 1 else 0) |> with_tensor_gc

let linalg_lu_solve_out ~(out @ local) ~(lu @ local) ~(pivots @ local) ~(b @ local) ~left ~adjoint =
  stubs_linalg_lu_solve_out (globalize_gc_tensor out) (globalize_gc_tensor lu) (globalize_gc_tensor pivots) (globalize_gc_tensor b) (if left then 1 else 0) (if adjoint then 1 else 0) |> with_tensor_gc

let linalg_matmul (self @ local) (other @ local) =
  stubs_linalg_matmul (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let linalg_matmul_out ~(out @ local) (self @ local) (other @ local) =
  stubs_linalg_matmul_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let linalg_matrix_exp (self @ local) =
  stubs_linalg_matrix_exp (globalize_gc_tensor self) |> with_tensor_gc

let linalg_matrix_exp_out ~(out @ local) (self @ local) =
  stubs_linalg_matrix_exp_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let linalg_matrix_power (self @ local) ~n =
  stubs_linalg_matrix_power (globalize_gc_tensor self) (Int64.of_int n) |> with_tensor_gc

let linalg_matrix_power_out ~(out @ local) (self @ local) ~n =
  stubs_linalg_matrix_power_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int n) |> with_tensor_gc

let linalg_matrix_rank (self @ local) ~tol ~hermitian =
  stubs_linalg_matrix_rank (globalize_gc_tensor self) tol (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_matrix_rank_atol_rtol_float (self @ local) ~atol ~rtol ~hermitian =
  stubs_linalg_matrix_rank_atol_rtol_float (globalize_gc_tensor self)  (Option.value atol ~default:0.0) (match atol with | Some _ -> 0 | None -> 1)   (Option.value rtol ~default:0.0) (match rtol with | Some _ -> 0 | None -> 1)  (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_matrix_rank_atol_rtol_float_out ~(out @ local) (self @ local) ~atol ~rtol ~hermitian =
  stubs_linalg_matrix_rank_atol_rtol_float_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (Option.value atol ~default:0.0) (match atol with | Some _ -> 0 | None -> 1)   (Option.value rtol ~default:0.0) (match rtol with | Some _ -> 0 | None -> 1)  (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_matrix_rank_atol_rtol_tensor (input @ local) ~(atol @ local) ~(rtol @ local) ~hermitian =
  stubs_linalg_matrix_rank_atol_rtol_tensor (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor atol) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor rtol) with | Some v -> v | None -> none_gc_tensor) (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_matrix_rank_atol_rtol_tensor_out ~(out @ local) (input @ local) ~(atol @ local) ~(rtol @ local) ~hermitian =
  stubs_linalg_matrix_rank_atol_rtol_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor atol) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor rtol) with | Some v -> v | None -> none_gc_tensor) (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_matrix_rank_out ~(out @ local) (self @ local) ~tol ~hermitian =
  stubs_linalg_matrix_rank_out (globalize_gc_tensor out) (globalize_gc_tensor self) tol (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_matrix_rank_out_tol_tensor ~(out @ local) (input @ local) ~(tol @ local) ~hermitian =
  stubs_linalg_matrix_rank_out_tol_tensor (globalize_gc_tensor out) (globalize_gc_tensor input) (globalize_gc_tensor tol) (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_matrix_rank_tol_tensor (input @ local) ~(tol @ local) ~hermitian =
  stubs_linalg_matrix_rank_tol_tensor (globalize_gc_tensor input) (globalize_gc_tensor tol) (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_multi_dot (tensors @ local) =
  stubs_linalg_multi_dot (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> with_tensor_gc

let linalg_multi_dot_out ~(out @ local) (tensors @ local) =
  stubs_linalg_multi_dot_out (globalize_gc_tensor out) (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> with_tensor_gc

let linalg_pinv (self @ local) ~rcond ~hermitian =
  stubs_linalg_pinv (globalize_gc_tensor self) rcond (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_pinv_atol_rtol_float (self @ local) ~atol ~rtol ~hermitian =
  stubs_linalg_pinv_atol_rtol_float (globalize_gc_tensor self)  (Option.value atol ~default:0.0) (match atol with | Some _ -> 0 | None -> 1)   (Option.value rtol ~default:0.0) (match rtol with | Some _ -> 0 | None -> 1)  (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_pinv_atol_rtol_float_out ~(out @ local) (self @ local) ~atol ~rtol ~hermitian =
  stubs_linalg_pinv_atol_rtol_float_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (Option.value atol ~default:0.0) (match atol with | Some _ -> 0 | None -> 1)   (Option.value rtol ~default:0.0) (match rtol with | Some _ -> 0 | None -> 1)  (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_pinv_atol_rtol_tensor (self @ local) ~(atol @ local) ~(rtol @ local) ~hermitian =
  stubs_linalg_pinv_atol_rtol_tensor (globalize_gc_tensor self) (match (Base.Option.globalize globalize_gc_tensor atol) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor rtol) with | Some v -> v | None -> none_gc_tensor) (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_pinv_atol_rtol_tensor_out ~(out @ local) (self @ local) ~(atol @ local) ~(rtol @ local) ~hermitian =
  stubs_linalg_pinv_atol_rtol_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match (Base.Option.globalize globalize_gc_tensor atol) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor rtol) with | Some v -> v | None -> none_gc_tensor) (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_pinv_out ~(out @ local) (self @ local) ~rcond ~hermitian =
  stubs_linalg_pinv_out (globalize_gc_tensor out) (globalize_gc_tensor self) rcond (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_pinv_out_rcond_tensor ~(out @ local) (self @ local) ~(rcond @ local) ~hermitian =
  stubs_linalg_pinv_out_rcond_tensor (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor rcond) (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_pinv_rcond_tensor (self @ local) ~(rcond @ local) ~hermitian =
  stubs_linalg_pinv_rcond_tensor (globalize_gc_tensor self) (globalize_gc_tensor rcond) (if hermitian then 1 else 0) |> with_tensor_gc

let linalg_qr ~(a @ local) ~mode =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_qr (CArray.start out__) (globalize_gc_tensor a) mode;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_qr_out ~(q @ local) ~(r @ local) ~(a @ local) ~mode =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_qr_out (CArray.start out__) (globalize_gc_tensor q) (globalize_gc_tensor r) (globalize_gc_tensor a) mode;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_slogdet ~(a @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_slogdet (CArray.start out__) (globalize_gc_tensor a);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_slogdet_out ~(sign @ local) ~(logabsdet @ local) ~(a @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_slogdet_out (CArray.start out__) (globalize_gc_tensor sign) (globalize_gc_tensor logabsdet) (globalize_gc_tensor a);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_solve ~(a @ local) ~(b @ local) ~left =
  stubs_linalg_solve (globalize_gc_tensor a) (globalize_gc_tensor b) (if left then 1 else 0) |> with_tensor_gc

let linalg_solve_ex ~(a @ local) ~(b @ local) ~left ~check_errors =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_solve_ex (CArray.start out__) (globalize_gc_tensor a) (globalize_gc_tensor b) (if left then 1 else 0) (if check_errors then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_solve_ex_out (result @ local) ~(info @ local) ~(a @ local) ~(b @ local) ~left ~check_errors =
  let out__ = CArray.make raw_tensor 2 in
  stubs_linalg_solve_ex_out (CArray.start out__) (globalize_gc_tensor result) (globalize_gc_tensor info) (globalize_gc_tensor a) (globalize_gc_tensor b) (if left then 1 else 0) (if check_errors then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let linalg_solve_out ~(out @ local) ~(a @ local) ~(b @ local) ~left =
  stubs_linalg_solve_out (globalize_gc_tensor out) (globalize_gc_tensor a) (globalize_gc_tensor b) (if left then 1 else 0) |> with_tensor_gc

let linalg_solve_triangular (self @ local) ~(b @ local) ~upper ~left ~unitriangular =
  stubs_linalg_solve_triangular (globalize_gc_tensor self) (globalize_gc_tensor b) (if upper then 1 else 0) (if left then 1 else 0) (if unitriangular then 1 else 0) |> with_tensor_gc

let linalg_solve_triangular_out ~(out @ local) (self @ local) ~(b @ local) ~upper ~left ~unitriangular =
  stubs_linalg_solve_triangular_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor b) (if upper then 1 else 0) (if left then 1 else 0) (if unitriangular then 1 else 0) |> with_tensor_gc

let linalg_svd ~(a @ local) ~full_matrices ~driver =
  let out__ = CArray.make raw_tensor 3 in
  stubs_linalg_svd (CArray.start out__) (globalize_gc_tensor a) (if full_matrices then 1 else 0)  (Option.value driver ~default:"") (match driver with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let linalg_svd_u ~(u @ local) ~(s @ local) ~(vh @ local) ~(a @ local) ~full_matrices ~driver =
  let out__ = CArray.make raw_tensor 3 in
  stubs_linalg_svd_u (CArray.start out__) (globalize_gc_tensor u) (globalize_gc_tensor s) (globalize_gc_tensor vh) (globalize_gc_tensor a) (if full_matrices then 1 else 0)  (Option.value driver ~default:"") (match driver with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let linalg_svdvals ~(a @ local) ~driver =
  stubs_linalg_svdvals (globalize_gc_tensor a)  (Option.value driver ~default:"") (match driver with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let linalg_svdvals_out ~(out @ local) ~(a @ local) ~driver =
  stubs_linalg_svdvals_out (globalize_gc_tensor out) (globalize_gc_tensor a)  (Option.value driver ~default:"") (match driver with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let linalg_tensorinv (self @ local) ~ind =
  stubs_linalg_tensorinv (globalize_gc_tensor self) (Int64.of_int ind) |> with_tensor_gc

let linalg_tensorinv_out ~(out @ local) (self @ local) ~ind =
  stubs_linalg_tensorinv_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int ind) |> with_tensor_gc

let linalg_tensorsolve (self @ local) (other @ local) ~dims =
  stubs_linalg_tensorsolve (globalize_gc_tensor self) (globalize_gc_tensor other) (match dims with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dims with | None -> -1 | Some v -> List.length v) |> with_tensor_gc

let linalg_tensorsolve_out ~(out @ local) (self @ local) (other @ local) ~dims =
  stubs_linalg_tensorsolve_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) (match dims with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dims with | None -> -1 | Some v -> List.length v) |> with_tensor_gc

let linalg_vander ~(x @ local) ~n =
  stubs_linalg_vander (globalize_gc_tensor x)  (match n with | None -> Int64.zero | Some v -> Int64.of_int v) (match n with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let linalg_vecdot ~(x @ local) ~(y @ local) ~dim =
  stubs_linalg_vecdot (globalize_gc_tensor x) (globalize_gc_tensor y) (Int64.of_int dim) |> with_tensor_gc

let linalg_vecdot_out ~(out @ local) ~(x @ local) ~(y @ local) ~dim =
  stubs_linalg_vecdot_out (globalize_gc_tensor out) (globalize_gc_tensor x) (globalize_gc_tensor y) (Int64.of_int dim) |> with_tensor_gc

let linear (input @ local) ~(weight @ local) ~(bias @ local) =
  stubs_linear (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let linear_out ~(out @ local) (input @ local) ~(weight @ local) ~(bias @ local) =
  stubs_linear_out (globalize_gc_tensor out) (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let linspace ~start ~end_ ~steps ~options =
  stubs_linspace start end_ (Int64.of_int steps) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let linspace_out ~(out @ local) ~start ~end_ ~steps =
  stubs_linspace_out (globalize_gc_tensor out) start end_ (Int64.of_int steps) |> with_tensor_gc

let linspace_scalar_tensor ~start ~(end_ @ local) ~steps ~options =
  stubs_linspace_scalar_tensor start (globalize_gc_tensor end_) (Int64.of_int steps) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let linspace_scalar_tensor_out ~(out @ local) ~start ~(end_ @ local) ~steps =
  stubs_linspace_scalar_tensor_out (globalize_gc_tensor out) start (globalize_gc_tensor end_) (Int64.of_int steps) |> with_tensor_gc

let linspace_tensor_scalar ~(start @ local) ~end_ ~steps ~options =
  stubs_linspace_tensor_scalar (globalize_gc_tensor start) end_ (Int64.of_int steps) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let linspace_tensor_scalar_out ~(out @ local) ~(start @ local) ~end_ ~steps =
  stubs_linspace_tensor_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor start) end_ (Int64.of_int steps) |> with_tensor_gc

let linspace_tensor_tensor ~(start @ local) ~(end_ @ local) ~steps ~options =
  stubs_linspace_tensor_tensor (globalize_gc_tensor start) (globalize_gc_tensor end_) (Int64.of_int steps) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let linspace_tensor_tensor_out ~(out @ local) ~(start @ local) ~(end_ @ local) ~steps =
  stubs_linspace_tensor_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor start) (globalize_gc_tensor end_) (Int64.of_int steps) |> with_tensor_gc

let log (self @ local) =
  stubs_log (globalize_gc_tensor self) |> with_tensor_gc

let log10 (self @ local) =
  stubs_log10 (globalize_gc_tensor self) |> with_tensor_gc

let log10_ (self @ local) =
  stubs_log10_ (globalize_gc_tensor self) |> with_tensor_gc

let log10_out ~(out @ local) (self @ local) =
  stubs_log10_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let log1p (self @ local) =
  stubs_log1p (globalize_gc_tensor self) |> with_tensor_gc

let log1p_ (self @ local) =
  stubs_log1p_ (globalize_gc_tensor self) |> with_tensor_gc

let log1p_out ~(out @ local) (self @ local) =
  stubs_log1p_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let log2 (self @ local) =
  stubs_log2 (globalize_gc_tensor self) |> with_tensor_gc

let log2_ (self @ local) =
  stubs_log2_ (globalize_gc_tensor self) |> with_tensor_gc

let log2_out ~(out @ local) (self @ local) =
  stubs_log2_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let log_ (self @ local) =
  stubs_log_ (globalize_gc_tensor self) |> with_tensor_gc

let log_normal (self @ local) ~mean ~std =
  stubs_log_normal (globalize_gc_tensor self) mean std |> with_tensor_gc

let log_normal_ (self @ local) ~mean ~std =
  stubs_log_normal_ (globalize_gc_tensor self) mean std |> with_tensor_gc

let log_normal_out ~(out @ local) (self @ local) ~mean ~std =
  stubs_log_normal_out (globalize_gc_tensor out) (globalize_gc_tensor self) mean std |> with_tensor_gc

let log_out ~(out @ local) (self @ local) =
  stubs_log_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let log_sigmoid (self @ local) =
  stubs_log_sigmoid (globalize_gc_tensor self) |> with_tensor_gc

let log_sigmoid_backward ~(grad_output @ local) (self @ local) ~(buffer @ local) =
  stubs_log_sigmoid_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor buffer) |> with_tensor_gc

let log_sigmoid_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~(buffer @ local) =
  stubs_log_sigmoid_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor buffer) |> with_tensor_gc

let log_sigmoid_out ~(out @ local) (self @ local) =
  stubs_log_sigmoid_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let log_softmax (self @ local) ~dim ~dtype =
  stubs_log_softmax (globalize_gc_tensor self) (Int64.of_int dim) (Kind.packed_to_int dtype) |> with_tensor_gc

let log_softmax_int_out ~(out @ local) (self @ local) ~dim ~dtype =
  stubs_log_softmax_int_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (Kind.packed_to_int dtype) |> with_tensor_gc

let logaddexp (self @ local) (other @ local) =
  stubs_logaddexp (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let logaddexp2 (self @ local) (other @ local) =
  stubs_logaddexp2 (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let logaddexp2_out ~(out @ local) (self @ local) (other @ local) =
  stubs_logaddexp2_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let logaddexp_out ~(out @ local) (self @ local) (other @ local) =
  stubs_logaddexp_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let logcumsumexp (self @ local) ~dim =
  stubs_logcumsumexp (globalize_gc_tensor self) (Int64.of_int dim) |> with_tensor_gc

let logcumsumexp_out ~(out @ local) (self @ local) ~dim =
  stubs_logcumsumexp_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) |> with_tensor_gc

let logdet (self @ local) =
  stubs_logdet (globalize_gc_tensor self) |> with_tensor_gc

let logical_and (self @ local) (other @ local) =
  stubs_logical_and (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let logical_and_ (self @ local) (other @ local) =
  stubs_logical_and_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let logical_and_out ~(out @ local) (self @ local) (other @ local) =
  stubs_logical_and_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let logical_not (self @ local) =
  stubs_logical_not (globalize_gc_tensor self) |> with_tensor_gc

let logical_not_ (self @ local) =
  stubs_logical_not_ (globalize_gc_tensor self) |> with_tensor_gc

let logical_not_out ~(out @ local) (self @ local) =
  stubs_logical_not_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let logical_or (self @ local) (other @ local) =
  stubs_logical_or (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let logical_or_ (self @ local) (other @ local) =
  stubs_logical_or_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let logical_or_out ~(out @ local) (self @ local) (other @ local) =
  stubs_logical_or_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let logical_xor (self @ local) (other @ local) =
  stubs_logical_xor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let logical_xor_ (self @ local) (other @ local) =
  stubs_logical_xor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let logical_xor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_logical_xor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let logit (self @ local) ~eps =
  stubs_logit (globalize_gc_tensor self)  (Option.value eps ~default:0.0) (match eps with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let logit_ (self @ local) ~eps =
  stubs_logit_ (globalize_gc_tensor self)  (Option.value eps ~default:0.0) (match eps with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let logit_backward ~(grad_output @ local) (self @ local) ~eps =
  stubs_logit_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self)  (Option.value eps ~default:0.0) (match eps with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let logit_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~eps =
  stubs_logit_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self)  (Option.value eps ~default:0.0) (match eps with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let logit_out ~(out @ local) (self @ local) ~eps =
  stubs_logit_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (Option.value eps ~default:0.0) (match eps with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let logspace ~start ~end_ ~steps ~base ~options =
  stubs_logspace start end_ (Int64.of_int steps) base (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let logspace_out ~(out @ local) ~start ~end_ ~steps ~base =
  stubs_logspace_out (globalize_gc_tensor out) start end_ (Int64.of_int steps) base |> with_tensor_gc

let logspace_scalar_tensor ~start ~(end_ @ local) ~steps ~base ~options =
  stubs_logspace_scalar_tensor start (globalize_gc_tensor end_) (Int64.of_int steps) base (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let logspace_scalar_tensor_out ~(out @ local) ~start ~(end_ @ local) ~steps ~base =
  stubs_logspace_scalar_tensor_out (globalize_gc_tensor out) start (globalize_gc_tensor end_) (Int64.of_int steps) base |> with_tensor_gc

let logspace_tensor_scalar ~(start @ local) ~end_ ~steps ~base ~options =
  stubs_logspace_tensor_scalar (globalize_gc_tensor start) end_ (Int64.of_int steps) base (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let logspace_tensor_scalar_out ~(out @ local) ~(start @ local) ~end_ ~steps ~base =
  stubs_logspace_tensor_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor start) end_ (Int64.of_int steps) base |> with_tensor_gc

let logspace_tensor_tensor ~(start @ local) ~(end_ @ local) ~steps ~base ~options =
  stubs_logspace_tensor_tensor (globalize_gc_tensor start) (globalize_gc_tensor end_) (Int64.of_int steps) base (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let logspace_tensor_tensor_out ~(out @ local) ~(start @ local) ~(end_ @ local) ~steps ~base =
  stubs_logspace_tensor_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor start) (globalize_gc_tensor end_) (Int64.of_int steps) base |> with_tensor_gc

let logsumexp (self @ local) ~dim ~keepdim =
  stubs_logsumexp (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) |> with_tensor_gc

let logsumexp_out ~(out @ local) (self @ local) ~dim ~keepdim =
  stubs_logsumexp_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) |> with_tensor_gc

let lstm (input @ local) ~(hx @ local) ~(params @ local) ~has_biases ~num_layers ~dropout ~train ~bidirectional ~batch_first =
  let out__ = CArray.make raw_tensor 3 in
  stubs_lstm (CArray.start out__) (globalize_gc_tensor input) (CArray.of_list gc_tensor (globalize_gc_tensor_list hx) |> CArray.start) (Base.List.length hx) (CArray.of_list gc_tensor (globalize_gc_tensor_list params) |> CArray.start) (Base.List.length params) (if has_biases then 1 else 0) (Int64.of_int num_layers) dropout (if train then 1 else 0) (if bidirectional then 1 else 0) (if batch_first then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let lstm_cell (input @ local) ~(hx @ local) ~(w_ih @ local) ~(w_hh @ local) ~(b_ih @ local) ~(b_hh @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_lstm_cell (CArray.start out__) (globalize_gc_tensor input) (CArray.of_list gc_tensor (globalize_gc_tensor_list hx) |> CArray.start) (Base.List.length hx) (globalize_gc_tensor w_ih) (globalize_gc_tensor w_hh) (match (Base.Option.globalize globalize_gc_tensor b_ih) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor b_hh) with | Some v -> v | None -> none_gc_tensor);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let lstm_data ~(data @ local) ~(batch_sizes @ local) ~(hx @ local) ~(params @ local) ~has_biases ~num_layers ~dropout ~train ~bidirectional =
  let out__ = CArray.make raw_tensor 3 in
  stubs_lstm_data (CArray.start out__) (globalize_gc_tensor data) (globalize_gc_tensor batch_sizes) (CArray.of_list gc_tensor (globalize_gc_tensor_list hx) |> CArray.start) (Base.List.length hx) (CArray.of_list gc_tensor (globalize_gc_tensor_list params) |> CArray.start) (Base.List.length params) (if has_biases then 1 else 0) (Int64.of_int num_layers) dropout (if train then 1 else 0) (if bidirectional then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let lstm_mps_backward ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) ~(grad_y @ local) ~(grad_hy @ local) ~(grad_cy @ local) ~(z_state @ local) ~(cell_state_fwd @ local) (input @ local) ~(layersoutputs @ local) ~(hx @ local) ~(params @ local) ~has_biases ~num_layers ~dropout ~train ~bidirectional ~batch_first =
  stubs_lstm_mps_backward (globalize_gc_tensor out0) (CArray.of_list gc_tensor (globalize_gc_tensor_list out1) |> CArray.start) (Base.List.length out1) (CArray.of_list gc_tensor (globalize_gc_tensor_list out2) |> CArray.start) (Base.List.length out2) (match (Base.Option.globalize globalize_gc_tensor grad_y) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor grad_hy) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor grad_cy) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor z_state) (globalize_gc_tensor cell_state_fwd) (globalize_gc_tensor input) (globalize_gc_tensor layersoutputs) (CArray.of_list gc_tensor (globalize_gc_tensor_list hx) |> CArray.start) (Base.List.length hx) (CArray.of_list gc_tensor (globalize_gc_tensor_list params) |> CArray.start) (Base.List.length params) (if has_biases then 1 else 0) (Int64.of_int num_layers) dropout (if train then 1 else 0) (if bidirectional then 1 else 0) (if batch_first then 1 else 0)

let lt (self @ local) other =
  stubs_lt (globalize_gc_tensor self) other |> with_tensor_gc

let lt_ (self @ local) other =
  stubs_lt_ (globalize_gc_tensor self) other |> with_tensor_gc

let lt_scalar_out ~(out @ local) (self @ local) other =
  stubs_lt_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let lt_tensor (self @ local) (other @ local) =
  stubs_lt_tensor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let lt_tensor_ (self @ local) (other @ local) =
  stubs_lt_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let lt_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_lt_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let lu_solve (self @ local) ~(lu_data @ local) ~(lu_pivots @ local) =
  stubs_lu_solve (globalize_gc_tensor self) (globalize_gc_tensor lu_data) (globalize_gc_tensor lu_pivots) |> with_tensor_gc

let lu_solve_out ~(out @ local) (self @ local) ~(lu_data @ local) ~(lu_pivots @ local) =
  stubs_lu_solve_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor lu_data) (globalize_gc_tensor lu_pivots) |> with_tensor_gc

let lu_unpack ~(lu_data @ local) ~(lu_pivots @ local) ~unpack_data ~unpack_pivots =
  let out__ = CArray.make raw_tensor 3 in
  stubs_lu_unpack (CArray.start out__) (globalize_gc_tensor lu_data) (globalize_gc_tensor lu_pivots) (if unpack_data then 1 else 0) (if unpack_pivots then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let lu_unpack_out ~(p @ local) ~(l @ local) ~(u @ local) ~(lu_data @ local) ~(lu_pivots @ local) ~unpack_data ~unpack_pivots =
  let out__ = CArray.make raw_tensor 3 in
  stubs_lu_unpack_out (CArray.start out__) (globalize_gc_tensor p) (globalize_gc_tensor l) (globalize_gc_tensor u) (globalize_gc_tensor lu_data) (globalize_gc_tensor lu_pivots) (if unpack_data then 1 else 0) (if unpack_pivots then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let margin_ranking_loss ~(input1 @ local) ~(input2 @ local) ~(target @ local) ~margin ~reduction =
  stubs_margin_ranking_loss (globalize_gc_tensor input1) (globalize_gc_tensor input2) (globalize_gc_tensor target) margin (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let masked_fill (self @ local) ~(mask @ local) ~value =
  stubs_masked_fill (globalize_gc_tensor self) (globalize_gc_tensor mask) value |> with_tensor_gc

let masked_fill_ (self @ local) ~(mask @ local) ~value =
  stubs_masked_fill_ (globalize_gc_tensor self) (globalize_gc_tensor mask) value |> with_tensor_gc

let masked_fill_scalar_out ~(out @ local) (self @ local) ~(mask @ local) ~value =
  stubs_masked_fill_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mask) value |> with_tensor_gc

let masked_fill_tensor (self @ local) ~(mask @ local) ~(value @ local) =
  stubs_masked_fill_tensor (globalize_gc_tensor self) (globalize_gc_tensor mask) (globalize_gc_tensor value) |> with_tensor_gc

let masked_fill_tensor_ (self @ local) ~(mask @ local) ~(value @ local) =
  stubs_masked_fill_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor mask) (globalize_gc_tensor value) |> with_tensor_gc

let masked_fill_tensor_out ~(out @ local) (self @ local) ~(mask @ local) ~(value @ local) =
  stubs_masked_fill_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mask) (globalize_gc_tensor value) |> with_tensor_gc

let masked_scatter (self @ local) ~(mask @ local) ~(source @ local) =
  stubs_masked_scatter (globalize_gc_tensor self) (globalize_gc_tensor mask) (globalize_gc_tensor source) |> with_tensor_gc

let masked_scatter_ (self @ local) ~(mask @ local) ~(source @ local) =
  stubs_masked_scatter_ (globalize_gc_tensor self) (globalize_gc_tensor mask) (globalize_gc_tensor source) |> with_tensor_gc

let masked_scatter_backward ~(grad_output @ local) ~(mask @ local) ~sizes =
  stubs_masked_scatter_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor mask) (List.map Int64.of_int sizes |> CArray.of_list int64_t |> CArray.start) (List.length sizes) |> with_tensor_gc

let masked_scatter_out ~(out @ local) (self @ local) ~(mask @ local) ~(source @ local) =
  stubs_masked_scatter_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mask) (globalize_gc_tensor source) |> with_tensor_gc

let masked_select (self @ local) ~(mask @ local) =
  stubs_masked_select (globalize_gc_tensor self) (globalize_gc_tensor mask) |> with_tensor_gc

let masked_select_backward ~(grad @ local) (input @ local) ~(mask @ local) =
  stubs_masked_select_backward (globalize_gc_tensor grad) (globalize_gc_tensor input) (globalize_gc_tensor mask) |> with_tensor_gc

let masked_select_out ~(out @ local) (self @ local) ~(mask @ local) =
  stubs_masked_select_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mask) |> with_tensor_gc

let matmul (self @ local) (other @ local) =
  stubs_matmul (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let matmul_out ~(out @ local) (self @ local) (other @ local) =
  stubs_matmul_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let matrix_exp (self @ local) =
  stubs_matrix_exp (globalize_gc_tensor self) |> with_tensor_gc

let matrix_exp_backward (self @ local) ~(grad @ local) =
  stubs_matrix_exp_backward (globalize_gc_tensor self) (globalize_gc_tensor grad) |> with_tensor_gc

let matrix_h (self @ local) =
  stubs_matrix_h (globalize_gc_tensor self) |> with_tensor_gc

let matrix_power (self @ local) ~n =
  stubs_matrix_power (globalize_gc_tensor self) (Int64.of_int n) |> with_tensor_gc

let matrix_power_out ~(out @ local) (self @ local) ~n =
  stubs_matrix_power_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int n) |> with_tensor_gc

let max (self @ local) =
  stubs_max (globalize_gc_tensor self) |> with_tensor_gc

let max_dim (self @ local) ~dim ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_max_dim (CArray.start out__) (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let max_dim_max ~(max @ local) ~(max_values @ local) (self @ local) ~dim ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_max_dim_max (CArray.start out__) (globalize_gc_tensor max) (globalize_gc_tensor max_values) (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let max_other (self @ local) (other @ local) =
  stubs_max_other (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let max_out ~(out @ local) (self @ local) (other @ local) =
  stubs_max_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let max_pool1d (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_max_pool1d (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let max_pool1d_with_indices (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  let out__ = CArray.make raw_tensor 2 in
  stubs_max_pool1d_with_indices (CArray.start out__) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let max_pool2d (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_max_pool2d (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let max_pool2d_backward ~(grad_output @ local) (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_max_pool2d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let max_pool2d_backward_out ~(out @ local) ~(grad_output @ local) (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_max_pool2d_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let max_pool2d_with_indices (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  let out__ = CArray.make raw_tensor 2 in
  stubs_max_pool2d_with_indices (CArray.start out__) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let max_pool2d_with_indices_backward ~(grad_output @ local) (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode ~(indices @ local) =
  stubs_max_pool2d_with_indices_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) (globalize_gc_tensor indices) |> with_tensor_gc

let max_pool2d_with_indices_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode ~(indices @ local) =
  stubs_max_pool2d_with_indices_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) (globalize_gc_tensor indices) |> with_tensor_gc

let max_pool2d_with_indices_out ~(out @ local) ~(indices @ local) (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  let out__ = CArray.make raw_tensor 2 in
  stubs_max_pool2d_with_indices_out (CArray.start out__) (globalize_gc_tensor out) (globalize_gc_tensor indices) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let max_pool3d (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_max_pool3d (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let max_pool3d_with_indices (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  let out__ = CArray.make raw_tensor 2 in
  stubs_max_pool3d_with_indices (CArray.start out__) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let max_pool3d_with_indices_backward ~(grad_output @ local) (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode ~(indices @ local) =
  stubs_max_pool3d_with_indices_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) (globalize_gc_tensor indices) |> with_tensor_gc

let max_pool3d_with_indices_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode ~(indices @ local) =
  stubs_max_pool3d_with_indices_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) (globalize_gc_tensor indices) |> with_tensor_gc

let max_pool3d_with_indices_out ~(out @ local) ~(indices @ local) (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  let out__ = CArray.make raw_tensor 2 in
  stubs_max_pool3d_with_indices_out (CArray.start out__) (globalize_gc_tensor out) (globalize_gc_tensor indices) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let max_unary_out ~(out @ local) (self @ local) =
  stubs_max_unary_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let max_unpool2d (self @ local) ~(indices @ local) ~output_size =
  stubs_max_unpool2d (globalize_gc_tensor self) (globalize_gc_tensor indices) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) |> with_tensor_gc

let max_unpool2d_out ~(out @ local) (self @ local) ~(indices @ local) ~output_size =
  stubs_max_unpool2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor indices) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) |> with_tensor_gc

let max_unpool3d (self @ local) ~(indices @ local) ~output_size ~stride ~padding =
  stubs_max_unpool3d (globalize_gc_tensor self) (globalize_gc_tensor indices) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let max_unpool3d_out ~(out @ local) (self @ local) ~(indices @ local) ~output_size ~stride ~padding =
  stubs_max_unpool3d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor indices) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let maximum (self @ local) (other @ local) =
  stubs_maximum (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let maximum_out ~(out @ local) (self @ local) (other @ local) =
  stubs_maximum_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let mean (self @ local) ~dtype =
  stubs_mean (globalize_gc_tensor self) (Kind.packed_to_int dtype) |> with_tensor_gc

let mean_dim (self @ local) ~dim ~keepdim ~dtype =
  stubs_mean_dim (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let mean_dtype_out ~(out @ local) (self @ local) ~dtype =
  stubs_mean_dtype_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Kind.packed_to_int dtype) |> with_tensor_gc

let mean_out ~(out @ local) (self @ local) ~dim ~keepdim ~dtype =
  stubs_mean_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let median (self @ local) =
  stubs_median (globalize_gc_tensor self) |> with_tensor_gc

let median_dim (self @ local) ~dim ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_median_dim (CArray.start out__) (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let median_dim_values ~(values @ local) ~(indices @ local) (self @ local) ~dim ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_median_dim_values (CArray.start out__) (globalize_gc_tensor values) (globalize_gc_tensor indices) (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let median_out ~(out @ local) (self @ local) =
  stubs_median_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let meshgrid (tensors @ local) =
  stubs_meshgrid (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> to_tensor_list

let meshgrid_indexing (tensors @ local) ~indexing =
  stubs_meshgrid_indexing (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) indexing |> to_tensor_list

let mh (self @ local) =
  stubs_mh (globalize_gc_tensor self) |> with_tensor_gc

let min (self @ local) =
  stubs_min (globalize_gc_tensor self) |> with_tensor_gc

let min_dim (self @ local) ~dim ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_min_dim (CArray.start out__) (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let min_dim_min ~(min @ local) ~(min_indices @ local) (self @ local) ~dim ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_min_dim_min (CArray.start out__) (globalize_gc_tensor min) (globalize_gc_tensor min_indices) (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let min_other (self @ local) (other @ local) =
  stubs_min_other (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let min_out ~(out @ local) (self @ local) (other @ local) =
  stubs_min_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let min_unary_out ~(out @ local) (self @ local) =
  stubs_min_unary_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let minimum (self @ local) (other @ local) =
  stubs_minimum (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let minimum_out ~(out @ local) (self @ local) (other @ local) =
  stubs_minimum_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let miopen_batch_norm (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~training ~exponential_average_factor ~epsilon =
  let out__ = CArray.make raw_tensor 3 in
  stubs_miopen_batch_norm (CArray.start out__) (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) (if training then 1 else 0) exponential_average_factor epsilon;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let miopen_batch_norm_backward (input @ local) ~(grad_output @ local) ~(weight @ local) ~(running_mean @ local) ~(running_var @ local) ~(save_mean @ local) ~(save_var @ local) ~epsilon =
  let out__ = CArray.make raw_tensor 3 in
  stubs_miopen_batch_norm_backward (CArray.start out__) (globalize_gc_tensor input) (globalize_gc_tensor grad_output) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor save_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor save_var) with | Some v -> v | None -> none_gc_tensor) epsilon;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let miopen_batch_norm_backward_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) (input @ local) ~(grad_output @ local) ~(weight @ local) ~(running_mean @ local) ~(running_var @ local) ~(save_mean @ local) ~(save_var @ local) ~epsilon =
  let out__ = CArray.make raw_tensor 3 in
  stubs_miopen_batch_norm_backward_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor input) (globalize_gc_tensor grad_output) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor save_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor save_var) with | Some v -> v | None -> none_gc_tensor) epsilon;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let miopen_batch_norm_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~training ~exponential_average_factor ~epsilon =
  let out__ = CArray.make raw_tensor 3 in
  stubs_miopen_batch_norm_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor input) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) (if training then 1 else 0) exponential_average_factor epsilon;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let miopen_convolution (self @ local) ~(weight @ local) ~(bias @ local) ~padding ~stride ~dilation ~groups ~benchmark ~deterministic =
  stubs_miopen_convolution (globalize_gc_tensor self) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) (if benchmark then 1 else 0) (if deterministic then 1 else 0) |> with_tensor_gc

let miopen_convolution_add_relu (self @ local) ~(weight @ local) ~(z @ local) ~alpha ~(bias @ local) ~stride ~padding ~dilation ~groups =
  stubs_miopen_convolution_add_relu (globalize_gc_tensor self) (globalize_gc_tensor weight) (globalize_gc_tensor z) alpha (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let miopen_convolution_out ~(out @ local) (self @ local) ~(weight @ local) ~(bias @ local) ~padding ~stride ~dilation ~groups ~benchmark ~deterministic =
  stubs_miopen_convolution_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) (if benchmark then 1 else 0) (if deterministic then 1 else 0) |> with_tensor_gc

let miopen_convolution_relu (self @ local) ~(weight @ local) ~(bias @ local) ~stride ~padding ~dilation ~groups =
  stubs_miopen_convolution_relu (globalize_gc_tensor self) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let miopen_convolution_transpose (self @ local) ~(weight @ local) ~(bias @ local) ~padding ~output_padding ~stride ~dilation ~groups ~benchmark ~deterministic =
  stubs_miopen_convolution_transpose (globalize_gc_tensor self) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) (if benchmark then 1 else 0) (if deterministic then 1 else 0) |> with_tensor_gc

let miopen_convolution_transpose_out ~(out @ local) (self @ local) ~(weight @ local) ~(bias @ local) ~padding ~output_padding ~stride ~dilation ~groups ~benchmark ~deterministic =
  stubs_miopen_convolution_transpose_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) (if benchmark then 1 else 0) (if deterministic then 1 else 0) |> with_tensor_gc

let miopen_ctc_loss ~(log_probs @ local) ~(targets @ local) ~input_lengths ~target_lengths ~blank ~deterministic ~zero_infinity =
  let out__ = CArray.make raw_tensor 2 in
  stubs_miopen_ctc_loss (CArray.start out__) (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (List.map Int64.of_int input_lengths |> CArray.of_list int64_t |> CArray.start) (List.length input_lengths) (List.map Int64.of_int target_lengths |> CArray.of_list int64_t |> CArray.start) (List.length target_lengths) (Int64.of_int blank) (if deterministic then 1 else 0) (if zero_infinity then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let miopen_ctc_loss_out ~(out0 @ local) ~(out1 @ local) ~(log_probs @ local) ~(targets @ local) ~input_lengths ~target_lengths ~blank ~deterministic ~zero_infinity =
  let out__ = CArray.make raw_tensor 2 in
  stubs_miopen_ctc_loss_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (List.map Int64.of_int input_lengths |> CArray.of_list int64_t |> CArray.start) (List.length input_lengths) (List.map Int64.of_int target_lengths |> CArray.of_list int64_t |> CArray.start) (List.length target_lengths) (Int64.of_int blank) (if deterministic then 1 else 0) (if zero_infinity then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let miopen_ctc_loss_tensor ~(log_probs @ local) ~(targets @ local) ~(input_lengths @ local) ~(target_lengths @ local) ~blank ~deterministic ~zero_infinity =
  let out__ = CArray.make raw_tensor 2 in
  stubs_miopen_ctc_loss_tensor (CArray.start out__) (globalize_gc_tensor log_probs) (globalize_gc_tensor targets) (globalize_gc_tensor input_lengths) (globalize_gc_tensor target_lengths) (Int64.of_int blank) (if deterministic then 1 else 0) (if zero_infinity then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let miopen_depthwise_convolution (self @ local) ~(weight @ local) ~(bias @ local) ~padding ~stride ~dilation ~groups ~benchmark ~deterministic =
  stubs_miopen_depthwise_convolution (globalize_gc_tensor self) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) (if benchmark then 1 else 0) (if deterministic then 1 else 0) |> with_tensor_gc

let miopen_depthwise_convolution_out ~(out @ local) (self @ local) ~(weight @ local) ~(bias @ local) ~padding ~stride ~dilation ~groups ~benchmark ~deterministic =
  stubs_miopen_depthwise_convolution_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) (if benchmark then 1 else 0) (if deterministic then 1 else 0) |> with_tensor_gc

let miopen_rnn (input @ local) ~(weight @ local) ~weight_stride0 ~(hx @ local) ~(cx @ local) ~mode ~hidden_size ~num_layers ~batch_first ~dropout ~train ~bidirectional ~batch_sizes ~(dropout_state @ local) =
  let out__ = CArray.make raw_tensor 5 in
  stubs_miopen_rnn (CArray.start out__) (globalize_gc_tensor input) (CArray.of_list gc_tensor (globalize_gc_tensor_list weight) |> CArray.start) (Base.List.length weight) (Int64.of_int weight_stride0) (globalize_gc_tensor hx) (match (Base.Option.globalize globalize_gc_tensor cx) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int mode) (Int64.of_int hidden_size) (Int64.of_int num_layers) (if batch_first then 1 else 0) dropout (if train then 1 else 0) (if bidirectional then 1 else 0) (List.map Int64.of_int batch_sizes |> CArray.of_list int64_t |> CArray.start) (List.length batch_sizes) (match (Base.Option.globalize globalize_gc_tensor dropout_state) with | Some v -> v | None -> none_gc_tensor);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  t0, t1, t2, t3, t4

let miopen_rnn_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) ~(out3 @ local) ~(out4 @ local) (input @ local) ~(weight @ local) ~weight_stride0 ~(hx @ local) ~(cx @ local) ~mode ~hidden_size ~num_layers ~batch_first ~dropout ~train ~bidirectional ~batch_sizes ~(dropout_state @ local) =
  let out__ = CArray.make raw_tensor 5 in
  stubs_miopen_rnn_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor out3) (globalize_gc_tensor out4) (globalize_gc_tensor input) (CArray.of_list gc_tensor (globalize_gc_tensor_list weight) |> CArray.start) (Base.List.length weight) (Int64.of_int weight_stride0) (globalize_gc_tensor hx) (match (Base.Option.globalize globalize_gc_tensor cx) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int mode) (Int64.of_int hidden_size) (Int64.of_int num_layers) (if batch_first then 1 else 0) dropout (if train then 1 else 0) (if bidirectional then 1 else 0) (List.map Int64.of_int batch_sizes |> CArray.of_list int64_t |> CArray.start) (List.length batch_sizes) (match (Base.Option.globalize globalize_gc_tensor dropout_state) with | Some v -> v | None -> none_gc_tensor);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  t0, t1, t2, t3, t4

let mish (self @ local) =
  stubs_mish (globalize_gc_tensor self) |> with_tensor_gc

let mish_ (self @ local) =
  stubs_mish_ (globalize_gc_tensor self) |> with_tensor_gc

let mish_backward ~(grad_output @ local) (self @ local) =
  stubs_mish_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) |> with_tensor_gc

let mish_out ~(out @ local) (self @ local) =
  stubs_mish_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let mkldnn_adaptive_avg_pool2d (self @ local) ~output_size =
  stubs_mkldnn_adaptive_avg_pool2d (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) |> with_tensor_gc

let mkldnn_adaptive_avg_pool2d_backward ~(grad_output @ local) (self @ local) =
  stubs_mkldnn_adaptive_avg_pool2d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) |> with_tensor_gc

let mkldnn_adaptive_avg_pool2d_backward_out ~(out @ local) ~(grad_output @ local) (self @ local) =
  stubs_mkldnn_adaptive_avg_pool2d_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) |> with_tensor_gc

let mkldnn_adaptive_avg_pool2d_out ~(out @ local) (self @ local) ~output_size =
  stubs_mkldnn_adaptive_avg_pool2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) |> with_tensor_gc

let mkldnn_convolution (self @ local) ~(weight @ local) ~(bias @ local) ~padding ~stride ~dilation ~groups =
  stubs_mkldnn_convolution (globalize_gc_tensor self) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let mkldnn_convolution_out ~(out @ local) (self @ local) ~(weight @ local) ~(bias @ local) ~padding ~stride ~dilation ~groups =
  stubs_mkldnn_convolution_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) |> with_tensor_gc

let mkldnn_linear (self @ local) ~(weight @ local) ~(bias @ local) =
  stubs_mkldnn_linear (globalize_gc_tensor self) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let mkldnn_linear_backward_input ~input_size ~(grad_output @ local) ~(weight @ local) =
  stubs_mkldnn_linear_backward_input (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (globalize_gc_tensor grad_output) (globalize_gc_tensor weight) |> with_tensor_gc

let mkldnn_linear_backward_input_out ~(out @ local) ~input_size ~(grad_output @ local) ~(weight @ local) =
  stubs_mkldnn_linear_backward_input_out (globalize_gc_tensor out) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (globalize_gc_tensor grad_output) (globalize_gc_tensor weight) |> with_tensor_gc

let mkldnn_linear_backward_weights ~(grad_output @ local) (input @ local) ~(weight @ local) ~bias_defined =
  let out__ = CArray.make raw_tensor 2 in
  stubs_mkldnn_linear_backward_weights (CArray.start out__) (globalize_gc_tensor grad_output) (globalize_gc_tensor input) (globalize_gc_tensor weight) (if bias_defined then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let mkldnn_linear_backward_weights_out ~(out0 @ local) ~(out1 @ local) ~(grad_output @ local) (input @ local) ~(weight @ local) ~bias_defined =
  let out__ = CArray.make raw_tensor 2 in
  stubs_mkldnn_linear_backward_weights_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor grad_output) (globalize_gc_tensor input) (globalize_gc_tensor weight) (if bias_defined then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let mkldnn_linear_out ~(out @ local) (self @ local) ~(weight @ local) ~(bias @ local) =
  stubs_mkldnn_linear_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let mkldnn_max_pool2d (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_mkldnn_max_pool2d (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let mkldnn_max_pool2d_backward ~(grad_output @ local) ~(output @ local) (input @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_mkldnn_max_pool2d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor output) (globalize_gc_tensor input) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let mkldnn_max_pool2d_backward_out ~(out @ local) ~(grad_output @ local) ~(output @ local) (input @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_mkldnn_max_pool2d_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (globalize_gc_tensor output) (globalize_gc_tensor input) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let mkldnn_max_pool2d_out ~(out @ local) (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_mkldnn_max_pool2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let mkldnn_max_pool3d (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_mkldnn_max_pool3d (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let mkldnn_max_pool3d_backward ~(grad_output @ local) ~(output @ local) (input @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_mkldnn_max_pool3d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor output) (globalize_gc_tensor input) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let mkldnn_max_pool3d_backward_out ~(out @ local) ~(grad_output @ local) ~(output @ local) (input @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_mkldnn_max_pool3d_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (globalize_gc_tensor output) (globalize_gc_tensor input) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let mkldnn_max_pool3d_out ~(out @ local) (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_mkldnn_max_pool3d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let mkldnn_reorder_conv2d_weight (self @ local) ~padding ~stride ~dilation ~groups ~input_size =
  stubs_mkldnn_reorder_conv2d_weight (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) (match input_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match input_size with | None -> -1 | Some v -> List.length v) |> with_tensor_gc

let mkldnn_reorder_conv2d_weight_out ~(out @ local) (self @ local) ~padding ~stride ~dilation ~groups ~input_size =
  stubs_mkldnn_reorder_conv2d_weight_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) (match input_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match input_size with | None -> -1 | Some v -> List.length v) |> with_tensor_gc

let mkldnn_reorder_conv3d_weight (self @ local) ~padding ~stride ~dilation ~groups ~input_size =
  stubs_mkldnn_reorder_conv3d_weight (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) (match input_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match input_size with | None -> -1 | Some v -> List.length v) |> with_tensor_gc

let mkldnn_reorder_conv3d_weight_out ~(out @ local) (self @ local) ~padding ~stride ~dilation ~groups ~input_size =
  stubs_mkldnn_reorder_conv3d_weight_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (Int64.of_int groups) (match input_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match input_size with | None -> -1 | Some v -> List.length v) |> with_tensor_gc

let mkldnn_rnn_layer (input @ local) ~(weight0 @ local) ~(weight1 @ local) ~(weight2 @ local) ~(weight3 @ local) ~(hx_ @ local) ~(cx_ @ local) ~reverse ~batch_sizes ~mode ~hidden_size ~num_layers ~has_biases ~bidirectional ~batch_first ~train =
  let out__ = CArray.make raw_tensor 4 in
  stubs_mkldnn_rnn_layer (CArray.start out__) (globalize_gc_tensor input) (globalize_gc_tensor weight0) (globalize_gc_tensor weight1) (globalize_gc_tensor weight2) (globalize_gc_tensor weight3) (globalize_gc_tensor hx_) (globalize_gc_tensor cx_) (if reverse then 1 else 0) (List.map Int64.of_int batch_sizes |> CArray.of_list int64_t |> CArray.start) (List.length batch_sizes) (Int64.of_int mode) (Int64.of_int hidden_size) (Int64.of_int num_layers) (if has_biases then 1 else 0) (if bidirectional then 1 else 0) (if batch_first then 1 else 0) (if train then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let mkldnn_rnn_layer_backward (input @ local) ~(weight1 @ local) ~(weight2 @ local) ~(weight3 @ local) ~(weight4 @ local) ~(hx_ @ local) ~(cx_tmp @ local) ~(output @ local) ~(hy_ @ local) ~(cy_ @ local) ~(grad_output @ local) ~(grad_hy @ local) ~(grad_cy @ local) ~reverse ~mode ~hidden_size ~num_layers ~has_biases ~train ~bidirectional ~batch_sizes ~batch_first ~(workspace @ local) =
  let out__ = CArray.make raw_tensor 7 in
  stubs_mkldnn_rnn_layer_backward (CArray.start out__) (globalize_gc_tensor input) (globalize_gc_tensor weight1) (globalize_gc_tensor weight2) (globalize_gc_tensor weight3) (globalize_gc_tensor weight4) (globalize_gc_tensor hx_) (globalize_gc_tensor cx_tmp) (globalize_gc_tensor output) (globalize_gc_tensor hy_) (globalize_gc_tensor cy_) (match (Base.Option.globalize globalize_gc_tensor grad_output) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor grad_hy) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor grad_cy) with | Some v -> v | None -> none_gc_tensor) (if reverse then 1 else 0) (Int64.of_int mode) (Int64.of_int hidden_size) (Int64.of_int num_layers) (if has_biases then 1 else 0) (if train then 1 else 0) (if bidirectional then 1 else 0) (List.map Int64.of_int batch_sizes |> CArray.of_list int64_t |> CArray.start) (List.length batch_sizes) (if batch_first then 1 else 0) (globalize_gc_tensor workspace);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  let t5 = CArray.get out__ 5 |> with_tensor_gc in
  let t6 = CArray.get out__ 6 |> with_tensor_gc in
  t0, t1, t2, t3, t4, t5, t6

let mkldnn_rnn_layer_backward_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) ~(out3 @ local) ~(out4 @ local) ~(out5 @ local) ~(out6 @ local) (input @ local) ~(weight1 @ local) ~(weight2 @ local) ~(weight3 @ local) ~(weight4 @ local) ~(hx_ @ local) ~(cx_tmp @ local) ~(output @ local) ~(hy_ @ local) ~(cy_ @ local) ~(grad_output @ local) ~(grad_hy @ local) ~(grad_cy @ local) ~reverse ~mode ~hidden_size ~num_layers ~has_biases ~train ~bidirectional ~batch_sizes ~batch_first ~(workspace @ local) =
  let out__ = CArray.make raw_tensor 7 in
  stubs_mkldnn_rnn_layer_backward_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor out3) (globalize_gc_tensor out4) (globalize_gc_tensor out5) (globalize_gc_tensor out6) (globalize_gc_tensor input) (globalize_gc_tensor weight1) (globalize_gc_tensor weight2) (globalize_gc_tensor weight3) (globalize_gc_tensor weight4) (globalize_gc_tensor hx_) (globalize_gc_tensor cx_tmp) (globalize_gc_tensor output) (globalize_gc_tensor hy_) (globalize_gc_tensor cy_) (match (Base.Option.globalize globalize_gc_tensor grad_output) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor grad_hy) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor grad_cy) with | Some v -> v | None -> none_gc_tensor) (if reverse then 1 else 0) (Int64.of_int mode) (Int64.of_int hidden_size) (Int64.of_int num_layers) (if has_biases then 1 else 0) (if train then 1 else 0) (if bidirectional then 1 else 0) (List.map Int64.of_int batch_sizes |> CArray.of_list int64_t |> CArray.start) (List.length batch_sizes) (if batch_first then 1 else 0) (globalize_gc_tensor workspace);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  let t4 = CArray.get out__ 4 |> with_tensor_gc in
  let t5 = CArray.get out__ 5 |> with_tensor_gc in
  let t6 = CArray.get out__ 6 |> with_tensor_gc in
  t0, t1, t2, t3, t4, t5, t6

let mkldnn_rnn_layer_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) ~(out3 @ local) (input @ local) ~(weight0 @ local) ~(weight1 @ local) ~(weight2 @ local) ~(weight3 @ local) ~(hx_ @ local) ~(cx_ @ local) ~reverse ~batch_sizes ~mode ~hidden_size ~num_layers ~has_biases ~bidirectional ~batch_first ~train =
  let out__ = CArray.make raw_tensor 4 in
  stubs_mkldnn_rnn_layer_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor out3) (globalize_gc_tensor input) (globalize_gc_tensor weight0) (globalize_gc_tensor weight1) (globalize_gc_tensor weight2) (globalize_gc_tensor weight3) (globalize_gc_tensor hx_) (globalize_gc_tensor cx_) (if reverse then 1 else 0) (List.map Int64.of_int batch_sizes |> CArray.of_list int64_t |> CArray.start) (List.length batch_sizes) (Int64.of_int mode) (Int64.of_int hidden_size) (Int64.of_int num_layers) (if has_biases then 1 else 0) (if bidirectional then 1 else 0) (if batch_first then 1 else 0) (if train then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  let t3 = CArray.get out__ 3 |> with_tensor_gc in
  t0, t1, t2, t3

let mm (self @ local) ~(mat2 @ local) =
  stubs_mm (globalize_gc_tensor self) (globalize_gc_tensor mat2) |> with_tensor_gc

let mm_dtype (self @ local) ~(mat2 @ local) ~out_dtype =
  stubs_mm_dtype (globalize_gc_tensor self) (globalize_gc_tensor mat2) (Kind.packed_to_int out_dtype) |> with_tensor_gc

let mm_dtype_out ~(out @ local) (self @ local) ~(mat2 @ local) ~out_dtype =
  stubs_mm_dtype_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mat2) (Kind.packed_to_int out_dtype) |> with_tensor_gc

let mm_out ~(out @ local) (self @ local) ~(mat2 @ local) =
  stubs_mm_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mat2) |> with_tensor_gc

let mode (self @ local) ~dim ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_mode (CArray.start out__) (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let mode_values ~(values @ local) ~(indices @ local) (self @ local) ~dim ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_mode_values (CArray.start out__) (globalize_gc_tensor values) (globalize_gc_tensor indices) (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let moveaxis (self @ local) ~source ~destination =
  stubs_moveaxis (globalize_gc_tensor self) (List.map Int64.of_int source |> CArray.of_list int64_t |> CArray.start) (List.length source) (List.map Int64.of_int destination |> CArray.of_list int64_t |> CArray.start) (List.length destination) |> with_tensor_gc

let moveaxis_int (self @ local) ~source ~destination =
  stubs_moveaxis_int (globalize_gc_tensor self) (Int64.of_int source) (Int64.of_int destination) |> with_tensor_gc

let movedim (self @ local) ~source ~destination =
  stubs_movedim (globalize_gc_tensor self) (List.map Int64.of_int source |> CArray.of_list int64_t |> CArray.start) (List.length source) (List.map Int64.of_int destination |> CArray.of_list int64_t |> CArray.start) (List.length destination) |> with_tensor_gc

let movedim_int (self @ local) ~source ~destination =
  stubs_movedim_int (globalize_gc_tensor self) (Int64.of_int source) (Int64.of_int destination) |> with_tensor_gc

let mse_loss (self @ local) ~(target @ local) ~reduction =
  stubs_mse_loss (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let mse_loss_backward ~(grad_output @ local) (self @ local) ~(target @ local) ~reduction =
  stubs_mse_loss_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let mse_loss_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~(target @ local) ~reduction =
  stubs_mse_loss_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let mse_loss_out ~(out @ local) (self @ local) ~(target @ local) ~reduction =
  stubs_mse_loss_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let msort (self @ local) =
  stubs_msort (globalize_gc_tensor self) |> with_tensor_gc

let msort_out ~(out @ local) (self @ local) =
  stubs_msort_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let mt (self @ local) =
  stubs_mt (globalize_gc_tensor self) |> with_tensor_gc

let mul (self @ local) (other @ local) =
  stubs_mul (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let mul_ (self @ local) (other @ local) =
  stubs_mul_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let mul_out ~(out @ local) (self @ local) (other @ local) =
  stubs_mul_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let mul_scalar (self @ local) other =
  stubs_mul_scalar (globalize_gc_tensor self) other |> with_tensor_gc

let mul_scalar_ (self @ local) other =
  stubs_mul_scalar_ (globalize_gc_tensor self) other |> with_tensor_gc

let mul_scalar_out ~(out @ local) (self @ local) other =
  stubs_mul_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let multi_margin_loss_backward ~(grad_output @ local) (self @ local) ~(target @ local) ~p ~margin ~(weight @ local) ~reduction =
  stubs_multi_margin_loss_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) p margin (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let multi_margin_loss_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~(target @ local) ~p ~margin ~(weight @ local) ~reduction =
  stubs_multi_margin_loss_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) p margin (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let multilabel_margin_loss (self @ local) ~(target @ local) ~reduction =
  stubs_multilabel_margin_loss (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let multilabel_margin_loss_backward ~(grad_output @ local) (self @ local) ~(target @ local) ~reduction ~(is_target @ local) =
  stubs_multilabel_margin_loss_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) (globalize_gc_tensor is_target) |> with_tensor_gc

let multilabel_margin_loss_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~(target @ local) ~reduction ~(is_target @ local) =
  stubs_multilabel_margin_loss_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) (globalize_gc_tensor is_target) |> with_tensor_gc

let multilabel_margin_loss_out ~(out @ local) (self @ local) ~(target @ local) ~reduction =
  stubs_multilabel_margin_loss_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let multinomial (self @ local) ~num_samples ~replacement =
  stubs_multinomial (globalize_gc_tensor self) (Int64.of_int num_samples) (if replacement then 1 else 0) |> with_tensor_gc

let multinomial_out ~(out @ local) (self @ local) ~num_samples ~replacement =
  stubs_multinomial_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int num_samples) (if replacement then 1 else 0) |> with_tensor_gc

let multiply (self @ local) (other @ local) =
  stubs_multiply (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let multiply_ (self @ local) (other @ local) =
  stubs_multiply_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let multiply_out ~(out @ local) (self @ local) (other @ local) =
  stubs_multiply_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let multiply_scalar (self @ local) other =
  stubs_multiply_scalar (globalize_gc_tensor self) other |> with_tensor_gc

let multiply_scalar_ (self @ local) other =
  stubs_multiply_scalar_ (globalize_gc_tensor self) other |> with_tensor_gc

let mv (self @ local) ~(vec @ local) =
  stubs_mv (globalize_gc_tensor self) (globalize_gc_tensor vec) |> with_tensor_gc

let mv_out ~(out @ local) (self @ local) ~(vec @ local) =
  stubs_mv_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor vec) |> with_tensor_gc

let mvlgamma (self @ local) ~p =
  stubs_mvlgamma (globalize_gc_tensor self) (Int64.of_int p) |> with_tensor_gc

let mvlgamma_ (self @ local) ~p =
  stubs_mvlgamma_ (globalize_gc_tensor self) (Int64.of_int p) |> with_tensor_gc

let mvlgamma_out ~(out @ local) (self @ local) ~p =
  stubs_mvlgamma_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int p) |> with_tensor_gc

let nan_to_num (self @ local) ~nan ~posinf ~neginf =
  stubs_nan_to_num (globalize_gc_tensor self)  (Option.value nan ~default:0.0) (match nan with | Some _ -> 0 | None -> 1)   (Option.value posinf ~default:0.0) (match posinf with | Some _ -> 0 | None -> 1)   (Option.value neginf ~default:0.0) (match neginf with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let nan_to_num_ (self @ local) ~nan ~posinf ~neginf =
  stubs_nan_to_num_ (globalize_gc_tensor self)  (Option.value nan ~default:0.0) (match nan with | Some _ -> 0 | None -> 1)   (Option.value posinf ~default:0.0) (match posinf with | Some _ -> 0 | None -> 1)   (Option.value neginf ~default:0.0) (match neginf with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let nan_to_num_out ~(out @ local) (self @ local) ~nan ~posinf ~neginf =
  stubs_nan_to_num_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (Option.value nan ~default:0.0) (match nan with | Some _ -> 0 | None -> 1)   (Option.value posinf ~default:0.0) (match posinf with | Some _ -> 0 | None -> 1)   (Option.value neginf ~default:0.0) (match neginf with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let nanmean (self @ local) ~dim ~keepdim ~dtype =
  stubs_nanmean (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let nanmean_out ~(out @ local) (self @ local) ~dim ~keepdim ~dtype =
  stubs_nanmean_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let nanmedian (self @ local) =
  stubs_nanmedian (globalize_gc_tensor self) |> with_tensor_gc

let nanmedian_dim (self @ local) ~dim ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_nanmedian_dim (CArray.start out__) (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let nanmedian_dim_values ~(values @ local) ~(indices @ local) (self @ local) ~dim ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_nanmedian_dim_values (CArray.start out__) (globalize_gc_tensor values) (globalize_gc_tensor indices) (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let nanmedian_out ~(out @ local) (self @ local) =
  stubs_nanmedian_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let nanquantile (self @ local) ~(q @ local) ~dim ~keepdim ~interpolation =
  stubs_nanquantile (globalize_gc_tensor self) (globalize_gc_tensor q)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  (if keepdim then 1 else 0) interpolation |> with_tensor_gc

let nanquantile_out ~(out @ local) (self @ local) ~(q @ local) ~dim ~keepdim ~interpolation =
  stubs_nanquantile_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor q)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  (if keepdim then 1 else 0) interpolation |> with_tensor_gc

let nanquantile_scalar (self @ local) ~q ~dim ~keepdim ~interpolation =
  stubs_nanquantile_scalar (globalize_gc_tensor self) q  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  (if keepdim then 1 else 0) interpolation |> with_tensor_gc

let nanquantile_scalar_out ~(out @ local) (self @ local) ~q ~dim ~keepdim ~interpolation =
  stubs_nanquantile_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) q  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  (if keepdim then 1 else 0) interpolation |> with_tensor_gc

let nansum (self @ local) ~dim ~keepdim ~dtype =
  stubs_nansum (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let nansum_out ~(out @ local) (self @ local) ~dim ~keepdim ~dtype =
  stubs_nansum_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let narrow (self @ local) ~dim ~start ~length =
  stubs_narrow (globalize_gc_tensor self) (Int64.of_int dim) (Int64.of_int start) (Int64.of_int length) |> with_tensor_gc

let narrow_copy (self @ local) ~dim ~start ~length =
  stubs_narrow_copy (globalize_gc_tensor self) (Int64.of_int dim) (Int64.of_int start) (Int64.of_int length) |> with_tensor_gc

let narrow_copy_out ~(out @ local) (self @ local) ~dim ~start ~length =
  stubs_narrow_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (Int64.of_int start) (Int64.of_int length) |> with_tensor_gc

let narrow_tensor (self @ local) ~dim ~(start @ local) ~length =
  stubs_narrow_tensor (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor start) (Int64.of_int length) |> with_tensor_gc

let native_batch_norm (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~training ~momentum ~eps =
  let out__ = CArray.make raw_tensor 3 in
  stubs_native_batch_norm (CArray.start out__) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) (if training then 1 else 0) momentum eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let native_batch_norm_out ~(out @ local) ~(save_mean @ local) ~(save_invstd @ local) (input @ local) ~(weight @ local) ~(bias @ local) ~(running_mean @ local) ~(running_var @ local) ~training ~momentum ~eps =
  let out__ = CArray.make raw_tensor 3 in
  stubs_native_batch_norm_out (CArray.start out__) (globalize_gc_tensor out) (globalize_gc_tensor save_mean) (globalize_gc_tensor save_invstd) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_mean) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor running_var) with | Some v -> v | None -> none_gc_tensor) (if training then 1 else 0) momentum eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let native_channel_shuffle (self @ local) ~groups =
  stubs_native_channel_shuffle (globalize_gc_tensor self) (Int64.of_int groups) |> with_tensor_gc

let native_dropout (input @ local) ~p ~train =
  let out__ = CArray.make raw_tensor 2 in
  stubs_native_dropout (CArray.start out__) (globalize_gc_tensor input) p (if train then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let native_dropout_backward ~(grad_output @ local) ~(mask @ local) ~scale =
  stubs_native_dropout_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor mask) scale |> with_tensor_gc

let native_dropout_backward_out ~(out @ local) ~(grad_output @ local) ~(mask @ local) ~scale =
  stubs_native_dropout_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (globalize_gc_tensor mask) scale |> with_tensor_gc

let native_dropout_out ~(out0 @ local) ~(out1 @ local) (input @ local) ~p ~train =
  let out__ = CArray.make raw_tensor 2 in
  stubs_native_dropout_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor input) p (if train then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let native_group_norm (input @ local) ~(weight @ local) ~(bias @ local) ~n ~c ~hxw ~group ~eps =
  let out__ = CArray.make raw_tensor 3 in
  stubs_native_group_norm (CArray.start out__) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int n) (Int64.of_int c) (Int64.of_int hxw) (Int64.of_int group) eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let native_group_norm_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) (input @ local) ~(weight @ local) ~(bias @ local) ~n ~c ~hxw ~group ~eps =
  let out__ = CArray.make raw_tensor 3 in
  stubs_native_group_norm_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int n) (Int64.of_int c) (Int64.of_int hxw) (Int64.of_int group) eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let native_layer_norm (input @ local) ~normalized_shape ~(weight @ local) ~(bias @ local) ~eps =
  let out__ = CArray.make raw_tensor 3 in
  stubs_native_layer_norm (CArray.start out__) (globalize_gc_tensor input) (List.map Int64.of_int normalized_shape |> CArray.of_list int64_t |> CArray.start) (List.length normalized_shape) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let native_layer_norm_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) (input @ local) ~normalized_shape ~(weight @ local) ~(bias @ local) ~eps =
  let out__ = CArray.make raw_tensor 3 in
  stubs_native_layer_norm_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor input) (List.map Int64.of_int normalized_shape |> CArray.of_list int64_t |> CArray.start) (List.length normalized_shape) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) eps;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let native_norm ?p (self @ local) =
  stubs_native_norm (globalize_gc_tensor self) (match p with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let native_norm_out ?p ~(out @ local) (self @ local) =
  stubs_native_norm_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match p with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let native_norm_scalaropt_dim_dtype (self @ local) ~p ~dim ~keepdim ~dtype =
  stubs_native_norm_scalaropt_dim_dtype (globalize_gc_tensor self) p (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let native_norm_scalaropt_dim_dtype_out ~(out @ local) (self @ local) ~p ~dim ~keepdim ~dtype =
  stubs_native_norm_scalaropt_dim_dtype_out (globalize_gc_tensor out) (globalize_gc_tensor self) p (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let ne (self @ local) other =
  stubs_ne (globalize_gc_tensor self) other |> with_tensor_gc

let ne_ (self @ local) other =
  stubs_ne_ (globalize_gc_tensor self) other |> with_tensor_gc

let ne_scalar_out ~(out @ local) (self @ local) other =
  stubs_ne_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let ne_tensor (self @ local) (other @ local) =
  stubs_ne_tensor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let ne_tensor_ (self @ local) (other @ local) =
  stubs_ne_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let ne_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_ne_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let neg (self @ local) =
  stubs_neg (globalize_gc_tensor self) |> with_tensor_gc

let neg_ (self @ local) =
  stubs_neg_ (globalize_gc_tensor self) |> with_tensor_gc

let neg_out ~(out @ local) (self @ local) =
  stubs_neg_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let negative (self @ local) =
  stubs_negative (globalize_gc_tensor self) |> with_tensor_gc

let negative_ (self @ local) =
  stubs_negative_ (globalize_gc_tensor self) |> with_tensor_gc

let negative_out ~(out @ local) (self @ local) =
  stubs_negative_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let nested_to_padded_tensor (self @ local) ~padding ~output_size =
  stubs_nested_to_padded_tensor (globalize_gc_tensor self) padding (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) |> with_tensor_gc

let new_empty (self @ local) ~size ~options =
  stubs_new_empty (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let new_empty_out ~(out @ local) (self @ local) ~size =
  stubs_new_empty_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let new_empty_strided (self @ local) ~size ~stride ~options =
  stubs_new_empty_strided (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let new_empty_strided_out ~(out @ local) (self @ local) ~size ~stride =
  stubs_new_empty_strided_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) |> with_tensor_gc

let new_full (self @ local) ~size ~fill_value ~options =
  stubs_new_full (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) fill_value (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let new_full_out ~(out @ local) (self @ local) ~size ~fill_value =
  stubs_new_full_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) fill_value |> with_tensor_gc

let new_ones (self @ local) ~size ~options =
  stubs_new_ones (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let new_ones_out ~(out @ local) (self @ local) ~size =
  stubs_new_ones_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let new_zeros (self @ local) ~size ~options =
  stubs_new_zeros (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let new_zeros_out ~(out @ local) (self @ local) ~size =
  stubs_new_zeros_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let nextafter (self @ local) (other @ local) =
  stubs_nextafter (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let nextafter_ (self @ local) (other @ local) =
  stubs_nextafter_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let nextafter_out ~(out @ local) (self @ local) (other @ local) =
  stubs_nextafter_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let nll_loss (self @ local) ~(target @ local) ~(weight @ local) ~reduction ~ignore_index =
  stubs_nll_loss (globalize_gc_tensor self) (globalize_gc_tensor target) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) (Int64.of_int ignore_index) |> with_tensor_gc

let nll_loss2d (self @ local) ~(target @ local) ~(weight @ local) ~reduction ~ignore_index =
  stubs_nll_loss2d (globalize_gc_tensor self) (globalize_gc_tensor target) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) (Int64.of_int ignore_index) |> with_tensor_gc

let nll_loss2d_backward ~(grad_output @ local) (self @ local) ~(target @ local) ~(weight @ local) ~reduction ~ignore_index ~(total_weight @ local) =
  stubs_nll_loss2d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) (Int64.of_int ignore_index) (globalize_gc_tensor total_weight) |> with_tensor_gc

let nll_loss2d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~(target @ local) ~(weight @ local) ~reduction ~ignore_index ~(total_weight @ local) =
  stubs_nll_loss2d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) (Int64.of_int ignore_index) (globalize_gc_tensor total_weight) |> with_tensor_gc

let nll_loss2d_out ~(out @ local) (self @ local) ~(target @ local) ~(weight @ local) ~reduction ~ignore_index =
  stubs_nll_loss2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor target) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) (Int64.of_int ignore_index) |> with_tensor_gc

let nll_loss_backward ~(grad_output @ local) (self @ local) ~(target @ local) ~(weight @ local) ~reduction ~ignore_index ~(total_weight @ local) =
  stubs_nll_loss_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) (Int64.of_int ignore_index) (globalize_gc_tensor total_weight) |> with_tensor_gc

let nll_loss_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~(target @ local) ~(weight @ local) ~reduction ~ignore_index ~(total_weight @ local) =
  stubs_nll_loss_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) (Int64.of_int ignore_index) (globalize_gc_tensor total_weight) |> with_tensor_gc

let nll_loss_nd (self @ local) ~(target @ local) ~(weight @ local) ~reduction ~ignore_index =
  stubs_nll_loss_nd (globalize_gc_tensor self) (globalize_gc_tensor target) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) (Int64.of_int ignore_index) |> with_tensor_gc

let nll_loss_out ~(out @ local) (self @ local) ~(target @ local) ~(weight @ local) ~reduction ~ignore_index =
  stubs_nll_loss_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor target) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (Reduction.to_int reduction |> Int64.of_int) (Int64.of_int ignore_index) |> with_tensor_gc

let nonzero (self @ local) =
  stubs_nonzero (globalize_gc_tensor self) |> with_tensor_gc

let nonzero_numpy (self @ local) =
  stubs_nonzero_numpy (globalize_gc_tensor self) |> to_tensor_list

let nonzero_out ~(out @ local) (self @ local) =
  stubs_nonzero_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let nonzero_static (self @ local) ~size ~fill_value =
  stubs_nonzero_static (globalize_gc_tensor self) (Int64.of_int size) (Int64.of_int fill_value) |> with_tensor_gc

let nonzero_static_out ~(out @ local) (self @ local) ~size ~fill_value =
  stubs_nonzero_static_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int size) (Int64.of_int fill_value) |> with_tensor_gc

let norm ?p (self @ local) =
  stubs_norm (globalize_gc_tensor self) (match p with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let norm_dtype_out ~(out @ local) (self @ local) ~p ~dim ~keepdim ~dtype =
  stubs_norm_dtype_out (globalize_gc_tensor out) (globalize_gc_tensor self) p (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let norm_except_dim ~(v @ local) ~pow ~dim =
  stubs_norm_except_dim (globalize_gc_tensor v) (Int64.of_int pow) (Int64.of_int dim) |> with_tensor_gc

let norm_out ~(out @ local) (self @ local) ~p ~dim ~keepdim =
  stubs_norm_out (globalize_gc_tensor out) (globalize_gc_tensor self) p (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) |> with_tensor_gc

let norm_scalar_out ?p ~(out @ local) (self @ local) =
  stubs_norm_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match p with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let norm_scalaropt_dim (self @ local) ~p ~dim ~keepdim =
  stubs_norm_scalaropt_dim (globalize_gc_tensor self) p (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) |> with_tensor_gc

let norm_scalaropt_dim_dtype (self @ local) ~p ~dim ~keepdim ~dtype =
  stubs_norm_scalaropt_dim_dtype (globalize_gc_tensor self) p (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let norm_scalaropt_dtype (self @ local) ~p ~dtype =
  stubs_norm_scalaropt_dtype (globalize_gc_tensor self) p (Kind.packed_to_int dtype) |> with_tensor_gc

let norm_scalaropt_dtype_out ~(out @ local) (self @ local) ~p ~dtype =
  stubs_norm_scalaropt_dtype_out (globalize_gc_tensor out) (globalize_gc_tensor self) p (Kind.packed_to_int dtype) |> with_tensor_gc

let normal_ (self @ local) ~mean ~std =
  stubs_normal_ (globalize_gc_tensor self) mean std |> with_tensor_gc

let normal_functional (self @ local) ~mean ~std =
  stubs_normal_functional (globalize_gc_tensor self) mean std |> with_tensor_gc

let not_equal (self @ local) other =
  stubs_not_equal (globalize_gc_tensor self) other |> with_tensor_gc

let not_equal_ (self @ local) other =
  stubs_not_equal_ (globalize_gc_tensor self) other |> with_tensor_gc

let not_equal_scalar_out ~(out @ local) (self @ local) other =
  stubs_not_equal_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let not_equal_tensor (self @ local) (other @ local) =
  stubs_not_equal_tensor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let not_equal_tensor_ (self @ local) (other @ local) =
  stubs_not_equal_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let not_equal_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_not_equal_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let nuclear_norm (self @ local) ~keepdim =
  stubs_nuclear_norm (globalize_gc_tensor self) (if keepdim then 1 else 0) |> with_tensor_gc

let nuclear_norm_dim (self @ local) ~dim ~keepdim =
  stubs_nuclear_norm_dim (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) |> with_tensor_gc

let nuclear_norm_dim_out ~(out @ local) (self @ local) ~dim ~keepdim =
  stubs_nuclear_norm_dim_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) |> with_tensor_gc

let nuclear_norm_out ~(out @ local) (self @ local) ~keepdim =
  stubs_nuclear_norm_out (globalize_gc_tensor out) (globalize_gc_tensor self) (if keepdim then 1 else 0) |> with_tensor_gc

let numel (self @ local) =
  stubs_numel (globalize_gc_tensor self)

let numpy_t (self @ local) =
  stubs_numpy_t (globalize_gc_tensor self) |> with_tensor_gc

let one_hot (self @ local) ~num_classes =
  stubs_one_hot (globalize_gc_tensor self) (Int64.of_int num_classes) |> with_tensor_gc

let ones ~size ~options =
  stubs_ones (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let ones_like (self @ local) =
  stubs_ones_like (globalize_gc_tensor self) |> with_tensor_gc

let ones_like_out ~(out @ local) (self @ local) =
  stubs_ones_like_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let ones_out ~(out @ local) ~size =
  stubs_ones_out (globalize_gc_tensor out) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let orgqr (self @ local) ~(input2 @ local) =
  stubs_orgqr (globalize_gc_tensor self) (globalize_gc_tensor input2) |> with_tensor_gc

let orgqr_out ~(out @ local) (self @ local) ~(input2 @ local) =
  stubs_orgqr_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor input2) |> with_tensor_gc

let ormqr (self @ local) ~(input2 @ local) ~(input3 @ local) ~left ~transpose =
  stubs_ormqr (globalize_gc_tensor self) (globalize_gc_tensor input2) (globalize_gc_tensor input3) (if left then 1 else 0) (if transpose then 1 else 0) |> with_tensor_gc

let ormqr_out ~(out @ local) (self @ local) ~(input2 @ local) ~(input3 @ local) ~left ~transpose =
  stubs_ormqr_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor input2) (globalize_gc_tensor input3) (if left then 1 else 0) (if transpose then 1 else 0) |> with_tensor_gc

let outer (self @ local) ~(vec2 @ local) =
  stubs_outer (globalize_gc_tensor self) (globalize_gc_tensor vec2) |> with_tensor_gc

let outer_out ~(out @ local) (self @ local) ~(vec2 @ local) =
  stubs_outer_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor vec2) |> with_tensor_gc

let output_nr (self @ local) =
  stubs_output_nr (globalize_gc_tensor self)

let pad (self @ local) ~pad ~mode ~value =
  stubs_pad (globalize_gc_tensor self) (List.map Int64.of_int pad |> CArray.of_list int64_t |> CArray.start) (List.length pad) mode  (Option.value value ~default:0.0) (match value with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let pad_sequence ~(sequences @ local) ~batch_first ~padding_value ~padding_side =
  stubs_pad_sequence (CArray.of_list gc_tensor (globalize_gc_tensor_list sequences) |> CArray.start) (Base.List.length sequences) (if batch_first then 1 else 0) padding_value padding_side |> with_tensor_gc

let pairwise_distance ~(x1 @ local) ~(x2 @ local) ~p ~eps ~keepdim =
  stubs_pairwise_distance (globalize_gc_tensor x1) (globalize_gc_tensor x2) p eps (if keepdim then 1 else 0) |> with_tensor_gc

let pdist (self @ local) ~p =
  stubs_pdist (globalize_gc_tensor self) p |> with_tensor_gc

let permute (self @ local) ~dims =
  stubs_permute (globalize_gc_tensor self) (List.map Int64.of_int dims |> CArray.of_list int64_t |> CArray.start) (List.length dims) |> with_tensor_gc

let permute_copy (self @ local) ~dims =
  stubs_permute_copy (globalize_gc_tensor self) (List.map Int64.of_int dims |> CArray.of_list int64_t |> CArray.start) (List.length dims) |> with_tensor_gc

let permute_copy_out ~(out @ local) (self @ local) ~dims =
  stubs_permute_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dims |> CArray.of_list int64_t |> CArray.start) (List.length dims) |> with_tensor_gc

let pin_memory (self @ local) ~device =
  stubs_pin_memory (globalize_gc_tensor self)  (Device.option_to_int device)  |> with_tensor_gc

let pinverse (self @ local) ~rcond =
  stubs_pinverse (globalize_gc_tensor self) rcond |> with_tensor_gc

let pixel_shuffle (self @ local) ~upscale_factor =
  stubs_pixel_shuffle (globalize_gc_tensor self) (Int64.of_int upscale_factor) |> with_tensor_gc

let pixel_shuffle_out ~(out @ local) (self @ local) ~upscale_factor =
  stubs_pixel_shuffle_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int upscale_factor) |> with_tensor_gc

let pixel_unshuffle (self @ local) ~downscale_factor =
  stubs_pixel_unshuffle (globalize_gc_tensor self) (Int64.of_int downscale_factor) |> with_tensor_gc

let pixel_unshuffle_out ~(out @ local) (self @ local) ~downscale_factor =
  stubs_pixel_unshuffle_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int downscale_factor) |> with_tensor_gc

let poisson (self @ local) =
  stubs_poisson (globalize_gc_tensor self) |> with_tensor_gc

let poisson_nll_loss (input @ local) ~(target @ local) ~log_input ~full ~eps ~reduction =
  stubs_poisson_nll_loss (globalize_gc_tensor input) (globalize_gc_tensor target) (if log_input then 1 else 0) (if full then 1 else 0) eps (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let poisson_out ~(out @ local) (self @ local) =
  stubs_poisson_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let polar ~(abs @ local) ~(angle @ local) =
  stubs_polar (globalize_gc_tensor abs) (globalize_gc_tensor angle) |> with_tensor_gc

let polar_out ~(out @ local) ~(abs @ local) ~(angle @ local) =
  stubs_polar_out (globalize_gc_tensor out) (globalize_gc_tensor abs) (globalize_gc_tensor angle) |> with_tensor_gc

let polygamma ~n (self @ local) =
  stubs_polygamma (Int64.of_int n) (globalize_gc_tensor self) |> with_tensor_gc

let polygamma_ (self @ local) ~n =
  stubs_polygamma_ (globalize_gc_tensor self) (Int64.of_int n) |> with_tensor_gc

let polygamma_out ~(out @ local) ~n (self @ local) =
  stubs_polygamma_out (globalize_gc_tensor out) (Int64.of_int n) (globalize_gc_tensor self) |> with_tensor_gc

let positive (self @ local) =
  stubs_positive (globalize_gc_tensor self) |> with_tensor_gc

let pow (self @ local) ~(exponent @ local) =
  stubs_pow (globalize_gc_tensor self) (globalize_gc_tensor exponent) |> with_tensor_gc

let pow_ (self @ local) ~exponent =
  stubs_pow_ (globalize_gc_tensor self) exponent |> with_tensor_gc

let pow_scalar self ~(exponent @ local) =
  stubs_pow_scalar self (globalize_gc_tensor exponent) |> with_tensor_gc

let pow_scalar_out ~(out @ local) self ~(exponent @ local) =
  stubs_pow_scalar_out (globalize_gc_tensor out) self (globalize_gc_tensor exponent) |> with_tensor_gc

let pow_tensor_ (self @ local) ~(exponent @ local) =
  stubs_pow_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor exponent) |> with_tensor_gc

let pow_tensor_scalar (self @ local) ~exponent =
  stubs_pow_tensor_scalar (globalize_gc_tensor self) exponent |> with_tensor_gc

let pow_tensor_scalar_out ~(out @ local) (self @ local) ~exponent =
  stubs_pow_tensor_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) exponent |> with_tensor_gc

let pow_tensor_tensor_out ~(out @ local) (self @ local) ~(exponent @ local) =
  stubs_pow_tensor_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor exponent) |> with_tensor_gc

let prelu (self @ local) ~(weight @ local) =
  stubs_prelu (globalize_gc_tensor self) (globalize_gc_tensor weight) |> with_tensor_gc

let prod (self @ local) ~dtype =
  stubs_prod (globalize_gc_tensor self) (Kind.packed_to_int dtype) |> with_tensor_gc

let prod_dim_int (self @ local) ~dim ~keepdim ~dtype =
  stubs_prod_dim_int (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let prod_int_out ~(out @ local) (self @ local) ~dim ~keepdim ~dtype =
  stubs_prod_int_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let prod_out ~(out @ local) (self @ local) ~dtype =
  stubs_prod_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Kind.packed_to_int dtype) |> with_tensor_gc

let put (self @ local) ~(index @ local) ~(source @ local) ~accumulate =
  stubs_put (globalize_gc_tensor self) (globalize_gc_tensor index) (globalize_gc_tensor source) (if accumulate then 1 else 0) |> with_tensor_gc

let put_ (self @ local) ~(index @ local) ~(source @ local) ~accumulate =
  stubs_put_ (globalize_gc_tensor self) (globalize_gc_tensor index) (globalize_gc_tensor source) (if accumulate then 1 else 0) |> with_tensor_gc

let put_out ~(out @ local) (self @ local) ~(index @ local) ~(source @ local) ~accumulate =
  stubs_put_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor index) (globalize_gc_tensor source) (if accumulate then 1 else 0) |> with_tensor_gc

let q_per_channel_axis (self @ local) =
  stubs_q_per_channel_axis (globalize_gc_tensor self)

let q_per_channel_scales (self @ local) =
  stubs_q_per_channel_scales (globalize_gc_tensor self) |> with_tensor_gc

let q_per_channel_scales_out ~(out @ local) (self @ local) =
  stubs_q_per_channel_scales_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let q_per_channel_zero_points (self @ local) =
  stubs_q_per_channel_zero_points (globalize_gc_tensor self) |> with_tensor_gc

let q_per_channel_zero_points_out ~(out @ local) (self @ local) =
  stubs_q_per_channel_zero_points_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let q_scale (self @ local) =
  stubs_q_scale (globalize_gc_tensor self)

let q_zero_point (self @ local) =
  stubs_q_zero_point (globalize_gc_tensor self)

let qr (self @ local) ~some =
  let out__ = CArray.make raw_tensor 2 in
  stubs_qr (CArray.start out__) (globalize_gc_tensor self) (if some then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let qr_q ~(q @ local) ~(r @ local) (self @ local) ~some =
  let out__ = CArray.make raw_tensor 2 in
  stubs_qr_q (CArray.start out__) (globalize_gc_tensor q) (globalize_gc_tensor r) (globalize_gc_tensor self) (if some then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let quantile (self @ local) ~(q @ local) ~dim ~keepdim ~interpolation =
  stubs_quantile (globalize_gc_tensor self) (globalize_gc_tensor q)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  (if keepdim then 1 else 0) interpolation |> with_tensor_gc

let quantile_out ~(out @ local) (self @ local) ~(q @ local) ~dim ~keepdim ~interpolation =
  stubs_quantile_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor q)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  (if keepdim then 1 else 0) interpolation |> with_tensor_gc

let quantile_scalar (self @ local) ~q ~dim ~keepdim ~interpolation =
  stubs_quantile_scalar (globalize_gc_tensor self) q  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  (if keepdim then 1 else 0) interpolation |> with_tensor_gc

let quantile_scalar_out ~(out @ local) (self @ local) ~q ~dim ~keepdim ~interpolation =
  stubs_quantile_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) q  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  (if keepdim then 1 else 0) interpolation |> with_tensor_gc

let quantize_per_channel (self @ local) ~(scales @ local) ~(zero_points @ local) ~axis ~dtype =
  stubs_quantize_per_channel (globalize_gc_tensor self) (globalize_gc_tensor scales) (globalize_gc_tensor zero_points) (Int64.of_int axis) (Kind.packed_to_int dtype) |> with_tensor_gc

let quantize_per_channel_out ~(out @ local) (self @ local) ~(scales @ local) ~(zero_points @ local) ~axis ~dtype =
  stubs_quantize_per_channel_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor scales) (globalize_gc_tensor zero_points) (Int64.of_int axis) (Kind.packed_to_int dtype) |> with_tensor_gc

let quantize_per_tensor (self @ local) ~scale ~zero_point ~dtype =
  stubs_quantize_per_tensor (globalize_gc_tensor self) scale (Int64.of_int zero_point) (Kind.packed_to_int dtype) |> with_tensor_gc

let quantize_per_tensor_dynamic (self @ local) ~dtype ~reduce_range =
  stubs_quantize_per_tensor_dynamic (globalize_gc_tensor self) (Kind.packed_to_int dtype) (if reduce_range then 1 else 0) |> with_tensor_gc

let quantize_per_tensor_dynamic_out ~(out @ local) (self @ local) ~dtype ~reduce_range =
  stubs_quantize_per_tensor_dynamic_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Kind.packed_to_int dtype) (if reduce_range then 1 else 0) |> with_tensor_gc

let quantize_per_tensor_out ~(out @ local) (self @ local) ~scale ~zero_point ~dtype =
  stubs_quantize_per_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) scale (Int64.of_int zero_point) (Kind.packed_to_int dtype) |> with_tensor_gc

let quantize_per_tensor_tensor_qparams (self @ local) ~(scale @ local) ~(zero_point @ local) ~dtype =
  stubs_quantize_per_tensor_tensor_qparams (globalize_gc_tensor self) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) (Kind.packed_to_int dtype) |> with_tensor_gc

let quantize_per_tensor_tensor_qparams_out ~(out @ local) (self @ local) ~(scale @ local) ~(zero_point @ local) ~dtype =
  stubs_quantize_per_tensor_tensor_qparams_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor scale) (globalize_gc_tensor zero_point) (Kind.packed_to_int dtype) |> with_tensor_gc

let quantize_per_tensor_tensors (tensors @ local) ~(scales @ local) ~(zero_points @ local) ~dtype =
  stubs_quantize_per_tensor_tensors (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) (globalize_gc_tensor scales) (globalize_gc_tensor zero_points) (Kind.packed_to_int dtype) |> to_tensor_list

let quantize_per_tensor_tensors_out ~(out @ local) (tensors @ local) ~(scales @ local) ~(zero_points @ local) ~dtype =
  stubs_quantize_per_tensor_tensors_out (CArray.of_list gc_tensor (globalize_gc_tensor_list out) |> CArray.start) (Base.List.length out) (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) (globalize_gc_tensor scales) (globalize_gc_tensor zero_points) (Kind.packed_to_int dtype)

let quantized_batch_norm (input @ local) ~(weight @ local) ~(bias @ local) ~(mean @ local) ~(var @ local) ~eps ~output_scale ~output_zero_point =
  stubs_quantized_batch_norm (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor mean) (globalize_gc_tensor var) eps output_scale (Int64.of_int output_zero_point) |> with_tensor_gc

let quantized_batch_norm_out ~(out @ local) (input @ local) ~(weight @ local) ~(bias @ local) ~(mean @ local) ~(var @ local) ~eps ~output_scale ~output_zero_point =
  stubs_quantized_batch_norm_out (globalize_gc_tensor out) (globalize_gc_tensor input) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (globalize_gc_tensor mean) (globalize_gc_tensor var) eps output_scale (Int64.of_int output_zero_point) |> with_tensor_gc

let quantized_gru_cell (input @ local) ~(hx @ local) ~(w_ih @ local) ~(w_hh @ local) ~(b_ih @ local) ~(b_hh @ local) ~(packed_ih @ local) ~(packed_hh @ local) ~(col_offsets_ih @ local) ~(col_offsets_hh @ local) ~scale_ih ~scale_hh ~zero_point_ih ~zero_point_hh =
  stubs_quantized_gru_cell (globalize_gc_tensor input) (globalize_gc_tensor hx) (globalize_gc_tensor w_ih) (globalize_gc_tensor w_hh) (globalize_gc_tensor b_ih) (globalize_gc_tensor b_hh) (globalize_gc_tensor packed_ih) (globalize_gc_tensor packed_hh) (globalize_gc_tensor col_offsets_ih) (globalize_gc_tensor col_offsets_hh) scale_ih scale_hh zero_point_ih zero_point_hh |> with_tensor_gc

let quantized_lstm_cell (input @ local) ~(hx @ local) ~(w_ih @ local) ~(w_hh @ local) ~(b_ih @ local) ~(b_hh @ local) ~(packed_ih @ local) ~(packed_hh @ local) ~(col_offsets_ih @ local) ~(col_offsets_hh @ local) ~scale_ih ~scale_hh ~zero_point_ih ~zero_point_hh =
  let out__ = CArray.make raw_tensor 2 in
  stubs_quantized_lstm_cell (CArray.start out__) (globalize_gc_tensor input) (CArray.of_list gc_tensor (globalize_gc_tensor_list hx) |> CArray.start) (Base.List.length hx) (globalize_gc_tensor w_ih) (globalize_gc_tensor w_hh) (globalize_gc_tensor b_ih) (globalize_gc_tensor b_hh) (globalize_gc_tensor packed_ih) (globalize_gc_tensor packed_hh) (globalize_gc_tensor col_offsets_ih) (globalize_gc_tensor col_offsets_hh) scale_ih scale_hh zero_point_ih zero_point_hh;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let quantized_max_pool1d (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_quantized_max_pool1d (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let quantized_max_pool1d_out ~(out @ local) (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_quantized_max_pool1d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let quantized_max_pool2d (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_quantized_max_pool2d (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let quantized_max_pool2d_out ~(out @ local) (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_quantized_max_pool2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let quantized_max_pool3d (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_quantized_max_pool3d (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let quantized_max_pool3d_out ~(out @ local) (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  stubs_quantized_max_pool3d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) (if ceil_mode then 1 else 0) |> with_tensor_gc

let quantized_rnn_relu_cell (input @ local) ~(hx @ local) ~(w_ih @ local) ~(w_hh @ local) ~(b_ih @ local) ~(b_hh @ local) ~(packed_ih @ local) ~(packed_hh @ local) ~(col_offsets_ih @ local) ~(col_offsets_hh @ local) ~scale_ih ~scale_hh ~zero_point_ih ~zero_point_hh =
  stubs_quantized_rnn_relu_cell (globalize_gc_tensor input) (globalize_gc_tensor hx) (globalize_gc_tensor w_ih) (globalize_gc_tensor w_hh) (globalize_gc_tensor b_ih) (globalize_gc_tensor b_hh) (globalize_gc_tensor packed_ih) (globalize_gc_tensor packed_hh) (globalize_gc_tensor col_offsets_ih) (globalize_gc_tensor col_offsets_hh) scale_ih scale_hh zero_point_ih zero_point_hh |> with_tensor_gc

let quantized_rnn_tanh_cell (input @ local) ~(hx @ local) ~(w_ih @ local) ~(w_hh @ local) ~(b_ih @ local) ~(b_hh @ local) ~(packed_ih @ local) ~(packed_hh @ local) ~(col_offsets_ih @ local) ~(col_offsets_hh @ local) ~scale_ih ~scale_hh ~zero_point_ih ~zero_point_hh =
  stubs_quantized_rnn_tanh_cell (globalize_gc_tensor input) (globalize_gc_tensor hx) (globalize_gc_tensor w_ih) (globalize_gc_tensor w_hh) (globalize_gc_tensor b_ih) (globalize_gc_tensor b_hh) (globalize_gc_tensor packed_ih) (globalize_gc_tensor packed_hh) (globalize_gc_tensor col_offsets_ih) (globalize_gc_tensor col_offsets_hh) scale_ih scale_hh zero_point_ih zero_point_hh |> with_tensor_gc

let rad2deg (self @ local) =
  stubs_rad2deg (globalize_gc_tensor self) |> with_tensor_gc

let rad2deg_ (self @ local) =
  stubs_rad2deg_ (globalize_gc_tensor self) |> with_tensor_gc

let rad2deg_out ~(out @ local) (self @ local) =
  stubs_rad2deg_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let rand ~size ~options =
  stubs_rand (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let rand_like (self @ local) =
  stubs_rand_like (globalize_gc_tensor self) |> with_tensor_gc

let rand_like_out ~(out @ local) (self @ local) =
  stubs_rand_like_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let rand_out ~(out @ local) ~size =
  stubs_rand_out (globalize_gc_tensor out) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let randint ~high ~size ~options =
  stubs_randint (Int64.of_int high) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let randint_like (self @ local) ~high =
  stubs_randint_like (globalize_gc_tensor self) (Int64.of_int high) |> with_tensor_gc

let randint_like_low_dtype (self @ local) ~low ~high =
  stubs_randint_like_low_dtype (globalize_gc_tensor self) (Int64.of_int low) (Int64.of_int high) |> with_tensor_gc

let randint_like_low_dtype_out ~(out @ local) (self @ local) ~low ~high =
  stubs_randint_like_low_dtype_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int low) (Int64.of_int high) |> with_tensor_gc

let randint_like_out ~(out @ local) (self @ local) ~high =
  stubs_randint_like_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int high) |> with_tensor_gc

let randint_like_tensor (self @ local) ~(high @ local) =
  stubs_randint_like_tensor (globalize_gc_tensor self) (globalize_gc_tensor high) |> with_tensor_gc

let randint_like_tensor_out ~(out @ local) (self @ local) ~(high @ local) =
  stubs_randint_like_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor high) |> with_tensor_gc

let randint_low ~low ~high ~size ~options =
  stubs_randint_low (Int64.of_int low) (Int64.of_int high) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let randint_low_out ~(out @ local) ~low ~high ~size =
  stubs_randint_low_out (globalize_gc_tensor out) (Int64.of_int low) (Int64.of_int high) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let randint_out ~(out @ local) ~high ~size =
  stubs_randint_out (globalize_gc_tensor out) (Int64.of_int high) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let randn ~size ~options =
  stubs_randn (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let randn_like (self @ local) =
  stubs_randn_like (globalize_gc_tensor self) |> with_tensor_gc

let randn_like_out ~(out @ local) (self @ local) =
  stubs_randn_like_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let randn_out ~(out @ local) ~size =
  stubs_randn_out (globalize_gc_tensor out) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let random (self @ local) =
  stubs_random (globalize_gc_tensor self) |> with_tensor_gc

let random_ (self @ local) =
  stubs_random_ (globalize_gc_tensor self) |> with_tensor_gc

let random_from (self @ local) ~from ~to_ =
  stubs_random_from (globalize_gc_tensor self) (Int64.of_int from)  (match to_ with | None -> Int64.zero | Some v -> Int64.of_int v) (match to_ with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let random_from_ (self @ local) ~from ~to_ =
  stubs_random_from_ (globalize_gc_tensor self) (Int64.of_int from)  (match to_ with | None -> Int64.zero | Some v -> Int64.of_int v) (match to_ with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let random_from_out ~(out @ local) (self @ local) ~from ~to_ =
  stubs_random_from_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int from)  (match to_ with | None -> Int64.zero | Some v -> Int64.of_int v) (match to_ with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let random_out ~(out @ local) (self @ local) =
  stubs_random_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let random_to (self @ local) ~to_ =
  stubs_random_to (globalize_gc_tensor self) (Int64.of_int to_) |> with_tensor_gc

let random_to_ (self @ local) ~to_ =
  stubs_random_to_ (globalize_gc_tensor self) (Int64.of_int to_) |> with_tensor_gc

let random_to_out ~(out @ local) (self @ local) ~to_ =
  stubs_random_to_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int to_) |> with_tensor_gc

let randperm ~n ~options =
  stubs_randperm (Int64.of_int n) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let randperm_out ~(out @ local) ~n =
  stubs_randperm_out (globalize_gc_tensor out) (Int64.of_int n) |> with_tensor_gc

let range ~start ~end_ ~options =
  stubs_range start end_ (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let range_out ?step ~(out @ local) ~start ~end_ () =
  stubs_range_out (globalize_gc_tensor out) start end_ (match step with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let range_out_ ~(out @ local) ~start ~end_ =
  stubs_range_out_ (globalize_gc_tensor out) start end_ |> with_tensor_gc

let range_step ?step ~start ~end_ ~options () =
  stubs_range_step start end_ (match step with | Some v -> v | None -> none_scalar) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let ravel (self @ local) =
  stubs_ravel (globalize_gc_tensor self) |> with_tensor_gc

let real (self @ local) =
  stubs_real (globalize_gc_tensor self) |> with_tensor_gc

let reciprocal (self @ local) =
  stubs_reciprocal (globalize_gc_tensor self) |> with_tensor_gc

let reciprocal_ (self @ local) =
  stubs_reciprocal_ (globalize_gc_tensor self) |> with_tensor_gc

let reciprocal_out ~(out @ local) (self @ local) =
  stubs_reciprocal_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let reflection_pad1d (self @ local) ~padding =
  stubs_reflection_pad1d (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let reflection_pad1d_backward ~(grad_output @ local) (self @ local) ~padding =
  stubs_reflection_pad1d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let reflection_pad1d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~padding =
  stubs_reflection_pad1d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let reflection_pad1d_out ~(out @ local) (self @ local) ~padding =
  stubs_reflection_pad1d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let reflection_pad2d (self @ local) ~padding =
  stubs_reflection_pad2d (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let reflection_pad2d_backward ~(grad_output @ local) (self @ local) ~padding =
  stubs_reflection_pad2d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let reflection_pad2d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~padding =
  stubs_reflection_pad2d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let reflection_pad2d_out ~(out @ local) (self @ local) ~padding =
  stubs_reflection_pad2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let reflection_pad3d (self @ local) ~padding =
  stubs_reflection_pad3d (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let reflection_pad3d_backward ~(grad_output @ local) (self @ local) ~padding =
  stubs_reflection_pad3d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let reflection_pad3d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~padding =
  stubs_reflection_pad3d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let reflection_pad3d_out ~(out @ local) (self @ local) ~padding =
  stubs_reflection_pad3d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let relu (self @ local) =
  stubs_relu (globalize_gc_tensor self) |> with_tensor_gc

let relu6 (self @ local) =
  stubs_relu6 (globalize_gc_tensor self) |> with_tensor_gc

let relu6_ (self @ local) =
  stubs_relu6_ (globalize_gc_tensor self) |> with_tensor_gc

let relu_ (self @ local) =
  stubs_relu_ (globalize_gc_tensor self) |> with_tensor_gc

let relu_out ~(out @ local) (self @ local) =
  stubs_relu_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let remainder (self @ local) other =
  stubs_remainder (globalize_gc_tensor self) other |> with_tensor_gc

let remainder_ (self @ local) other =
  stubs_remainder_ (globalize_gc_tensor self) other |> with_tensor_gc

let remainder_scalar_out ~(out @ local) (self @ local) other =
  stubs_remainder_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let remainder_scalar_tensor self (other @ local) =
  stubs_remainder_scalar_tensor self (globalize_gc_tensor other) |> with_tensor_gc

let remainder_scalar_tensor_out ~(out @ local) self (other @ local) =
  stubs_remainder_scalar_tensor_out (globalize_gc_tensor out) self (globalize_gc_tensor other) |> with_tensor_gc

let remainder_tensor (self @ local) (other @ local) =
  stubs_remainder_tensor (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let remainder_tensor_ (self @ local) (other @ local) =
  stubs_remainder_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let remainder_tensor_out ~(out @ local) (self @ local) (other @ local) =
  stubs_remainder_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let renorm (self @ local) ~p ~dim ~maxnorm =
  stubs_renorm (globalize_gc_tensor self) p (Int64.of_int dim) maxnorm |> with_tensor_gc

let renorm_ (self @ local) ~p ~dim ~maxnorm =
  stubs_renorm_ (globalize_gc_tensor self) p (Int64.of_int dim) maxnorm |> with_tensor_gc

let renorm_out ~(out @ local) (self @ local) ~p ~dim ~maxnorm =
  stubs_renorm_out (globalize_gc_tensor out) (globalize_gc_tensor self) p (Int64.of_int dim) maxnorm |> with_tensor_gc

let repeat (self @ local) ~repeats =
  stubs_repeat (globalize_gc_tensor self) (List.map Int64.of_int repeats |> CArray.of_list int64_t |> CArray.start) (List.length repeats) |> with_tensor_gc

let repeat_interleave ~(repeats @ local) ~output_size =
  stubs_repeat_interleave (globalize_gc_tensor repeats)  (match output_size with | None -> Int64.zero | Some v -> Int64.of_int v) (match output_size with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let repeat_interleave_self_int (self @ local) ~repeats ~dim ~output_size =
  stubs_repeat_interleave_self_int (globalize_gc_tensor self) (Int64.of_int repeats)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)   (match output_size with | None -> Int64.zero | Some v -> Int64.of_int v) (match output_size with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let repeat_interleave_self_tensor (self @ local) ~(repeats @ local) ~dim ~output_size =
  stubs_repeat_interleave_self_tensor (globalize_gc_tensor self) (globalize_gc_tensor repeats)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)   (match output_size with | None -> Int64.zero | Some v -> Int64.of_int v) (match output_size with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let repeat_interleave_tensor_out ~(out @ local) ~(repeats @ local) ~output_size =
  stubs_repeat_interleave_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor repeats)  (match output_size with | None -> Int64.zero | Some v -> Int64.of_int v) (match output_size with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let repeat_out ~(out @ local) (self @ local) ~repeats =
  stubs_repeat_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int repeats |> CArray.of_list int64_t |> CArray.start) (List.length repeats) |> with_tensor_gc

let replication_pad1d (self @ local) ~padding =
  stubs_replication_pad1d (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let replication_pad1d_backward ~(grad_output @ local) (self @ local) ~padding =
  stubs_replication_pad1d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let replication_pad1d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~padding =
  stubs_replication_pad1d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let replication_pad1d_out ~(out @ local) (self @ local) ~padding =
  stubs_replication_pad1d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let replication_pad2d (self @ local) ~padding =
  stubs_replication_pad2d (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let replication_pad2d_backward ~(grad_output @ local) (self @ local) ~padding =
  stubs_replication_pad2d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let replication_pad2d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~padding =
  stubs_replication_pad2d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let replication_pad2d_out ~(out @ local) (self @ local) ~padding =
  stubs_replication_pad2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let replication_pad3d (self @ local) ~padding =
  stubs_replication_pad3d (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let replication_pad3d_backward ~(grad_output @ local) (self @ local) ~padding =
  stubs_replication_pad3d_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let replication_pad3d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~padding =
  stubs_replication_pad3d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let replication_pad3d_out ~(out @ local) (self @ local) ~padding =
  stubs_replication_pad3d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let requires_grad_ (self @ local) ~requires_grad =
  stubs_requires_grad_ (globalize_gc_tensor self) (if requires_grad then 1 else 0) |> with_tensor_gc

let reshape (self @ local) ~shape =
  stubs_reshape (globalize_gc_tensor self) (List.map Int64.of_int shape |> CArray.of_list int64_t |> CArray.start) (List.length shape) |> with_tensor_gc

let reshape_as (self @ local) (other @ local) =
  stubs_reshape_as (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let resize (self @ local) ~size =
  stubs_resize (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let resize_ (self @ local) ~size =
  stubs_resize_ (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let resize_as (self @ local) ~(the_template @ local) =
  stubs_resize_as (globalize_gc_tensor self) (globalize_gc_tensor the_template) |> with_tensor_gc

let resize_as_ (self @ local) ~(the_template @ local) =
  stubs_resize_as_ (globalize_gc_tensor self) (globalize_gc_tensor the_template) |> with_tensor_gc

let resize_as_out ~(out @ local) (self @ local) ~(the_template @ local) =
  stubs_resize_as_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor the_template) |> with_tensor_gc

let resize_as_sparse (self @ local) ~(the_template @ local) =
  stubs_resize_as_sparse (globalize_gc_tensor self) (globalize_gc_tensor the_template) |> with_tensor_gc

let resize_as_sparse_ (self @ local) ~(the_template @ local) =
  stubs_resize_as_sparse_ (globalize_gc_tensor self) (globalize_gc_tensor the_template) |> with_tensor_gc

let resize_as_sparse_out ~(out @ local) (self @ local) ~(the_template @ local) =
  stubs_resize_as_sparse_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor the_template) |> with_tensor_gc

let resize_out ~(out @ local) (self @ local) ~size =
  stubs_resize_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let resolve_conj (self @ local) =
  stubs_resolve_conj (globalize_gc_tensor self) |> with_tensor_gc

let resolve_neg (self @ local) =
  stubs_resolve_neg (globalize_gc_tensor self) |> with_tensor_gc

let retains_grad (self @ local) =
  stubs_retains_grad (globalize_gc_tensor self)

let rms_norm (input @ local) ~normalized_shape ~(weight @ local) ~eps =
  stubs_rms_norm (globalize_gc_tensor input) (List.map Int64.of_int normalized_shape |> CArray.of_list int64_t |> CArray.start) (List.length normalized_shape) (match (Base.Option.globalize globalize_gc_tensor weight) with | Some v -> v | None -> none_gc_tensor)  (Option.value eps ~default:0.0) (match eps with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let rnn_relu (input @ local) ~(hx @ local) ~(params @ local) ~has_biases ~num_layers ~dropout ~train ~bidirectional ~batch_first =
  let out__ = CArray.make raw_tensor 2 in
  stubs_rnn_relu (CArray.start out__) (globalize_gc_tensor input) (globalize_gc_tensor hx) (CArray.of_list gc_tensor (globalize_gc_tensor_list params) |> CArray.start) (Base.List.length params) (if has_biases then 1 else 0) (Int64.of_int num_layers) dropout (if train then 1 else 0) (if bidirectional then 1 else 0) (if batch_first then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let rnn_relu_cell (input @ local) ~(hx @ local) ~(w_ih @ local) ~(w_hh @ local) ~(b_ih @ local) ~(b_hh @ local) =
  stubs_rnn_relu_cell (globalize_gc_tensor input) (globalize_gc_tensor hx) (globalize_gc_tensor w_ih) (globalize_gc_tensor w_hh) (match (Base.Option.globalize globalize_gc_tensor b_ih) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor b_hh) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let rnn_relu_data ~(data @ local) ~(batch_sizes @ local) ~(hx @ local) ~(params @ local) ~has_biases ~num_layers ~dropout ~train ~bidirectional =
  let out__ = CArray.make raw_tensor 2 in
  stubs_rnn_relu_data (CArray.start out__) (globalize_gc_tensor data) (globalize_gc_tensor batch_sizes) (globalize_gc_tensor hx) (CArray.of_list gc_tensor (globalize_gc_tensor_list params) |> CArray.start) (Base.List.length params) (if has_biases then 1 else 0) (Int64.of_int num_layers) dropout (if train then 1 else 0) (if bidirectional then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let rnn_tanh (input @ local) ~(hx @ local) ~(params @ local) ~has_biases ~num_layers ~dropout ~train ~bidirectional ~batch_first =
  let out__ = CArray.make raw_tensor 2 in
  stubs_rnn_tanh (CArray.start out__) (globalize_gc_tensor input) (globalize_gc_tensor hx) (CArray.of_list gc_tensor (globalize_gc_tensor_list params) |> CArray.start) (Base.List.length params) (if has_biases then 1 else 0) (Int64.of_int num_layers) dropout (if train then 1 else 0) (if bidirectional then 1 else 0) (if batch_first then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let rnn_tanh_cell (input @ local) ~(hx @ local) ~(w_ih @ local) ~(w_hh @ local) ~(b_ih @ local) ~(b_hh @ local) =
  stubs_rnn_tanh_cell (globalize_gc_tensor input) (globalize_gc_tensor hx) (globalize_gc_tensor w_ih) (globalize_gc_tensor w_hh) (match (Base.Option.globalize globalize_gc_tensor b_ih) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor b_hh) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let rnn_tanh_data ~(data @ local) ~(batch_sizes @ local) ~(hx @ local) ~(params @ local) ~has_biases ~num_layers ~dropout ~train ~bidirectional =
  let out__ = CArray.make raw_tensor 2 in
  stubs_rnn_tanh_data (CArray.start out__) (globalize_gc_tensor data) (globalize_gc_tensor batch_sizes) (globalize_gc_tensor hx) (CArray.of_list gc_tensor (globalize_gc_tensor_list params) |> CArray.start) (Base.List.length params) (if has_biases then 1 else 0) (Int64.of_int num_layers) dropout (if train then 1 else 0) (if bidirectional then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let roll (self @ local) ~shifts ~dims =
  stubs_roll (globalize_gc_tensor self) (List.map Int64.of_int shifts |> CArray.of_list int64_t |> CArray.start) (List.length shifts) (List.map Int64.of_int dims |> CArray.of_list int64_t |> CArray.start) (List.length dims) |> with_tensor_gc

let roll_out ~(out @ local) (self @ local) ~shifts ~dims =
  stubs_roll_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int shifts |> CArray.of_list int64_t |> CArray.start) (List.length shifts) (List.map Int64.of_int dims |> CArray.of_list int64_t |> CArray.start) (List.length dims) |> with_tensor_gc

let rot90 (self @ local) ~k ~dims =
  stubs_rot90 (globalize_gc_tensor self) (Int64.of_int k) (List.map Int64.of_int dims |> CArray.of_list int64_t |> CArray.start) (List.length dims) |> with_tensor_gc

let rot90_out ~(out @ local) (self @ local) ~k ~dims =
  stubs_rot90_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int k) (List.map Int64.of_int dims |> CArray.of_list int64_t |> CArray.start) (List.length dims) |> with_tensor_gc

let round (self @ local) =
  stubs_round (globalize_gc_tensor self) |> with_tensor_gc

let round_ (self @ local) =
  stubs_round_ (globalize_gc_tensor self) |> with_tensor_gc

let round_decimals (self @ local) ~decimals =
  stubs_round_decimals (globalize_gc_tensor self) (Int64.of_int decimals) |> with_tensor_gc

let round_decimals_ (self @ local) ~decimals =
  stubs_round_decimals_ (globalize_gc_tensor self) (Int64.of_int decimals) |> with_tensor_gc

let round_decimals_out ~(out @ local) (self @ local) ~decimals =
  stubs_round_decimals_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int decimals) |> with_tensor_gc

let round_out ~(out @ local) (self @ local) =
  stubs_round_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let row_indices (self @ local) =
  stubs_row_indices (globalize_gc_tensor self) |> with_tensor_gc

let row_indices_copy (self @ local) =
  stubs_row_indices_copy (globalize_gc_tensor self) |> with_tensor_gc

let row_indices_copy_out ~(out @ local) (self @ local) =
  stubs_row_indices_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let row_stack (tensors @ local) =
  stubs_row_stack (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> with_tensor_gc

let row_stack_out ~(out @ local) (tensors @ local) =
  stubs_row_stack_out (globalize_gc_tensor out) (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> with_tensor_gc

let rrelu ?lower ?upper (self @ local) ~training =
  stubs_rrelu (globalize_gc_tensor self) (match lower with | Some v -> v | None -> none_scalar) (match upper with | Some v -> v | None -> none_scalar) (if training then 1 else 0) |> with_tensor_gc

let rrelu_ ?lower ?upper (self @ local) ~training =
  stubs_rrelu_ (globalize_gc_tensor self) (match lower with | Some v -> v | None -> none_scalar) (match upper with | Some v -> v | None -> none_scalar) (if training then 1 else 0) |> with_tensor_gc

let rrelu_with_noise ?lower ?upper (self @ local) ~(noise @ local) ~training =
  stubs_rrelu_with_noise (globalize_gc_tensor self) (globalize_gc_tensor noise) (match lower with | Some v -> v | None -> none_scalar) (match upper with | Some v -> v | None -> none_scalar) (if training then 1 else 0) |> with_tensor_gc

let rrelu_with_noise_ ?lower ?upper (self @ local) ~(noise @ local) ~training =
  stubs_rrelu_with_noise_ (globalize_gc_tensor self) (globalize_gc_tensor noise) (match lower with | Some v -> v | None -> none_scalar) (match upper with | Some v -> v | None -> none_scalar) (if training then 1 else 0) |> with_tensor_gc

let rrelu_with_noise_backward ~(grad_output @ local) (self @ local) ~(noise @ local) ~lower ~upper ~training ~self_is_result =
  stubs_rrelu_with_noise_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor noise) lower upper (if training then 1 else 0) (if self_is_result then 1 else 0) |> with_tensor_gc

let rrelu_with_noise_backward_out ~(out @ local) ~(grad_output @ local) (self @ local) ~(noise @ local) ~lower ~upper ~training ~self_is_result =
  stubs_rrelu_with_noise_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor noise) lower upper (if training then 1 else 0) (if self_is_result then 1 else 0) |> with_tensor_gc

let rrelu_with_noise_functional ?lower ?upper (self @ local) ~(noise @ local) ~training =
  let out__ = CArray.make raw_tensor 2 in
  stubs_rrelu_with_noise_functional (CArray.start out__) (globalize_gc_tensor self) (globalize_gc_tensor noise) (match lower with | Some v -> v | None -> none_scalar) (match upper with | Some v -> v | None -> none_scalar) (if training then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let rrelu_with_noise_out ?lower ?upper ~(out @ local) (self @ local) ~(noise @ local) ~training =
  stubs_rrelu_with_noise_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor noise) (match lower with | Some v -> v | None -> none_scalar) (match upper with | Some v -> v | None -> none_scalar) (if training then 1 else 0) |> with_tensor_gc

let rsqrt (self @ local) =
  stubs_rsqrt (globalize_gc_tensor self) |> with_tensor_gc

let rsqrt_ (self @ local) =
  stubs_rsqrt_ (globalize_gc_tensor self) |> with_tensor_gc

let rsqrt_out ~(out @ local) (self @ local) =
  stubs_rsqrt_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let rsub ?alpha (self @ local) (other @ local) =
  stubs_rsub (globalize_gc_tensor self) (globalize_gc_tensor other) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let rsub_scalar ?alpha (self @ local) other =
  stubs_rsub_scalar (globalize_gc_tensor self) other (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let rsub_scalar_out ?alpha ~(out @ local) (self @ local) other =
  stubs_rsub_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let rsub_tensor_out ?alpha ~(out @ local) (self @ local) (other @ local) =
  stubs_rsub_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let scalar_tensor ~s ~options =
  stubs_scalar_tensor s (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let scalar_tensor_out ~(out @ local) ~s =
  stubs_scalar_tensor_out (globalize_gc_tensor out) s |> with_tensor_gc

let scaled_dot_product_attention ~(query @ local) ~(key @ local) ~(value @ local) ~(attn_mask @ local) ~dropout_p ~is_causal ~scale ~enable_gqa =
  stubs_scaled_dot_product_attention (globalize_gc_tensor query) (globalize_gc_tensor key) (globalize_gc_tensor value) (match (Base.Option.globalize globalize_gc_tensor attn_mask) with | Some v -> v | None -> none_gc_tensor) dropout_p (if is_causal then 1 else 0)  (Option.value scale ~default:0.0) (match scale with | Some _ -> 0 | None -> 1)  (if enable_gqa then 1 else 0) |> with_tensor_gc

let scatter (self @ local) ~dim ~(index @ local) ~(src @ local) =
  stubs_scatter (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor src) |> with_tensor_gc

let scatter_ (self @ local) ~dim ~(index @ local) ~(src @ local) =
  stubs_scatter_ (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor src) |> with_tensor_gc

let scatter_add (self @ local) ~dim ~(index @ local) ~(src @ local) =
  stubs_scatter_add (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor src) |> with_tensor_gc

let scatter_add_ (self @ local) ~dim ~(index @ local) ~(src @ local) =
  stubs_scatter_add_ (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor src) |> with_tensor_gc

let scatter_add_out ~(out @ local) (self @ local) ~dim ~(index @ local) ~(src @ local) =
  stubs_scatter_add_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor src) |> with_tensor_gc

let scatter_reduce (self @ local) ~dim ~(index @ local) ~(src @ local) ~reduce =
  stubs_scatter_reduce (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor src) reduce |> with_tensor_gc

let scatter_reduce_ (self @ local) ~dim ~(index @ local) ~(src @ local) ~reduce =
  stubs_scatter_reduce_ (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor src) reduce |> with_tensor_gc

let scatter_reduce_out ~(out @ local) (self @ local) ~dim ~(index @ local) ~(src @ local) ~reduce =
  stubs_scatter_reduce_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor src) reduce |> with_tensor_gc

let scatter_src_out ~(out @ local) (self @ local) ~dim ~(index @ local) ~(src @ local) =
  stubs_scatter_src_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) (globalize_gc_tensor src) |> with_tensor_gc

let scatter_value (self @ local) ~dim ~(index @ local) ~value =
  stubs_scatter_value (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) value |> with_tensor_gc

let scatter_value_ (self @ local) ~dim ~(index @ local) ~value =
  stubs_scatter_value_ (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) value |> with_tensor_gc

let scatter_value_out ~(out @ local) (self @ local) ~dim ~(index @ local) ~value =
  stubs_scatter_value_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) value |> with_tensor_gc

let scatter_value_reduce (self @ local) ~dim ~(index @ local) ~value ~reduce =
  stubs_scatter_value_reduce (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) value reduce |> with_tensor_gc

let scatter_value_reduce_ (self @ local) ~dim ~(index @ local) ~value ~reduce =
  stubs_scatter_value_reduce_ (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) value reduce |> with_tensor_gc

let scatter_value_reduce_out ~(out @ local) (self @ local) ~dim ~(index @ local) ~value ~reduce =
  stubs_scatter_value_reduce_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (globalize_gc_tensor index) value reduce |> with_tensor_gc

let searchsorted ~(sorted_sequence @ local) (self @ local) ~out_int32 ~right ~side ~(sorter @ local) =
  stubs_searchsorted (globalize_gc_tensor sorted_sequence) (globalize_gc_tensor self) (if out_int32 then 1 else 0) (if right then 1 else 0)  (Option.value side ~default:"") (match side with | Some _ -> 0 | None -> 1)  (match (Base.Option.globalize globalize_gc_tensor sorter) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let searchsorted_scalar ~(sorted_sequence @ local) self ~out_int32 ~right ~side ~(sorter @ local) =
  stubs_searchsorted_scalar (globalize_gc_tensor sorted_sequence) self (if out_int32 then 1 else 0) (if right then 1 else 0)  (Option.value side ~default:"") (match side with | Some _ -> 0 | None -> 1)  (match (Base.Option.globalize globalize_gc_tensor sorter) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let searchsorted_scalar_out ~(out @ local) ~(sorted_sequence @ local) self ~out_int32 ~right ~side ~(sorter @ local) =
  stubs_searchsorted_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor sorted_sequence) self (if out_int32 then 1 else 0) (if right then 1 else 0)  (Option.value side ~default:"") (match side with | Some _ -> 0 | None -> 1)  (match (Base.Option.globalize globalize_gc_tensor sorter) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let searchsorted_tensor_out ~(out @ local) ~(sorted_sequence @ local) (self @ local) ~out_int32 ~right ~side ~(sorter @ local) =
  stubs_searchsorted_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor sorted_sequence) (globalize_gc_tensor self) (if out_int32 then 1 else 0) (if right then 1 else 0)  (Option.value side ~default:"") (match side with | Some _ -> 0 | None -> 1)  (match (Base.Option.globalize globalize_gc_tensor sorter) with | Some v -> v | None -> none_gc_tensor) |> with_tensor_gc

let segment_reduce ~(data @ local) ~reduce ~(lengths @ local) ~(indices @ local) ~(offsets @ local) ~axis ~unsafe ~initial =
  stubs_segment_reduce (globalize_gc_tensor data) reduce (match (Base.Option.globalize globalize_gc_tensor lengths) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor indices) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor offsets) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int axis) (if unsafe then 1 else 0) initial |> with_tensor_gc

let segment_reduce_out ~(out @ local) ~(data @ local) ~reduce ~(lengths @ local) ~(indices @ local) ~(offsets @ local) ~axis ~unsafe ~initial =
  stubs_segment_reduce_out (globalize_gc_tensor out) (globalize_gc_tensor data) reduce (match (Base.Option.globalize globalize_gc_tensor lengths) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor indices) with | Some v -> v | None -> none_gc_tensor) (match (Base.Option.globalize globalize_gc_tensor offsets) with | Some v -> v | None -> none_gc_tensor) (Int64.of_int axis) (if unsafe then 1 else 0) initial |> with_tensor_gc

let select (self @ local) ~dim ~index =
  stubs_select (globalize_gc_tensor self) (Int64.of_int dim) (Int64.of_int index) |> with_tensor_gc

let select_backward ~(grad_output @ local) ~input_sizes ~dim ~index =
  stubs_select_backward (globalize_gc_tensor grad_output) (List.map Int64.of_int input_sizes |> CArray.of_list int64_t |> CArray.start) (List.length input_sizes) (Int64.of_int dim) (Int64.of_int index) |> with_tensor_gc

let select_backward_out ~(out @ local) ~(grad_output @ local) ~input_sizes ~dim ~index =
  stubs_select_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (List.map Int64.of_int input_sizes |> CArray.of_list int64_t |> CArray.start) (List.length input_sizes) (Int64.of_int dim) (Int64.of_int index) |> with_tensor_gc

let select_copy (self @ local) ~dim ~index =
  stubs_select_copy (globalize_gc_tensor self) (Int64.of_int dim) (Int64.of_int index) |> with_tensor_gc

let select_copy_int_out ~(out @ local) (self @ local) ~dim ~index =
  stubs_select_copy_int_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (Int64.of_int index) |> with_tensor_gc

let select_scatter (self @ local) ~(src @ local) ~dim ~index =
  stubs_select_scatter (globalize_gc_tensor self) (globalize_gc_tensor src) (Int64.of_int dim) (Int64.of_int index) |> with_tensor_gc

let select_scatter_out ~(out @ local) (self @ local) ~(src @ local) ~dim ~index =
  stubs_select_scatter_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor src) (Int64.of_int dim) (Int64.of_int index) |> with_tensor_gc

let selu (self @ local) =
  stubs_selu (globalize_gc_tensor self) |> with_tensor_gc

let selu_ (self @ local) =
  stubs_selu_ (globalize_gc_tensor self) |> with_tensor_gc

let set (self @ local) =
  stubs_set (globalize_gc_tensor self) |> with_tensor_gc

let set_ (self @ local) =
  stubs_set_ (globalize_gc_tensor self) |> with_tensor_gc

let set_out ~(out @ local) (self @ local) =
  stubs_set_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let set_requires_grad (self @ local) ~r =
  stubs_set_requires_grad (globalize_gc_tensor self) (if r then 1 else 0) |> with_tensor_gc

let set_source_tensor (self @ local) ~(source @ local) =
  stubs_set_source_tensor (globalize_gc_tensor self) (globalize_gc_tensor source) |> with_tensor_gc

let set_source_tensor_ (self @ local) ~(source @ local) =
  stubs_set_source_tensor_ (globalize_gc_tensor self) (globalize_gc_tensor source) |> with_tensor_gc

let set_source_tensor_out ~(out @ local) (self @ local) ~(source @ local) =
  stubs_set_source_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor source) |> with_tensor_gc

let set_source_tensor_storage_offset_ (self @ local) ~(source @ local) ~storage_offset ~size ~stride =
  stubs_set_source_tensor_storage_offset_ (globalize_gc_tensor self) (globalize_gc_tensor source) (Int64.of_int storage_offset) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) |> with_tensor_gc

let sgn (self @ local) =
  stubs_sgn (globalize_gc_tensor self) |> with_tensor_gc

let sgn_ (self @ local) =
  stubs_sgn_ (globalize_gc_tensor self) |> with_tensor_gc

let sgn_out ~(out @ local) (self @ local) =
  stubs_sgn_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let sigmoid (self @ local) =
  stubs_sigmoid (globalize_gc_tensor self) |> with_tensor_gc

let sigmoid_ (self @ local) =
  stubs_sigmoid_ (globalize_gc_tensor self) |> with_tensor_gc

let sigmoid_backward ~(grad_output @ local) ~(output @ local) =
  stubs_sigmoid_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor output) |> with_tensor_gc

let sigmoid_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) ~(output @ local) =
  stubs_sigmoid_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor output) |> with_tensor_gc

let sigmoid_out ~(out @ local) (self @ local) =
  stubs_sigmoid_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let sign (self @ local) =
  stubs_sign (globalize_gc_tensor self) |> with_tensor_gc

let sign_ (self @ local) =
  stubs_sign_ (globalize_gc_tensor self) |> with_tensor_gc

let sign_out ~(out @ local) (self @ local) =
  stubs_sign_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let signbit (self @ local) =
  stubs_signbit (globalize_gc_tensor self) |> with_tensor_gc

let signbit_out ~(out @ local) (self @ local) =
  stubs_signbit_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let silu (self @ local) =
  stubs_silu (globalize_gc_tensor self) |> with_tensor_gc

let silu_ (self @ local) =
  stubs_silu_ (globalize_gc_tensor self) |> with_tensor_gc

let silu_backward ~(grad_output @ local) (self @ local) =
  stubs_silu_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) |> with_tensor_gc

let silu_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) =
  stubs_silu_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) |> with_tensor_gc

let silu_out ~(out @ local) (self @ local) =
  stubs_silu_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let sin (self @ local) =
  stubs_sin (globalize_gc_tensor self) |> with_tensor_gc

let sin_ (self @ local) =
  stubs_sin_ (globalize_gc_tensor self) |> with_tensor_gc

let sin_out ~(out @ local) (self @ local) =
  stubs_sin_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let sinc (self @ local) =
  stubs_sinc (globalize_gc_tensor self) |> with_tensor_gc

let sinc_ (self @ local) =
  stubs_sinc_ (globalize_gc_tensor self) |> with_tensor_gc

let sinc_out ~(out @ local) (self @ local) =
  stubs_sinc_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let sinh (self @ local) =
  stubs_sinh (globalize_gc_tensor self) |> with_tensor_gc

let sinh_ (self @ local) =
  stubs_sinh_ (globalize_gc_tensor self) |> with_tensor_gc

let sinh_out ~(out @ local) (self @ local) =
  stubs_sinh_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let size (self @ local) ~dim =
  stubs_size (globalize_gc_tensor self) (Int64.of_int dim)

let slice (self @ local) ~dim ~start ~end_ ~step =
  stubs_slice (globalize_gc_tensor self) (Int64.of_int dim)  (match start with | None -> Int64.zero | Some v -> Int64.of_int v) (match start with | Some _ -> 0 | None -> 1)   (match end_ with | None -> Int64.zero | Some v -> Int64.of_int v) (match end_ with | Some _ -> 0 | None -> 1)  (Int64.of_int step) |> with_tensor_gc

let slice_backward ~(grad_output @ local) ~input_sizes ~dim ~start ~end_ ~step =
  stubs_slice_backward (globalize_gc_tensor grad_output) (List.map Int64.of_int input_sizes |> CArray.of_list int64_t |> CArray.start) (List.length input_sizes) (Int64.of_int dim) (Int64.of_int start) (Int64.of_int end_) (Int64.of_int step) |> with_tensor_gc

let slice_backward_out ~(out @ local) ~(grad_output @ local) ~input_sizes ~dim ~start ~end_ ~step =
  stubs_slice_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad_output) (List.map Int64.of_int input_sizes |> CArray.of_list int64_t |> CArray.start) (List.length input_sizes) (Int64.of_int dim) (Int64.of_int start) (Int64.of_int end_) (Int64.of_int step) |> with_tensor_gc

let slice_copy (self @ local) ~dim ~start ~end_ ~step =
  stubs_slice_copy (globalize_gc_tensor self) (Int64.of_int dim)  (match start with | None -> Int64.zero | Some v -> Int64.of_int v) (match start with | Some _ -> 0 | None -> 1)   (match end_ with | None -> Int64.zero | Some v -> Int64.of_int v) (match end_ with | Some _ -> 0 | None -> 1)  (Int64.of_int step) |> with_tensor_gc

let slice_copy_tensor_out ~(out @ local) (self @ local) ~dim ~start ~end_ ~step =
  stubs_slice_copy_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim)  (match start with | None -> Int64.zero | Some v -> Int64.of_int v) (match start with | Some _ -> 0 | None -> 1)   (match end_ with | None -> Int64.zero | Some v -> Int64.of_int v) (match end_ with | Some _ -> 0 | None -> 1)  (Int64.of_int step) |> with_tensor_gc

let slice_inverse (self @ local) ~(src @ local) ~dim ~start ~end_ ~step =
  stubs_slice_inverse (globalize_gc_tensor self) (globalize_gc_tensor src) (Int64.of_int dim)  (match start with | None -> Int64.zero | Some v -> Int64.of_int v) (match start with | Some _ -> 0 | None -> 1)   (match end_ with | None -> Int64.zero | Some v -> Int64.of_int v) (match end_ with | Some _ -> 0 | None -> 1)  (Int64.of_int step) |> with_tensor_gc

let slice_scatter (self @ local) ~(src @ local) ~dim ~start ~end_ ~step =
  stubs_slice_scatter (globalize_gc_tensor self) (globalize_gc_tensor src) (Int64.of_int dim)  (match start with | None -> Int64.zero | Some v -> Int64.of_int v) (match start with | Some _ -> 0 | None -> 1)   (match end_ with | None -> Int64.zero | Some v -> Int64.of_int v) (match end_ with | Some _ -> 0 | None -> 1)  (Int64.of_int step) |> with_tensor_gc

let slice_scatter_out ~(out @ local) (self @ local) ~(src @ local) ~dim ~start ~end_ ~step =
  stubs_slice_scatter_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor src) (Int64.of_int dim)  (match start with | None -> Int64.zero | Some v -> Int64.of_int v) (match start with | Some _ -> 0 | None -> 1)   (match end_ with | None -> Int64.zero | Some v -> Int64.of_int v) (match end_ with | Some _ -> 0 | None -> 1)  (Int64.of_int step) |> with_tensor_gc

let slogdet (self @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_slogdet (CArray.start out__) (globalize_gc_tensor self);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let slogdet_out ~(sign @ local) ~(logabsdet @ local) (self @ local) =
  let out__ = CArray.make raw_tensor 2 in
  stubs_slogdet_out (CArray.start out__) (globalize_gc_tensor sign) (globalize_gc_tensor logabsdet) (globalize_gc_tensor self);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let slow_conv3d (self @ local) ~(weight @ local) ~kernel_size ~(bias @ local) ~stride ~padding =
  stubs_slow_conv3d (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let slow_conv3d_out ~(out @ local) (self @ local) ~(weight @ local) ~kernel_size ~(bias @ local) ~stride ~padding =
  stubs_slow_conv3d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) |> with_tensor_gc

let slow_conv_dilated2d (self @ local) ~(weight @ local) ~kernel_size ~(bias @ local) ~stride ~padding ~dilation =
  stubs_slow_conv_dilated2d (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) |> with_tensor_gc

let slow_conv_dilated2d_out ~(out @ local) (self @ local) ~(weight @ local) ~kernel_size ~(bias @ local) ~stride ~padding ~dilation =
  stubs_slow_conv_dilated2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) |> with_tensor_gc

let slow_conv_dilated3d (self @ local) ~(weight @ local) ~kernel_size ~(bias @ local) ~stride ~padding ~dilation =
  stubs_slow_conv_dilated3d (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) |> with_tensor_gc

let slow_conv_dilated3d_out ~(out @ local) (self @ local) ~(weight @ local) ~kernel_size ~(bias @ local) ~stride ~padding ~dilation =
  stubs_slow_conv_dilated3d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) |> with_tensor_gc

let slow_conv_transpose2d (self @ local) ~(weight @ local) ~kernel_size ~(bias @ local) ~stride ~padding ~output_padding ~dilation =
  stubs_slow_conv_transpose2d (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) |> with_tensor_gc

let slow_conv_transpose2d_out ~(out @ local) (self @ local) ~(weight @ local) ~kernel_size ~(bias @ local) ~stride ~padding ~output_padding ~dilation =
  stubs_slow_conv_transpose2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) |> with_tensor_gc

let slow_conv_transpose3d (self @ local) ~(weight @ local) ~kernel_size ~(bias @ local) ~stride ~padding ~output_padding ~dilation =
  stubs_slow_conv_transpose3d (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) |> with_tensor_gc

let slow_conv_transpose3d_out ~(out @ local) (self @ local) ~(weight @ local) ~kernel_size ~(bias @ local) ~stride ~padding ~output_padding ~dilation =
  stubs_slow_conv_transpose3d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor weight) (List.map Int64.of_int kernel_size |> CArray.of_list int64_t |> CArray.start) (List.length kernel_size) (match (Base.Option.globalize globalize_gc_tensor bias) with | Some v -> v | None -> none_gc_tensor) (List.map Int64.of_int stride |> CArray.of_list int64_t |> CArray.start) (List.length stride) (List.map Int64.of_int padding |> CArray.of_list int64_t |> CArray.start) (List.length padding) (List.map Int64.of_int output_padding |> CArray.of_list int64_t |> CArray.start) (List.length output_padding) (List.map Int64.of_int dilation |> CArray.of_list int64_t |> CArray.start) (List.length dilation) |> with_tensor_gc

let smm (self @ local) ~(mat2 @ local) =
  stubs_smm (globalize_gc_tensor self) (globalize_gc_tensor mat2) |> with_tensor_gc

let smooth_l1_loss (self @ local) ~(target @ local) ~reduction ~beta =
  stubs_smooth_l1_loss (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) beta |> with_tensor_gc

let smooth_l1_loss_backward ~(grad_output @ local) (self @ local) ~(target @ local) ~reduction ~beta =
  stubs_smooth_l1_loss_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) beta |> with_tensor_gc

let smooth_l1_loss_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~(target @ local) ~reduction ~beta =
  stubs_smooth_l1_loss_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) beta |> with_tensor_gc

let smooth_l1_loss_out ~(out @ local) (self @ local) ~(target @ local) ~reduction ~beta =
  stubs_smooth_l1_loss_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) beta |> with_tensor_gc

let soft_margin_loss (self @ local) ~(target @ local) ~reduction =
  stubs_soft_margin_loss (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let soft_margin_loss_backward ~(grad_output @ local) (self @ local) ~(target @ local) ~reduction =
  stubs_soft_margin_loss_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let soft_margin_loss_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~(target @ local) ~reduction =
  stubs_soft_margin_loss_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let soft_margin_loss_out ~(out @ local) (self @ local) ~(target @ local) ~reduction =
  stubs_soft_margin_loss_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor target) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let softmax (self @ local) ~dim ~dtype =
  stubs_softmax (globalize_gc_tensor self) (Int64.of_int dim) (Kind.packed_to_int dtype) |> with_tensor_gc

let softmax_int_out ~(out @ local) (self @ local) ~dim ~dtype =
  stubs_softmax_int_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) (Kind.packed_to_int dtype) |> with_tensor_gc

let softplus ?beta ?threshold (self @ local) =
  stubs_softplus (globalize_gc_tensor self) (match beta with | Some v -> v | None -> none_scalar) (match threshold with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let softplus_backward ~(grad_output @ local) (self @ local) ~beta ~threshold =
  stubs_softplus_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) beta threshold |> with_tensor_gc

let softplus_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~beta ~threshold =
  stubs_softplus_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) beta threshold |> with_tensor_gc

let softplus_out ?beta ?threshold ~(out @ local) (self @ local) =
  stubs_softplus_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match beta with | Some v -> v | None -> none_scalar) (match threshold with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let softshrink ?lambd (self @ local) =
  stubs_softshrink (globalize_gc_tensor self) (match lambd with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let softshrink_backward ~(grad_output @ local) (self @ local) ~lambd =
  stubs_softshrink_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) lambd |> with_tensor_gc

let softshrink_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~lambd =
  stubs_softshrink_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) lambd |> with_tensor_gc

let softshrink_out ?lambd ~(out @ local) (self @ local) =
  stubs_softshrink_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match lambd with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let sort (self @ local) ~dim ~descending =
  let out__ = CArray.make raw_tensor 2 in
  stubs_sort (CArray.start out__) (globalize_gc_tensor self) (Int64.of_int dim) (if descending then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let sort_stable (self @ local) ~stable ~dim ~descending =
  let out__ = CArray.make raw_tensor 2 in
  stubs_sort_stable (CArray.start out__) (globalize_gc_tensor self) (if stable then 1 else 0) (Int64.of_int dim) (if descending then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let sort_values ~(values @ local) ~(indices @ local) (self @ local) ~dim ~descending =
  let out__ = CArray.make raw_tensor 2 in
  stubs_sort_values (CArray.start out__) (globalize_gc_tensor values) (globalize_gc_tensor indices) (globalize_gc_tensor self) (Int64.of_int dim) (if descending then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let sort_values_stable ~(values @ local) ~(indices @ local) (self @ local) ~stable ~dim ~descending =
  let out__ = CArray.make raw_tensor 2 in
  stubs_sort_values_stable (CArray.start out__) (globalize_gc_tensor values) (globalize_gc_tensor indices) (globalize_gc_tensor self) (if stable then 1 else 0) (Int64.of_int dim) (if descending then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let sparse_bsc_tensor ~(ccol_indices @ local) ~(row_indices @ local) ~(values @ local) ~options =
  stubs_sparse_bsc_tensor (globalize_gc_tensor ccol_indices) (globalize_gc_tensor row_indices) (globalize_gc_tensor values) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let sparse_bsc_tensor_ccol_row_value_size ~(ccol_indices @ local) ~(row_indices @ local) ~(values @ local) ~size ~options =
  stubs_sparse_bsc_tensor_ccol_row_value_size (globalize_gc_tensor ccol_indices) (globalize_gc_tensor row_indices) (globalize_gc_tensor values) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let sparse_bsr_tensor ~(crow_indices @ local) ~(col_indices @ local) ~(values @ local) ~options =
  stubs_sparse_bsr_tensor (globalize_gc_tensor crow_indices) (globalize_gc_tensor col_indices) (globalize_gc_tensor values) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let sparse_bsr_tensor_crow_col_value_size ~(crow_indices @ local) ~(col_indices @ local) ~(values @ local) ~size ~options =
  stubs_sparse_bsr_tensor_crow_col_value_size (globalize_gc_tensor crow_indices) (globalize_gc_tensor col_indices) (globalize_gc_tensor values) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let sparse_compressed_tensor ~(compressed_indices @ local) ~(plain_indices @ local) ~(values @ local) ~options =
  stubs_sparse_compressed_tensor (globalize_gc_tensor compressed_indices) (globalize_gc_tensor plain_indices) (globalize_gc_tensor values) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let sparse_compressed_tensor_comp_plain_value_size ~(compressed_indices @ local) ~(plain_indices @ local) ~(values @ local) ~size ~options =
  stubs_sparse_compressed_tensor_comp_plain_value_size (globalize_gc_tensor compressed_indices) (globalize_gc_tensor plain_indices) (globalize_gc_tensor values) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let sparse_coo_tensor ~size ~options =
  stubs_sparse_coo_tensor (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let sparse_coo_tensor_indices ~(indices @ local) ~(values @ local) ~options ~is_coalesced =
  stubs_sparse_coo_tensor_indices (globalize_gc_tensor indices) (globalize_gc_tensor values) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) (if is_coalesced then 1 else 0) |> with_tensor_gc

let sparse_coo_tensor_indices_size ~(indices @ local) ~(values @ local) ~size ~options ~is_coalesced =
  stubs_sparse_coo_tensor_indices_size (globalize_gc_tensor indices) (globalize_gc_tensor values) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) (if is_coalesced then 1 else 0) |> with_tensor_gc

let sparse_coo_tensor_size_out ~(out @ local) ~size =
  stubs_sparse_coo_tensor_size_out (globalize_gc_tensor out) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let sparse_csc_tensor ~(ccol_indices @ local) ~(row_indices @ local) ~(values @ local) ~options =
  stubs_sparse_csc_tensor (globalize_gc_tensor ccol_indices) (globalize_gc_tensor row_indices) (globalize_gc_tensor values) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let sparse_csc_tensor_ccol_row_value_size ~(ccol_indices @ local) ~(row_indices @ local) ~(values @ local) ~size ~options =
  stubs_sparse_csc_tensor_ccol_row_value_size (globalize_gc_tensor ccol_indices) (globalize_gc_tensor row_indices) (globalize_gc_tensor values) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let sparse_csr_tensor ~(crow_indices @ local) ~(col_indices @ local) ~(values @ local) ~options =
  stubs_sparse_csr_tensor (globalize_gc_tensor crow_indices) (globalize_gc_tensor col_indices) (globalize_gc_tensor values) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let sparse_csr_tensor_crow_col_value_size ~(crow_indices @ local) ~(col_indices @ local) ~(values @ local) ~size ~options =
  stubs_sparse_csr_tensor_crow_col_value_size (globalize_gc_tensor crow_indices) (globalize_gc_tensor col_indices) (globalize_gc_tensor values) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let sparse_dim (self @ local) =
  stubs_sparse_dim (globalize_gc_tensor self)

let sparse_mask (self @ local) ~(mask @ local) =
  stubs_sparse_mask (globalize_gc_tensor self) (globalize_gc_tensor mask) |> with_tensor_gc

let sparse_mask_out ~(out @ local) (self @ local) ~(mask @ local) =
  stubs_sparse_mask_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mask) |> with_tensor_gc

let sparse_resize (self @ local) ~size ~sparse_dim ~dense_dim =
  stubs_sparse_resize (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Int64.of_int sparse_dim) (Int64.of_int dense_dim) |> with_tensor_gc

let sparse_resize_ (self @ local) ~size ~sparse_dim ~dense_dim =
  stubs_sparse_resize_ (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Int64.of_int sparse_dim) (Int64.of_int dense_dim) |> with_tensor_gc

let sparse_resize_and_clear (self @ local) ~size ~sparse_dim ~dense_dim =
  stubs_sparse_resize_and_clear (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Int64.of_int sparse_dim) (Int64.of_int dense_dim) |> with_tensor_gc

let sparse_resize_and_clear_ (self @ local) ~size ~sparse_dim ~dense_dim =
  stubs_sparse_resize_and_clear_ (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Int64.of_int sparse_dim) (Int64.of_int dense_dim) |> with_tensor_gc

let sparse_resize_and_clear_out ~(out @ local) (self @ local) ~size ~sparse_dim ~dense_dim =
  stubs_sparse_resize_and_clear_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Int64.of_int sparse_dim) (Int64.of_int dense_dim) |> with_tensor_gc

let sparse_resize_out ~(out @ local) (self @ local) ~size ~sparse_dim ~dense_dim =
  stubs_sparse_resize_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Int64.of_int sparse_dim) (Int64.of_int dense_dim) |> with_tensor_gc

let sparse_sampled_addmm ?beta ?alpha (self @ local) ~(mat1 @ local) ~(mat2 @ local) =
  stubs_sparse_sampled_addmm (globalize_gc_tensor self) (globalize_gc_tensor mat1) (globalize_gc_tensor mat2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let sparse_sampled_addmm_out ?beta ?alpha ~(out @ local) (self @ local) ~(mat1 @ local) ~(mat2 @ local) =
  stubs_sparse_sampled_addmm_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mat1) (globalize_gc_tensor mat2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let special_airy_ai ~(x @ local) =
  stubs_special_airy_ai (globalize_gc_tensor x) |> with_tensor_gc

let special_airy_ai_out ~(out @ local) ~(x @ local) =
  stubs_special_airy_ai_out (globalize_gc_tensor out) (globalize_gc_tensor x) |> with_tensor_gc

let special_bessel_j0 (self @ local) =
  stubs_special_bessel_j0 (globalize_gc_tensor self) |> with_tensor_gc

let special_bessel_j0_out ~(out @ local) (self @ local) =
  stubs_special_bessel_j0_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_bessel_j1 (self @ local) =
  stubs_special_bessel_j1 (globalize_gc_tensor self) |> with_tensor_gc

let special_bessel_j1_out ~(out @ local) (self @ local) =
  stubs_special_bessel_j1_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_bessel_y0 (self @ local) =
  stubs_special_bessel_y0 (globalize_gc_tensor self) |> with_tensor_gc

let special_bessel_y0_out ~(out @ local) (self @ local) =
  stubs_special_bessel_y0_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_bessel_y1 (self @ local) =
  stubs_special_bessel_y1 (globalize_gc_tensor self) |> with_tensor_gc

let special_bessel_y1_out ~(out @ local) (self @ local) =
  stubs_special_bessel_y1_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_chebyshev_polynomial_t ~(x @ local) ~(n @ local) =
  stubs_special_chebyshev_polynomial_t (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_chebyshev_polynomial_t_n_scalar ~(x @ local) ~n =
  stubs_special_chebyshev_polynomial_t_n_scalar (globalize_gc_tensor x) n |> with_tensor_gc

let special_chebyshev_polynomial_t_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  stubs_special_chebyshev_polynomial_t_n_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor x) n |> with_tensor_gc

let special_chebyshev_polynomial_t_out ~(out @ local) ~(x @ local) ~(n @ local) =
  stubs_special_chebyshev_polynomial_t_out (globalize_gc_tensor out) (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_chebyshev_polynomial_t_x_scalar ~x ~(n @ local) =
  stubs_special_chebyshev_polynomial_t_x_scalar x (globalize_gc_tensor n) |> with_tensor_gc

let special_chebyshev_polynomial_t_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  stubs_special_chebyshev_polynomial_t_x_scalar_out (globalize_gc_tensor out) x (globalize_gc_tensor n) |> with_tensor_gc

let special_chebyshev_polynomial_u ~(x @ local) ~(n @ local) =
  stubs_special_chebyshev_polynomial_u (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_chebyshev_polynomial_u_n_scalar ~(x @ local) ~n =
  stubs_special_chebyshev_polynomial_u_n_scalar (globalize_gc_tensor x) n |> with_tensor_gc

let special_chebyshev_polynomial_u_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  stubs_special_chebyshev_polynomial_u_n_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor x) n |> with_tensor_gc

let special_chebyshev_polynomial_u_out ~(out @ local) ~(x @ local) ~(n @ local) =
  stubs_special_chebyshev_polynomial_u_out (globalize_gc_tensor out) (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_chebyshev_polynomial_u_x_scalar ~x ~(n @ local) =
  stubs_special_chebyshev_polynomial_u_x_scalar x (globalize_gc_tensor n) |> with_tensor_gc

let special_chebyshev_polynomial_u_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  stubs_special_chebyshev_polynomial_u_x_scalar_out (globalize_gc_tensor out) x (globalize_gc_tensor n) |> with_tensor_gc

let special_chebyshev_polynomial_v ~(x @ local) ~(n @ local) =
  stubs_special_chebyshev_polynomial_v (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_chebyshev_polynomial_v_n_scalar ~(x @ local) ~n =
  stubs_special_chebyshev_polynomial_v_n_scalar (globalize_gc_tensor x) n |> with_tensor_gc

let special_chebyshev_polynomial_v_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  stubs_special_chebyshev_polynomial_v_n_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor x) n |> with_tensor_gc

let special_chebyshev_polynomial_v_out ~(out @ local) ~(x @ local) ~(n @ local) =
  stubs_special_chebyshev_polynomial_v_out (globalize_gc_tensor out) (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_chebyshev_polynomial_v_x_scalar ~x ~(n @ local) =
  stubs_special_chebyshev_polynomial_v_x_scalar x (globalize_gc_tensor n) |> with_tensor_gc

let special_chebyshev_polynomial_v_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  stubs_special_chebyshev_polynomial_v_x_scalar_out (globalize_gc_tensor out) x (globalize_gc_tensor n) |> with_tensor_gc

let special_chebyshev_polynomial_w ~(x @ local) ~(n @ local) =
  stubs_special_chebyshev_polynomial_w (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_chebyshev_polynomial_w_n_scalar ~(x @ local) ~n =
  stubs_special_chebyshev_polynomial_w_n_scalar (globalize_gc_tensor x) n |> with_tensor_gc

let special_chebyshev_polynomial_w_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  stubs_special_chebyshev_polynomial_w_n_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor x) n |> with_tensor_gc

let special_chebyshev_polynomial_w_out ~(out @ local) ~(x @ local) ~(n @ local) =
  stubs_special_chebyshev_polynomial_w_out (globalize_gc_tensor out) (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_chebyshev_polynomial_w_x_scalar ~x ~(n @ local) =
  stubs_special_chebyshev_polynomial_w_x_scalar x (globalize_gc_tensor n) |> with_tensor_gc

let special_chebyshev_polynomial_w_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  stubs_special_chebyshev_polynomial_w_x_scalar_out (globalize_gc_tensor out) x (globalize_gc_tensor n) |> with_tensor_gc

let special_digamma (self @ local) =
  stubs_special_digamma (globalize_gc_tensor self) |> with_tensor_gc

let special_digamma_out ~(out @ local) (self @ local) =
  stubs_special_digamma_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_entr (self @ local) =
  stubs_special_entr (globalize_gc_tensor self) |> with_tensor_gc

let special_entr_out ~(out @ local) (self @ local) =
  stubs_special_entr_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_erf (self @ local) =
  stubs_special_erf (globalize_gc_tensor self) |> with_tensor_gc

let special_erf_out ~(out @ local) (self @ local) =
  stubs_special_erf_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_erfc (self @ local) =
  stubs_special_erfc (globalize_gc_tensor self) |> with_tensor_gc

let special_erfc_out ~(out @ local) (self @ local) =
  stubs_special_erfc_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_erfcx (self @ local) =
  stubs_special_erfcx (globalize_gc_tensor self) |> with_tensor_gc

let special_erfcx_out ~(out @ local) (self @ local) =
  stubs_special_erfcx_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_erfinv (self @ local) =
  stubs_special_erfinv (globalize_gc_tensor self) |> with_tensor_gc

let special_erfinv_out ~(out @ local) (self @ local) =
  stubs_special_erfinv_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_exp2 (self @ local) =
  stubs_special_exp2 (globalize_gc_tensor self) |> with_tensor_gc

let special_exp2_out ~(out @ local) (self @ local) =
  stubs_special_exp2_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_expit (self @ local) =
  stubs_special_expit (globalize_gc_tensor self) |> with_tensor_gc

let special_expit_out ~(out @ local) (self @ local) =
  stubs_special_expit_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_expm1 (self @ local) =
  stubs_special_expm1 (globalize_gc_tensor self) |> with_tensor_gc

let special_expm1_out ~(out @ local) (self @ local) =
  stubs_special_expm1_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_gammainc (self @ local) (other @ local) =
  stubs_special_gammainc (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let special_gammainc_out ~(out @ local) (self @ local) (other @ local) =
  stubs_special_gammainc_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let special_gammaincc (self @ local) (other @ local) =
  stubs_special_gammaincc (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let special_gammaincc_out ~(out @ local) (self @ local) (other @ local) =
  stubs_special_gammaincc_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let special_gammaln (self @ local) =
  stubs_special_gammaln (globalize_gc_tensor self) |> with_tensor_gc

let special_gammaln_out ~(out @ local) (self @ local) =
  stubs_special_gammaln_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_hermite_polynomial_h ~(x @ local) ~(n @ local) =
  stubs_special_hermite_polynomial_h (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_hermite_polynomial_h_n_scalar ~(x @ local) ~n =
  stubs_special_hermite_polynomial_h_n_scalar (globalize_gc_tensor x) n |> with_tensor_gc

let special_hermite_polynomial_h_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  stubs_special_hermite_polynomial_h_n_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor x) n |> with_tensor_gc

let special_hermite_polynomial_h_out ~(out @ local) ~(x @ local) ~(n @ local) =
  stubs_special_hermite_polynomial_h_out (globalize_gc_tensor out) (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_hermite_polynomial_h_x_scalar ~x ~(n @ local) =
  stubs_special_hermite_polynomial_h_x_scalar x (globalize_gc_tensor n) |> with_tensor_gc

let special_hermite_polynomial_h_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  stubs_special_hermite_polynomial_h_x_scalar_out (globalize_gc_tensor out) x (globalize_gc_tensor n) |> with_tensor_gc

let special_hermite_polynomial_he ~(x @ local) ~(n @ local) =
  stubs_special_hermite_polynomial_he (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_hermite_polynomial_he_n_scalar ~(x @ local) ~n =
  stubs_special_hermite_polynomial_he_n_scalar (globalize_gc_tensor x) n |> with_tensor_gc

let special_hermite_polynomial_he_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  stubs_special_hermite_polynomial_he_n_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor x) n |> with_tensor_gc

let special_hermite_polynomial_he_out ~(out @ local) ~(x @ local) ~(n @ local) =
  stubs_special_hermite_polynomial_he_out (globalize_gc_tensor out) (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_hermite_polynomial_he_x_scalar ~x ~(n @ local) =
  stubs_special_hermite_polynomial_he_x_scalar x (globalize_gc_tensor n) |> with_tensor_gc

let special_hermite_polynomial_he_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  stubs_special_hermite_polynomial_he_x_scalar_out (globalize_gc_tensor out) x (globalize_gc_tensor n) |> with_tensor_gc

let special_i0 (self @ local) =
  stubs_special_i0 (globalize_gc_tensor self) |> with_tensor_gc

let special_i0_out ~(out @ local) (self @ local) =
  stubs_special_i0_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_i0e (self @ local) =
  stubs_special_i0e (globalize_gc_tensor self) |> with_tensor_gc

let special_i0e_out ~(out @ local) (self @ local) =
  stubs_special_i0e_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_i1 (self @ local) =
  stubs_special_i1 (globalize_gc_tensor self) |> with_tensor_gc

let special_i1_out ~(out @ local) (self @ local) =
  stubs_special_i1_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_i1e (self @ local) =
  stubs_special_i1e (globalize_gc_tensor self) |> with_tensor_gc

let special_i1e_out ~(out @ local) (self @ local) =
  stubs_special_i1e_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_laguerre_polynomial_l ~(x @ local) ~(n @ local) =
  stubs_special_laguerre_polynomial_l (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_laguerre_polynomial_l_n_scalar ~(x @ local) ~n =
  stubs_special_laguerre_polynomial_l_n_scalar (globalize_gc_tensor x) n |> with_tensor_gc

let special_laguerre_polynomial_l_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  stubs_special_laguerre_polynomial_l_n_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor x) n |> with_tensor_gc

let special_laguerre_polynomial_l_out ~(out @ local) ~(x @ local) ~(n @ local) =
  stubs_special_laguerre_polynomial_l_out (globalize_gc_tensor out) (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_laguerre_polynomial_l_x_scalar ~x ~(n @ local) =
  stubs_special_laguerre_polynomial_l_x_scalar x (globalize_gc_tensor n) |> with_tensor_gc

let special_laguerre_polynomial_l_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  stubs_special_laguerre_polynomial_l_x_scalar_out (globalize_gc_tensor out) x (globalize_gc_tensor n) |> with_tensor_gc

let special_legendre_polynomial_p ~(x @ local) ~(n @ local) =
  stubs_special_legendre_polynomial_p (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_legendre_polynomial_p_n_scalar ~(x @ local) ~n =
  stubs_special_legendre_polynomial_p_n_scalar (globalize_gc_tensor x) n |> with_tensor_gc

let special_legendre_polynomial_p_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  stubs_special_legendre_polynomial_p_n_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor x) n |> with_tensor_gc

let special_legendre_polynomial_p_out ~(out @ local) ~(x @ local) ~(n @ local) =
  stubs_special_legendre_polynomial_p_out (globalize_gc_tensor out) (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_legendre_polynomial_p_x_scalar ~x ~(n @ local) =
  stubs_special_legendre_polynomial_p_x_scalar x (globalize_gc_tensor n) |> with_tensor_gc

let special_legendre_polynomial_p_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  stubs_special_legendre_polynomial_p_x_scalar_out (globalize_gc_tensor out) x (globalize_gc_tensor n) |> with_tensor_gc

let special_log1p (self @ local) =
  stubs_special_log1p (globalize_gc_tensor self) |> with_tensor_gc

let special_log1p_out ~(out @ local) (self @ local) =
  stubs_special_log1p_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_log_ndtr (self @ local) =
  stubs_special_log_ndtr (globalize_gc_tensor self) |> with_tensor_gc

let special_log_ndtr_out ~(out @ local) (self @ local) =
  stubs_special_log_ndtr_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_log_softmax (self @ local) ~dim ~dtype =
  stubs_special_log_softmax (globalize_gc_tensor self) (Int64.of_int dim) (Kind.packed_to_int dtype) |> with_tensor_gc

let special_logit (self @ local) ~eps =
  stubs_special_logit (globalize_gc_tensor self)  (Option.value eps ~default:0.0) (match eps with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let special_logit_out ~(out @ local) (self @ local) ~eps =
  stubs_special_logit_out (globalize_gc_tensor out) (globalize_gc_tensor self)  (Option.value eps ~default:0.0) (match eps with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let special_logsumexp (self @ local) ~dim ~keepdim =
  stubs_special_logsumexp (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) |> with_tensor_gc

let special_logsumexp_out ~(out @ local) (self @ local) ~dim ~keepdim =
  stubs_special_logsumexp_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) (if keepdim then 1 else 0) |> with_tensor_gc

let special_modified_bessel_i0 (self @ local) =
  stubs_special_modified_bessel_i0 (globalize_gc_tensor self) |> with_tensor_gc

let special_modified_bessel_i0_out ~(out @ local) (self @ local) =
  stubs_special_modified_bessel_i0_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_modified_bessel_i1 (self @ local) =
  stubs_special_modified_bessel_i1 (globalize_gc_tensor self) |> with_tensor_gc

let special_modified_bessel_i1_out ~(out @ local) (self @ local) =
  stubs_special_modified_bessel_i1_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_modified_bessel_k0 (self @ local) =
  stubs_special_modified_bessel_k0 (globalize_gc_tensor self) |> with_tensor_gc

let special_modified_bessel_k0_out ~(out @ local) (self @ local) =
  stubs_special_modified_bessel_k0_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_modified_bessel_k1 (self @ local) =
  stubs_special_modified_bessel_k1 (globalize_gc_tensor self) |> with_tensor_gc

let special_modified_bessel_k1_out ~(out @ local) (self @ local) =
  stubs_special_modified_bessel_k1_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_multigammaln (self @ local) ~p =
  stubs_special_multigammaln (globalize_gc_tensor self) (Int64.of_int p) |> with_tensor_gc

let special_multigammaln_out ~(out @ local) (self @ local) ~p =
  stubs_special_multigammaln_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int p) |> with_tensor_gc

let special_ndtr (self @ local) =
  stubs_special_ndtr (globalize_gc_tensor self) |> with_tensor_gc

let special_ndtr_out ~(out @ local) (self @ local) =
  stubs_special_ndtr_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_ndtri (self @ local) =
  stubs_special_ndtri (globalize_gc_tensor self) |> with_tensor_gc

let special_ndtri_out ~(out @ local) (self @ local) =
  stubs_special_ndtri_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_polygamma ~n (self @ local) =
  stubs_special_polygamma (Int64.of_int n) (globalize_gc_tensor self) |> with_tensor_gc

let special_polygamma_out ~(out @ local) ~n (self @ local) =
  stubs_special_polygamma_out (globalize_gc_tensor out) (Int64.of_int n) (globalize_gc_tensor self) |> with_tensor_gc

let special_psi (self @ local) =
  stubs_special_psi (globalize_gc_tensor self) |> with_tensor_gc

let special_psi_out ~(out @ local) (self @ local) =
  stubs_special_psi_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_round (self @ local) ~decimals =
  stubs_special_round (globalize_gc_tensor self) (Int64.of_int decimals) |> with_tensor_gc

let special_round_out ~(out @ local) (self @ local) ~decimals =
  stubs_special_round_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int decimals) |> with_tensor_gc

let special_scaled_modified_bessel_k0 ~(x @ local) =
  stubs_special_scaled_modified_bessel_k0 (globalize_gc_tensor x) |> with_tensor_gc

let special_scaled_modified_bessel_k0_out ~(out @ local) ~(x @ local) =
  stubs_special_scaled_modified_bessel_k0_out (globalize_gc_tensor out) (globalize_gc_tensor x) |> with_tensor_gc

let special_scaled_modified_bessel_k1 ~(x @ local) =
  stubs_special_scaled_modified_bessel_k1 (globalize_gc_tensor x) |> with_tensor_gc

let special_scaled_modified_bessel_k1_out ~(out @ local) ~(x @ local) =
  stubs_special_scaled_modified_bessel_k1_out (globalize_gc_tensor out) (globalize_gc_tensor x) |> with_tensor_gc

let special_shifted_chebyshev_polynomial_t ~(x @ local) ~(n @ local) =
  stubs_special_shifted_chebyshev_polynomial_t (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_shifted_chebyshev_polynomial_t_n_scalar ~(x @ local) ~n =
  stubs_special_shifted_chebyshev_polynomial_t_n_scalar (globalize_gc_tensor x) n |> with_tensor_gc

let special_shifted_chebyshev_polynomial_t_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  stubs_special_shifted_chebyshev_polynomial_t_n_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor x) n |> with_tensor_gc

let special_shifted_chebyshev_polynomial_t_out ~(out @ local) ~(x @ local) ~(n @ local) =
  stubs_special_shifted_chebyshev_polynomial_t_out (globalize_gc_tensor out) (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_shifted_chebyshev_polynomial_t_x_scalar ~x ~(n @ local) =
  stubs_special_shifted_chebyshev_polynomial_t_x_scalar x (globalize_gc_tensor n) |> with_tensor_gc

let special_shifted_chebyshev_polynomial_t_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  stubs_special_shifted_chebyshev_polynomial_t_x_scalar_out (globalize_gc_tensor out) x (globalize_gc_tensor n) |> with_tensor_gc

let special_shifted_chebyshev_polynomial_u ~(x @ local) ~(n @ local) =
  stubs_special_shifted_chebyshev_polynomial_u (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_shifted_chebyshev_polynomial_u_n_scalar ~(x @ local) ~n =
  stubs_special_shifted_chebyshev_polynomial_u_n_scalar (globalize_gc_tensor x) n |> with_tensor_gc

let special_shifted_chebyshev_polynomial_u_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  stubs_special_shifted_chebyshev_polynomial_u_n_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor x) n |> with_tensor_gc

let special_shifted_chebyshev_polynomial_u_out ~(out @ local) ~(x @ local) ~(n @ local) =
  stubs_special_shifted_chebyshev_polynomial_u_out (globalize_gc_tensor out) (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_shifted_chebyshev_polynomial_u_x_scalar ~x ~(n @ local) =
  stubs_special_shifted_chebyshev_polynomial_u_x_scalar x (globalize_gc_tensor n) |> with_tensor_gc

let special_shifted_chebyshev_polynomial_u_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  stubs_special_shifted_chebyshev_polynomial_u_x_scalar_out (globalize_gc_tensor out) x (globalize_gc_tensor n) |> with_tensor_gc

let special_shifted_chebyshev_polynomial_v ~(x @ local) ~(n @ local) =
  stubs_special_shifted_chebyshev_polynomial_v (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_shifted_chebyshev_polynomial_v_n_scalar ~(x @ local) ~n =
  stubs_special_shifted_chebyshev_polynomial_v_n_scalar (globalize_gc_tensor x) n |> with_tensor_gc

let special_shifted_chebyshev_polynomial_v_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  stubs_special_shifted_chebyshev_polynomial_v_n_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor x) n |> with_tensor_gc

let special_shifted_chebyshev_polynomial_v_out ~(out @ local) ~(x @ local) ~(n @ local) =
  stubs_special_shifted_chebyshev_polynomial_v_out (globalize_gc_tensor out) (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_shifted_chebyshev_polynomial_v_x_scalar ~x ~(n @ local) =
  stubs_special_shifted_chebyshev_polynomial_v_x_scalar x (globalize_gc_tensor n) |> with_tensor_gc

let special_shifted_chebyshev_polynomial_v_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  stubs_special_shifted_chebyshev_polynomial_v_x_scalar_out (globalize_gc_tensor out) x (globalize_gc_tensor n) |> with_tensor_gc

let special_shifted_chebyshev_polynomial_w ~(x @ local) ~(n @ local) =
  stubs_special_shifted_chebyshev_polynomial_w (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_shifted_chebyshev_polynomial_w_n_scalar ~(x @ local) ~n =
  stubs_special_shifted_chebyshev_polynomial_w_n_scalar (globalize_gc_tensor x) n |> with_tensor_gc

let special_shifted_chebyshev_polynomial_w_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  stubs_special_shifted_chebyshev_polynomial_w_n_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor x) n |> with_tensor_gc

let special_shifted_chebyshev_polynomial_w_out ~(out @ local) ~(x @ local) ~(n @ local) =
  stubs_special_shifted_chebyshev_polynomial_w_out (globalize_gc_tensor out) (globalize_gc_tensor x) (globalize_gc_tensor n) |> with_tensor_gc

let special_shifted_chebyshev_polynomial_w_x_scalar ~x ~(n @ local) =
  stubs_special_shifted_chebyshev_polynomial_w_x_scalar x (globalize_gc_tensor n) |> with_tensor_gc

let special_shifted_chebyshev_polynomial_w_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  stubs_special_shifted_chebyshev_polynomial_w_x_scalar_out (globalize_gc_tensor out) x (globalize_gc_tensor n) |> with_tensor_gc

let special_sinc (self @ local) =
  stubs_special_sinc (globalize_gc_tensor self) |> with_tensor_gc

let special_sinc_out ~(out @ local) (self @ local) =
  stubs_special_sinc_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let special_softmax (self @ local) ~dim ~dtype =
  stubs_special_softmax (globalize_gc_tensor self) (Int64.of_int dim) (Kind.packed_to_int dtype) |> with_tensor_gc

let special_spherical_bessel_j0 ~(x @ local) =
  stubs_special_spherical_bessel_j0 (globalize_gc_tensor x) |> with_tensor_gc

let special_spherical_bessel_j0_out ~(out @ local) ~(x @ local) =
  stubs_special_spherical_bessel_j0_out (globalize_gc_tensor out) (globalize_gc_tensor x) |> with_tensor_gc

let special_xlog1py (self @ local) (other @ local) =
  stubs_special_xlog1py (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let special_xlog1py_other_scalar (self @ local) other =
  stubs_special_xlog1py_other_scalar (globalize_gc_tensor self) other |> with_tensor_gc

let special_xlog1py_other_scalar_out ~(out @ local) (self @ local) other =
  stubs_special_xlog1py_other_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let special_xlog1py_out ~(out @ local) (self @ local) (other @ local) =
  stubs_special_xlog1py_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let special_xlog1py_self_scalar self (other @ local) =
  stubs_special_xlog1py_self_scalar self (globalize_gc_tensor other) |> with_tensor_gc

let special_xlog1py_self_scalar_out ~(out @ local) self (other @ local) =
  stubs_special_xlog1py_self_scalar_out (globalize_gc_tensor out) self (globalize_gc_tensor other) |> with_tensor_gc

let special_xlogy (self @ local) (other @ local) =
  stubs_special_xlogy (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let special_xlogy_other_scalar (self @ local) other =
  stubs_special_xlogy_other_scalar (globalize_gc_tensor self) other |> with_tensor_gc

let special_xlogy_other_scalar_out ~(out @ local) (self @ local) other =
  stubs_special_xlogy_other_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let special_xlogy_out ~(out @ local) (self @ local) (other @ local) =
  stubs_special_xlogy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let special_xlogy_self_scalar self (other @ local) =
  stubs_special_xlogy_self_scalar self (globalize_gc_tensor other) |> with_tensor_gc

let special_xlogy_self_scalar_out ~(out @ local) self (other @ local) =
  stubs_special_xlogy_self_scalar_out (globalize_gc_tensor out) self (globalize_gc_tensor other) |> with_tensor_gc

let special_zeta (self @ local) (other @ local) =
  stubs_special_zeta (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let special_zeta_other_scalar (self @ local) other =
  stubs_special_zeta_other_scalar (globalize_gc_tensor self) other |> with_tensor_gc

let special_zeta_other_scalar_out ~(out @ local) (self @ local) other =
  stubs_special_zeta_other_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let special_zeta_out ~(out @ local) (self @ local) (other @ local) =
  stubs_special_zeta_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let special_zeta_self_scalar self (other @ local) =
  stubs_special_zeta_self_scalar self (globalize_gc_tensor other) |> with_tensor_gc

let special_zeta_self_scalar_out ~(out @ local) self (other @ local) =
  stubs_special_zeta_self_scalar_out (globalize_gc_tensor out) self (globalize_gc_tensor other) |> with_tensor_gc

let split (self @ local) ~split_size ~dim =
  stubs_split (globalize_gc_tensor self) (Int64.of_int split_size) (Int64.of_int dim) |> to_tensor_list

let split_copy (self @ local) ~split_size ~dim =
  stubs_split_copy (globalize_gc_tensor self) (Int64.of_int split_size) (Int64.of_int dim) |> to_tensor_list

let split_copy_tensor_out ~(out @ local) (self @ local) ~split_size ~dim =
  stubs_split_copy_tensor_out (CArray.of_list gc_tensor (globalize_gc_tensor_list out) |> CArray.start) (Base.List.length out) (globalize_gc_tensor self) (Int64.of_int split_size) (Int64.of_int dim)

let split_sizes (self @ local) ~split_size ~dim =
  stubs_split_sizes (globalize_gc_tensor self) (List.map Int64.of_int split_size |> CArray.of_list int64_t |> CArray.start) (List.length split_size) (Int64.of_int dim) |> to_tensor_list

let split_with_sizes (self @ local) ~split_sizes ~dim =
  stubs_split_with_sizes (globalize_gc_tensor self) (List.map Int64.of_int split_sizes |> CArray.of_list int64_t |> CArray.start) (List.length split_sizes) (Int64.of_int dim) |> to_tensor_list

let split_with_sizes_copy (self @ local) ~split_sizes ~dim =
  stubs_split_with_sizes_copy (globalize_gc_tensor self) (List.map Int64.of_int split_sizes |> CArray.of_list int64_t |> CArray.start) (List.length split_sizes) (Int64.of_int dim) |> to_tensor_list

let split_with_sizes_copy_out ~(out @ local) (self @ local) ~split_sizes ~dim =
  stubs_split_with_sizes_copy_out (CArray.of_list gc_tensor (globalize_gc_tensor_list out) |> CArray.start) (Base.List.length out) (globalize_gc_tensor self) (List.map Int64.of_int split_sizes |> CArray.of_list int64_t |> CArray.start) (List.length split_sizes) (Int64.of_int dim)

let sqrt (self @ local) =
  stubs_sqrt (globalize_gc_tensor self) |> with_tensor_gc

let sqrt_ (self @ local) =
  stubs_sqrt_ (globalize_gc_tensor self) |> with_tensor_gc

let sqrt_out ~(out @ local) (self @ local) =
  stubs_sqrt_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let square (self @ local) =
  stubs_square (globalize_gc_tensor self) |> with_tensor_gc

let square_ (self @ local) =
  stubs_square_ (globalize_gc_tensor self) |> with_tensor_gc

let square_out ~(out @ local) (self @ local) =
  stubs_square_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let squeeze (self @ local) =
  stubs_squeeze (globalize_gc_tensor self) |> with_tensor_gc

let squeeze_ (self @ local) =
  stubs_squeeze_ (globalize_gc_tensor self) |> with_tensor_gc

let squeeze_copy (self @ local) =
  stubs_squeeze_copy (globalize_gc_tensor self) |> with_tensor_gc

let squeeze_copy_dim (self @ local) ~dim =
  stubs_squeeze_copy_dim (globalize_gc_tensor self) (Int64.of_int dim) |> with_tensor_gc

let squeeze_copy_dim_out ~(out @ local) (self @ local) ~dim =
  stubs_squeeze_copy_dim_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) |> with_tensor_gc

let squeeze_copy_dims (self @ local) ~dim =
  stubs_squeeze_copy_dims (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) |> with_tensor_gc

let squeeze_copy_dims_out ~(out @ local) (self @ local) ~dim =
  stubs_squeeze_copy_dims_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) |> with_tensor_gc

let squeeze_copy_out ~(out @ local) (self @ local) =
  stubs_squeeze_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let squeeze_dim (self @ local) ~dim =
  stubs_squeeze_dim (globalize_gc_tensor self) (Int64.of_int dim) |> with_tensor_gc

let squeeze_dim_ (self @ local) ~dim =
  stubs_squeeze_dim_ (globalize_gc_tensor self) (Int64.of_int dim) |> with_tensor_gc

let squeeze_dims (self @ local) ~dim =
  stubs_squeeze_dims (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) |> with_tensor_gc

let squeeze_dims_ (self @ local) ~dim =
  stubs_squeeze_dims_ (globalize_gc_tensor self) (List.map Int64.of_int dim |> CArray.of_list int64_t |> CArray.start) (List.length dim) |> with_tensor_gc

let sspaddmm ?beta ?alpha (self @ local) ~(mat1 @ local) ~(mat2 @ local) =
  stubs_sspaddmm (globalize_gc_tensor self) (globalize_gc_tensor mat1) (globalize_gc_tensor mat2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let sspaddmm_out ?beta ?alpha ~(out @ local) (self @ local) ~(mat1 @ local) ~(mat2 @ local) =
  stubs_sspaddmm_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor mat1) (globalize_gc_tensor mat2) (match beta with | Some v -> v | None -> none_scalar) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let stack (tensors @ local) ~dim =
  stubs_stack (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) (Int64.of_int dim) |> with_tensor_gc

let stack_out ~(out @ local) (tensors @ local) ~dim =
  stubs_stack_out (globalize_gc_tensor out) (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) (Int64.of_int dim) |> with_tensor_gc

let std (self @ local) ~unbiased =
  stubs_std (globalize_gc_tensor self) (if unbiased then 1 else 0) |> with_tensor_gc

let std_correction (self @ local) ~dim ~correction ~keepdim =
  stubs_std_correction (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) correction (if keepdim then 1 else 0) |> with_tensor_gc

let std_correction_out ~(out @ local) (self @ local) ~dim ~correction ~keepdim =
  stubs_std_correction_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) correction (if keepdim then 1 else 0) |> with_tensor_gc

let std_dim (self @ local) ~dim ~unbiased ~keepdim =
  stubs_std_dim (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if unbiased then 1 else 0) (if keepdim then 1 else 0) |> with_tensor_gc

let std_mean (self @ local) ~unbiased =
  let out__ = CArray.make raw_tensor 2 in
  stubs_std_mean (CArray.start out__) (globalize_gc_tensor self) (if unbiased then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let std_mean_correction (self @ local) ~dim ~correction ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_std_mean_correction (CArray.start out__) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) correction (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let std_mean_correction_out ~(out0 @ local) ~(out1 @ local) (self @ local) ~dim ~correction ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_std_mean_correction_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) correction (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let std_mean_dim (self @ local) ~dim ~unbiased ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_std_mean_dim (CArray.start out__) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if unbiased then 1 else 0) (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let std_out ~(out @ local) (self @ local) ~dim ~unbiased ~keepdim =
  stubs_std_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if unbiased then 1 else 0) (if keepdim then 1 else 0) |> with_tensor_gc

let stft (self @ local) ~n_fft ~hop_length ~win_length ~(window @ local) ~normalized ~onesided ~return_complex ~align_to_window =
  stubs_stft (globalize_gc_tensor self) (Int64.of_int n_fft)  (match hop_length with | None -> Int64.zero | Some v -> Int64.of_int v) (match hop_length with | Some _ -> 0 | None -> 1)   (match win_length with | None -> Int64.zero | Some v -> Int64.of_int v) (match win_length with | Some _ -> 0 | None -> 1)  (match (Base.Option.globalize globalize_gc_tensor window) with | Some v -> v | None -> none_gc_tensor) (if normalized then 1 else 0) (if onesided then 1 else 0) (if return_complex then 1 else 0) (if align_to_window then 1 else 0) |> with_tensor_gc

let stft_center (self @ local) ~n_fft ~hop_length ~win_length ~(window @ local) ~center ~pad_mode ~normalized ~onesided ~return_complex ~align_to_window =
  stubs_stft_center (globalize_gc_tensor self) (Int64.of_int n_fft)  (match hop_length with | None -> Int64.zero | Some v -> Int64.of_int v) (match hop_length with | Some _ -> 0 | None -> 1)   (match win_length with | None -> Int64.zero | Some v -> Int64.of_int v) (match win_length with | Some _ -> 0 | None -> 1)  (match (Base.Option.globalize globalize_gc_tensor window) with | Some v -> v | None -> none_gc_tensor) (if center then 1 else 0) pad_mode (if normalized then 1 else 0) (if onesided then 1 else 0) (if return_complex then 1 else 0) (if align_to_window then 1 else 0) |> with_tensor_gc

let storage_offset (self @ local) =
  stubs_storage_offset (globalize_gc_tensor self)

let stride (self @ local) ~dim =
  stubs_stride (globalize_gc_tensor self) (Int64.of_int dim)

let sub ?alpha (self @ local) (other @ local) =
  stubs_sub (globalize_gc_tensor self) (globalize_gc_tensor other) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let sub_ ?alpha (self @ local) (other @ local) =
  stubs_sub_ (globalize_gc_tensor self) (globalize_gc_tensor other) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let sub_out ?alpha ~(out @ local) (self @ local) (other @ local) =
  stubs_sub_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let sub_scalar ?alpha (self @ local) other =
  stubs_sub_scalar (globalize_gc_tensor self) other (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let sub_scalar_ ?alpha (self @ local) other =
  stubs_sub_scalar_ (globalize_gc_tensor self) other (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let sub_scalar_out ?alpha ~(out @ local) (self @ local) other =
  stubs_sub_scalar_out (globalize_gc_tensor out) (globalize_gc_tensor self) other (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let subtract ?alpha (self @ local) (other @ local) =
  stubs_subtract (globalize_gc_tensor self) (globalize_gc_tensor other) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let subtract_ ?alpha (self @ local) (other @ local) =
  stubs_subtract_ (globalize_gc_tensor self) (globalize_gc_tensor other) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let subtract_out ?alpha ~(out @ local) (self @ local) (other @ local) =
  stubs_subtract_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let subtract_scalar ?alpha (self @ local) other =
  stubs_subtract_scalar (globalize_gc_tensor self) other (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let subtract_scalar_ ?alpha (self @ local) other =
  stubs_subtract_scalar_ (globalize_gc_tensor self) other (match alpha with | Some v -> v | None -> none_scalar) |> with_tensor_gc

let sum (self @ local) ~dtype =
  stubs_sum (globalize_gc_tensor self) (Kind.packed_to_int dtype) |> with_tensor_gc

let sum_dim_intlist (self @ local) ~dim ~keepdim ~dtype =
  stubs_sum_dim_intlist (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let sum_intlist_out ~(out @ local) (self @ local) ~dim ~keepdim ~dtype =
  stubs_sum_intlist_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if keepdim then 1 else 0) (Kind.packed_to_int dtype) |> with_tensor_gc

let sum_out ~(out @ local) (self @ local) ~dtype =
  stubs_sum_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Kind.packed_to_int dtype) |> with_tensor_gc

let sum_to_size (self @ local) ~size =
  stubs_sum_to_size (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let svd (self @ local) ~some ~compute_uv =
  let out__ = CArray.make raw_tensor 3 in
  stubs_svd (CArray.start out__) (globalize_gc_tensor self) (if some then 1 else 0) (if compute_uv then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let svd_u ~(u @ local) ~(s @ local) ~(v @ local) (self @ local) ~some ~compute_uv =
  let out__ = CArray.make raw_tensor 3 in
  stubs_svd_u (CArray.start out__) (globalize_gc_tensor u) (globalize_gc_tensor s) (globalize_gc_tensor v) (globalize_gc_tensor self) (if some then 1 else 0) (if compute_uv then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let swapaxes (self @ local) ~axis0 ~axis1 =
  stubs_swapaxes (globalize_gc_tensor self) (Int64.of_int axis0) (Int64.of_int axis1) |> with_tensor_gc

let swapaxes_ (self @ local) ~axis0 ~axis1 =
  stubs_swapaxes_ (globalize_gc_tensor self) (Int64.of_int axis0) (Int64.of_int axis1) |> with_tensor_gc

let swapdims (self @ local) ~dim0 ~dim1 =
  stubs_swapdims (globalize_gc_tensor self) (Int64.of_int dim0) (Int64.of_int dim1) |> with_tensor_gc

let swapdims_ (self @ local) ~dim0 ~dim1 =
  stubs_swapdims_ (globalize_gc_tensor self) (Int64.of_int dim0) (Int64.of_int dim1) |> with_tensor_gc

let tr (self @ local) =
  stubs_tr (globalize_gc_tensor self) |> with_tensor_gc

let t_ (self @ local) =
  stubs_t_ (globalize_gc_tensor self) |> with_tensor_gc

let t_copy (self @ local) =
  stubs_t_copy (globalize_gc_tensor self) |> with_tensor_gc

let t_copy_out ~(out @ local) (self @ local) =
  stubs_t_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let take (self @ local) ~(index @ local) =
  stubs_take (globalize_gc_tensor self) (globalize_gc_tensor index) |> with_tensor_gc

let take_along_dim (self @ local) ~(indices @ local) ~dim =
  stubs_take_along_dim (globalize_gc_tensor self) (globalize_gc_tensor indices)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let take_along_dim_out ~(out @ local) (self @ local) ~(indices @ local) ~dim =
  stubs_take_along_dim_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor indices)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let take_out ~(out @ local) (self @ local) ~(index @ local) =
  stubs_take_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor index) |> with_tensor_gc

let tan (self @ local) =
  stubs_tan (globalize_gc_tensor self) |> with_tensor_gc

let tan_ (self @ local) =
  stubs_tan_ (globalize_gc_tensor self) |> with_tensor_gc

let tan_out ~(out @ local) (self @ local) =
  stubs_tan_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let tanh (self @ local) =
  stubs_tanh (globalize_gc_tensor self) |> with_tensor_gc

let tanh_ (self @ local) =
  stubs_tanh_ (globalize_gc_tensor self) |> with_tensor_gc

let tanh_backward ~(grad_output @ local) ~(output @ local) =
  stubs_tanh_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor output) |> with_tensor_gc

let tanh_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) ~(output @ local) =
  stubs_tanh_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor output) |> with_tensor_gc

let tanh_out ~(out @ local) (self @ local) =
  stubs_tanh_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let tensor_split (self @ local) ~sections ~dim =
  stubs_tensor_split (globalize_gc_tensor self) (Int64.of_int sections) (Int64.of_int dim) |> to_tensor_list

let tensor_split_indices (self @ local) ~indices ~dim =
  stubs_tensor_split_indices (globalize_gc_tensor self) (List.map Int64.of_int indices |> CArray.of_list int64_t |> CArray.start) (List.length indices) (Int64.of_int dim) |> to_tensor_list

let tensor_split_tensor_indices_or_sections (self @ local) ~(tensor_indices_or_sections @ local) ~dim =
  stubs_tensor_split_tensor_indices_or_sections (globalize_gc_tensor self) (globalize_gc_tensor tensor_indices_or_sections) (Int64.of_int dim) |> to_tensor_list

let tensordot (self @ local) (other @ local) ~dims_self ~dims_other =
  stubs_tensordot (globalize_gc_tensor self) (globalize_gc_tensor other) (List.map Int64.of_int dims_self |> CArray.of_list int64_t |> CArray.start) (List.length dims_self) (List.map Int64.of_int dims_other |> CArray.of_list int64_t |> CArray.start) (List.length dims_other) |> with_tensor_gc

let tensordot_out ~(out @ local) (self @ local) (other @ local) ~dims_self ~dims_other =
  stubs_tensordot_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) (List.map Int64.of_int dims_self |> CArray.of_list int64_t |> CArray.start) (List.length dims_self) (List.map Int64.of_int dims_other |> CArray.of_list int64_t |> CArray.start) (List.length dims_other) |> with_tensor_gc

let threshold (self @ local) ~threshold ~value =
  stubs_threshold (globalize_gc_tensor self) threshold value |> with_tensor_gc

let threshold_ (self @ local) ~threshold ~value =
  stubs_threshold_ (globalize_gc_tensor self) threshold value |> with_tensor_gc

let threshold_backward ~(grad_output @ local) (self @ local) ~threshold =
  stubs_threshold_backward (globalize_gc_tensor grad_output) (globalize_gc_tensor self) threshold |> with_tensor_gc

let threshold_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) ~threshold =
  stubs_threshold_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (globalize_gc_tensor self) threshold |> with_tensor_gc

let threshold_out ~(out @ local) (self @ local) ~threshold ~value =
  stubs_threshold_out (globalize_gc_tensor out) (globalize_gc_tensor self) threshold value |> with_tensor_gc

let tile (self @ local) ~dims =
  stubs_tile (globalize_gc_tensor self) (List.map Int64.of_int dims |> CArray.of_list int64_t |> CArray.start) (List.length dims) |> with_tensor_gc

let to_ (self @ local) ~device =
  stubs_to_ (globalize_gc_tensor self) (Device.to_int device) |> with_tensor_gc

let to_dense (self @ local) ~dtype ~masked_grad =
  stubs_to_dense (globalize_gc_tensor self) (Kind.packed_to_int dtype) (if masked_grad then 1 else 0) |> with_tensor_gc

let to_dense_backward ~(grad @ local) (input @ local) ~masked_grad =
  stubs_to_dense_backward (globalize_gc_tensor grad) (globalize_gc_tensor input) (if masked_grad then 1 else 0) |> with_tensor_gc

let to_device (self @ local) ~device ~dtype ~non_blocking ~copy =
  stubs_to_device (globalize_gc_tensor self) (Device.to_int device) (Kind.packed_to_int dtype) (if non_blocking then 1 else 0) (if copy then 1 else 0) |> with_tensor_gc

let to_dtype (self @ local) ~dtype ~non_blocking ~copy =
  stubs_to_dtype (globalize_gc_tensor self) (Kind.packed_to_int dtype) (if non_blocking then 1 else 0) (if copy then 1 else 0) |> with_tensor_gc

let to_dtype_layout (self @ local) ~options ~non_blocking ~copy =
  stubs_to_dtype_layout (globalize_gc_tensor self) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) (if non_blocking then 1 else 0) (if copy then 1 else 0) |> with_tensor_gc

let to_mkldnn (self @ local) ~dtype =
  stubs_to_mkldnn (globalize_gc_tensor self) (Kind.packed_to_int dtype) |> with_tensor_gc

let to_mkldnn_backward ~(grad @ local) (input @ local) =
  stubs_to_mkldnn_backward (globalize_gc_tensor grad) (globalize_gc_tensor input) |> with_tensor_gc

let to_mkldnn_out ~(out @ local) (self @ local) ~dtype =
  stubs_to_mkldnn_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Kind.packed_to_int dtype) |> with_tensor_gc

let to_other (self @ local) (other @ local) ~non_blocking ~copy =
  stubs_to_other (globalize_gc_tensor self) (globalize_gc_tensor other) (if non_blocking then 1 else 0) (if copy then 1 else 0) |> with_tensor_gc

let to_padded_tensor (self @ local) ~padding ~output_size =
  stubs_to_padded_tensor (globalize_gc_tensor self) padding (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) |> with_tensor_gc

let to_padded_tensor_out ~(out @ local) (self @ local) ~padding ~output_size =
  stubs_to_padded_tensor_out (globalize_gc_tensor out) (globalize_gc_tensor self) padding (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) |> with_tensor_gc

let topk (self @ local) ~k ~dim ~largest ~sorted =
  let out__ = CArray.make raw_tensor 2 in
  stubs_topk (CArray.start out__) (globalize_gc_tensor self) (Int64.of_int k) (Int64.of_int dim) (if largest then 1 else 0) (if sorted then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let topk_values ~(values @ local) ~(indices @ local) (self @ local) ~k ~dim ~largest ~sorted =
  let out__ = CArray.make raw_tensor 2 in
  stubs_topk_values (CArray.start out__) (globalize_gc_tensor values) (globalize_gc_tensor indices) (globalize_gc_tensor self) (Int64.of_int k) (Int64.of_int dim) (if largest then 1 else 0) (if sorted then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let totype (self @ local) ~scalar_type =
  stubs_totype (globalize_gc_tensor self) (Kind.packed_to_int scalar_type) |> with_tensor_gc

let trace (self @ local) =
  stubs_trace (globalize_gc_tensor self) |> with_tensor_gc

let trace_backward ~(grad @ local) ~sizes =
  stubs_trace_backward (globalize_gc_tensor grad) (List.map Int64.of_int sizes |> CArray.of_list int64_t |> CArray.start) (List.length sizes) |> with_tensor_gc

let trace_out ~(out @ local) (self @ local) =
  stubs_trace_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let transpose (self @ local) ~dim0 ~dim1 =
  stubs_transpose (globalize_gc_tensor self) (Int64.of_int dim0) (Int64.of_int dim1) |> with_tensor_gc

let transpose_ (self @ local) ~dim0 ~dim1 =
  stubs_transpose_ (globalize_gc_tensor self) (Int64.of_int dim0) (Int64.of_int dim1) |> with_tensor_gc

let transpose_copy (self @ local) ~dim0 ~dim1 =
  stubs_transpose_copy (globalize_gc_tensor self) (Int64.of_int dim0) (Int64.of_int dim1) |> with_tensor_gc

let transpose_copy_int_out ~(out @ local) (self @ local) ~dim0 ~dim1 =
  stubs_transpose_copy_int_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim0) (Int64.of_int dim1) |> with_tensor_gc

let trapezoid ~(y @ local) ~(x @ local) ~dim =
  stubs_trapezoid (globalize_gc_tensor y) (globalize_gc_tensor x) (Int64.of_int dim) |> with_tensor_gc

let trapezoid_dx ?dx ~(y @ local) ~dim () =
  stubs_trapezoid_dx (globalize_gc_tensor y) (match dx with | Some v -> v | None -> none_scalar) (Int64.of_int dim) |> with_tensor_gc

let trapz ~(y @ local) ~(x @ local) ~dim =
  stubs_trapz (globalize_gc_tensor y) (globalize_gc_tensor x) (Int64.of_int dim) |> with_tensor_gc

let trapz_dx ~(y @ local) ~dx ~dim =
  stubs_trapz_dx (globalize_gc_tensor y) dx (Int64.of_int dim) |> with_tensor_gc

let triangular_solve (self @ local) ~(a @ local) ~upper ~transpose ~unitriangular =
  let out__ = CArray.make raw_tensor 2 in
  stubs_triangular_solve (CArray.start out__) (globalize_gc_tensor self) (globalize_gc_tensor a) (if upper then 1 else 0) (if transpose then 1 else 0) (if unitriangular then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let triangular_solve_x ~(x @ local) ~(m @ local) (self @ local) ~(a @ local) ~upper ~transpose ~unitriangular =
  let out__ = CArray.make raw_tensor 2 in
  stubs_triangular_solve_x (CArray.start out__) (globalize_gc_tensor x) (globalize_gc_tensor m) (globalize_gc_tensor self) (globalize_gc_tensor a) (if upper then 1 else 0) (if transpose then 1 else 0) (if unitriangular then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let tril (self @ local) ~diagonal =
  stubs_tril (globalize_gc_tensor self) (Int64.of_int diagonal) |> with_tensor_gc

let tril_ (self @ local) ~diagonal =
  stubs_tril_ (globalize_gc_tensor self) (Int64.of_int diagonal) |> with_tensor_gc

let tril_indices ~row ~col ~offset ~options =
  stubs_tril_indices (Int64.of_int row) (Int64.of_int col) (Int64.of_int offset) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let tril_indices_out ~(out @ local) ~row ~col ~offset =
  stubs_tril_indices_out (globalize_gc_tensor out) (Int64.of_int row) (Int64.of_int col) (Int64.of_int offset) |> with_tensor_gc

let tril_out ~(out @ local) (self @ local) ~diagonal =
  stubs_tril_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int diagonal) |> with_tensor_gc

let triplet_margin_loss ~(anchor @ local) ~(positive @ local) ~(negative @ local) ~margin ~p ~eps ~swap ~reduction =
  stubs_triplet_margin_loss (globalize_gc_tensor anchor) (globalize_gc_tensor positive) (globalize_gc_tensor negative) margin p eps (if swap then 1 else 0) (Reduction.to_int reduction |> Int64.of_int) |> with_tensor_gc

let triu (self @ local) ~diagonal =
  stubs_triu (globalize_gc_tensor self) (Int64.of_int diagonal) |> with_tensor_gc

let triu_ (self @ local) ~diagonal =
  stubs_triu_ (globalize_gc_tensor self) (Int64.of_int diagonal) |> with_tensor_gc

let triu_indices ~row ~col ~offset ~options =
  stubs_triu_indices (Int64.of_int row) (Int64.of_int col) (Int64.of_int offset) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let triu_indices_out ~(out @ local) ~row ~col ~offset =
  stubs_triu_indices_out (globalize_gc_tensor out) (Int64.of_int row) (Int64.of_int col) (Int64.of_int offset) |> with_tensor_gc

let triu_out ~(out @ local) (self @ local) ~diagonal =
  stubs_triu_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int diagonal) |> with_tensor_gc

let true_divide (self @ local) (other @ local) =
  stubs_true_divide (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let true_divide_ (self @ local) (other @ local) =
  stubs_true_divide_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let true_divide_out ~(out @ local) (self @ local) (other @ local) =
  stubs_true_divide_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let true_divide_scalar (self @ local) other =
  stubs_true_divide_scalar (globalize_gc_tensor self) other |> with_tensor_gc

let true_divide_scalar_ (self @ local) other =
  stubs_true_divide_scalar_ (globalize_gc_tensor self) other |> with_tensor_gc

let trunc (self @ local) =
  stubs_trunc (globalize_gc_tensor self) |> with_tensor_gc

let trunc_ (self @ local) =
  stubs_trunc_ (globalize_gc_tensor self) |> with_tensor_gc

let trunc_out ~(out @ local) (self @ local) =
  stubs_trunc_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let type_as (self @ local) (other @ local) =
  stubs_type_as (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let unbind (self @ local) ~dim =
  stubs_unbind (globalize_gc_tensor self) (Int64.of_int dim) |> to_tensor_list

let unbind_copy (self @ local) ~dim =
  stubs_unbind_copy (globalize_gc_tensor self) (Int64.of_int dim) |> to_tensor_list

let unbind_copy_int_out ~(out @ local) (self @ local) ~dim =
  stubs_unbind_copy_int_out (CArray.of_list gc_tensor (globalize_gc_tensor_list out) |> CArray.start) (Base.List.length out) (globalize_gc_tensor self) (Int64.of_int dim)

let unflatten (self @ local) ~dim ~sizes =
  stubs_unflatten (globalize_gc_tensor self) (Int64.of_int dim) (List.map Int64.of_int sizes |> CArray.of_list int64_t |> CArray.start) (List.length sizes) |> with_tensor_gc

let unflatten_dense_tensors ~(flat @ local) (tensors @ local) =
  stubs_unflatten_dense_tensors (globalize_gc_tensor flat) (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> to_tensor_list

let unfold (self @ local) ~dimension ~size ~step =
  stubs_unfold (globalize_gc_tensor self) (Int64.of_int dimension) (Int64.of_int size) (Int64.of_int step) |> with_tensor_gc

let unfold_backward ~(grad_in @ local) ~input_sizes ~dim ~size ~step =
  stubs_unfold_backward (globalize_gc_tensor grad_in) (List.map Int64.of_int input_sizes |> CArray.of_list int64_t |> CArray.start) (List.length input_sizes) (Int64.of_int dim) (Int64.of_int size) (Int64.of_int step) |> with_tensor_gc

let unfold_backward_out ~(out @ local) ~(grad_in @ local) ~input_sizes ~dim ~size ~step =
  stubs_unfold_backward_out (globalize_gc_tensor out) (globalize_gc_tensor grad_in) (List.map Int64.of_int input_sizes |> CArray.of_list int64_t |> CArray.start) (List.length input_sizes) (Int64.of_int dim) (Int64.of_int size) (Int64.of_int step) |> with_tensor_gc

let unfold_copy (self @ local) ~dimension ~size ~step =
  stubs_unfold_copy (globalize_gc_tensor self) (Int64.of_int dimension) (Int64.of_int size) (Int64.of_int step) |> with_tensor_gc

let unfold_copy_out ~(out @ local) (self @ local) ~dimension ~size ~step =
  stubs_unfold_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dimension) (Int64.of_int size) (Int64.of_int step) |> with_tensor_gc

let uniform (self @ local) ~from ~to_ =
  stubs_uniform (globalize_gc_tensor self) from to_ |> with_tensor_gc

let uniform_ (self @ local) ~from ~to_ =
  stubs_uniform_ (globalize_gc_tensor self) from to_ |> with_tensor_gc

let uniform_out ~(out @ local) (self @ local) ~from ~to_ =
  stubs_uniform_out (globalize_gc_tensor out) (globalize_gc_tensor self) from to_ |> with_tensor_gc

let unique_consecutive (self @ local) ~return_inverse ~return_counts ~dim =
  let out__ = CArray.make raw_tensor 3 in
  stubs_unique_consecutive (CArray.start out__) (globalize_gc_tensor self) (if return_inverse then 1 else 0) (if return_counts then 1 else 0)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let unique_consecutive_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) (self @ local) ~return_inverse ~return_counts ~dim =
  let out__ = CArray.make raw_tensor 3 in
  stubs_unique_consecutive_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor self) (if return_inverse then 1 else 0) (if return_counts then 1 else 0)  (match dim with | None -> Int64.zero | Some v -> Int64.of_int v) (match dim with | Some _ -> 0 | None -> 1) ;
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let unique_dim (self @ local) ~dim ~sorted ~return_inverse ~return_counts =
  let out__ = CArray.make raw_tensor 3 in
  stubs_unique_dim (CArray.start out__) (globalize_gc_tensor self) (Int64.of_int dim) (if sorted then 1 else 0) (if return_inverse then 1 else 0) (if return_counts then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let unique_dim_consecutive (self @ local) ~dim ~return_inverse ~return_counts =
  let out__ = CArray.make raw_tensor 3 in
  stubs_unique_dim_consecutive (CArray.start out__) (globalize_gc_tensor self) (Int64.of_int dim) (if return_inverse then 1 else 0) (if return_counts then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let unique_dim_consecutive_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) (self @ local) ~dim ~return_inverse ~return_counts =
  let out__ = CArray.make raw_tensor 3 in
  stubs_unique_dim_consecutive_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor self) (Int64.of_int dim) (if return_inverse then 1 else 0) (if return_counts then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let unique_dim_out ~(out0 @ local) ~(out1 @ local) ~(out2 @ local) (self @ local) ~dim ~sorted ~return_inverse ~return_counts =
  let out__ = CArray.make raw_tensor 3 in
  stubs_unique_dim_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor out2) (globalize_gc_tensor self) (Int64.of_int dim) (if sorted then 1 else 0) (if return_inverse then 1 else 0) (if return_counts then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  let t2 = CArray.get out__ 2 |> with_tensor_gc in
  t0, t1, t2

let unsafe_chunk (self @ local) ~chunks ~dim =
  stubs_unsafe_chunk (globalize_gc_tensor self) (Int64.of_int chunks) (Int64.of_int dim) |> to_tensor_list

let unsafe_split (self @ local) ~split_size ~dim =
  stubs_unsafe_split (globalize_gc_tensor self) (Int64.of_int split_size) (Int64.of_int dim) |> to_tensor_list

let unsafe_split_tensor_out ~(out @ local) (self @ local) ~split_size ~dim =
  stubs_unsafe_split_tensor_out (CArray.of_list gc_tensor (globalize_gc_tensor_list out) |> CArray.start) (Base.List.length out) (globalize_gc_tensor self) (Int64.of_int split_size) (Int64.of_int dim)

let unsafe_split_with_sizes (self @ local) ~split_sizes ~dim =
  stubs_unsafe_split_with_sizes (globalize_gc_tensor self) (List.map Int64.of_int split_sizes |> CArray.of_list int64_t |> CArray.start) (List.length split_sizes) (Int64.of_int dim) |> to_tensor_list

let unsafe_split_with_sizes_out ~(out @ local) (self @ local) ~split_sizes ~dim =
  stubs_unsafe_split_with_sizes_out (CArray.of_list gc_tensor (globalize_gc_tensor_list out) |> CArray.start) (Base.List.length out) (globalize_gc_tensor self) (List.map Int64.of_int split_sizes |> CArray.of_list int64_t |> CArray.start) (List.length split_sizes) (Int64.of_int dim)

let unsqueeze (self @ local) ~dim =
  stubs_unsqueeze (globalize_gc_tensor self) (Int64.of_int dim) |> with_tensor_gc

let unsqueeze_ (self @ local) ~dim =
  stubs_unsqueeze_ (globalize_gc_tensor self) (Int64.of_int dim) |> with_tensor_gc

let unsqueeze_copy (self @ local) ~dim =
  stubs_unsqueeze_copy (globalize_gc_tensor self) (Int64.of_int dim) |> with_tensor_gc

let unsqueeze_copy_out ~(out @ local) (self @ local) ~dim =
  stubs_unsqueeze_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Int64.of_int dim) |> with_tensor_gc

let upsample_bicubic2d (self @ local) ~output_size ~align_corners ~scales_h ~scales_w =
  stubs_upsample_bicubic2d (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_bicubic2d_backward ~(grad_output @ local) ~output_size ~input_size ~align_corners ~scales_h ~scales_w =
  stubs_upsample_bicubic2d_backward (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_bicubic2d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) ~output_size ~input_size ~align_corners ~scales_h ~scales_w =
  stubs_upsample_bicubic2d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_bicubic2d_out ~(out @ local) (self @ local) ~output_size ~align_corners ~scales_h ~scales_w =
  stubs_upsample_bicubic2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_bicubic2d_vec (input @ local) ~output_size ~align_corners ~scale_factors =
  stubs_upsample_bicubic2d_vec (globalize_gc_tensor input) (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) (if align_corners then 1 else 0) (scale_factors |> CArray.of_list double |> CArray.start) (List.length scale_factors) |> with_tensor_gc

let upsample_bilinear2d (self @ local) ~output_size ~align_corners ~scales_h ~scales_w =
  stubs_upsample_bilinear2d (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_bilinear2d_backward ~(grad_output @ local) ~output_size ~input_size ~align_corners ~scales_h ~scales_w =
  stubs_upsample_bilinear2d_backward (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_bilinear2d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) ~output_size ~input_size ~align_corners ~scales_h ~scales_w =
  stubs_upsample_bilinear2d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_bilinear2d_out ~(out @ local) (self @ local) ~output_size ~align_corners ~scales_h ~scales_w =
  stubs_upsample_bilinear2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (if align_corners then 1 else 0)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_bilinear2d_vec (input @ local) ~output_size ~align_corners ~scale_factors =
  stubs_upsample_bilinear2d_vec (globalize_gc_tensor input) (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) (if align_corners then 1 else 0) (scale_factors |> CArray.of_list double |> CArray.start) (List.length scale_factors) |> with_tensor_gc

let upsample_bilinear2d_vec_out ~(out @ local) (input @ local) ~output_size ~align_corners ~scale_factors =
  stubs_upsample_bilinear2d_vec_out (globalize_gc_tensor out) (globalize_gc_tensor input) (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) (if align_corners then 1 else 0) (scale_factors |> CArray.of_list double |> CArray.start) (List.length scale_factors) |> with_tensor_gc

let upsample_linear1d (self @ local) ~output_size ~align_corners ~scales =
  stubs_upsample_linear1d (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (if align_corners then 1 else 0)  (Option.value scales ~default:0.0) (match scales with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_linear1d_backward ~(grad_output @ local) ~output_size ~input_size ~align_corners ~scales =
  stubs_upsample_linear1d_backward (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (if align_corners then 1 else 0)  (Option.value scales ~default:0.0) (match scales with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_linear1d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) ~output_size ~input_size ~align_corners ~scales =
  stubs_upsample_linear1d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (if align_corners then 1 else 0)  (Option.value scales ~default:0.0) (match scales with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_linear1d_out ~(out @ local) (self @ local) ~output_size ~align_corners ~scales =
  stubs_upsample_linear1d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (if align_corners then 1 else 0)  (Option.value scales ~default:0.0) (match scales with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_linear1d_vec (input @ local) ~output_size ~align_corners ~scale_factors =
  stubs_upsample_linear1d_vec (globalize_gc_tensor input) (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) (if align_corners then 1 else 0) (scale_factors |> CArray.of_list double |> CArray.start) (List.length scale_factors) |> with_tensor_gc

let upsample_nearest1d (self @ local) ~output_size ~scales =
  stubs_upsample_nearest1d (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size)  (Option.value scales ~default:0.0) (match scales with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_nearest1d_backward ~(grad_output @ local) ~output_size ~input_size ~scales =
  stubs_upsample_nearest1d_backward (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size)  (Option.value scales ~default:0.0) (match scales with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_nearest1d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) ~output_size ~input_size ~scales =
  stubs_upsample_nearest1d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size)  (Option.value scales ~default:0.0) (match scales with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_nearest1d_out ~(out @ local) (self @ local) ~output_size ~scales =
  stubs_upsample_nearest1d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size)  (Option.value scales ~default:0.0) (match scales with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_nearest1d_vec (input @ local) ~output_size ~scale_factors =
  stubs_upsample_nearest1d_vec (globalize_gc_tensor input) (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) (scale_factors |> CArray.of_list double |> CArray.start) (List.length scale_factors) |> with_tensor_gc

let upsample_nearest2d (self @ local) ~output_size ~scales_h ~scales_w =
  stubs_upsample_nearest2d (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_nearest2d_backward ~(grad_output @ local) ~output_size ~input_size ~scales_h ~scales_w =
  stubs_upsample_nearest2d_backward (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_nearest2d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) ~output_size ~input_size ~scales_h ~scales_w =
  stubs_upsample_nearest2d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_nearest2d_out ~(out @ local) (self @ local) ~output_size ~scales_h ~scales_w =
  stubs_upsample_nearest2d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size)  (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_nearest2d_vec (input @ local) ~output_size ~scale_factors =
  stubs_upsample_nearest2d_vec (globalize_gc_tensor input) (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) (scale_factors |> CArray.of_list double |> CArray.start) (List.length scale_factors) |> with_tensor_gc

let upsample_nearest2d_vec_out ~(out @ local) (input @ local) ~output_size ~scale_factors =
  stubs_upsample_nearest2d_vec_out (globalize_gc_tensor out) (globalize_gc_tensor input) (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) (scale_factors |> CArray.of_list double |> CArray.start) (List.length scale_factors) |> with_tensor_gc

let upsample_nearest3d (self @ local) ~output_size ~scales_d ~scales_h ~scales_w =
  stubs_upsample_nearest3d (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size)  (Option.value scales_d ~default:0.0) (match scales_d with | Some _ -> 0 | None -> 1)   (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_nearest3d_backward ~(grad_output @ local) ~output_size ~input_size ~scales_d ~scales_h ~scales_w =
  stubs_upsample_nearest3d_backward (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size)  (Option.value scales_d ~default:0.0) (match scales_d with | Some _ -> 0 | None -> 1)   (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_nearest3d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) ~output_size ~input_size ~scales_d ~scales_h ~scales_w =
  stubs_upsample_nearest3d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size)  (Option.value scales_d ~default:0.0) (match scales_d with | Some _ -> 0 | None -> 1)   (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_nearest3d_out ~(out @ local) (self @ local) ~output_size ~scales_d ~scales_h ~scales_w =
  stubs_upsample_nearest3d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size)  (Option.value scales_d ~default:0.0) (match scales_d with | Some _ -> 0 | None -> 1)   (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_nearest3d_vec (input @ local) ~output_size ~scale_factors =
  stubs_upsample_nearest3d_vec (globalize_gc_tensor input) (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) (scale_factors |> CArray.of_list double |> CArray.start) (List.length scale_factors) |> with_tensor_gc

let upsample_trilinear3d (self @ local) ~output_size ~align_corners ~scales_d ~scales_h ~scales_w =
  stubs_upsample_trilinear3d (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (if align_corners then 1 else 0)  (Option.value scales_d ~default:0.0) (match scales_d with | Some _ -> 0 | None -> 1)   (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_trilinear3d_backward ~(grad_output @ local) ~output_size ~input_size ~align_corners ~scales_d ~scales_h ~scales_w =
  stubs_upsample_trilinear3d_backward (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (if align_corners then 1 else 0)  (Option.value scales_d ~default:0.0) (match scales_d with | Some _ -> 0 | None -> 1)   (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_trilinear3d_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) ~output_size ~input_size ~align_corners ~scales_d ~scales_h ~scales_w =
  stubs_upsample_trilinear3d_backward_grad_input (globalize_gc_tensor grad_input) (globalize_gc_tensor grad_output) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (List.map Int64.of_int input_size |> CArray.of_list int64_t |> CArray.start) (List.length input_size) (if align_corners then 1 else 0)  (Option.value scales_d ~default:0.0) (match scales_d with | Some _ -> 0 | None -> 1)   (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_trilinear3d_out ~(out @ local) (self @ local) ~output_size ~align_corners ~scales_d ~scales_h ~scales_w =
  stubs_upsample_trilinear3d_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int output_size |> CArray.of_list int64_t |> CArray.start) (List.length output_size) (if align_corners then 1 else 0)  (Option.value scales_d ~default:0.0) (match scales_d with | Some _ -> 0 | None -> 1)   (Option.value scales_h ~default:0.0) (match scales_h with | Some _ -> 0 | None -> 1)   (Option.value scales_w ~default:0.0) (match scales_w with | Some _ -> 0 | None -> 1)  |> with_tensor_gc

let upsample_trilinear3d_vec (input @ local) ~output_size ~align_corners ~scale_factors =
  stubs_upsample_trilinear3d_vec (globalize_gc_tensor input) (match output_size with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match output_size with | None -> -1 | Some v -> List.length v) (if align_corners then 1 else 0) (scale_factors |> CArray.of_list double |> CArray.start) (List.length scale_factors) |> with_tensor_gc

let value_selecting_reduction_backward ~(grad @ local) ~dim ~(indices @ local) ~sizes ~keepdim =
  stubs_value_selecting_reduction_backward (globalize_gc_tensor grad) (Int64.of_int dim) (globalize_gc_tensor indices) (List.map Int64.of_int sizes |> CArray.of_list int64_t |> CArray.start) (List.length sizes) (if keepdim then 1 else 0) |> with_tensor_gc

let values (self @ local) =
  stubs_values (globalize_gc_tensor self) |> with_tensor_gc

let values_copy (self @ local) =
  stubs_values_copy (globalize_gc_tensor self) |> with_tensor_gc

let values_copy_out ~(out @ local) (self @ local) =
  stubs_values_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let vander ~(x @ local) ~n ~increasing =
  stubs_vander (globalize_gc_tensor x)  (match n with | None -> Int64.zero | Some v -> Int64.of_int v) (match n with | Some _ -> 0 | None -> 1)  (if increasing then 1 else 0) |> with_tensor_gc

let var (self @ local) ~unbiased =
  stubs_var (globalize_gc_tensor self) (if unbiased then 1 else 0) |> with_tensor_gc

let var_correction (self @ local) ~dim ~correction ~keepdim =
  stubs_var_correction (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) correction (if keepdim then 1 else 0) |> with_tensor_gc

let var_correction_out ~(out @ local) (self @ local) ~dim ~correction ~keepdim =
  stubs_var_correction_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) correction (if keepdim then 1 else 0) |> with_tensor_gc

let var_dim (self @ local) ~dim ~unbiased ~keepdim =
  stubs_var_dim (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if unbiased then 1 else 0) (if keepdim then 1 else 0) |> with_tensor_gc

let var_mean (self @ local) ~unbiased =
  let out__ = CArray.make raw_tensor 2 in
  stubs_var_mean (CArray.start out__) (globalize_gc_tensor self) (if unbiased then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let var_mean_correction (self @ local) ~dim ~correction ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_var_mean_correction (CArray.start out__) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) correction (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let var_mean_correction_out ~(out0 @ local) ~(out1 @ local) (self @ local) ~dim ~correction ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_var_mean_correction_out (CArray.start out__) (globalize_gc_tensor out0) (globalize_gc_tensor out1) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) correction (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let var_mean_dim (self @ local) ~dim ~unbiased ~keepdim =
  let out__ = CArray.make raw_tensor 2 in
  stubs_var_mean_dim (CArray.start out__) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if unbiased then 1 else 0) (if keepdim then 1 else 0);
  let t0 = CArray.get out__ 0 |> with_tensor_gc in
  let t1 = CArray.get out__ 1 |> with_tensor_gc in
  t0, t1

let var_out ~(out @ local) (self @ local) ~dim ~unbiased ~keepdim =
  stubs_var_out (globalize_gc_tensor out) (globalize_gc_tensor self) (match dim with | None -> from_voidp int64_t null | Some v -> List.map Int64.of_int v |> CArray.of_list int64_t |> CArray.start) (match dim with | None -> -1 | Some v -> List.length v) (if unbiased then 1 else 0) (if keepdim then 1 else 0) |> with_tensor_gc

let vdot (self @ local) (other @ local) =
  stubs_vdot (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let vdot_out ~(out @ local) (self @ local) (other @ local) =
  stubs_vdot_out (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let view (self @ local) ~size =
  stubs_view (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let view_as (self @ local) (other @ local) =
  stubs_view_as (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let view_as_complex (self @ local) =
  stubs_view_as_complex (globalize_gc_tensor self) |> with_tensor_gc

let view_as_complex_copy (self @ local) =
  stubs_view_as_complex_copy (globalize_gc_tensor self) |> with_tensor_gc

let view_as_complex_copy_out ~(out @ local) (self @ local) =
  stubs_view_as_complex_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let view_as_real (self @ local) =
  stubs_view_as_real (globalize_gc_tensor self) |> with_tensor_gc

let view_as_real_copy (self @ local) =
  stubs_view_as_real_copy (globalize_gc_tensor self) |> with_tensor_gc

let view_as_real_copy_out ~(out @ local) (self @ local) =
  stubs_view_as_real_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let view_copy (self @ local) ~size =
  stubs_view_copy (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let view_copy_dtype (self @ local) ~dtype =
  stubs_view_copy_dtype (globalize_gc_tensor self) (Kind.packed_to_int dtype) |> with_tensor_gc

let view_copy_dtype_out ~(out @ local) (self @ local) ~dtype =
  stubs_view_copy_dtype_out (globalize_gc_tensor out) (globalize_gc_tensor self) (Kind.packed_to_int dtype) |> with_tensor_gc

let view_copy_out ~(out @ local) (self @ local) ~size =
  stubs_view_copy_out (globalize_gc_tensor out) (globalize_gc_tensor self) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

let view_dtype (self @ local) ~dtype =
  stubs_view_dtype (globalize_gc_tensor self) (Kind.packed_to_int dtype) |> with_tensor_gc

let vsplit (self @ local) ~sections =
  stubs_vsplit (globalize_gc_tensor self) (Int64.of_int sections) |> to_tensor_list

let vsplit_array (self @ local) ~indices =
  stubs_vsplit_array (globalize_gc_tensor self) (List.map Int64.of_int indices |> CArray.of_list int64_t |> CArray.start) (List.length indices) |> to_tensor_list

let vstack (tensors @ local) =
  stubs_vstack (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> with_tensor_gc

let vstack_out ~(out @ local) (tensors @ local) =
  stubs_vstack_out (globalize_gc_tensor out) (CArray.of_list gc_tensor (globalize_gc_tensor_list tensors) |> CArray.start) (Base.List.length tensors) |> with_tensor_gc

let where ~(condition @ local) =
  stubs_where (globalize_gc_tensor condition) |> to_tensor_list

let where_scalar ~(condition @ local) self other =
  stubs_where_scalar (globalize_gc_tensor condition) self other |> with_tensor_gc

let where_scalarother ~(condition @ local) (self @ local) other =
  stubs_where_scalarother (globalize_gc_tensor condition) (globalize_gc_tensor self) other |> with_tensor_gc

let where_scalarself ~(condition @ local) self (other @ local) =
  stubs_where_scalarself (globalize_gc_tensor condition) self (globalize_gc_tensor other) |> with_tensor_gc

let where_self ~(condition @ local) (self @ local) (other @ local) =
  stubs_where_self (globalize_gc_tensor condition) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let where_self_out ~(out @ local) ~(condition @ local) (self @ local) (other @ local) =
  stubs_where_self_out (globalize_gc_tensor out) (globalize_gc_tensor condition) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let xlogy (self @ local) (other @ local) =
  stubs_xlogy (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let xlogy_ (self @ local) (other @ local) =
  stubs_xlogy_ (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let xlogy_outscalar_other ~(out @ local) (self @ local) other =
  stubs_xlogy_outscalar_other (globalize_gc_tensor out) (globalize_gc_tensor self) other |> with_tensor_gc

let xlogy_outscalar_self ~(out @ local) self (other @ local) =
  stubs_xlogy_outscalar_self (globalize_gc_tensor out) self (globalize_gc_tensor other) |> with_tensor_gc

let xlogy_outtensor ~(out @ local) (self @ local) (other @ local) =
  stubs_xlogy_outtensor (globalize_gc_tensor out) (globalize_gc_tensor self) (globalize_gc_tensor other) |> with_tensor_gc

let xlogy_scalar_other (self @ local) other =
  stubs_xlogy_scalar_other (globalize_gc_tensor self) other |> with_tensor_gc

let xlogy_scalar_other_ (self @ local) other =
  stubs_xlogy_scalar_other_ (globalize_gc_tensor self) other |> with_tensor_gc

let xlogy_scalar_self self (other @ local) =
  stubs_xlogy_scalar_self self (globalize_gc_tensor other) |> with_tensor_gc

let zero (self @ local) =
  stubs_zero (globalize_gc_tensor self) |> with_tensor_gc

let zero_ (self @ local) =
  stubs_zero_ (globalize_gc_tensor self) |> with_tensor_gc

let zero_out ~(out @ local) (self @ local) =
  stubs_zero_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let zeros ~size ~options =
  stubs_zeros (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) (Kind.packed_to_int (fst options)) (Device.to_int (snd options)) |> with_tensor_gc

let zeros_like (self @ local) =
  stubs_zeros_like (globalize_gc_tensor self) |> with_tensor_gc

let zeros_like_out ~(out @ local) (self @ local) =
  stubs_zeros_like_out (globalize_gc_tensor out) (globalize_gc_tensor self) |> with_tensor_gc

let zeros_out ~(out @ local) ~size =
  stubs_zeros_out (globalize_gc_tensor out) (List.map Int64.of_int size |> CArray.of_list int64_t |> CArray.start) (List.length size) |> with_tensor_gc

